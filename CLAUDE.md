# REVERB JP — 인플루언서 체험단 플랫폼

> 이 문서는 **현재 가동 중인 동작·핵심 컨벤션** 위주. 마이그레이션 번호·PR 노트·deprecated 메모 등 이력성 메타데이터는 [`docs/CLAUDE-ARCHIVE.md`](docs/CLAUDE-ARCHIVE.md) 참조.

## Overview
일본 시장 대상 인플루언서 체험단(리뷰어/기프팅/방문형) 모집 플랫폼.
브랜드가 캠페인을 등록하고, 인플루언서가 신청하는 구조.

## Tech Stack
- Language: HTML/CSS/JavaScript (vanilla, 프레임워크 없음)
- Backend: Supabase (Auth + Database + Storage) + localStorage 폴백
- Deployment: Vercel Pro (Team 플랜)
- Package Manager: 없음 (CDN 기반)

## Key URLs
- 운영 (인플루언서): https://globalreverb.com
- 운영 (관리자): https://globalreverb.com/admin/
- 운영 (광고주 신청 폼): https://sales.globalreverb.com (별도 Vercel 프로젝트 `reverb-sales`, Root Directory=`sales/`)
- 스테이징 (인플루언서): https://dev.globalreverb.com
- 스테이징 (관리자): https://dev.globalreverb.com/admin/ (admin@kemo.jp / admin1234)
- GitHub: github.com/jfun-reverb/kemo
- Supabase (production): https://nrwtujmlbktxjgdwlpjj.supabase.co (🇯🇵 Tokyo `ap-northeast-1`, Pro/NANO) — 2026-05-27 도쿄 이관 완료
- Supabase (staging): https://qysmxtipobomefudyixw.supabase.co (🇯🇵 Tokyo `ap-northeast-1`, Pro/MICRO)
- LINE: @reverb.jp

## Environments
- **도메인 기반 자동 분기**: `dev/lib/supabase.js`의 `resolveSupabaseEnv()`가 `location.hostname` 판별
  - `globalreverb.com`, `www.globalreverb.com` → 운영서버 Supabase
  - 그 외 (dev.globalreverb.com, localhost 등) → 개발서버 Supabase
- Supabase URL/Key는 `SUPABASE_ENVS` 객체에서만 관리 (다른 파일 하드코딩 금지)
- 관리자 페이지 헤더에 개발서버에서만 주황색 `STAGING` 배지 + `[DEV]` 탭 제목/파비콘 표시
- 운영 배포는 반드시 개발서버 검증 후 main merge
- Supabase Client 옵션: `flowType: 'pkce'`, `detectSessionInUrl: true` (비밀번호 재설정 안정성)

## Email / SMTP
- 양 서버 모두 **Brevo Custom SMTP** 사용 (`smtp-relay.brevo.com:587`)
- **Brevo 플랜: Starter 20,000 emails/월** (Monthly $29, 갱신일 매월 16일). Marketing+Transactional 공용 쿼터, 일일 한도 없음
- 발신: `noreply@globalreverb.com`, 발신명: 운영 `REVERB JP` / 개발 `REVERB JP [DEV]`
- 발신 도메인 DNS 인증 완료 (SPF/DKIM/DMARC, cafe24 DNS 관리)

### 가동 중인 메일 파이프라인 (세부 구현은 `supabase/functions/*` + 사양서가 source of truth)
- **광고주 신청 접수 알림** (`notify-brand-application`): `brand_applications` INSERT 직후 호출. 수신자 = `get_subscribed_admin_emails('brand_notify')` + env `NOTIFY_ADMIN_EMAILS`
- **오리엔시트 제출 알림** (`notify-orient-submitted`, 마이그202·234): 브랜드가 작성 폼에서 제출하면 관리자에게 즉시 1통(1인 1통 분리). 트리거 = `orient_sheets` UPDATE 데이터베이스 웹훅(Row filter `status='submitted'`). 신규/재제출 구분(웹훅 `old_record.submitted_at IS NULL`=신규), 제목 「[신규 제출]/[수정 재제출] {브랜드명}」. 본문=브랜드명·형식·제출시각·연결 신청번호·관리자 링크(`data jsonb` 본문 미탑재). 수신자 = `get_subscribed_admin_emails('brand_notify')` + env `NOTIFY_ADMIN_EMAILS`. 메일 best-effort(제출 트랜잭션과 분리). **과다 발송 방지 게이트 2단(마이그234)**: ①`last_submitted_at`이 `old_record`와 같으면 스킵 — 부분 발행(`mark_orient_card_consumed`)은 `status='submitted'` 유지한 채 UPDATE라 Row filter를 재통과하지만 실제 제출이 아니므로 차단(+이 함수 자신의 `last_notified_at` UPDATE 재트리거 무한루프도 동일 게이트로 차단). ②재제출은 `last_submitted_at - last_notified_at < 30분`이면 스킵(디바운스), 신규 제출은 항상 발송. 발송 1통+ 성공 시 `last_notified_at = record.last_submitted_at` 갱신. 전체 카드 발행 완료로 `status=consumed` 전이만 Row filter에서 자연 제외됨. 웹훅·Edge Function 배포는 Dashboard 수동(개발/운영 각각). 사양서 `docs/specs/2026-06-30-orient-submit-notification.md`
- **브랜드 일일 보고** (`notify-brand-daily-digest`, 마이그203·cron 204): pg_cron 매일 KST 09:00(job `brand-daily-digest-0900kst`). 2섹션(신규 제출 · 수정 재제출) — 시각 기준 집계(status 필터 없음). 신규=`orient_sheets.submitted_at` 어제 KST 윈도우, 재제출=`last_submitted_at` 어제 KST 윈도우 AND `submitted_at` < 어제시작. 두 조건 상호배타. `brand_daily_digest_runs.digest_date UNIQUE` mutex. 0건=미발송. 수신자 = `get_subscribed_admin_emails('brand_digest')` + env `NOTIFY_ADMIN_EMAILS` (마이그203 `lookup_values` `admin_email_kind` 코드 `brand_digest` 추가로 관리자 메일 수신 설정 모달에 자동 노출). 사양서 `docs/specs/2026-06-30-orient-submit-notification.md`
- **관리자 일일 통합 다이제스트** (`notify-admin-daily-digest`): pg_cron 매일 KST 09:00. 4섹션 1통(신청 접수·응모 취소·결과물 제출·재처리), 캠페인별 그룹 표. 인플 표시 4종(한자·가나·이메일·SNS링크) 통일. `admin_daily_digest_runs.digest_date UNIQUE` mutex. 수신자 = `get_subscribed_admin_emails('daily_digest')` + env `NOTIFY_ADMIN_EMAILS` (마이그레이션 164 에서 구 `application_cancel`·`application_received` 2종 구독 → `daily_digest` 단일 구독으로 통합)
- **인플루언서 일일 다이제스트** (`notify-influencer-daily-digest`): pg_cron 매일 KST 09:00. 어제 신청·승인·반려 + 오늘 D-5/D-1 마감 4섹션. `deadline_reminder_email_sent` 4-tuple UNIQUE 재발송 차단. marketing_opt_in 무시(트랜잭션)
- **캠페인 홍보 메일** (`notify-campaign-promo-digest`): pg_cron 매주 월·목 KST 09:00. 신규+D-1 임박 캠페인, `marketing_opt_in=true`+자격 매칭 인플(1통씩 분리, 노출 최대 2회·클릭 시 제외). 첫 배치에서 `campaign_promo` 토글 관리자에게도 동시 발송. 사양서 `docs/specs/2026-05-27-admin-promo-email-subscription.md`
- **공통 패턴**: ①인플 대상 메일은 **4줄 푸터 필수**(自動送信 안내·LINE @reverb.jp·© JFUN·사이트 URL, 글자색 #999) — 신규 인플 메일 추가 시 의무 ②관리자 일괄 발송은 **1명당 1통 분리**(To 헤더 노출 차단) ③부분 실패는 `status='sent'`+실패 명단 누적(전원 실패만 `failed`)
- **메일 템플릿**: `docs/email-templates/` 가 source of truth(카탈로그 `index.html`). Edge Function 은 `_templates/` 미러를 `render({{key}})` 로 읽음 — 배포 전 `scripts/sync-email-templates.sh` 동기화 필수

### Auth URL Configuration
- 운영: Site URL `https://globalreverb.com` + Redirect `https://globalreverb.com/**`, `https://www.globalreverb.com/**`
- 개발: Site URL `https://dev.globalreverb.com` + Redirect `https://dev.globalreverb.com/**`

### Auth Rate Limits
- 운영: `Rate limit for sending emails` = **100 emails/h** (가입+초대+재설정 메일 공용)
- 개발: 기본값 30/h (Confirm email OFF, 트래픽 적어 충분)
- 한도 소진 시 `429 email rate limit exceeded`. Logs & Analytics → Auth 에서 확인

## i18n (개발서버 한정)
- 인플루언서 페이지 KO/JA 토글 (마이페이지 메뉴)
- 키-값: `dev/lib/i18n/{ja,ko}.js`, 런타임: `dev/lib/i18n/index.js`
- HTML: `data-i18n="key"` (textContent), `data-i18n-html="key"` (innerHTML, `<br>` 허용)
- JS 동적: `t('key')` 헬퍼
- 기본값 `ja`, navigator.language 자동 감지 사용 안 함
- 운영서버 배포 전 (테스터 검증 완료 후 결정)
- 적용 범위: 마이페이지, 인증, GNB/홈, 캠페인 목록·상세, 신청 모달, 활동관리, 알림, DB 에러 메시지 로케일 분기 (`friendlyErrorJa`)

## 내부 문서 (개발서버 한정)
- `docs/service-flow.html` — 전체 서비스 플로우차트
- `docs/flowchart-i18n.html` — i18n 다국어 흐름도
- 접속: https://dev.globalreverb.com/docs/service-flow.html, https://dev.globalreverb.com/docs/flowchart-i18n.html
- **운영서버(main)에 머지하지 않음** — 내부 직원용 참고 자료

## Architecture
- 인플루언서 앱: `dev/index.html` (모바일 480px, GNB + 우측 슬라이드 햄버거 메뉴)
- 관리자 앱: `dev/admin/index.html` (PC 전체폭, 별도 페이지, **2단 고정 레이아웃** — 사이드바/메인 독립 스크롤, 상단 GNB 없음)
- **광고주 신청 앱(sales)**: `sales/{index,reviewer,seeding}.html` — 별도 Vercel 프로젝트 `reverb-sales`(Root Directory=`sales/`), `sales.globalreverb.com` / `sales-dev.globalreverb.com` 서브도메인. anon 으로 `submit_brand_application` RPC 경유 `brand_applications` INSERT. Vercel `cleanUrls`+catch-all rewrite 로 `/reviewer` → `reviewer.html`. 파일 업로드 기능 없음 (텍스트 입력만)
- 배포용: 루트 `index.html` (build.sh 로 생성)
- 개발 폴더 구조:
  - `dev/js/` — 인플루언서: app, ui, campaign, application, auth, mypage, notifications, messaging
    - 관리자(admin.js 페인 분리 완료, 2026-05-25): `admin.js`(캠페인 목록·폼만 잔류) + `admin-core.js`(공용 헬퍼: 페인 라우팅·다중필터·확인모달·이미지 라이트박스[`#imageLightbox` — 배경 안 덮는 드래그·리사이즈 떠있는 창, 뒤 검수 모달 입력 가능]·모달 드래그/리사이즈 인프라·태그입력) + `admin-notices.js` + `admin-faq.js` + `admin-influencers.js` + `admin-deliverables.js` + `admin-excel.js` + `admin-dashboard.js` + `admin-roadmap.js`(오픈 예정 기능 보드) + `admin-applications.js`(신청+신청자) + `admin-accounts.js`(내계정+관리자계정) + `admin-lookups.js`(기준데이터+번들3종+미니에디터) + `admin-errors.js`(오류 로그 페인 — 마이그레이션 165) + `admin-brand.js`/`admin-company.js`/`admin-brand-ops.js`/`admin-messaging.js`(브랜드 서베이) + `admin-orient.js`(오리엔시트 발급·조회 페인 — PR3)
    - 빌드는 ES 모듈이 아니라 단순 이어붙이기(concat) — 전역 스코프 1개. `admin-core.js`가 다른 admin-* 파일보다 앞, `admin.js`가 페인 파일들보다 뒤, `admin/app.js`가 맨 마지막 (build.sh `ADMIN_JS_FILES` 순서)
  - `dev/css/` — base, components, campaign, auth, mypage, admin
  - `dev/lib/` — supabase(설정), shared(전역변수), storage(DB/Storage API)
  - `dev/build.sh` — dev/ → 루트 index.html 빌드
  - `sales/` — 광고주 신청 폼 (빌드 없이 정적 파일 그대로 배포)
  - `supabase/functions/notify-brand-application/` — 광고주 신청 알림 Edge Function
- Supabase 미연결 시 localStorage 로 동작 (DEMO_MODE)

## Features — 인플루언서 (모바일)
- **회원가입**: 1단계 폼 (이름 한자/가나 + 이메일 + 비밀번호), 추가정보는 마이페이지에서 입력
- **로그인/로그아웃**: 이메일+비밀번호, 세션 복원, 관리자 로그인 시 admin 페이지 자동 오픈
- **비밀번호 재설정**: 이메일 입력 → Supabase 재설정 메일 발송 → 앱 내 새 비밀번호 설정 (`#page-forgot`, `#page-reset-pw`)
- **GNB**: 비로그인 시 Log In/Sign Up 버튼, 로그인 시 우측 햄버거 메뉴 (계정 카드[우측 알림 벨] + 홈/캠페인/마이페이지 아코디언/로그아웃/회원탈퇴), 관리자는 Admin 버튼
- **회원가입 이메일 확인**: 운영서버 한정 Supabase Confirm sign-up 활성, 가입 후 확인 메일 안내 화면 표시, 미확인 시 로그인/신청 차단. 개발서버는 Confirm email OFF. auth.js 는 `data.session` 유무로 자동 분기
- **캠페인 목록**: 채널필터(동적 생성), 모집유형 필터(리뷰어/기프팅/방문형). 노출 대상은 active + scheduled + closed(노출 ON)
- **캠페인 카드 배지**: 좌상단 `募集中`(active), 우상단 `NEW`(7일 이내), 제목 위 `締切間近`(deadline<5일 또는 잔여 slots≤30%), 콘텐츠 종류 아래 모집타입 pill + `{applied}/{slots}名` 슬롯 카운트, 이미지 좌하단 첫 채널+`+N`
- **캠페인 상세**: 이미지 캐러셀(최대9장), 상품정보, 모집조건, 참가방법, 가이드라인, NG사항, LINE/Instagram CTA, 조회수 자동 카운트, closed 시 신청버튼 비활성(募集締切). 채널 pill 사이에 `or` 또는 `&` 구분자 (`channel_match`)
- **캠페인 신청**: 이메일 인증 필수, 필수정보 사전체크(채널별 SNS / zip+prefecture+city+phone / PayPal 이메일) → 동기메시지 + 배송지 + PR태그 동의, 중복신청 방지, 최소 팔로워수 미달 시 알럿 차단
- **주의사항 동의**: 캠페인의 `caution_items` 가 있으면 ①상세 "주의 사항" 섹션 + ②신청 모달 상단 빨간 박스 양쪽 렌더. 모달 하단 "全ての注意事項を確認しました" 단일 체크박스 + 미체크 시 차단. 동의 시 `applications.caution_agreed_at` + `caution_snapshot`(jsonb) 저장. 캠페인 items 스냅샷을 신청 시점 그대로 보존 → 번들 수정 후에도 기존 신청 데이터 영향 없음. 응모이력에 동의 시각 작은 배지 노출
- **응모 차단**: 리뷰어(monitor) 캠페인은 `applied_count >= slots` 일 때 신규 응모 차단 (기프팅·방문형은 초과 응모 허용)
- **마이페이지**: 랜딩(목차) 화면 없이 입력 폼 화면 7종(応募履歴/基本情報/SNSアカウント/配送先/PayPal/パスワード変更/メール受信設定)의 컨테이너(`#mypage-list` 제거, 햄버거 메뉴로 흡수). 진입은 GNB 햄버거 「マイページ」 아코디언 서브항목. 폼 화면에는 백버튼 없음(햄버거로 이동 + 브라우저 뒤로가기). `navigate('mypage')`·`#mypage`/`#mypage-*` popstate 는 모두 応募履歴(`closeMypageSub`)로 복귀. 대표SNS 선택 가능, 필수 미입력 항목 "未登録" 배지(햄버거 서브항목에 표시, `computeProfileBadges` 헬퍼 공용)
- **메일 수신 설정**(`#mypage-email-settings`, 2026-05-20 PR 4): 마케팅(캠페인 홍보) 메일 ON/OFF 토글 + 업무 알림 메일(응모 접수·검수·마감) 상시 발송 안내 박스. 토글 ON → `resubscribe_marketing()` 원격 호출 함수(동의 시각 `marketing_agreed_at` 갱신 — 특정전자메일법 동의 근거 기록), OFF → `influencers.marketing_opt_in=false` + `marketing_unsubscribed_at` 본인 행 직접 UPDATE. `storage.js` 의 `resubscribeMarketing()`/`updateMarketingOptIn(value)` (ON 은 내부적으로 RPC 위임해 동의시각 누락 차단)
- **메일 수신거부 라우트**(`#unsubscribe?token=...`, 2026-05-20 PR 3): 홍보 메일 본문 하단 1-click 수신거부 링크. 비로그인 상태에서 토큰만으로 동작 — `unsubscribe_by_token(token)` 익명 RPC 호출 후 성공/무효 화면 표시. 잘못된·만료 토큰은 「リンクが無効です」 안내. `app.js` 가 쿼리 붙은 해시(`#unsubscribe?token=`)를 파싱해 `handleUnsubscribePage(token)` 실행
- **GNB 햄버거 메뉴**: 상단 우측 ☰ 버튼에 미읽음 알림 배지(9+), 클릭 시 우측 슬라이드 패널. 로그인 시 구성 — 최상단 계정 카드(이름·SNS핸들·이메일 + **우측 알림 벨 아이콘**·미읽음 배지, 아바타 없음) → 홈/캠페인 → 「マイページ」 접기/펼치기 아코디언(기본 펼침, 클릭 시 토글만·화면 이동 없음, 서브 7종 각 `min-height:48px`, 기본정보/SNS/배송지/PayPal 「未登録」 배지) → ログアウト → 하단 退会する(`margin-top` 으로 간격) 링크. **メッセージ 메뉴 항목은 제거**(응모이력과 목적지 중복 — 응모건 메시지는 응모이력 카드의 메시지 버튼으로 진입, 답장은 `message_received` 알림으로 확인). **通知도 별도 항목이 아니라 계정 카드 우측 벨로 통합**. 열 때 프로필 비동기 새로고침으로 계정 카드·배지 최신화(미읽음 수는 `_lastUnread` 캐시 → 재렌더 시 `applyNotifBadge` 로 즉시 복원). flex 레이아웃에서 1px 구분선·항목이 찌부러지지 않게 `.nav-menu>*{flex-shrink:0}` 필수. 비로그인은 로그인/회원가입, 인증 페이지에선 햄버거 숨김. 마이페이지 랜딩 화면 흡수(2026-05-22)
- **알림 모달**: deliverables.status 트리거로 생성된 rejected/changed/approved 3종. 항목 클릭 시 읽음 처리 + 활동관리로 이동. "모두 읽음" 버튼
- **활동관리**: 승인된 캠페인에서 결과물 제출. recruit_type 분기 — monitor=영수증(이미지 + 주문번호·구매일·구매금액 3종 필수; 이미지 선택 후 「画像から自動入力」 버튼으로 기기 안 글자인식(Tesseract.js jpn+eng, CDN lazy-load) 보조입력 — **빈 칸만 채움·외부 전송 0·최종 확인 필수**, 로드/인식 실패가 제출을 막지 않음. 헬퍼 `dev/lib/ocr-receipt.js`의 `runReceiptOcr`/`extractReceiptFields`), gifting/visit=SNS 게시물 URL(자동 채널 판별 + 실패 시 수동 드롭다운, **수동 드롭다운은 캠페인 채널로 제한** + **캠페인 요구 채널과 불일치 시 toast 차단** — 인플이 캠페인과 다른 SNS 링크 제출 방지, 헬퍼 `postChannelMatchesCampaign`(shared.js), 마이그레이션 182). **URL 오타 자동 보정**(`normalizeUrlInput`, shared.js — `ttp://`·스킴 누락 등 흔한 실수를 `https://`로 보정, 위험 스킴 차단, 보정 시 toast 안내. 인플·관리자 대리 등록 공통). 모두 `deliverables` 직접 INSERT + `submit_deliverable` RPC. 반려된 결과물은 상단 빨간 배너에 사유 표시, 재제출 시 pending 복귀 (동일 URL 은 `post_submissions` 배열에 날짜 누적). `submission_end` 경과 시 폼 비활성
- **응모이력**: 제목 우측 **상태 드롭다운**(進行中[심사중+당첨, 진입 기본]/すべて/審査中/当選/落選/取消, 각 건수 병기), 캠페인상태/채널/정렬 필터. 진입 시 進行中만 노출, 진행중 0건이면 「すべて表示」 안내. 승인 캠페인 클릭→활동관리, 기타→캠페인 상세
- **응모건 메시지**(운영 배포 완료 2026-05-28): 응모이력 카드의 메시지 버튼(미읽음 배지) → 게시판형 **페이지**(`#page-messages`, 해시 `#messages-{id}`, 2026-05-22 모달→페이지 전환 — 모바일 키보드가 모달을 가리던 문제 해결, `#appShell` 키보드 패턴 상속). 헤더 뒤로가기→응모이력. **취소(cancelled) 응모는 진입 차단**(toast 안내). 운영팀에 텍스트+이미지(자동 압축/HEIC 변환, 최대 5장) 발송, 25분 내 본인 회수, 숨김·회수 메시지는 가림(placeholder) 표시. 본문/첨부 마스킹은 서버(`get_application_messages` RPC). 진입 `openMessagesPage(appId, from)`·이탈 정리 `cleanupMessagesPage()`(navigate 훅). `dev/js/messaging.js`. 관리자 발신·GNB 메뉴·알림 포함 운영 배포 완료. **자동 번역 병기**(마이그레이션 235, dev — 운영 미배포): 메시지 INSERT → 데이터베이스 웹훅(Dashboard 수동 설정) → Edge Function `translate-message` → Google Cloud Translation v2 → `body_translated`/`translated_lang`/`translate_status` UPDATE. 받은 메시지에 번역문을 본문 위치에·원문을 아래 작은 글씨로 병기(인플=일본어 번역, 관리자=한국어 번역 + 받은편지함 미리보기·대화 검색도 번역본 활용). 번역 NULL/failed/과거 메시지는 원문만(폴백 — 발송·조회 절대 안 막음), 마스킹 행은 번역본도 서버에서 NULL. 웹훅 서버 간 호출이라 CORS 불필요. secrets `GOOGLE_TRANSLATE_API_KEY` 필요. 사양서 `docs/specs/2026-07-13-message-translation.md`(개인정보처리방침 Google 위탁 추가는 PR3 — 운영 배포 전 필수)
- **모집 마감 후 응모 차단**(2026-07-30, 마이그레이션 272 — 서버 강제): `applications` BEFORE INSERT 트리거 `trg_application_deadline_guard` 가 `(now() AT TIME ZONE 'Asia/Tokyo')::date <= campaigns.deadline` 을 검사해 마감 다음 날부터 거부(`recruit_deadline_passed:` 접두어 → `friendlyErrorJa` 가 일본어 문구로 변환). 통과 조항 2종 = `auth.uid() IS NULL`(배치·서비스 키. 익명 쓰기는 행 단위 보안 정책에서 이미 차단돼 도달 불가) · `is_admin()`(현재는 도달하지 않는 대비 조항 — 관리자 대리 응모 경로가 없음). 마감일 NULL 은 무기한 통과. **감사용 계정도 차단**(실제 사용자 동선 재현). ⚠️ 트리거 이름을 `a…` 로 지어 정원 가드(`trg_monitor_*`)보다 **먼저** 실행 — 마감·정원이 동시에 걸리면 마감 메시지가 먼저 나온다. ⚠️ SQL Editor 는 서비스 키라 이 트리거를 재현 못 함(검증법은 마이그레이션 파일 하단 주석). 화면 차단은 여전히 캠페인 상태(`closed`/`ended`) 기준이고 자동 마감은 브라우저 목록 조회 시 도는 구조라, **서버 트리거가 유일한 최종 방어선**. 사양서 `docs/specs/2026-07-29-deadline-server-enforcement.md`. **2단계(결과물 제출 마감 차단, 마이그레이션 274)·동시 저장 방어(275)는 개발·운영 적용 완료**(2026-07-30). ⚠️ 적용 전 선행 조건으로 「채널 코드 이관(옛 `channel-96r9y3` ↔ 새 `cosme`) 26건」이 걸려 있었으나, 운영 실측 결과 **26건 전부 반려 상태가 아니고 이미 마감이 지난 과거 데이터**라 반려 예외가 필요한 대상이 0건 — 마감 차단으로 막힌 사람은 없다. 그 26건은 **2026-07-31 「그대로 유지」로 결정**됐다 — 실측 결과 같은 응모의 새 코드(`cosme`) 행이 **전부 승인**이라 화면·인증 성공·정산이 모두 정상이고, 옛 코드 행은 어느 화면에도 그려지지 않는 잉여 데이터다(양쪽 화면 모두 **캠페인의 채널 목록 기준**으로 카드를 만든다). 지우면 승인 이력이 CASCADE 로 함께 사라지고 이미지 파일은 고아로 남아 손실만 크다. 이 26건은 **감지 장치(마이그레이션 277·278)가 하드코딩 예외 없이 자동 통과**시킨다 — 「같은 응모·같은 종류에 캠페인 채널과 정확히 일치하는 행이 있으면 뺀다」는 일반 규칙이라 코드에 이 사고를 박아 넣지 않았다(아래 「감지 장치」 항목 참조). **3단계(화면 판정 서버 일원화 + 게시물 예외 채널 단위, 마이그레이션 276)는 개발·운영 적용 완료**(2026-07-31). 배치 조회 함수 `get_deliverable_gate(신청id)` 가 그 신청에 필요한 항목(영수증 / 캠페인 채널별 게시물 / 캠페인 채널별 리뷰 인증샷)의 제출 가부를 한 번에 반환하고, **화면은 그 결과만 소비한다**(`gateAllows`/`gateAllowsAny`, 화면 판정 `_latestNonDraftIsRejected` 삭제 — 판정 단일 소스). 게시물 반려 예외도 리뷰 인증샷과 같은 **채널 단위**로 전환 — 인스타그램이 반려됐으면 마감 후 인스타그램만 재제출 가능하고 틱톡 신규 제출은 차단된다(274 까지는 한 채널만 반려돼도 전 채널이 열렸다). ⚠️ **조회 실패(`null`)와 「해당 항목 0건」(`[]`)을 반드시 구분**한다 — 폼 활성(`gateAllowsAny`)과 마감 안내문은 폴백 방향이 같아야 하고, 서버에 못 물어본 경우엔 **둘 다 막지 않는다**(최종 방어선은 트리거). ⚠️ 방문형(visit)은 게시물뿐 아니라 **현장 사진(`receipt`)도 배치 조회에 포함**된다(화면이 방문형에도 영수증 폼을 띄우기 때문 — 인증 성공 판정이 방문형에서 영수증을 안 보는 것과는 다른 축)
- **본인 응모 취소**: `cancel_application(uuid, reason_code, reason_note, acknowledged)` RPC — 본인 검증·결과물 승인 차단·구매기간 이후 사유·동의 강제
- **홈 하단 푸터**: 株式会社ジェイファン 회사 정보 + 会社紹介/利用規約/個人情報処理方針 링크 (슬라이드업 모달), Instagram·X SNS 아이콘
- **성능 최적화**: preconnect(Supabase/Fonts/jsDelivr), 캠페인 카드/마이페이지 썸네일 lazy loading + decoding=async, Supabase Storage 이미지 transform(`/render/image/public/?width=&quality=`)으로 썸네일 용량 축소, 이미지 로드 실패 시 원본 URL 자동 폴백

## Features — 관리자 (PC)
- **사이드바**: Material Icons, 접기/펼치기 토글, `data-pane` 속성 기반 라우팅, pending 배지 항상 표시
- **페이지 새로고침**: `visibility:hidden` cloak 기법 (깜빡임 완전 방지), 서브패널 새로고침 시 부모 패널로 리다이렉트
- **대시보드**: KPI 카드(캠페인/인플/신청/승인), 상태별·채널별 캠페인 분포(채널 복수 선택 캠페인은 중복 집계), 회원가입 추이 차트(Chart.js, 7일/30일/전체), 오늘·이번주 가입 KPI, 프로필 완성률(SNS·배송지·PayPal), 배송지 도도부현 분포 도넛(Top 10 + 未登録/海外, 47개 현 한국어 라벨), 최근 신청 테이블
- **로딩 UX**: 테이블/대시보드 KPI/차트 영역에 인라인 스피너 (전체화면 오버레이 제거)

### 캠페인 관리
- CRUD + 복제 + **보관 삭제(soft delete)**(확인모달, 30일 보관 후 자동 완전삭제) + 순서변경 모드 + 더보기 메뉴(결과물 엑셀·신청자 엑셀·변경 이력)
- **삭제 복구**: 삭제 시 캠페인 행만 30일 보관(신청·결과물 즉시 파기), 캠페인 목록 상태 탭의 **「삭제됨」 탭**에서 목록 확인 + 복구(campaign_admin)·완전삭제(super_admin). 「삭제됨」 탭은 활성 목록과 데이터 소스(`fetchDeletedCampaigns`)·행 동작이 달라 별도 렌더(`renderDeletedCampsPane`/`buildDeletedCampRow`), 진입 시 활성 전용 필터·검색·툴바 숨김. `soft_delete_campaign`/`restore_campaign`/`purge_campaign`/`purge_expired_deleted_campaigns`(마이그레이션 254~258) + 운영현황·홍보메일 RPC deleted_at 제외(259). 사양서 `docs/specs/2026-07-22-campaign-soft-delete-restore.md`
- **캠페인 번호 채번**: `B{brand_seq}-A{app_seq}-C{camp_seq}` (외부 캠페인은 `B{brand_seq}-C{ext_seq}`). 자릿수 brand 4/신청 3/캠 3. 신규 INSERT 시 트리거 자동 채번. 캠페인 등록 폼은 brands 드롭다운 + 신청 cascade + 신규 brand 인라인 모달 패턴. **서베이 신청 선택 UI는 공개 제출 중단(마이그206)으로 안전 숨김(2026-07-15)** — 신규 폼은 선택 UI 미표시, 편집 폼은 기존 연결만 읽기전용 라벨(`renderSurveyLinkReadonly`)로 표시. hidden native select `#{prefix}CampSourceAppId`·저장 로직·`source_application_id` 컬럼·진행현황 비용 카드는 무변경(오리엔 발행 승계·편집 저장 보존). 사양서 `docs/specs/2026-07-15-campaign-form-hide-survey-link.md`. 기존 v1 `CAMP-YYYY-NNNN`/`JFUN-{Q|N}-YYYYMMDD-NNN` 은 `legacy_no` 컬럼·`numbering_legacy_map` 에 보존
- **캠페인 등록/편집 폼**: 4개 섹션 그룹핑 (기본정보/제품정보/모집조건/콘텐츠가이드). 모집타입 라디오버튼 UI, 채널은 복수 선택 체크박스(Instagram/X/Qoo10/TikTok/YouTube/LIPS/@cosme, 콤마 구분 저장 `"instagram,x"`. LIPS·@cosme는 리뷰어형 전용 — `lookup_values.recruit_types=['monitor']`). 채널 2개+ 선택 시 `or`/`&` 라디오 노출 → `campaigns.channel_match`. 자격 검증은 `primary_channel` 단일 기준
- **콘텐츠 가이드 리치 텍스트** (Quill v2, 3개 필드): Notion 복사·붙여넣기 서식 유지. 이미지 태그는 저장 시 제거. XSS 방어 DOMPurify 저장+렌더 이중 sanitize. 헬퍼는 `dev/lib/shared.js`
- **참여방법·주의사항·NG 미니 에디터**: 굵게/기울이기/링크/이미지 첨부 가능. 이미지는 `campaign-images/content/` 업로드 (5MB / jpg·png·webp) → `<img class="rich-img">` 삽입. 클릭 팝오버로 Small/Medium/Large/Original 크기 조정. XSS 방어는 src 화이트리스트 (https + `*.supabase.co`)
- **캠페인 목록**: 썸네일+이미지수, 상태별 탭(전체/준비/모집예정/모집중/모집마감/종료/노출종료, 각 건수 표시·단일 선택) + 타입 드롭다운 필터, 검색(캠페인명+브랜드+제품+campaign_no), 헤더 정렬(조회/신청/등록일/수정일 ▲▼), D-day 라벨, 승인수/모집수 표시 + 대기 배지
- **캠페인 미리보기**: 캠페인 제목 클릭 시 모바일 크기 프리뷰 모달 (편집 버튼 포함)
- **캠페인 상태 6단계**: `draft` → `scheduled` → `active` → `closed`(모집마감) → `ended`(종료), `expired`(노출마감). 자동 전이 3종: `scheduled→active`(recruit_start 도래), `active→closed`(deadline 경과), `closed→ended`(submission_end 경과 — `autoEndCampaigns`, 마이그레이션 156). `expired` 는 운영자 「캠페인 노출」 토글 OFF 로만 진입. `closed`(모집마감)·`ended`(종료) 는 운영자 OFF 까지 인플 화면에 노출(closed=募集締切, ended=終了 오버레이). 노출 그룹(scheduled/active/closed/ended) 컬러 배지(closed=핑크, ended=남보라 `badge-done`), 비노출 그룹(draft/expired) 회색·점선
- **캠페인 노출 토글**: 등록/편집 폼 최상단 + 목록 「상태」 컬럼 빠른 토글. OFF 시 확인 모달 후 status=expired, ON 시 자연 상태 재계산. draft 는 비활성. `toggleCampaignVisibility` + `computeCampaignStatus`
- **상태 변경 드롭다운 전이 규칙**(목록 「상태」 배지 클릭): `CAMP_STATUS_TRANSITIONS` 기준으로 현재 상태에서 갈 수 없는 상태는 회색 비활성(`status-dropdown-item.disabled`). draft→[scheduled,active] / scheduled→[draft,active,closed] / active→[scheduled,closed] / closed→[active,ended] / ended→[closed] / expired→[]. 자기 자신·노출종료(노출 토글 OFF 전용)·마감일 지난 건의 active/scheduled 도 비활성. expired 캠페인은 항목 대신 「노출 토글로만 변경」 안내(`status-dropdown-note`). 렌더 `toggleStatusDropdown`/`buildStatusDropdownItem`, 실제 변경 `changeCampStatus`(deadline 차단 유지)
- **마감일 연장 시 상태 자동 전환 확인**(2026-07-30): 캠페인 편집에서 모집 마감일을 과거→미래로 바꿨고 현재 상태가 `closed`(모집마감)면 확인 모달 → 「확인」 시 마감일+`status='active'` **함께 저장**, 「취소」 시 **마감일도 저장 안 함**. 서버 트리거(272)는 마감일 날짜만 보고 응모를 받으므로 「마감일만 저장」을 허용하면 「서버는 받는데 인플 화면 버튼은 닫힌」 어긋남이 생긴다. `closed`→`active` 한 방향만(ended·expired·draft 제외 — 종료는 결과물 제출 마감까지 지난 상태, 노출종료는 노출 토글 전용). 상태 드롭다운으로 이미 `active` 를 고른 경우엔 확인창 없음. `saveCampaignEdit()`
- **자동 시작·종료**: `fetchCampaigns` 호출 시 `autoOpenCampaigns()` → `autoCloseCampaigns()`. deadline 지난 캠페인은 active/scheduled 저장 불가
- **날짜 입력**: flatpickr range picker 2개(모집·구매/방문) + single picker 1개(`submission_end`). 모집 종료일 선택 시 `submission_end` +14일 자동 제안. 구매·방문 기간은 모집 시작~결과물 제출 마감 윈도우로 자동 clamp. monitor 일 때 콘텐츠 종류 옵션 영상/이미지만 자동 필터링
- **모집인원 초과 승인 차단**: 승인 수가 slots 에 도달하면 알럿 모달 차단
- **조회수**: `campaigns.view_count`, 캠페인 상세 열 때 +1, 관리자 목록에 표시
- **이미지 관리**: 드래그앤드롭 업로드, 크롭, 미리보기, Supabase Storage 저장

### 신청·결과물 관리
- **신청 관리**: 테이블 UI (캠페인 썸네일, 타입/캠페인상태/검색 필터 + **신청 상태 탭**, 상태 정렬), 인플루언서 상세 모달, 모집인원/빈자리 표시. `reviewed_by`/`reviewed_at` 기록, 되돌리기(pending 복귀). 빈자리 없으면 승인버튼 비활성. 빨간 배너로 결과물 반려 사유 표시. **신청 상태는 다중 필터가 아니라 단일 선택 상태 탭**(전체/심사중/승인/미승인/취소, 기본 전체 — `status-tab-bar` 패턴, 정산·결과물 페인과 통일. `APP_STATUS_TABS`/`_appStatusTab`/`renderAppStatusTabs`, admin-applications.js). 신청 1건=상태 4종 중 하나로 상호 배타라 탭이 개념에 맞음
- **결과물 관리** (`/admin#deliverables`): 영수증/게시물 URL 통합 검수 페인. 필터(캠페인·타입·채널·영수증상태·결과물상태·인플루언서 검색·기간) + **인증 상태 탭**(전체/미제출/인증샷 제출중/인증성공/검수 불필요, 기본 전체 — `status-tab-bar` 패턴, `DELIV_CERT_STATUS_TABS`/`_delivCertTab`/`renderDelivCertStatusTabs`, admin-deliverables.js. 인증 상태는 `computeCertStatus` 4종 중 하나로 상호 배타라 다중 필터 대신 탭. 전체 탭이 검수 불필요 노출을 계승 — 구 「검수 불필요 포함」 토글 제거. 사이드바 검수대기 배지 클릭(`_delivPendingOnly`)도 탭 건수 집계에 반영해 「전체(N)」 정합) + 오래된 순 정렬. 상세 모달에 이력 타임라인 + 승인/반려/되돌리기. 반려 사유 템플릿(6종) + 자유입력. 낙관적 락(`version`) 기반 동시 처리 충돌 차단 — 후순위는 "이미 처리됨" 토스트
- **인증 상태 컬럼**(목록 + 엑셀, 신청 1건 단위 4종, 위치=인플루언서 다음·영수증 앞): `검수 불필요`(신청이 승인 후 반려·취소됨 — 아래 참조) · `인증성공`(리뷰어=영수증 승인+채널별 인증샷 모두 승인 / 시딩·방문=게시물 승인) · `인증샷 제출중`(검수중·반려·일부 미제출 등 진행 중) · `미제출`(아무것도 안 냄). 헬퍼 `computeCertStatus`/`certStatusBadge`(admin-deliverables.js), 엑셀은 `_excelCertStatus*`(admin-excel.js). 단일 캠페인 엑셀도 전체 상태 로드(`fetchDeliverables` status 필터 없음 + `fetchApplications({status:'approved'})` 조인으로 결과물 0건 승인 신청도 미제출 빈 행 포함) → 화면 목록·단일/다중 엑셀 모두 정확 표시
- **반려·취소된 신청의 결과물 자동 제외**(2026-07-21, DB 변경 없음): 승인됐던 신청이 반려·취소되면 그 결과물은 "검수 불필요"로 처리. **결과물 status 는 안 바꾸고 신청 status 를 참조**(임베드 `applications:application_id (status)`)하므로 재승인 시 자동 복원. 판정 `isCertExcluded(g)`(신청 rejected/cancelled) → `computeCertStatus` 앞단에서 `'excluded'`. 제외 적용 지점: 결과물 검수 목록(「검수 불필요 포함」 토글 **기본 켜짐**, 끄면 숨김·건수 표시)·인증 상태 열·영수증/결과물 상태 카운트·캠페인/모집/채널 카운트(토글 OFF 시)·인증성공 진행바·엑셀·**사이드바 검수대기 배지**. **검수 모달**: 검수 불필요 건은 승인·반려·되돌리기·대리등록·영수증수정 등 검수 액션 전부 차단 + "검수 불필요 — 캠페인 신청이 반려·취소됨" 안내. 영수증 단계 없는(gifting/visit) 모집 타입은 「영수증(해당 없음)」 패널 미렌더 + 모달 폭 620px 축소. 사양서 `docs/specs/2026-07-21-rejected-application-deliverable-and-settlement.md`
- **사이드바 검수대기 배지 정합**(2026-07-21, 마이그레이션 248): 배지가 재제출로 쌓인 옛 pending 결과물 행까지 세어 과대(운영 31→실제 훨씬 적음)하던 문제. **신청 단위** 집계 `count_pending_review_applications()` RPC(SECURITY DEFINER·`search_path=''`·`is_admin()`)로 교체 — 신청별 최신 결과물이 pending 이고 신청이 반려·취소 아닌 신청 수. 최신 판정 기준(화면 buildDeliverableGroups 정합, 마이그레이션 249·250): **review_image 만 채널별** 최신 · **post·receipt 는 채널 무관 신청당 최신 1건** · **채널 미지정 review_image(legacy) 제외**. (248 은 (kind,post_channel) 일괄이라 채널 미지정·게시물 다채널을 과대 카운트 → 운영 배지 9·6 vs 목록 1 사례. 249=채널 미지정 제외, 250=post 채널 무관으로 최종 정합) `fetchPendingDeliverableCount` 가 이 RPC 호출. 화면 정합 헬퍼 `groupHasPendingReview(g)`(receipt 최신 pending OR monitor result_status_repr='pending'/채널별 최신 pending OR post 최신 pending, isCertExcluded 제외). **배지(숫자) 클릭 → 「검수대기만」 필터**(`_delivPendingOnly` + `openDelivPendingReview`, 다른 필터 초기화 후 검수대기 신청만) → 배지 숫자=목록 길이 일치. ⚠️ RPC는 배지 전용 SQL 재현(단일 소스 아님) — `buildDeliverableGroups` 판정 변경 시 함께 검토. gifting/visit 다채널은 근소 차이 감수(사용자 결정).
- **캠페인 진행현황**(캠페인 → 신청자 보기): 페인 상단 요약 카드 3종(개요[썸네일·제품·캠페인번호·타입/채널/판매가·기간] / 모집·결과물 현황[진행바 3종: 모집·제출·**인증 성공**(인증성공 인플/모집인원) + 신청/승인/심사중 보조] / 비용[조건부 — `isCampaignAdminOrAbove()` + 캠페인 `source_application_id` 존재 시만, 연결 신청 기준 견적·운영비(모집비 총액)·견적서 링크]) + 신청자 테이블에 OT 발송 체크박스(gifting/visit 승인 건만 활성) + 결과물 상태 요약(승인/검수대기/반려 건수 + 최신 상세 모달 링크). 진입은 ①캠페인 관리 목록 `○/○명` 버튼(뒤로가기→캠페인 목록) ②운영현황 브랜드 상세 미니카드 「상세」(`openCampApplicants(id, null, 'brand-ops')`, 뒤로가기→운영현황 브랜드 상세 복귀). 요약 렌더 `renderCampOpsSummary`/`campOpsOverviewCard`/`campOpsStatusCard`/`appendCampOpsCostCard`/`campOpsCostCard`(admin-applications.js). 운영현황 직접 진입 시 `allCampaigns` 미로드 대비 `fetchCampaigns()` 폴백. DB 변경 없음(기존 컬럼·`fetchBrandApplicationById` 재사용). 사양서 `docs/specs/2026-06-12-campaign-ops-detail.md`
- **영수증 필수 필드**(리뷰어 monitor): 인플 폼(제출)은 `order_number` + `purchase_date` + `purchase_amount` 3종 필수 유지. **관리자 검수 모달 인플레이스 수정은 3종 중 최소 1개만 입력하면 저장**(마이그레이션 178 — 운영자가 아는 정보만 부분 수정 가능, 인플 제출 경로와 무관). `renderReceiptInfoBlock(d)` 공통 헬퍼 + campaign_admin 이상 인플레이스 수정 + 「변경 이력 보기」 토글. 수정 모달에 `receipt_url` 있으면 「영수증에서 읽기」 버튼(`runReceiptOcrAdmin` — 업로드된 영수증 이미지를 기기 안 글자인식으로 읽어 **빈 칸만 채움**, 저장 전 확인 필수). **대리등록 모달**에도 같은 버튼(`runProxyReceiptOcr` — 선택한 영수증 이미지에서 읽어 주문번호·구매일·구매금액 빈 칸만 채움). `update_receipt_admin` RPC (SECURITY DEFINER, campaign_admin 가드, FOR UPDATE 행 잠금, 빈 항목 NULL 저장·3종 모두 빈값이면 거부). 변경 시 `receipt_edit_history` 자동 INSERT
- **엑셀 내보내기**: 단일 캠페인은 더보기 메뉴 `결과물 엑셀`/`신청자 엑셀`, 다중 캠페인은 목록 체크박스 + 「선택 N개 …엑셀」. ExcelJS CDN lazy-load, 영수증 이미지 셀 임베드. 50개+ confirm() + 5초 쿨다운 + 동시 진행 lock. 시트1 「캠페인 정보」 + 시트2 「결과물/신청자」. 이름 한자/가나 분리, SNS 핸들 → 공식 전체 URL, 우편번호 별도 컬럼. 공용 헬퍼는 `_excel*` 시리즈

### 브랜드 서베이 (광고주 신청 관리)
- **오리엔시트 발급·조회 페인**(`/admin#orient-sheets`, `dev/js/admin-orient.js`, PR3+PR5): 브랜드에게 작성 토큰 링크를 발급(`create_orient_sheet`, `is_admin()` 가드 — campaign_manager 포함 전체 관리자)하고 제출 내용을 확인. 목록(브랜드·타입·상태·발급일·작성기한·링크복사) + 발급 모달(브랜드 드롭다운·선택적 신청 연결[타입 자동 승계]·타입 선택[비우면 브랜드가 0단계에서 선택]·서베이 연결 시 제품 선택 행) + 상세 모달(data jsonb를 §11 스키마대로 한국어 렌더, 타입별 분기, 카테고리 code→라벨 변환, 이미지 링크 http/https만 허용). 링크 도메인 hostname 분기(운영 sales.globalreverb.com / 개발 sales-dev). 모달은 동적 생성. 만료는 클라 판정(조회 함수가 status 미전환). **상태 탭·배지**: 시트 1건=카드 N개라 카드별로 따로 캠페인 발행(마이그196). 카드 일부만 발행된 「부분 발행」은 「발행됨」 탭에 포함하고 「일부 발행 (n/m)」 앰버 배지로 구분(n=발행된 카드 수, `osCardCounts`). `osStatusKey` 우선순위 = consumed·부분발행 먼저 → expired 후판정(발행 시작한 시트가 토큰 만료돼도 발행됨으로 분류). 전 카드 발행 시 status='consumed'(「발행됨」 배지).
  - **발급 목록 삭제**(마이그레이션 199 `delete_orient_sheet` RPC): 목록 행 「삭제」 → 브랜드명 재입력 확인 모달(캠페인 삭제 패턴 미러, `osOpenDelete`/`osCheckDeleteConfirm`/`osExecuteDelete`). 분기는 발행 캠페인 연결 여부 — **연결 없으면**(미발행) 시트 행만 삭제(`is_admin`), **연결 있으면**(발행) 그 캠페인들에 신청(applications)이 1건이라도 있으면 차단(`blocked_has_applications`)·전부 0건이면 캠페인+시트 함께 삭제(`is_campaign_admin`, FOR UPDATE 잠금·트랜잭션). 발행 캠페인 N개는 `data.cards[].campaign_id` 순회 수집(DISTINCT). `storage.js` `deleteOrientSheet`. 신청 있는 캠페인 보호로 운영 데이터(인플 신청·결과물) 파괴 방지.
  - **발급 직후 메일 자동 발송**(PR6, 마이그레이션 198 + Edge Function `notify-orient-sheet`): 발급 성공 시 `osSubmitCreate`가 `sendOrientInviteMail(id)`(storage.js, `db.functions.invoke`)로 브랜드 담당자에게 작성 링크 메일 자동 발송. 발급 결과 화면에 발송 성공/실패/수신자 없음을 인라인 표시(`osCreateMailStatus`). Edge Function이 service_role로 수신자 결정(서베이 연결 건=`brand_applications.applicant_email`→`email` / 비-서베이=`brands` 대표 담당자 `contacts` is_primary→`primary_email`), 링크 도메인은 서버 환경변수 `PUBLIC_SALES_URL`(클라 조작 방지), Brevo 1통 발송, 메일 양식 `docs/email-templates/orient-sheet-invite.html`(브랜드 한국어·인플 4줄 푸터 의무 없음). **발송 성공 + 연결 신청 있으면** `advance_to_orient_sheet_sent(application_id)` RPC(198)로 신청 단계를 `orient_sheet_sent`로 **자동 전진**(안전장치 ①역행 방지=이전 5단계[new~kakao_room_created]일 때만 ②신청 연결 건만, FOR UPDATE·멱등·service_role 전용). 수신자 없으면 발급은 성공시키되 발송·단계전이 건너뜀. 발송 실패는 발급을 무효화하지 않음(throw 안 함). **재발송 버튼·발송 이력은 후속 조각**. 메일 발송 테스트는 운영에서만(supabase.md).
  - **발급 함수 확장**(마이그레이션 192, 190 3인자 DROP→4인자): `create_orient_sheet(p_brand_id, p_application_id, p_form_type, p_product_idx)`. `p_product_idx` 지정 시 서버가 `brand_applications`를 직접 읽어 **서베이 정보를 data에 prefill**(brand_name→`data.brand.name`, products[idx].name→`data.product.name`, reviewer면 url→`urls`/price→`prices`. **카테고리는 prefill 제외** — 서베이 products에 category 키 없음). 반려(`products[idx].status='rejected'`) 제품은 발급 차단(`reason:'product_rejected'`).
  - **발급 진입점 3개**(PR5): ①`#orient-sheets` 페인 「신규 발급」(브랜드·신청 자유 선택, 전체 조회 허브) ②브랜드 서베이 신청 목록 더보기 「오리엔시트 링크생성」(`osIssueFromApplication`→신청·제품 컨텍스트 고정·prefill, admin-brand.js) ③브랜드 상세 모달 「오리엔시트 발급」(`osOpenCreate({brandId,lockBrand})`, 비-서베이·빈 폼). `osOpenCreate(opts)` 파라미터화로 한 모달 재사용, 진입 시마다 컨텍스트 초기화.
  - brand-applications 신청 목록 「오리엔시트」 **통합 열**(`renderOrientCombinedCell` — 한 셀 2줄: 「시스템」 상태 배지+건수+최근 1건 작성링크 복사/열기 + 「구글시트」 외부 URL 열기/✎수정). 기존 「오리엔트 전달」(`orient_sheet_sent_url`)·「셀프 오리엔시트」 2열을 1열로 통합(2026-06-22, **데이터 무변경** — 컬럼·테이블 공존, 표시만 통합). 구글시트 줄만 `.brand-app-orient-cell` 로 감싸 인라인 ✎ 편집이 시스템 줄 보존. `fetchOrientSheets` 1회 조회 후 `_orientByApp` 그룹(N+1 회피), 시스템 N건은 최근 1건만(전체는 `#orient-sheets`). 사양서 `docs/specs/2026-06-18-brand-self-orient-sheet.md` §7·§14
- **현황 대시보드**(`/admin#brand-dashboard`): KPI 8개 + 견적 합계(예상·확정) + **전환 깔때기 10단계** + 폼·상태 도넛 2개 + 일별 추이 바차트(7/30/90일) + 최근 신청 5건 + 장기 대기(new 3일+) + Vercel Web Analytics 외부 링크
- **회사 관리 페인**(`/admin#companies`): 회사(`companies`) 마스터 CRUD + 브랜드 일괄 할당(4단 계층 회사>브랜드>신청>캠페인). 목록(회사명 한·일·브랜드 수·상태[한글 활성/보관]) + 상태 필터 + 검색. 추가/수정 모달(`name_ko` 필수, 담당자 입력칸 없음) + 브랜드 할당 모달(미분류 다중 체크박스) + 보관/복귀 + 소속 0건 시 완전 삭제. SELECT 모든 관리자(campaign_manager 는 읽기 전용)·CUD `is_campaign_admin()` 이상. **브랜드 관리 폼은 회사 드롭다운으로 `company_id` 연결**(2026-06-05 일원화 — 자유텍스트 `company_name` 입력 폐지·드롭다운 선택 회사명 동기화 보존, 신규 회사 인라인 등록은 회사 모달 중첩, 사업자번호·청구이메일은 읽기전용 카드, 담당자는 브랜드 정보로 이관). `dev/js/admin-company.js`. 사양서 `docs/specs/2026-05-13-brand-ops-redesign.md` PR 2 + `2026-06-04-brand-company-linking.md`
- **브랜드 삭제·병합·목록 보강**(2026-06-09): 브랜드 상세 모달에 「삭제」(연결 0건만 — `delete_brand` RPC, brands DELETE RLS 없어 RPC 필수, 카운터 CASCADE) + 「병합」(`merge_brands` RPC — **회사 무관**, 원본 신청·캠페인을 대상 브랜드로 이동 + 채번 재발급[옛 번호 `legacy_no` 보존·`numbering_legacy_map` UPSERT, 121 패턴 차용] + 비정규화 `brand/brand_ja/brand_en` 동기화 + 원본 `archived`, advisory_xact_lock 2단, `is_campaign_admin()`). 오병합 방지는 병합 모달의 대상 회사·캠페인 수 표시 + 되돌리기 불가 확인. 브랜드 목록에 「캠페인 수」 열(`fetchCampaignCountsByBrand`) + 회사명 `company_id` 기준 표시(보조 컬럼 `company_name` 미동기화 대비). 마이그레이션 174·175. 사양서 `docs/specs/2026-06-09-brand-delete-merge.md`
- **신청 관리**(`/admin#brand-applications`, **UI 라벨: "브랜드 서베이"**): 내부 용어·DB(`brand_applications`)·라우트·함수명은 `광고주 신청` 그대로. 영업팀이 비공개 URL(`sales.globalreverb.com/reviewer`, `/seeding`)로 받은 신청을 검수·견적 확정·상태 관리. 모든 관리자 접근(`is_admin()`)
- **리스트**: 필터(폼타입/상태/기간/검색) + pending(new) 배지 + 상세 모달(제품 테이블·견적·견적서 URL·OT 시트 URL·입금 날짜 `paid_at` 인라인 편집·제품별 multi-entry 메모·낙관적 락 version)
- **상태 전이 10단계**: `new → reviewing → quoted → paid → kakao_room_created → orient_sheet_sent → schedule_sent → campaign_registered → done` / `rejected`. "되돌리기"(any → new). 깔때기·드롭다운·통계 표시 순서는 실제 영업 워크플로에 맞게 정렬. `kakao_room_created`(카톡방 생성)는 입금 확인 후 카카오 단톡방 개설 가시화
- **제품별 메모**: 셀 ✎ 클릭으로 모달 진입. 분홍 배지 = 본인 미확인 메모 수 (`brand_application_memo_reads`). 모달 진입 시 자동 read. RPC `mark_brand_app_memos_read` 일괄 읽음 + `get_brand_app_memo_summaries()` 페어 단위 집계
- **입금여부 4종 칩**(`payment_flags jsonb`): {recruit, product, transfer, free}. products 변경 시 recruit/product/transfer 자동 재계산 트리거. free 키는 OLD 값 보존(관리자 명시 토글 보호). 무료모집 칩만 초록 톤(#E8F5E9/#16A34A)
- **가격체크**: `products[i].price_check` (`'higher'|'lower'|'equal'`, optional) — 마켓 등록 가격 vs 신청 금액 비교. 신청 목록 표 「가격체크」 드롭다운 컬럼 토글. 미선택이면 키 자체 없음
- **URL 입력 자동 prefix**: `normalizeBrandUrlInput(raw)` — 스킴 없는 입력(`example.com`) 에 `https://` 자동 prefix, 위험 스킴(javascript:, data:) 차단. 견적서/오리엔시트 셀 적용
- **엑셀 내보내기**: 페인 헤더에 엑셀 다운로드 버튼 — 신청일·신청번호·폼타입·업체/브랜드명·담당자·이메일·연락처·세금계산서 주소·예상견적·상태
- **운영 현황 페인**: 회사 기반 브랜드 카드 그리드. `get_brand_ops_overview(p_company_id)` 집계 + alert_level 4단계(danger/warning/caution/normal). 카드 **사유 배너**(정상 외 카드 한정) — 서버 `alert_reasons text[]`(조건 코드) + `soonest_deadline`/`d1_count` 를 화면(`brandOpsAlertReasonLines`)이 한국어 문구로 조립(예: 「모집률 28% · 마감 5일 남음」). 브랜드 상세는 `get_brand_ops_detail(p_brand_id)` jsonb 통합 반환. 사양서 `docs/specs/2026-05-13-brand-ops-redesign.md`
- **캠페인 ↔ 신청 연결/해제**: `link_campaign_to_application` / `unlink_campaign_from_application` RPC. 같은 brand_id 검증 후 채번 재발급 + `legacy_no` 콤마 누적 + `numbering_legacy_map` UPSERT. 동시성 `pg_advisory_xact_lock` 2단 잠금. 멱등성 `unchanged:true` 반환. 가드 `is_campaign_admin()` 이상

### 인플루언서 관리
- **목록**: 채널/인증/위반 드롭다운 + 주소지(도도부현 다중선택, 「未登録」·「海外」 포함) + 팔로워 범위(채널 선택 기준, 전체=4채널 합계) sticky-header + 통합 검색 + 결과 인원수 표시. 주소지·팔로워는 클라 in-memory 필터(`getFilteredInfluencersForView` 공통 함수 — 화면·엑셀 공용, 채널 선택 시 그 채널 등록자만 행 필터. `classifyPrefecture`/`followerValueByChannel` 헬퍼, admin-core.js). SNS 채널 선택 시 열은 항상 전체 10컬럼 고정
- **엑셀 내보내기**(`exportInfluencersExcel`, admin-excel.js): 현재 필터 결과(화면에 보이는 대상)를 .xlsx 로. 기본 16열(이름 한자/가나·이메일·대표SNS·4채널 핸들+팔로워·합계·도도부현·시군구·등록일). 민감정보 6열(전화·LINE·PayPal·우편번호·건물·상세주소)은 `canRead('influencer.excel_sensitive')` 권한 등급 + 「민감정보 포함」 체크 시만(PR3-D 동적 권한). ✅ **민감정보 서버 실차단(PR3, 마이그레이션 212·213)**: `fetchInfluencers`/`fetchInfluencersByIds` 가 가림막 뷰 `influencers_admin_view`(security_invoker) 경유로 바뀌어, `has_permission('influencer.sensitive_pii','read')` 가 false 인 등급(마이그213으로 campaign_manager=hidden)에게 6종을 **서버에서 NULL 마스킹**(네트워크 응답 자체에서 제거) + 존재 여부 불리언 `has_phone`/`has_line`/`has_paypal` 로 배지·필터 동작. 화면 마스킹 칸은 「열람 권한 없음」. 레거시 `pw`·`bank_*` 6컬럼은 마이그레이션 216 에서 완전 삭제(뷰에서도 이미 제외돼 있었음). 신청자·결과물 엑셀도 뷰 경유로 자동 마스킹. ✅ 엑셀 「민감정보 포함」 게이트는 `canRead('influencer.excel_sensitive')` 동적 권한으로 일원화(PR3-D 완료 — 화면 열람 `sensitive_pii` 와 **별개 항목**이라 엑셀 대량 유출을 화면 열람과 따로 통제 가능). 기존 `_excel*` 헬퍼 + ExcelJS lazy-load 재사용
- **상태 관리**(인증/위반/블랙리스트): 상세 모달에 상태 관리 카드 + 관리자 이력 카드 (사유별 누적 pill + 타임라인 + 위반 행 편집). 인증/위반 배지는 이름 옆 노출 (블랙일 땐 블랙 단독). 사유는 `blacklist_reason` ∪ `violation_reason` lookup 통합. 증빙 파일 업로드 (`influencer-flag-evidence` 비공개 버킷, 10MB, image/PDF). RPC: `setInfluencerVerified`/`setInfluencerBlacklist`/`recordInfluencerViolation`/`updateInfluencerViolation` (evidence_paths 미변경=null, 전체 삭제=[])
- **전화번호 표시 포맷**(`formatPhoneDisplay` in ui.js): KR/JP 번호 정규화 (11자리 3-4-4, 10자리 02/03/06 → 2-4-4 else 3-3-4, `+81`/`+82` 지원). 매칭 실패 시 원문 폴백

### 아웃바운드 인플루언서 명단 (추천 도구 1·2단계, dev — 운영 미배포)
- **별도 입구 URL + 화면 이동 드롭다운**(`/admin/?app=outbound`, `dev/admin/app.js`): 영업팀 전용 진입 경로. 이 쿼리로 접속하면 「아웃바운드 전용 모드」 — 사이드바에서 아웃바운드·내계정 외 항목·그룹 라벨 숨기고 첫 화면을 명단으로. 같은 앱·같은 로그인(기존 관리자가 두 모드 오감). 화면 전환은 **사이드바 하단 드롭다운**(`#adminScreenSelect`, 패밀리 사이트 셀렉터형) — 전체 관리자 화면 / 인플루언서 추천 명단(전용) / 인플루언서 화면 3종을 `switchAdminScreen(target)`로 이동, `setAdminScreenSelect()`가 현재 모드로 현재값 반영. `window._adminAppMode`+`applyOutboundModeUI()`. **보안 경계 아님**(데이터는 `outbound.view` RLS 서버 강제) — 화면 배치 제어. 권한 없는 campaign_manager 진입 시 모드 해제+대시보드(빈 화면 함정 방지). DB 변경 없음. 페인 전환 시 쿼리 유지(`pushState('#'+pane)` 프래그먼트 상대참조).
- **명단 관리 페인**(`/admin#outbound`, `dev/js/admin-outbound.js`): 영업팀이 직접 컨택하는 아웃바운드 시딩·타이업 인플루언서 명단(globalreverb 미가입·별도 자산). 기존 `influencers`(auth 계정 1:1·정산/마스킹 결합)와 **분리한 신규 테이블**. 상단 「명단 / 조건 추천」 전환 탭. **명단 탭**: 목록(대표이미지·한글명·계정·계열·세분·등급·주채널 팔로워·가용상태 배지·소속사, IntersectionObserver lazy-load) + 필터(계열/세분 다중·등급·가용상태·채널·검색) + 등록/편집 모달(채널4·가격5[엔화 ¥·내부전용]·대표게시물 링크 최대5·대표이미지 업로드) + 삭제(네이티브 confirm — 편집 모달 열린 채라 z-index 회피). 세분→계열 자동채움(`OB_CATEGORY_SERIES` shared.js). 권한 `outbound.view`(campaign_admin=write/campaign_manager=hidden). 이미지 업로드 스모크 검증 완료(2026-07-14, 마이그229 `has_permission` Storage 정책 정상, 폴백 불필요).
- **조건 추천 탭**(2단계, `runOutboundRecommend`·`outboundRecoScore`·`compareOutboundReco`): 계열(필수)·모집형식·등급·채널·예산(¥)·인원수·광고목적 입력 → **DB 변경 없이** 전건 조회 + 클라 계산. **계열 필수 필터 + `is_active=false` 제외** → 점수(`RECO_SCORE`: 등급 정확 +30/인접 +10[`sort_order`], 채널 보유 +20, 예산 내 +20/초과 −20/미상 0) 정렬. 정렬 순서 ①가용 그룹(available 먼저·조율중/불가 하위) ②점수 내림 ③주채널 팔로워 내림 ④등록 최신. 예산은 선택 모집형식 단가(`price_feed/reels/story/tiktok`)로 판정·형식 미선택 시 축 제외. 광고목적은 입력만(정렬 미반영·3단계 성과 후 활용). 결과=전체 정렬 + 요청 인원 N번째 뒤 구분선 + 점수 근거 배지(콜드스타트 신뢰). 성과 기록=3단계·성과 반영=4단계·브랜드 뷰=5단계(미착수). 사양서 `docs/specs/2026-07-08-influencer-recommendation.md` §구현 결과 + HANDOFF `2026-07-09-influencer-recommendation-stage1-handoff.md`

### 기준 데이터·번들·관리자 계정
- **기준 데이터 관리**(`/admin#lookups`): 채널/카테고리/콘텐츠 종류/NG 사항/반려사유/블랙리스트·위반 사유/주의사항/취소 사유/메일 종류 등을 한국어·일본어로 관리(campaign_admin 이상). 항목 활성/비활성 토글, 순서 변경 모드, 사용 중이면 hard delete 차단(soft delete 만). 채널은 모집 타입(monitor/gifting/visit) 다중 지정. code 는 자동 생성·UI 비공개
- **자주 묻는 질문(FAQ) 관리**(`/admin#faq`, campaign_admin 이상, 운영 배포 완료 2026-05-28): 응모건 메시지 자동응답 등록 페인. 좌우 2단(카테고리 | 질문 + 측정 배지[조회수·직접문의 전환수]) + 편집 모달(한/일 2열·화면이동 드롭다운·handoff·단계 다중선택·미리보기)
- **주의사항 번들**(`caution_sets`): 캠페인 등록/편집 폼 콘텐츠 가이드 섹션에 "주의사항" 영역 — 번들 드롭다운(recruit_type 필터) + "번들 다시 불러오기" + 인라인 items 편집(본문 한/일, 선택적 링크). 캠페인 저장 시 스냅샷 복사. 신청 행 메시지 셀에 "주의사항 동의 ✓ {시각}" 작은 배지 (msgCell 헬퍼)
- **참여방법 번들**(`participation_sets`): 1~6단계 묶음, 각 단계 title/desc ko·ja, recruit_types[] 태깅으로 필터링. 캠페인 저장 시 스냅샷 복사(`participation_steps jsonb`). 인라인 개별 수정 + "번들 다시 불러오기" 지원
- **NG 번들**(`ng_sets`): caution_sets 패턴 미러링. items 구조 `{html_ko, html_ja}` 2필드 (DOMPurify sanitize, inline 서식만). `campaigns.ng_set_id` + `ng_items jsonb` 스냅샷. 인플루언서는 jsonb 우선 + legacy `campaigns.ng` 폴백
- **민감 항목 변경 경고**: 캠페인 편집에서 `caution_items`/`participation_steps`/`ng_items` 변경 시 `#sensitiveChangeModal` 경고. closed 캠페인은 변경 차단 트리거. 변경 이력은 `campaign_caution_history` audit 자동 기록 → 더보기 메뉴 「변경 이력」(super_admin) 타임라인 + 인플 응모이력 「現在の文言と比較」 토글
- **변경 이력 = 전체 항목**(2026-07-27, 마이그레이션 265·266): 더보기 「변경 이력」 모달이 주의사항·참여방법·NG 3영역뿐 아니라 **제목·기간·리워드·모집 인원·이미지·상태 등 48개 항목**의 변경을 함께 보여준다(두 소스를 시각 역순 한 목록에 병합). 카드는 날짜(좌) + 「주의사항 수정 1개·참여방법 추가 2개」 요약(중) + 변경자·신청자 수·저장 방식(하), 펼치면 3영역은 항목별 차이, 그 외 항목은 「항목 / 이전 / 이후」 표(한국어 라벨·코드값 한글화·이미지 썸네일). **내용이 실제로 바뀌지 않은 기록은 목록에서 접어 둠**. 라벨·값 변환은 `CAMPAIGN_FIELD_LABELS`/`campaignFieldValueText`(shared.js), 렌더는 `campaignChangeCardHtml`/`campaignChangeRowHtml`(admin.js). 열람은 여전히 super_admin 하드코딩 — 권한 관리 화면 일원화는 후속
- **변경 이력 차이 표시**(2026-07-27): 이력 모달이 전/후 목록을 좌우 2단으로 통째 나열하던 것을 **항목 단위 차이 목록**으로 교체(`renderSensitiveDiffSection`/`renderSensitiveDiffRow`, admin.js). 항목별 라벨 추가(초록)·삭제(빨강 취소선)·수정(변경 전 취소선 + 변경 후, 바뀐 글자만 노란 강조)·서식만 변경(원문 보기 토글)·같음(기본 접기). 한국어·일본어 둘 다 표시(한쪽만 고친 변경도 드러남). 문구는 그대로고 번들만 교체된 건은 「문구 변경 없음」 배지 + 안내 한 줄로 정리(사용자가 "뭐가 바뀐지 모르겠다" 한 사례). 비교 엔진은 `dev/lib/shared.js`(`diffSensitiveItemLists`/`diffChars`/`richToPlainText`/`textSimilarity`/`sensitiveListsIdentical`) — **리치 HTML에 강조 태그를 직접 끼우지 않고** sanitize→순수 텍스트→글자 비교→이스케이프 후 강조 순서 고정(태그 붕괴·저장형 XSS 차단). 글자 비교 상한 1200자·변경 비율 60% 초과 시 전문 비교로 자동 폴백. DB 변경 없음. 저장 전 경고 모달(`#sensitiveChangeModal`)은 아직 옛 2단 표시 — 후속
- **편집 모달 분리**: 참여방법/주의사항 편집을 별도 모달로. 카드 헤더 「편집」 버튼 1개로 정리, bundle summary 카드 한·일 양언어 풀 노출, 注意事項 미리보기 한·일 토글
- **오픈 예정 기능 보드**(`/admin#upcoming`, 사이드바 「공지」 섹션, 관리자 전원 읽기 전용): 곧 시행될 기능명·상세설명·D-day를 카드로 안내(시행일 가까운 순). 데이터는 **DB 미사용** — `dev/lib/shared.js`의 `UPCOMING_FEATURES` 코드 상수(개발 세션이 시행일 걸린 기능 배포 시 1줄 등록 → "개발 완료된 것만 노출" 자동 보장). D-day 배지 3종(예정 D-n 주황 / 당일 D-DAY 핑크 / 시행 완료 초록·시행 후 14일 유지 후 자동 숨김). 시각 판정은 클라 `+09:00`(한국/일본 표준시) 날짜 단위. ⚠️ **예고판일 뿐 기능 스위치 아님**(실제 활성은 별도, `effectiveDate`는 그 기능 실제 시행일과 단일 소스). 렌더 `dev/js/admin-roadmap.js`, 헬퍼 `upcomingFeatureStatus`/`Dday`/`visibleUpcomingFeatures`(shared.js). 사양서 `docs/specs/2026-06-15-admin-upcoming-features-board.md`
- **관리자 공지사항**(`/admin#admin-notices`): 사이드바 최상단 + 미읽음 건수 배지. 카테고리 4종(system_update/release/warning/general), 상단 고정(push_pin), Quill 리치텍스트 + HTML source 토글. **draft/published 분리** — 신규·draft 편집은 `[초안 저장][게시하기]`, published 편집은 `[게시 유지하며 저장][초안으로 되돌리고 저장]`. 보기 모달에 작성자/super 한정 `[지금 게시]`/`[게시 회수]`. 노출 채널 4개(사이드바 배지·로그인 팝업·대시보드 카드·목록 default)는 published 만. 로그인 시 미읽음 팝업 자동. 공지 초안 생성은 `/공지초안-관리자` 슬래시 커맨드
- **관리자 계정**: 3단계 권한(super_admin > campaign_admin > campaign_manager)
  - **추가**(2026-07-20 개편): super_admin 이 이메일+이름+역할 입력 → `invite_admin()` RPC 로 계정 생성 → Edge Function `notify-admin-invite` 가 **서버에서** 비밀번호 설정 링크 발급(`auth.admin.generateLink`) + 관리자 전용 **한국어** 메일 Brevo 발송 → 받은 사람이 자립형 페이지 `/admin-setpw.html` 에서 직접 설정. 재발송은 목록 「초대 메일」 열 「재발송」 버튼(super_admin 전용, 서버도 동일 가드) 또는 「비밀번호」 모달(`mode=reset`). `storage.js` `sendAdminInviteMail(email, mode)`
    - **발송 상태 표시**(마이그레이션 244): 목록 「초대 메일」 열 = `invite_completed_at` 있으면 「설정 완료」(초록) / `invite_mail_sent_at` 있으면 「발송 {날짜}」 / 없으면 「미발송」. 발송 시각·수신 주소는 Edge Function 이 발송 성공 시 기록, 설정 완료 시각은 자립형 페이지가 비밀번호 변경 직후 본인 행 UPDATE(`.is(null)` 로 최초 1회만)
    - **기존 계정 승격 시 비밀번호 보존**(마이그레이션 245): 이미 인플루언서로 가입한 이메일을 초대하면 관리자로 승격되는데, **기존 비밀번호를 덮어쓰지 않는다**. 245 이전에는 임시 비밀번호로 덮어써서 그 사람의 인플루언서 로그인이 즉시 끊기고 메일이 스팸함으로 가면 계정에서 잠기는 문제가 있었음. `admins.promoted_at` 에 승격 시각 기록 → 초대 메일 문구가 분기됨(승격자는 「권한이 추가되었습니다 · 기존 비밀번호로 로그인 가능」, 신규는 「비밀번호를 설정해 주세요」)
    - ⚠️ 이메일로 `admins` 를 조회할 땐 **`ilike` + 와일드카드 이스케이프**(`escapeLikePattern`). `eq` 는 대소문자를 구분해 대문자로 저장된 이메일을 못 찾고, 이스케이프 없는 `ilike` 는 `_`·`%` 가 든 이메일이 다른 관리자 행에 매칭된다
    - ⚠️ **Auth 기본 발송(`resetPasswordForEmail`)을 안 쓰는 이유**: ①메일 템플릿이 종류별 1개라 인플루언서 비밀번호 찾기와 문구를 공유 ②`flowType:'pkce'` 라 코드 교환 검증값이 **호출한 브라우저**(super_admin)에 저장돼, 링크를 여는 초대 대상의 다른 브라우저에서는 교환이 실패한다(초대가 구조적으로 성공 못 하던 원인). 서버 발급은 이 문제가 없음
    - **랜딩이 `/admin/` 밖 자립형 페이지인 이유**: 관리자 앱은 세션 없으면 인플루언서 앱으로 리다이렉트(`dev/admin/app.js`)라 로그인 전 화면을 둘 수 없음. `dev/admin-setpw.html` → `build.sh` 가 루트로 복사. 사양서 `docs/specs/2026-07-20-admin-invite-mail-and-setpw.md`
  - **관리자 전용 로그인 화면**(`/admin-setpw.html?mode=login`): `/admin/` 에 세션 없이 진입하거나 세션이 만료·로그아웃되면 이 화면으로. 예전엔 인플루언서 앱 `/#login`(일본어)으로 보냈다. 인플루언서 앱에서 관리자가 로그인하면 `/admin/` 으로 가는 기존 경로(`auth.js`)는 하위호환으로 보존. 관리자 아닌 계정으로 로그인하면 안내 + 인플루언서 앱 이동 버튼(막다른 골목 방지). ⚠️ **`persistSession` 이 모드마다 다름** — `login`=true(세션이 저장돼야 `/admin/` 으로 이어짐) / `setpw`=false(링크 착지 임시 세션이 두 앱과 저장소를 공유해 「비밀번호 바꾸기 전에 다른 탭에서 `/admin/` 이 열리는」 사고를 막음) + 설정 완료 후 `signOut()`
  - **관리자 비밀번호 찾기**(로그인 화면 「비밀번호를 잊으셨나요」 → forgot 서브 화면): 익명 Edge Function `admin-password-reset-request` 가 관리자면 링크 발급·발송, 아니면 아무것도 안 함. **계정 존재 여부 비노출** — 관리자든 아니든·요청 제한에 걸렸든·오류가 났든 응답·소요시간·화면이 모두 동일(모든 분기가 `{ok:true}` 200 + 최소 2500밀리초 대기로 수렴). **요청 제한 3층**(이메일별 5분1회·24시간5회 / 전역 시간당 30건 / 형식·길이) — Auth 자체 발송이 아니라 Brevo 직접 발송이라 Auth Rate Limits 보호를 못 받고, 한도 소진 시 광고주 알림·다이제스트까지 함께 죽기 때문. 제한에 걸려도 「요청이 많습니다」를 노출하지 않음(그 문구가 곧 실재 신호). 마이그레이션 243
  - **삭제 2택**: `remove_admin_role` (권한만 해제, 인플루언서 계정 유지) / `delete_admin_completely` (auth/influencers/applications/receipts cascade). 자기 자신 삭제 불가
- **메일 수신 설정**(`/admin#admin-accounts`): 각 행 「메일받기」 셀에 켜진 메일 종류 회색 칩 + 「설정」 버튼. 모달에서 메일 종류별 체크박스 일괄 on/off. `admin_email_subscriptions` 테이블 + `lookup_values(kind='admin_email_kind')` 카탈로그. super_admin 은 다른 관리자 설정도 편집, 그 외는 본인만. 신규 메일 종류 추가는 `lookup_values` 한 줄 추가만으로 가능
- **내 계정**: 이름/비밀번호 변경
- **감사용 계정 메커니즘**(마이그레이션 179·181, 운영 모니터링용): 운영팀이 인플루언서 동선을 본인 입장에서 시뮬레이션하는 공용 계정(`influencers.is_audit=true`). 응모수(`get_campaign_application_counts`)·슬롯(`check_monitor_slots`)·카드 「N명 신청」(`recompute_campaign_applied_count`)·대시보드 KPI(`loadAdminData` statsUsers/statsApps)·운영현황(`get_brand_ops_overview`/`detail`)에서 격리 → 실 운영 통계·산출물 영향 0. 6개 관리자 페인(인플/신청/캠페인별 신청자/결과물/대시보드 최근/메시지)에 「감사용」 주황 배지(`auditBadgeHtml`, shared.js). 엑셀 5개 export 다운로드 시 결과에 감사용 포함되면 「포함/제외」 확인 모달(`confirmAuditExport`, 0명이면 생략·세션 캐시). 흔적 청소 RPC 2종(`purge_audit_data_all`/`purge_audit_data_for_campaign`, super_admin 한정, Storage 파일 경로 반환→클라 후속 삭제): 캠페인 더보기 메뉴 「감사용 흔적 청소」 + 인플 상세 모달 「전체 청소」 섹션(감사용 행만). `fetchInfluencers(opts)` 기본 `includeAudit:true`(기존 동작 보존), 통계·엑셀만 false 명시. 사양서 `docs/specs/2026-05-28-audit-influencer-account.md`
- **오류 로그**(`/admin#errors`, 마이그레이션 165, `dev/js/admin-errors.js`): 사용자(인플루언서) 앱에서 발생한 오류를 모아 보는 페인. 사이드바 「오류 로그」(미해결 건수 빨강 배지). 목록(상태[기본 미해결]·앱·기간 필터 + 메시지/코드 검색 + lazy-load) + 상세 모달(전체 메시지·스택·발생정보 + 해결/무시/메모, `resolve_client_error` RPC). 개인정보는 수집 단계에서 마스킹됨. 수집은 인플 앱 `error-report.js`(전역 핸들러 + friendlyErrorJa 훅)
- **에러 처리**: `friendlyError()` 한국어 메시지 + 에러 코드
- **상태 뱃지**: `getStatusBadgeKo()` 한국어 상태 표시

## Database Schema (Supabase)

### 캠페인·신청·결과물
- `campaigns` — 캠페인 정보. 핵심 컬럼:
  - 기본: `title`, `brand`, `brand_ko`, `product`, `product_ko`, `type`, `channel`, `channel_match`('or'|'and'), `category`, `reward`, `reward_note`, `slots`, `min_followers`, `status`, `view_count`, `img1~img8`
  - 일정: `recruit_start date NULL`(NULL 이면 인플 화면 "오늘 ~ 마감" 폴백), `deadline`, `purchase_start/end`(monitor), `visit_start/end`(visit), `submission_end`
  - 번들 스냅샷: `participation_set_id`/`participation_steps jsonb`, `caution_set_id`/`caution_items jsonb`, `ng_set_id`/`ng_items jsonb`
  - 채번: `campaign_no`(현행 계층 채번), `legacy_no`(콤마 누적), `brand_id` FK, `source_application_id` FK
  - `reward_note` 는 리워드 금액 외 추가 안내(지급 조건·정산 시점) 자유 텍스트
  - 홍보 메일 트리거: `first_active_at timestamptz NULL` — 캠페인이 처음 active 상태로 전환된 시각. `BEFORE UPDATE OF status` 트리거(`_record_first_active_at`)가 자동 기록(이후 불변). 캠페인 홍보 메일이 「어제 KST 신규 캠페인」 판별에 사용 (마이그레이션 140)
  - 동시 저장 방어(마이그레이션 275): `version integer NOT NULL DEFAULT 1` — 관리자 두 명이 같은 캠페인을 동시에 저장할 때 나중 저장이 앞 저장을 조용히 덮는 것을 막는다. `campaigns_bump_version()` BEFORE UPDATE 트리거가 **제외 6개**(`version`·`updated_at`·`view_count`·`applied_count`·`order_index`·`first_active_at`)를 뺀 나머지가 실제로 바뀐 경우에만 +1. 새 컬럼이 생기면 자동으로 보호 대상이 된다(fail-closed). 편집 저장은 `updateCampaign(id, updates, expectedVersion)` 으로 `.eq('version', …)` 조건부 UPDATE → 0행이면 충돌 안내 후 목록 복귀. ⚠️ **코드보다 마이그레이션을 먼저 적용해야 한다**(순서가 바뀌면 컬럼이 없어 자동 마감이 실패). ⚠️ 자동 상태 전이(`autoOpen/Close/EndCampaigns`)도 version 을 올리므로 로컬 캐시를 `.select('version')` 으로 함께 갱신한다 — 안 하면 그 캠페인 편집 시 **가짜 충돌**
  - 오리엔시트 발행 보조(마이그레이션 197): `proxy_purchase boolean NOT NULL DEFAULT false`(가구매 캠페인 — 영수증만, 리뷰/게시 인증샷 미요구. 인증성공 판정·인플 활동관리·엑셀이 분기) + `emergency_publish_reason`/`emergency_published_by`/`emergency_published_at`(일본어 미보완 긴급 발행 기록)
- `campaigns_yearly_counter`, `brand_seq_counter`(싱글톤), `brand_application_counter`, `application_campaign_counter`, `brand_external_campaign_counter` — 채번 카운터. SECURITY DEFINER 트리거 전용 (직접 UPDATE 금지)
- `numbering_legacy_map` — 신구 채번 양방향 매핑
- `applications` — 캠페인 신청. `user_id`, `campaign_id`, `message`, `address`, `status`(pending/approved/rejected/cancelled), `reviewed_by`, `reviewed_at`, `oriented_at`(OT 발송 체크 수동 토글), `reviewed_version`(낙관적 락), `caution_agreed_at`, `caution_snapshot jsonb`. 취소 보조 5종: `cancelled_at`, `cancel_reason`, `cancel_reason_code`, `cancel_phase CHECK(recruit|purchase|visit|post|other)`, `previous_status`. `(user_id, campaign_id)` partial unique index (cancelled 행 제외) → 같은 캠페인 재응모 가능
- `deliverables` — 결과물 통합. `kind`('receipt'|'review_image'|'post'), `status`, `receipt_url`/`purchase_date`/`purchase_amount`/`order_number`(receipt), `post_url`/`post_channel`/`post_submissions`(post), `reject_reason`, `reviewed_by`/`reviewed_at`, `version` 낙관적 락. **관리자 대리 등록 4종**: `submitted_by_admin`(NULL=본인 제출, NOT NULL=대리 등록 식별) + `submitted_by_admin_reason_code`/`_reason`/`_at`. 부분 인덱스 `idx_deliverables_proxy WHERE submitted_by_admin IS NOT NULL`. **대리 등록 RPC `admin_create_deliverable_proxy` post 분기(마이그레이션 182)**: ①캠페인 요구 채널과 불일치 시 차단(서버 최종 방어선, channel 빈값이면 우회) ②같은 채널 반려·검수대기 게시물이 있으면 새 URL 로 **교체**(UPDATE, status='approved', post_submissions 누적, version+1) — 승인된 게시물은 교체 차단. 인플 본인 재제출(`insertDraftDeliverable`)의 채널별 교체와 정합. **잘못된 채널 잔존 행 정리**(마이그레이션 183, `delete_mismatched_post_deliverable`): 검수 화면에 캠페인 요구 채널과 다른 post_channel 행 「채널 불일치」 경고 배지 + super_admin 수동 삭제(`deleteMismatchedPostRow`). 반려·검수중·draft만 삭제(승인 행 보호), post_channel NULL·정상 채널 행은 서버 거부, deliverable_events CASCADE로 audit 함께 소멸
- `deliverable_events` — 결과물 상태 변경 이력. `action`(submit/resubmit/approve/reject/revert/admin_proxy_submit/admin_proxy_revoke/channel_assign/channel_unassign), 트리거/RPC 만 INSERT. **주의**: `deliverable_id` 에 `ON DELETE CASCADE` 가 걸려 있어 대리 회수가 결과물 행을 DELETE 하면 `admin_proxy_revoke` audit 도 함께 삭제됨(영구 감사 필요 시 별도 테이블 권장)
- `application_events` — 신청 status 변경 audit(운영자 액션 한정). `action`(approve|reject|revert_to_pending). `trg_application_status_event` 트리거가 자동 INSERT. 본인 취소·재응모는 미경유. RLS SELECT `is_admin()`, INSERT 트리거만
- `receipt_edit_history` — 관리자 영수증 수정 감사. `deliverable_id` FK CASCADE + 3종 prev/next 스냅샷(주문번호·구매일·구매금액). RLS SELECT `is_admin()`, INSERT 는 `update_receipt_admin` RPC 만
- `campaign_caution_history` — 캠페인 주의사항/참여방법/NG 변경 audit. 각 prev·next + `app_count_at_change`. RLS SELECT super_admin, INSERT 는 `record_caution_history()` RPC 만(campaign_admin 이상)
- `campaign_change_history` — **캠페인 전체 항목 변경 audit**(마이그레이션 265·266, 2026-07-27 운영 적용). 「바뀐 항목 1개 = 1행」 + `change_group_id`(저장 1회 묶음) + `changed_by`/`changed_by_name`/`changed_at` + `field_name`(CHECK 로 화이트리스트 **48개** 고정) + `old_value`/`new_value` jsonb + `change_source`(admin 사람 / auto 자동 전이 / system 세션 밖). RLS SELECT `is_super_admin()`, 쓰기 정책 없음(트리거만). 기록은 `record_campaign_change_history()` AFTER UPDATE 트리거 — 브라우저 호출 방식(구 `record_caution_history`)과 달리 같은 트랜잭션이라 감사 공백이 없다. ⚠️ **화이트리스트는 세 곳(265 CHECK · 266 v_fields · 266 트리거 `AFTER UPDATE OF` 목록)이 항상 같은 집합**이어야 함 — 어긋나면 CHECK 위반으로 INSERT 실패. 조회수·신청 수·수정일·표시 순서·채번은 제외(실사용 트래픽마다 쌓이는 것 방지). 주의사항·참여방법·NG 3영역은 기존 표 유지(병존). 사양서 `docs/specs/2026-07-27-campaign-full-change-history.md`

### 광고주 신청(브랜드 서베이)
- `brand_applications` — `form_type`(reviewer|seeding), `brand_name`, `contact_name`, `phone`, `email`, `billing_email`, `products jsonb`, `total_jpy/total_qty`, `estimated_krw`, `final_quote_krw`, `quote_sent_at`, `quote_sent_url`, `orient_sheet_sent_at`(timestamptz), `orient_sheet_sent_url`, `paid_at`, `payment_flags jsonb`, `status` 10단계 파이프라인, `request_note`, `version` 낙관적 락, `legacy_no`. **익명 INSERT 는 `submit_brand_application()` RPC(SECURITY DEFINER, BYPASSRLS) 경유 필수** — 직접 INSERT 는 42501
- `brand_application_memos` — 신청 메모 (multi-entry). `application_id` FK CASCADE, `product_idx integer NOT NULL DEFAULT 0`, `memo`, `created_by`/`created_by_name`/`created_at`
- `brand_application_memo_reads` — 메모 본인 읽음 기록. (memo_id FK CASCADE, auth_id) PRIMARY KEY
- `brand_application_history` — 광고주 신청 변경 audit
- `brand_app_daily_counter` — 일자별(JST) 채번 카운터. SECURITY DEFINER 트리거 전용
- `brand_survey_settings` — 공개 제출 차단 설정 싱글톤(id=1). `submissions_open boolean DEFAULT false`(false=공개 제출 차단, 기본). RLS SELECT `is_admin()` / UPDATE `is_super_admin()`(임시 재개 토글) / INSERT·DELETE 정책 없음. **`submit_brand_application` 0단계 가드가 이 값을 읽어 차단 시 `RAISE 'submissions_closed'`(P0001)** — 폼은 `error.message==='submissions_closed'` 로 분기. anon 조회용 `is_brand_survey_open()`(SECURITY DEFINER, bool만 노출)로 sales 폼이 로드 시 차단 안내. 관리자 예외 입력은 `admin_create_brand_application`(091). 마이그레이션 206. 사양서 `docs/specs/2026-06-30-brand-survey-submit-lock.md`. ⚠️`submissions_open=true` 임시 재개 시 `sales/index.html`(정적 차단 안내)도 함께 되돌려야 함(reviewer/seeding 폼은 `is_brand_survey_open()` 동적 반영)
- `companies` — 회사 마스터 (1개 회사 = N 개 brands, 4단 계층: 회사 > 브랜드 > 신청 > 캠페인). `name_ko`(NOT NULL)/`name_ja`/`name_en` + `name_normalized` UNIQUE NOT NULL(자동 정규화 트리거) + `business_no` + `address` + `homepage_url` + `billing_email`/`billing_address`/`memo` + `status CHECK(active|archived)` + `total_brands` 자동 재계산(`recalc_company_total_brands` 트리거, `brands.company_id` 변경 시). RLS SELECT `is_admin()`, CUD `is_campaign_admin()` 이상. ⚠️ `name_normalized` 정규화는 마이그레이션 119에서 공백 압축 포함 패턴(`lower(trim(regexp_replace(name_ko,'\s+',' ','g')))`)으로 교체됨(118 원본은 공백 압축 없음)
- `brands` — 브랜드 마스터. `name`, `name_normalized`(자동 정규화), `brand_seq` UNIQUE, `company_id` FK ON DELETE SET NULL
- `get_brand_ops_overview(p_company_id uuid)` — 운영 현황 22컬럼 집계 RPC (`SECURITY DEFINER + SET search_path='' + is_admin()`). 마이그레이션 148 로 `alert_reasons text[]`(alert 발생 조건 코드 배열) + `soonest_deadline date` + `d1_count bigint` 출력 추가(카드 사유 배너용). 임계값은 120 기준 유지, `flag_agg` CTE 로 임계값 1회 계산 후 alert_level/alert_reasons 가 동일 플래그 재참조. 마이그레이션 181 로 `app_agg`·`deliv_agg` 에 `JOIN influencers AND is_audit=false` 추가(감사용 응모·결과물 격리 — 광고주 보고 수치 보호)
- `get_brand_ops_detail(p_brand_id uuid)` — 브랜드 상세 jsonb 통합 RPC. 마이그레이션 149 로 캠페인 항목(신청 내부·외부 양쪽)에 `channel`/`channel_match`/`img1`(썸네일)/`recruit_start`/`submission_end` + 결과물 집계 `approved_app_count`(승인 신청 수)·`deliv_submitted_inf`(결과물 제출 distinct 인플)·`deliv_total`·`deliv_approved` 추가. 마이그레이션 150 으로 `purchase_start`/`purchase_end`(리뷰어 구매기간)·`visit_start`/`visit_end`(방문형 방문기간) 4키 추가. 미니카드가 모집률·제출률(제출인플/승인인플)·**인증 성공률(인증성공 인플/모집인원)** 3개 진행바 + 각 진행바 하단 날짜(모집 진행바=모집 시작~마감, 제출 진행바=리뷰어 구매기간/방문형 방문기간 + 제출마감) 표시에 사용. 3번째 「인증 성공」은 RPC 집계에 없어 `hydrateCampCertBars`(admin-brand-ops.js)가 캠페인별 `fetchDeliverablesByCampaign` 조회 후 `countCertSuccess`(admin-deliverables.js — `buildDeliverableGroups`+`computeCertStatus` 단일 소스, 결과물 관리 화면과 동일 판정)로 비동기 채움
- `link_campaign_to_application` / `unlink_campaign_from_application` — 캠페인↔신청 연결/해제 RPC (`is_campaign_admin()` 이상, advisory_xact_lock 2단)

### 브랜드 셀프 오리엔시트 (마이그레이션 186~195·200)
> 브랜드가 로그인 없이 토큰 링크로 캠페인 콘텐츠(오리엔시트)를 한국어로 작성·제출 → 관리자가 자동 채움으로 캠페인 발행. 사양서 `docs/specs/2026-06-18-brand-self-orient-sheet.md`(§15 형식 전면 재설계 진행 중). 전부 dev 배포(운영 보류 — 전체 완성 후 일괄).
- `orient_sheets` — `brand_id`(NOT NULL, ON DELETE RESTRICT) · `application_id`(NULL, SET NULL — 신청 연결 선택) · `token uuid UNIQUE`(작성 링크 식별자) · `form_type`(reviewer|seeding|proxy_purchase, NULL 허용 — **마이그194로 3종 확장**. §15-11 카드별 형식 구조라 컬럼 자체는 대표값/미사용, 형식은 `data.cards[].form_type`) · `data jsonb`(작성 내용 — **cards 배열**, 스키마는 사양서 §15-A) · `status CHECK(draft|submitted|consumed|expired)` · `campaign_id`(발행 캠페인 역참조, SET NULL) · `token_expires_at`(발급+30일) · `submitted_at`(최초 제출 시각·불변) · `last_submitted_at`(마지막 제출 시각·매 제출 갱신 — 마이그202, 제출 알림 신규/재제출 판정용) · `last_notified_at`(마지막 알림 발송 시각 스냅샷 — 마이그234, 재제출 30분 디바운스 기준. `notify-orient-submitted` Edge Function만 service_role로 UPDATE) · `version`(낙관적 락) · `created_by`. **익명 직접 접근 0**(RLS — 토큰 함수로만), `updated_at` 자동 갱신 트리거. ⚠️ `brand_id` RESTRICT → `delete_brand` RPC(마이그191)에 `orient_sheets` 카운트 체크
- 익명 함수 3종 (GRANT anon, SECURITY DEFINER + `search_path=''`, 토큰 미매칭/만료/consumed 차단, 140·187 패턴): `get_orient_sheet(token)`(조회 + 연결 신청 모집 희망값 `initial_values` 승계, 읽기 전용) · `save_orient_draft(token, data, version)`(임시저장·낙관적 락·jsonb 100KB·status 미변경) · `submit_orient_sheet(token, data, version)`(draft/submitted→submitted, 재제출 허용). data jsonb 투명 전달이라 cards 배열로 바뀌어도 무변경. **`set_orient_form_type`은 마이그194에서 DROP**(카드별 형식 구조로 "1링크=1형식" 폐기). 발급 `create_orient_sheet(brand_id, application_id)`는 마이그195로 2인자(form_type=NULL·빈 cards 배열·brand prefill)
- **카드 고유 번호 `data.cards[].uid`**(마이그293·294, 2026-08-05 — 내부 메모 1단계): 카드별 메모를 **순번이 아니라 고유 번호로** 붙이기 위해 심는 값. **번호를 만드는 곳은 저장 함수 한 곳뿐**이고 브랜드 작성 폼은 실어 나르기만 한다(만드는 곳이 둘이면 어느 쪽이 채웠는지에 따라 안전망 발동이 갈려 검증 자체가 불가능해진다). `_orient_apply_card_uids(들어온data, 저장된data)`(293)가 `save_orient_draft`·`submit_orient_sheet` 저장 직전에 돌며 ⓪유효성 정리(문자열 아님·빈값·공백뿐 → 없는 것으로 취급 + 배열 내 중복은 처음 것만 인정) ①있으면 유지 ②없고 **「들어온 카드 개수 = 저장된 카드 개수」일 때만** 같은 자리 기존 번호 물려받기 ③그래도 비면 새 번호. ⚠️**②의 개수 조건을 빼면 안 된다** — 조건 없이 물려주면 옛 폼이 카드를 지운 채 저장할 때 번호가 한 칸씩 밀려 붙어 **메모가 아무 표시 없이 엉뚱한 모집 건 아래** 나타난다(가장 나쁜 실패). 개수 조건이 있으면 그 경우 메모가 눈에 보이게 고아가 돼 관리자 상세의 「삭제된 모집 건의 메모」에 모인다. ⚠️**배포 순서 = 293(저장 함수) → 폼(`dev/sales/orient.html`) → 294(기존 시트 채우기)**. 293이 먼저 안 나가면 옛 폼이 한 번 저장하는 것만으로 그 시트 번호가 통째로 날아간다. ⚠️294는 **운영 시트의 `data`를 직접 고치는 유일한 작업**이고 「제출됨」 상태 시트도 건드려 제출 알림 웹훅 조건을 재통과한다 — 실제 발송은 마이그234 게이트(`last_submitted_at` 그대로면 스킵)가 막지만 **그 게이트는 마이그레이션이 아니라 `notify-orient-submitted` 함수 코드 쪽**이라 배포 상태를 눈으로 확인해야 한다. ⚠️`mark_orient_card_consumed`(196)·`link_orient_card_to_campaign`(237)·`unlink_orient_card`(238)는 `data` 전체를 덮어쓰지 않고 `jsonb_set`으로 자기 키만 고쳐 uid를 보존한다. ⚠️**발행 표시(`campaign_id`)는 여전히 순번 기반** — 메모는 고유 번호, 발행 표시는 순번이 한동안 공존한다(기존과 동일한 위험, 후속 작업). 사양서 `docs/specs/2026-08-05-orient-sheet-internal-memo.md`
- 작성 폼 `dev/sales/orient.html`(+ 빌드 복사본 `sales/orient.html`) — sales 정적 배포(storage.js 미사용·RPC 인라인 호출). **카드형**(공통 브랜드 1회 + 카드 N개 아코디언, 카드마다 형식 가구매/리뷰어/시딩 선택 → §15-12대로 입력 분기): 가구매·리뷰어=**판매처 Qoo10 고정**(드롭다운 제거, 2026-06-25)·판매 URL(ttp:// 등 자동 보정 + Qoo10 아니면 경고만)·**가격은 상시가만**(세일가·대형할인 제거)·리뷰어=리뷰가이드 / 시딩=등급(나노 IG한정·미들메가 다채널)·채널별 가이드·소구·해시태그·촬영가이드(미들메가). 제출 시 항상 읽기전용 미리보기 1회. 자동저장·낙관적 락·만료/소비 진입 분기. **예시 이미지=URL 직접 입력 또는 파일 업로드(카드당 5개)** — `orient-images` 공개 읽기 버킷(마이그200)에 `{token}/{난수}.{ext}` 경로로 익명 업로드, storage 정책이 `orient_token_can_upload(text)`(SECURITY DEFINER)로 살아있는 토큰만 허용. 이미지(jpg·png·webp·HEIC→JPEG)는 긴변 2048 압축 후 업로드, 문서(pdf·docx·xlsx·pptx)는 원본. 성공 시 public URL을 `data.cards[].images[].value`(type:'upload')에 저장 → 관리자 상세는 기존 http 링크 렌더 무변경. 압축/업로드 실패 시 URL 입력 폴백. svg·html 미허용. **리뷰 가이드·금지 표현(NG)·추가 안내 3필드는 간단 리치 텍스트**(B/I/U/S/링크, contenteditable+execCommand 인라인 + DOMPurify@3.1.6 CDN). 익명 작성→관리자 열람 저장형 XSS 대비 3중 sanitize(저장 richValue·미리보기 pvRowHtml·관리자 osFieldHtml+sanitizeCautionHtml). 기존 평문 draft는 로드 시 줄바꿈 보존(richInitial). **되돌려 보내는 값 3종**(2026-08-05): `cardHtml`이 카드 상자에 `data-uid`·`data-campaign-id`·`data-linked-existing`를 담고 `collectCard`가 **값이 있을 때만** `uid`·`campaign_id`·`linked_existing` 키로 되돌려 보낸다. ⚠️빈 키를 새로 만들면 관리자 화면이 「발행됨」으로 잘못 읽으므로 **값이 없으면 키를 아예 넣지 않는다**. 이전에는 `collectCard`가 이 두 값을 안 실어 보내, 카드 일부만 발행한 시트(status는 「제출됨」이라 저장이 열려 있음)를 브랜드가 한 번만 저장해도 **「발행됨」 표시가 조용히 사라지던** 결함이 있었다. ⚠️**배포 대상이 다른 프로젝트**(`reverb-sales`)라 커밋 머지 ≠ 페이지 반영 — 브라우저로 눈 확인 필요
- 관리자 발급·조회 페인(`/admin#orient-sheets`, `dev/js/admin-orient.js`): 발급 모달(브랜드+선택적 신청 연결만 — 형식·제품은 브랜드가 폼 카드에서 선택, `create_orient_sheet` 2인자) + 상세 모달(공통 브랜드 + `data.cards` 순회 형식별 렌더 — 가구매/리뷰어=판매처·가격, 리뷰어=리뷰가이드, 시딩=등급·채널·미들메가 촬영가이드) + 목록 「모집 형식」 컬럼(`osCardsSummary` 형식별 개수). **자동 채움 발행(PR⑦)**: 상세 모달 카드별 「이 카드로 발행」(제출됨·미발행 카드만) → `applyOrientCardPrefill`(cards→`newCamp*`, 한국어는 `_ko`칸·가이드 초안, 일본어 표시칸 비워 관리자 보완) → `add-campaign` 폼 → `addCampaign`이 일본어 게이트(가이드 비면 매니저 차단·campaign_admin 사유 긴급 발행) 후 발행 → `mark_orient_card_consumed`(마이그196, `data.cards[i].campaign_id` 기록·전 카드 발행 시 status=consumed). 가구매 카드=`proxy_purchase=true`. 발행 권한=`is_admin()`(매니저 포함), 일본어 긴급 우회만 `isCampaignAdminOrAbove()`. **기존 캠페인 연결·해제(마이그237·238·239)**: 「이 카드로 발행」 클릭 시 `#orientPublishModal` 선택 모달(신규 발행=기존 자동채움 / 기존 캠페인 연결). 기존 연결(`osConfirmLink`→`link_orient_card_to_campaign` 237)은 이 시트 브랜드 캠페인만(`allCampaigns` 필터+`osLinkedCampaignIds` 연결분 제외, `#orient-sheets` 딥링크 시 `fetchCampaigns` 폴백) 목록에서 선택→서버가 **브랜드 일치·전역 중복 연결 검증**(reason brand_mismatch/campaign_already_linked). 카드에 `linked_existing:true` 마커. 연결 해제(`osUnlinkCard`→`unlink_orient_card` 238)는 발행됨 배지 옆 버튼, 캠페인 행은 미삭제·카드 마커만 제거·consumed→submitted 복귀. `delete_orient_sheet`(239 갱신)는 `linked_existing:true` 카드의 캠페인을 삭제 대상에서 제외(관리자 독립 생성 캠페인 보호)

### 오프라인 팝업 방문 예약(티켓팅) — 마이그레이션 280~283 (작업 1만 완료, dev 미배포)
> 오프라인 행사 방문 예약 + 입장 QR 확인. 행사 2026-08-28~30. 사양서 `docs/specs/2026-07-30-offline-popup-ticketing.md`, 작업표 `…-breakdown.md`. **모집 형식을 새로 만들지 않고** 방문형(visit)에 「행사 모드」 표시만 얹는다. 브랜치 `feature/팝업티켓`.
- `campaigns` 컬럼 2개(280): `event_mode boolean`(행사 캠페인 — **판정 단일 소스**, 클라 헬퍼 `isEventCampaign()`) · `is_invite_only boolean`(비공개). ⚠️ `CHECK (NOT event_mode OR recruit_type='visit')` — 리뷰어형에 켜지면 자동 승인 트리거(049)가 「대기」를 승인으로 덮어써 **티켓은 대기인데 신청은 당선**인 모순이 생긴다. ⚠️ 두 컬럼은 275 낙관적 락의 제외 목록에 없어 **자동 보호 대상**(편집 저장은 `updateCampaign(id, updates, expectedVersion)` 필수). ⚠️ 변경 이력 화이트리스트(265·266)에는 **의도적으로 넣지 않았다**(세 곳 동시 수정 위험)
- **`event_groups`(291·292) + `campaigns.event_group_id`** — **같은 행사를 날짜별로 여러 캠페인으로 나눈 경우 하나로 묶는다.** 이번 팝업이 8/28(초대 전용)·8/29~30(일반 공개) 2개 캠페인이라, 현장 확인 화면을 캠페인 하나에 고정하면 **8/29 아침에 명단이 빈 화면**이 된다. 연결은 `ON DELETE SET NULL`(묶음을 지워도 캠페인 저장이 안 막힌다). RLS 조회 `is_admin()`·CUD `is_campaign_admin()`. ⚠️ **정규화는 공백(일반·전각)을 전부 제거**하고 비교한다 — 291 은 회사 마스터(119)의 공백 **압축** 패턴을 썼다가 「REVERB팝업」과 「REVERB 팝업」이 **둘 다 저장되는** 것이 개발 실측에서 드러나 292 로 교체했다(291 은 수정하지 않고 함수만 재정의). **압축과 제거는 목적이 다르다** — 회사명은 「제이 펀」과 「제이펀」을 다른 회사로 남기는 게 맞고, 행사 묶음은 같은 것으로 봐야 한다. ⚠️ **운영은 291 → 292 순서로 둘 다** 적용하고, 291 만 적용한 상태에서 291 의 옛 검증 [3]을 돌리면 통과하지 않는 게 정상(파일에 「돌리지 마세요」 표시). ⚠️ 275 낙관적 락 **자동 보호 대상**, 변경 이력 화이트리스트(265·266)에는 **넣지 않음**(앞선 행사 칸 3개와 같은 판단). ⚠️ 편집 폼은 **연결된 묶음이 보관 상태여도 선택지에 남긴다** — 빼면 칸이 빈 채로 열리고 다른 칸만 고쳐 저장해도 **연결이 경고 없이 끊긴다**(`fetchEventGroups(keepId)`). 같은 이유로 조회 실패는 `null`, 0건은 `[]` 로 **구분**한다(다른 조회 함수들이 예외를 삼키고 `[]` 를 주는 관행과 다름). 사양서 `docs/specs/2026-08-05-event-group-and-scoped-checkin.md`
- `event_invites`(280) — 초대 번호(캠페인당 1줄, 대문자 영숫자 8자리). ⚠️ **캠페인 표에 두지 않은 이유**: 캠페인은 조회가 공개고 인플 앱이 `select('*')`로 통째 받아와 **브라우저 개발자 도구로 번호가 그대로 보인다**. 그러면 예약 함수의 서버 재검증도 무력해진다. RLS 조회는 `is_campaign_admin()`만, 방문객은 `verify_event_invite()`로 「맞나/틀리나」만 받는다. ⚠️ 남은 위험 = 초대 전용 캠페인의 **내용 자체**(제목·이미지)는 여전히 브라우저로 내려온다(목록 제외가 화면 단계 필터)
- `event_slots`(281) — 타임(날짜+시각+정원) 1줄. 3일 합계 32줄. RLS 조회 **로그인 전체**(개인정보 없음·§0 결정 16), CUD `is_campaign_admin()`. **정원 판정은 이 표가 한다** — `campaigns.slots`는 표시용 합계일 뿐. ⚠️ 예약이 걸린 타임은 **삭제 차단 트리거**(지우면 티켓이 소리 없이 연쇄 삭제되고 신청 행만 남아 「당선인데 예약번호 없음」). 캠페인이 통째로 지워지는 중이면 통과시키는 탈출구 있음(연쇄 순서 미보장 → 완전 삭제가 막히는 것 방지). 닫으려면 `is_active=false`
- `event_tickets`(282) — 예약 1건. `ticket_code`(QR에 담기는 값, 혼동 글자 0·O·1·I·L 제외 31자 집합) · `status(confirmed|waitlist|cancelled)` · `waitlist_position` · `entered_at`/`entered_by`(**첫 입장 시각 불변**)/`scan_count` · `version`. RLS = 본인+관리자 **조회만**, 쓰기는 SECURITY DEFINER 함수만(예약번호 위조·정원 우회 차단, 217 선례). 부분 유니크 `(campaign_id, influencer_id) WHERE status<>'cancelled'` = 「한 캠페인 1타임」. ⚠️ `application_id ON DELETE CASCADE` — 캠페인 보관 삭제(255)가 신청을 즉시 파기할 때 **입장 기록도 함께 파기**(개인정보 원칙, 삭제 함수 자체는 무수정)
- 신청 행과의 짝: 확정→`approved` **INSERT** / 대기→`pending` INSERT / 취소→`cancelled`. ⚠️ **대기도 신청 행을 미리 만든다** — 안 만들면 승격 시점(마감 후)에 INSERT가 마감 가드(272)에 막혀 승격 자체가 실패
- ⚠️ **취소 후 재예약**: 신청 표에 옛 유일 제약(050 `uidx_applications_user_campaign`)이 **아직 살아 있다**(104가 지울 이름을 자동 생성 이름으로 적어 `IF EXISTS`가 조용히 통과 — 기존 백로그). 그래서 취소해도 **신청 행을 지우지 않고 `cancelled`로 두었다가 재예약 때 되살린다**. 플랫폼 전체 제약은 건드리지 않았다
- 서버 함수 5개(283, 전부 `SECURITY DEFINER`+`search_path=''`+행 잠금, 실패는 예외가 아니라 `{ok:false, reason}`): `reserve_event_ticket(slot, invite)` (타임 행 `FOR UPDATE` 잠금 후 정원 계산 → 확정/대기) · `check_in_ticket(code)`(관리자, 첫 입장 시각 보존·재확인은 `scan_count`만 증가, **입장을 막지 않는다**) · `cancel_event_ticket(ticket)`(본인, **시작 2시간 전까지** 일본 시각 서버 판정 + 대기 1번 자동 승격·순번 재정렬) · `verify_event_invite` · `get_event_slot_counts`(방문객은 남의 티켓을 못 봐 잔여를 직접 셀 수 없다)
- **대기 승격 알림**(283): 알림 종류에 `event_waitlist_promoted` 추가(273의 10종 → 11종, **목록 원본은 273**). 앞사람이 취소해 대기자가 확정으로 올라가면 `cancel_event_ticket` 이 **직접** 알림을 넣는다(트리거는 행사에서 조기 반환하므로 안 나간다). `ref_table='event_tickets'`. ⚠️ 알림 클릭 분기(티켓 화면으로 이동)는 **작업 4에서** 넣어야 한다 — 없으면 눌러도 아무 데도 안 간다
- ⚠️ `record_application_status_event()` 재정의(283, **베이스는 154**) — 행사 캠페인은 **조기 반환**. 알림 문구가 결과물 제출을 요구해 방문객에게 부적합(결정 14)이고, 대기 승격이 **다른 방문객의 취소 트랜잭션**에서 일어나 감사 기록의 처리자가 사실과 달라지기 때문
- ⚠️ 연령(180)·마감(272) 가드는 **신규 삽입 전용** 트리거라 되살리기(UPDATE) 경로에 안 돈다 → 예약 함수가 같은 판정을 먼저 하고, 트리거는 삽입 경로 최종 방어선으로 유지(공유 지점이라 무수정)
- ⚠️ 예약·취소는 **관리자 도구(SQL 편집기)로 검증 불가**(「본인 계정만」 가드 + 서비스 키는 로그인 사용자가 없다). 방문객 화면 완성 후 **테스트 계정 2개로 브라우저에서** 확인
- 접근 함수는 `dev/lib/storage.js` 한곳에 12개(`fetchEventSlots`/`fetchEventSlotCounts`/`upsertEventSlot`/`deleteEventSlot`/`reserveEventTicket`/`cancelEventTicket`/`checkInTicket`/`fetchMyEventTickets`/`fetchEventTicketsByCampaign`/`verifyInviteCode`/`fetchEventInvite`/`upsertEventInvite`), 헬퍼는 `dev/lib/shared.js`(`isEventCampaign`/`isInviteOnlyCampaign`/`eventCancelWindowPassed`)

### 인플루언서·관리자
- `influencers` — 인플루언서 프로필. `name`, SNS 계정+팔로워, 주소, `paypal_email`, `primary_sns`, `terms_agreed_at`, `privacy_agreed_at`, `marketing_opt_in` 등. 홍보 메일 PR 1 추가 컬럼: `unsubscribe_token uuid NOT NULL DEFAULT gen_random_uuid() UNIQUE`(영구 1개 토큰, 메일 수신거부·클릭 추적 익명 호출 식별자), `marketing_unsubscribed_at timestamptz NULL`(수신거부 시각 감사 — 재구독 시 NULL 초기화). 기존 인플 1,398행 백필 완료 (마이그레이션 140). **연령 정책 PR 1 추가 컬럼(마이그레이션 180)**: `birthdate date NULL`(생년월일 — 만 18세 판정, 최초 입력 후 본인 수정 잠금 트리거 `lock_influencer_birthdate`), `gender text NULL CHECK(male/female/other/undisclosed)`(성별 — UI 라벨 男性/女性/その他/回答しない), `age_consent_at timestamptz NULL`(마이그레이션 185 — 연령 정책 게이트에서 생년월일·성별 수집·이용에 동의한 시각 UTC, NULL=미동의. 가입 동의 `privacy_agreed_at`과 분리해 신규 수집 동의 별도 기록). 기존 행은 NULL 시작(응모 시점 게이트가 채움 — PR 3·4, 동의 체크박스 필수). `is_audit boolean NOT NULL DEFAULT false`(마이그레이션 179) — 감사용 계정(운영팀 시뮬레이션용). true 면 응모수·슬롯·`applied_count`·대시보드 KPI·운영현황·엑셀 산출물에서 격리. partial index `WHERE is_audit=true`
- `age_policy_settings` — 연령 정책 시행일 단일행 설정(마이그레이션 180). `id=1` 싱글톤, `effective_date date NULL`(NULL=차단 비활성, PR 5 통지 후 시행일로 UPDATE), `updated_at`/`updated_by`. RLS SELECT `is_admin()` / UPDATE `is_super_admin()`. 트리거 `check_age_policy`(응모 BEFORE INSERT — effective_date NULL이거나 시행 전이면 통과, 시행일 이후 birthdate NULL/18세 미만이면 P0002 차단, 관리자 예외) + 헬퍼 `calc_age_kst(date)`(KST 기준 만 나이). 사양서 `docs/specs/2026-05-27-age-minor-policy.md` PR 1
- `admins` — 관리자 계정. `auth_id`, `email`, `name`, `role`(super_admin/campaign_admin/campaign_manager). 초대 추적 4종(마이그레이션 244·245): `invite_mail_sent_at`/`invite_mail_sent_to`(최근 초대 메일 발송 성공 시각·수신 주소) · `invite_completed_at`(본인이 비밀번호를 실제로 설정한 최초 시각) · `promoted_at`(기존 계정이 관리자로 승격된 시각 — 메일 문구 분기 근거). 전부 NULL 허용·기존 행 백필 없음
- `admin_password_reset_requests` — 관리자 비밀번호 찾기 요청 기록(마이그레이션 243). 요청 제한 판정 전용. `email_hash`(sha256, 소문자·공백제거 후 — **원문 이메일 미저장**: 요청만 하면 행이 쌓이는 공개 경로라 유출 시 관리자 이메일 명단이 되면 안 됨)·`requested_at`·`matched`(내부 통계, 외부 미노출). RLS SELECT `is_super_admin()` / 쓰기 정책 없음(service_role 전용). 판정 함수 `check_admin_password_reset_rate_limit(email_hash, matched)`(SECURITY DEFINER·`search_path=''`·service_role 만 실행, `pg_advisory_xact_lock` 단일 키로 직렬화, 판정 결과와 무관하게 항상 1행 기록 — 미매칭도 세야 무작위 난사로 메일 한도를 태우는 걸 막음). 30일 경과 행 자동 삭제 `purge_old_admin_password_reset_requests()` + pg_cron `admin-password-reset-requests-retention-daily`(KST 04:15)
- `outbound_influencers` — 아웃바운드 시딩·타이업 인플루언서 명단(영업팀 직접 컨택, globalreverb 미가입·별도 자산, 마이그레이션 226). 기존 `influencers`와 물리 분리. 컬럼: 기본(`name_ko`/`account_id`)·분류(`series_code`/`category_code`/`tier_code` — lookup code 스냅샷)·채널4(핸들+팔로워 `bigint NULL`)·가격5(`price_feed`/`reels`/`story`/`tiktok`/`secondary` `bigint NULL`=미상, 엔화 ¥·내부전용)·운영(`contact_channel`/`agency`/`nego_memo`/`availability CHECK(available/unavailable/adjusting)`)·콘텐츠(`rep_image_path`/`rep_posts jsonb`/`content_consent`)·`is_active`. **RLS SELECT/INSERT/UPDATE/DELETE 전부 `has_permission('outbound.view',...)`**(⚠️`is_admin()` 금지 — campaign_manager 차단). 기준데이터 `lookup_values` kind 3종 추가(`ob_series`/`ob_category`/`ob_tier`, 마이그레이션 227 + `ob_category`에 `health`(헬스)·`tech`(테크) 2종 추가 마이그레이션 236, 세분→계열 매핑은 부모 컬럼 없이 `OB_CATEGORY_SERIES` 코드 상수 — health→life·tech→other). 권한 `menu.outbound`/`outbound.view`(마이그레이션 228). Storage 버킷 `outbound-influencer-images`(공개 읽기·관리자 쓰기, 마이그레이션 229 — ⚠️`has_permission` storage RLS 최초 사례, 업로드 스모크 검증 완료 2026-07-14·폴백 불필요). **데이터 이관 = 구글시트 917명**(seed `outbound_influencers_import.sql`, BEGIN/DELETE ALL/INSERT/COMMIT 멱등. ⚠️최초 "약 50명" 오판 → 2026-07-14 전량 917명 재이관 정정. 등급 없는 68명은 팔로워 자동 추정, 계정ID 없는 6명 포함). **dev 코드 완료·데이터는 개발서버 검증 후 되돌림·운영에만 등록 예정(운영 미배포)**. 사양서 `docs/specs/2026-07-08-influencer-recommendation.md`
- `admin_email_subscriptions` — 관리자별 메일 수신 구독. `(admin_id, mail_kind)` UNIQUE. `mail_kind` 는 `lookup_values(kind='admin_email_kind')` code 참조 (활성 항목 `brand_notify`/`daily_digest`/`campaign_promo`). 구 `application_cancel`·`application_received` 는 마이그레이션 164 에서 `daily_digest`(일일 통합 메일) 단일 항목으로 통합·비활성(soft, 행·구독 이력 보존). RLS SELECT 관리자 전체, CUD 본인 또는 super_admin. 헬퍼 `get_subscribed_admin_emails(p_mail_kind)`
- **동적 권한 관리 (PR1~PR3 운영 완료 · 마이그레이션 207~215 · 「기본값 복원」 포함)**: super_admin 이 `/admin#permissions` 「권한 관리」 화면에서 등급(campaign_admin/campaign_manager)×기능별 접근수준(쓰기/읽기/숨김)을 설정 → 사이드바 메뉴 노출이 바뀌고, **민감정보(전화·주소·PayPal 등)는 서버 가림막 뷰로 실제 차단**(PR3, 마이그212·213). 화면 메뉴 숨김은 클라 표시 제어(fail-open), 민감정보·`has_permission` 서버 판정은 fail-closed(미등록 거부). 「기본값 복원」 버튼으로 baseline(default_level) 되돌리기 가능(복원해도 민감정보 차단 유지).
  - `role_permissions` — 등급×기능별 접근수준. `(role, feature_key)` PK, `access_level CHECK(write|read|hidden)`, `default_level CHECK(write|read|hidden) NOT NULL`(「기본값 복원」 baseline — 마이그214, 207 시드+213 반영 명시 매핑), `role CHECK(campaign_admin|campaign_manager|**super_admin**)` — **마이그레이션 268 에서 super_admin 추가**(슈퍼관리자 자기 제한, 2026-07-29). `role_permission_history.role` CHECK 도 함께 확장(빠뜨리면 저장 트랜잭션 전체 롤백). RLS SELECT `is_admin()` / CUD `is_super_admin()`. 시드 = 등급 2종 74행 + **super_admin 42행(전부 write/write)** → 도입 즉시 동작 변화 0.
  - `role_permission_history` — 권한 변경 이력. RLS SELECT `is_super_admin()`, INSERT 는 RPC 만.
  - `has_permission(p_feature, p_min default 'read')` RPC (SECURITY DEFINER + `search_path=''`) — 서열 write>read>hidden. ⚠️ **미등록 시 판정이 등급별로 반대(마이그레이션 269, 비대칭 계약)**: super_admin 은 행이 없으면 **통과**(fail-open — 신규 기능 시드 누락이 슈퍼 잠금으로 이어지지 않게), campaign_admin·campaign_manager 는 행이 없으면 **거부**(fail-closed, 종전 그대로). 268 이전에는 super_admin 이 표를 아예 안 보고 무조건 통과했다.
  - `update_role_permissions(p_changes jsonb)` RPC (마이그레이션 211, SECURITY DEFINER `search_path=''`) — 일괄 저장(role_permissions UPDATE + history INSERT 원자). 가드: is_super_admin·role/level 검증·feature_key 존재·**denylist 2종(방향이 서로 반대라 분리된 블록으로 처리 — 마이그레이션 270)**: 등급 2종은 `permissions.manage`/`admin.manage` 가 **hidden 외 거부**(권한 상승 차단), super_admin 은 `permissions.manage`/`admin.manage`/`menu.permissions` 가 **write 외 거부**(잠금 방지)·낙관적 락(prev_level `SELECT FOR UPDATE` 비교)·일괄 상한 200(42키×3등급=126칸).
  - `restore_role_permissions_defaults()` RPC (마이그레이션 215, SECURITY DEFINER `search_path=''`·is_super_admin 가드) — 저장된 access_level 을 default_level(baseline)로 되돌림(`access_level<>default_level` 행만 UPDATE + history INSERT, 변경 건수 반환, 낙관적 락 없이 원자 덮어쓰기). denylist 행은 default hidden 이라 자연 skip. ⚠️ baseline 이 213 민감정보 차단 반영이라 **복원해도 campaign_manager sensitive_pii 는 hidden 유지**(안 풀림). 권한 관리 화면 「기본값 복원」 버튼(`admin-permissions.js` `restorePermDefaults`, 현재값=기본값이면 비활성). ⚠️ **향후 정책 변경 마이그레이션(213처럼 access_level 의도 변경)은 default_level 도 함께 UPDATE**(드리프트 방지).
  - **슈퍼관리자 자기 제한**(2026-07-29, 마이그레이션 268·269·270): 권한 관리 화면의 **슈퍼관리자 열도 편집 가능**. 되돌릴 길을 끊는 4종(`permissions.manage`·`admin.manage`·`menu.permissions`·`menu.admin-accounts`)만 「쓰기(고정)」. **잠금 방지 3중**: ①서버가 그 4종의 write 이탈 거부(270·271) ②진입 가드가 `permissions` 페인만 숨김 판정 자체를 건너뜀(`switchAdminPane`) ③`menu.admin-accounts` 가 슈퍼 write 고정이라 「관리자 계정」 화면이 항상 노출되고, 그 안의 「권한 관리」 버튼(`#btnPermManageBtn`)이 권한 관리 화면의 **유일한 진입점**이다. ⚠️ 2026-07-29 사이드바 「권한 관리」 상설 항목(`adminPermissionsSi`)은 **제거**됐고 `menu.admin-accounts` 잠금(271)이 그 역할을 대신한다 — 카탈로그에서도 `menu.permissions` 항목을 뺐다(제어 대상이 사라진 죽은 설정. 서버 잠금·클라 배열 항목은 방어로 잔존). 응급 복구는 `UPDATE public.role_permissions SET access_level = default_level WHERE role='super_admin';`(**함수가 아니라 직접 UPDATE** — 함수는 로그인 세션이 필요). 화면은 기능마다 효과 배지 3종(`permSuperEffect`, shared.js) — **서버 차단 5개**(`influencer.sensitive_pii`·`settlement.view`·`settlement.pay`·`outbound.view`·`campaign.caution_history_view`) / **화면에서만**(menu.* + `influencer.excel_sensitive`) / **슈퍼 적용 안 됨 12개**(서버 가드가 `is_campaign_admin()` 하드코딩이라 설정 무시 — 진짜로 막으려면 서버 가드 교체 별도 작업). 슈퍼 변경 저장은 별도 확인 문구(등급 전체 적용·되돌리는 법), 「슈퍼관리자만 복원」 버튼(`restoreSuperPermDefaults`)으로 다른 등급 설정을 건드리지 않고 전권 복구. ⚠️ 이 설정은 **계정별이 아니라 등급 전체**. 사양서 `docs/specs/2026-07-29-super-admin-self-restriction.md`
  - 권한 카탈로그 = 코드 상수 `ADMIN_PERMISSION_CATALOG`(shared.js, 화면 21[menu.*, `menu.settlements` 포함] + 주요기능 19 — 정산 `settlement.view`/`settlement.pay` 2종 포함[마이그레이션 220 시드, campaign_admin=write·campaign_manager=hidden]). 클라 헬퍼 `permLevel/canWrite/canRead/isHidden`(super=write·미로드=fail-open), 부팅 시 `fetchRolePermissions` 로드, `applyLookupMenuVisibility` 가 menu.* 순회로 사이드바 숨김. 설정 화면 `dev/js/admin-permissions.js`(super 전용, 그리드·경고배너·denylist 행 잠금·super 열 고정). `switchAdminPane` 진입 가드(permissions=super 전용·hidden 페인 대시보드 리다이렉트). 사양서 `docs/specs/2026-06-15-admin-permission-management.md`
- **`is_campaign_admin()` search_path 정정(마이그레이션 210, 갭3)**: 기존 `search_path='public, pg_temp'` → `''` 로 통일(`admins` 를 `public.admins` 로 명시). 판정 로직·GRANT 불변(`CREATE OR REPLACE` 시그니처 동일이라 권한 보존)
- `admin_notices` — 관리자 공지. `category`(system_update/release/warning/general), `pin`(bool), `title`, `body_html`(Quill rich), `created_by`/`created_at`/`updated_at`, `status CHECK(draft|published)`, `published_at`/`published_by`/`published_by_name`. SELECT RLS 는 published OR `is_super_admin()` OR `created_by=auth.uid()` (draft 는 작성자/super 한정). XSS 방어는 저장+렌더 이중 sanitize
- `admin_notice_reads` — 관리자별 읽음 기록. `(admin_id, notice_id, read_at)` UNIQUE. `upsert_admin_notice_read` RPC. 미읽음 카운트 = published - reads(by admin)
- `influencer_flags` — 인플루언서 관리자 마킹 이력. `influencer_id`, `action`(verify/violation/blacklist/clear), `reasons text[]` (blacklist_reason ∪ violation_reason lookup), `memo`, `evidence_paths text[]`, `updated_at/by/by_name`. 위반 행만 UPDATE RLS. **개인정보처리방침 「부정 이용·위반 기록 3년 보관」 집행**: `purge_old_influencer_flags()` 가 `set_at` 36개월 경과 행 자동 삭제, pg_cron job `influencer-flags-retention-daily` 매일 KST 04:00 실행 (마이그레이션 166)

### 응모건 메시지 (인플루언서 ↔ 관리자, 운영 배포 완료 2026-05-28, 마이그레이션 144·145)
> 응모(신청)건 단위 양방향 메시지. **PR 1·2 운영 배포 완료(2026-05-28)** — 인플루언서 발신(모달·응모이력 진입) + 관리자 발신(받은편지함 3단 페인 `#adminPane-messages` + 응모행 「메시지」 버튼 + 메시지 모달) + 강제 숨김/복구 + 수동 응대 완료 + 인플 GNB 「メッセージ」 메뉴 + 알림 `message_received`. 관리자 로직 `dev/js/admin-messaging.js`. 사양서 `docs/specs/2026-05-15-application-messaging.md`. **일괄 발송(PR 3·3-1·3-2)은 개발서버 구현 완료·운영 보류(약관 게이트)** — 캠페인 단위 BCC 발송 + 대상 필터(캠페인 상태 → 응모/영수증/결과물 상태 → 인플 인증·지역·SNS채널·팔로워) + 발송 이력·일괄 회수. RPC `send_application_message_bulk`/`withdraw_broadcast`/`resolve_bulk_recipients`/`get_broadcast_detail`. 마이그레이션 167~169 인자 변화·모달 흐름 상세는 `docs/CLAUDE-ARCHIVE.md` + 사양서 `docs/specs/2026-06-02-bulk-message-target-redesign.md`. 첨부·임의 다중선택(3페인 체크박스)·메일 지연 큐(PR 4)는 미구현.
- `application_messages` — 메시지 본문. `application_id` FK CASCADE, `sender_kind`(influencer|admin), `body`, `attachments jsonb`, `read_by_influencer_at`, 강제숨김 4컬럼, 본인회수 2컬럼, 메일큐 3컬럼, `broadcast_id` FK. RLS SELECT 본인 응모건 또는 `is_admin()`, INSERT/UPDATE 는 RPC 만(sender_kind 변조 차단)
- 부속 테이블: `application_message_admin_reads`(관리자 개인별 읽음) / `application_message_resolutions`(응대 완료) / `application_message_broadcasts`(일괄 발송 그룹) / `application_message_hide_history`(숨김 audit, append-only super_admin)
- 뷰 `application_message_summary`(security_invoker) + RPC: `get_application_messages`(역할별 4종 마스킹)·`send_application_message`(rate limit 100/h + 자동응대 + 관리자 발신 시 `message_received` 알림)·`mark_application_messages_read`·`withdraw_own_message`(25분 한도)·`mark_application_resolved`·`hide_application_message`(campaign_admin)·`unhide_application_message`(super_admin). 모두 SECURITY DEFINER
- lookup `message_hide_reason` 7종 + Storage 버킷 `application-message-attachments`(비공개). 첨부는 클라 압축(`dev/lib/image-compress.js`, HEIC→JPEG/2048px) 후 업로드

### 자동응답(FAQ 가이드형, 운영 배포 완료 2026-05-28, 마이그레이션 146)
> 응모건 메시지에 얹는 「문의 게이트」형 자주 묻는 질문(FAQ). PR A: 테이블 2개 + 기록 RPC + 관리자 등록 페인. PR B: 인플 게이트형 화면(개인화 상태 한 줄·유사 FAQ 제안·동적 치환). PR B2·C: 관리자 응대 보조(`dev/js/admin-messaging.js`). 사양서 `docs/specs/2026-05-21-message-faq.md`. **인플 게이트가 있는 문의 창구로 2026-05-28 운영 배포 완료(시드 31노드)**. 운영 FAQ 빈 구멍 실측 분석은 `docs/specs/2026-05-29-message-faq-improvement.md`.
- `faq_nodes` — 자기참조 트리. `parent_id`/`kind`(category|item), `label_ko/ja`, `body_ko/ja`, `action_type`/`action_target`(앱 해시 경로 8종), `is_human_handoff`, `relevant_stages text[]`, `sort_order`, `active`. RLS SELECT authenticated / CUD `is_campaign_admin()`. 시드 31노드
- `faq_interactions` — 측정. `action`(viewed|resolved|handoff), `view_count`. `viewed` 부분 유니크. RLS INSERT 본인행 / SELECT `is_admin()`
- `record_faq_interaction(...)` RPC(SECURITY DEFINER, `influencer_id=auth.uid()` 강제, `viewed` 멱등 UPSERT)
- 화면: 관리자 페인 `#adminPane-faq`(2단 마스터-디테일, `dev/js/admin-faq.js`) / 인플 「문의 게이트」형(`dev/js/messaging.js` — 문의 입력 중 유사 FAQ 제안 + 확인 시트) / 관리자 응대 보조(스레드 상단 상태·FAQ 열람 이력, `dev/js/admin-messaging.js`). 판정 로직 공용 `faqComputeStatus`/`faqComputeCancelPhase`(`dev/lib/shared.js`). 인플·응대 보조는 DB 변경 없이 RPC 재사용. 사양서 `docs/specs/2026-05-21-message-faq.md`

### 정산 (인플루언서 리워드, PR1·2·3 dev — 마이그레이션 217~223, 운영 미배포)
> 인플루언서가 캠페인 1건을 인증 성공하면 리워드가 정산 대기로 자동 등록 → 관리자가 외부 PayPal에서 수동 송금 후 완료 기록 → 인플은 마이페이지 조회. 자동 송금 API 없음(수동). 통화=엔화, 원천징수 없음·전액(약관 제13조). 사양서 `docs/specs/2026-06-22-influencer-settlement.md`
- `settlements` — 정산 1건 = 응모 1건(`application_id UNIQUE`, 멱등). `influencer_id`/`campaign_id`(비정규화)·`amount_jpy bigint`(생성 시점 `campaigns.reward` 스냅샷, 캠페인 수정 무영향)·`status CHECK(pending|paid|on_hold|cancelled)`·`paypal_email`(생성 시 인플 값 스냅샷)·`paid_at`/`paid_by`/`memo`/`version`(낙관적 락). 환수는 별도 status 없이 `on_hold`+메모. RLS SELECT 인플 본인행(`influencer_id=auth.uid()`) 또는 `has_permission('settlement.view','read')`(**server_enforced 동적 권한** — campaign_manager 는 hidden 이라 API 직접 조회도 차단). INSERT/UPDATE 직접 정책 없음(SECURITY DEFINER 함수만). ⚠️ `settlements.paypal_email` 은 인플 민감정보 가림막 뷰(마이그212·213)를 거치지 않는 직접 컬럼 — 현재 campaign_admin 만 조회 가능해 안전하나, 향후 campaign_manager 에게 `settlement.view` 를 열면 `influencer.sensitive_pii` 와 무관하게 PayPal 노출(PR2 설계 시 인지)
- `settlement_events` — 정산 상태 변경 이력(금전 감사, append-only). `settlement_id` **ON DELETE RESTRICT**(deliverable_events CASCADE 감사 유실 선례 반복 방지 — 정산행은 하드 삭제 대신 cancelled 소프트 상태)·`action`(create/pay/hold/cancel/revert)·`prev_status`/`next_status`·`actor`·`memo`·`at`. RLS SELECT `has_permission('settlement.view','read')`, INSERT SECURITY DEFINER 함수만
- `backfill_settlements()` RPC (마이그레이션 218, SECURITY DEFINER `search_path=''`) — 관리자가 정산 화면 열 때 호출(B안, 트리거 아님). 인증 성공 판정을 `computeCertStatus`(admin-deliverables.js) 그대로 SQL 재현(가구매=영수증만 / 리뷰어=영수증+캠페인 채널별 인증샷 전부 / 시딩·방문=게시물). `reward>0` AND `is_audit=false` AND 정산행 미존재 → UPSERT(`application_id` 충돌 시 skip, 멱등). PayPal 미등록이면 `settlement_paypal_required` 알림 멱등 발행. 가드 `has_permission('settlement.view','read')`. **컷오프(마이그레이션 231): 인증 성공일(판정에 쓰인 마지막 결과물 `reviewed_at`의 최댓값) ≥ `settlement_settings.cutoff_at` 인 것만 자동 생성** — 도입일 이후만 백필해 운영 배포 시 과거분 폭주·알림 폭주 방지. `cutoff_at` NULL이면 0건(안전측), `reviewed_at` 누락 레거시 건도 제외(과거 수동 처리로 이관). 역방향(인증 깨짐→on_hold)은 PR1 범위 밖. 반환 `(created_count, paypal_missing_count)`
- **정산 금액 출처 = 모집 형식별 분기(마이그레이션 261·262, 2026-07-23, dev — 운영 미배포)**: 리뷰어형(monitor)은 실무상 **캠페인 제품 가격(`campaigns.product_price`)을 페이백**하는데 후보 조건이 `c.reward > 0` 이라 **리뷰어형 캠페인 57개·승인 응모 1,065건이 통째로 정산에서 빠져 있던** 문제를 해소. ①**261**: `settlements` 에 `amount_source text CHECK(reward|product_price|product_plus_reward)` + `reward_part_jpy bigint`(합산 미구현·항상 NULL) 추가, 기존 행 `reward` 백필 ②**262**: `_settlement_cert_candidates()` DROP 후 재생성(반환 항목 추가) + `backfill_settlements`·`get_past_unregistered_settlements`·`register_past_settlements` 재정의. **후보 조건 `c.reward > 0` 제거**, 금액은 `recruit_type` 하나로만 분기(**monitor=`product_price`[가구매 `proxy_purchase` 여부 무관] / gifting·visit=`reward`**). 금액이 NULL·0 이하면 `amount_issue` 로 사유를 세우고 **INSERT 대상에서 제외** — `settlements.amount_jpy CHECK(>0)` 위반으로 배치 전체가 실패하는 것 방지. **인증 성공 판정(`is_success`/`cert_at`)은 232와 바이트 단위 동일**(화면 `computeCertStatus` 와 단일 소스). ⚠️ **`backfill_settlements()` 의 현재 유효한 원본은 232가 아니라 242**(알림 잠금 게이트 `v_notify := is_settlement_public()`) — 262 초안이 232 기준으로 재작성해 잠금이 소실됐다가 리뷰에서 복구. **함수를 `CREATE OR REPLACE` 로 재정의할 땐 파일 번호가 가장 큰 정의를 베이스로 삼을 것**. 사양서 `docs/specs/2026-07-23-settlement-reviewer-receipt-amount.md`. **운영 배포 전 「과거 미등록」 화면 안전장치(필터·선택 합계·금액 미확정 행 잠금·확인 모달 요약) 선행 필수** — 그 화면이 39건→수백 건으로 불어나는데 필터가 없고 전체 선택 옆이 되돌릴 수 없는 「송금완료 기록」 버튼. ③**264**(운영 실측 회귀 수정): 262가 `c.reward > 0` 을 없애면서 **현금 리워드 0원인 시딩(gifting)·방문형(visit) 정상 무보수 캠페인**(제품 제공만, 약관 제13조 3항)까지 후보에 들어와 `amount_issue`(금액 오류)로 오표시(운영 실측 gifting 인증성공 300건 중 261건). `_settlement_cert_candidates()` candidates CTE 에 `NOT (recruit_type<>'monitor' AND (reward IS NULL OR reward<=0))` 조건 추가로 이 캠페인들을 **후보에서 완전히 제외**(262 이전 동작 재현, monitor 영향 없음) + `amount_issue` 에서 도달 불가해진 시딩·방문형 2개 분기 제거(monitor product_price 분기는 유지). 반환 타입 불변(CREATE OR REPLACE, DROP 불필요), 인증 성공 판정·금액 계산 로직 자체는 무변경.
- `settlement_settings` — 정산 도입일(컷오프) 싱글톤 설정(id=1 고정, `cutoff_at timestamptz NULL`, 마이그레이션 230). `backfill_settlements()`가 이 값을 읽어 도입일 이후 인증성공만 자동 백필. NULL=미설정(자동 0건 — 값 세팅 전 폭주 차단). RLS SELECT `has_permission('settlement.view','read')` / UPDATE `is_super_admin()`(도입일은 과거분 전체를 좌우하는 스위치라 최고 관리자 전용). 도입일 이전 과거 인증성공분은 관리자가 정산 화면에서 수동 처리(**무알림**). 조회 `get_past_unregistered_settlements()`(마이그레이션 232, 인증성공 판정은 공통 헬퍼 `_settlement_cert_candidates()`로 3곳 공유, 명시적 컷오프 필터·PayPal은 `has_paypal` 불리언만)·처리 `register_past_settlements(uuid[], 'paid'|'pending', memo)`(마이그레이션 233, 서버 재검증·멱등·**알림 INSERT 전무**)·정산 페인 「과거 미등록」 별도 뷰(다중선택 일괄 「송금완료 기록」[확인 모달]/「정산대기 추가」). 사양서 `docs/specs/2026-07-09-settlement-cutoff-past-handling.md`
- **인플루언서 노출 잠금(마이그레이션 240·241·242, 2026-07-20 — 「관리자만 기록」 단계)**: `settlement_settings.influencer_visible boolean NOT NULL DEFAULT false` 로 **인플루언서 대상 출구 3곳을 컷오프와 별개로 잠금**. 판정은 `is_settlement_public()`(SECURITY DEFINER `search_path=''`·STABLE·`COALESCE(...,false)` fail-closed·GRANT `authenticated` 만 — `is_brand_survey_open()` 미러링). ①`settlements` 본인 조회 정책 `settlements_select_own` = `influencer_id = auth.uid() AND is_settlement_public()`(관리자 정책 `settlements_select_admin` 무변경) ②`mark_settlement_paid` 의 `settlement_paid` 알림 INSERT 조건화(241) ③`backfill_settlements` 의 `settlement_paypal_required` 알림 조건화(242 — `paypal_missing_count` 집계·컷오프·멱등 로직은 무변경). **`settlement_events` 감사 이력은 스위치와 무관하게 항상 기록**. 클라는 `settlementPublic()`(shared.js, fail-closed 캐시 — `app.js` 부팅·로그인 시 1회 로드)로 햄버거 「報酬・精算」 항목·`openMypageSub('settlements')` 진입(응모이력 폴백)·`fetchMyNotifications` 정산 kind 2종을 함께 차단. **공개 전환은 `UPDATE settlement_settings SET influencer_visible=true WHERE id=1` 한 줄(코드 재배포 불필요, 사용자 새로고침 후 반영)**. ⚠️ PayPal 미등록 안내 알림도 차단되므로, 잠금 기간에는 미등록 인플에게 관리자가 응모건 메시지·LINE 등 사람 경로로 등록을 요청해야 송금완료 기록이 가능(`mark_settlement_paid` 는 PayPal 없으면 거부)
- **관리자 정산 페인**(`/admin#settlements`, `dev/js/admin-settlements.js` — PR2): 사이드바 「회원 관리」 그룹 정산 관리 항목(정산대기 건수 배지). 진입 시 `backfillSettlements()` best-effort 호출로 인증 성공 응모 자동 등록 → 목록(인플·캠페인·금액¥·PayPal[미등록 경고]·상태·인증성공일·송금완료일). 상태 탭(전체/정산대기/송금완료/보류/취소, 각 건수 — 캠페인 상태 탭 패턴 미러 `status-tab-bar`, 기본 정산대기) + 캠페인 검색형 다중필터(`settlementCampMulti`, 결과물 페인 `syncCampMultiFilter` 재사용)·인플 검색·IntersectionObserver lazy-load·엑셀(현재 필터 결과). 행별 처리 버튼은 상태 전이 규칙대로 노출: **pending**=송금완료·보류·취소 / **paid**=보류(환수) / **on_hold**=보류 해제(정산대기 복귀)·취소 / **cancelled**=없음. 송금완료 모달은 PayPal·금액·건수 확인+메모. 낙관적 락 충돌 시 RPC `-1` 반환→「이미 처리됨」 토스트. 행별 「이력」 버튼(상태 무관 항상) → 정산 이력 모달(`settlement_events` 타임라인: 시각·동작[생성/송금완료/보류/취소/보류해제]·상태 전이·처리자·**사유(memo)**, `get_settlement_events` RPC 마이그225·`fetchSettlementEvents`) — 입력한 사유는 이 모달에서 확인. `menu.settlements` 권한 시드(마이그221, campaign_admin=write·campaign_manager=hidden — `settlement.view`와 동일)라 매니저는 사이드바·조회 모두 차단
- **정산 상태 변경 RPC 4종**(마이그레이션 222·223·224, SECURITY DEFINER·`has_permission('settlement.pay','write')` 가드·낙관적 락 version 충돌 시 -1 반환): `mark_settlement_paid`(pending→paid, paypal_email 재조회 갱신·미등록이면 차단·`settlement_paid` 알림[일본어]·events action='pay') / `mark_settlement_hold`(pending·paid→on_hold, paid_* 보존해 환수 근거 유지·알림 없음) / `mark_settlement_cancel`(pending·on_hold→cancelled, paid는 직접 취소 불가·먼저 보류) / `mark_settlement_revert`(마이그224, **on_hold→pending 보류 해제**, paid_* 보존, 알림 없음 — 보류 건 재정산 경로). 상태 전이: pending→paid/on_hold/cancelled, paid→on_hold, on_hold→pending(revert)/cancelled, cancelled=종료. storage.js `markSettlementPaid`/`markSettlementHold`/`markSettlementCancel`/`markSettlementRevert`(id, version, memo)
- **인플루언서 정산 조회 화면**(마이페이지 서브화면 `#mypage-sub-settlements` 「報酬・精算」, `dev/js/mypage.js` `renderMySettlements` — PR3): 햄버거 「マイページ」 아코디언 서브항목. 상단 누적 수령액 큰 카드(送金済み 합계만·¥) + 대기액 보조 + PayPal 미등록 시 안내 박스+「PayPal登録」 버튼(→PayPal 화면). 내역 리스트(캠페인 썸네일·명·금액¥·상태 pill·송금일). 상태 라벨 인플용: 精算待ち(pending)/送金済み(paid)/確認中(on_hold). **취소(cancelled)는 숨김**. i18n ja/ko 양쪽 등록·langchange 재렌더. 알림 라우팅: `settlement_paypal_required`→PayPal 화면, `settlement_paid`→정산 화면. `fetchMySettlements()`(RLS 본인행). DB 변경 없음(순수 프론트)
- **정산 자동 보류 + 반려 가드 2종**(2026-07-21, 마이그레이션 246·247, 트리거): 승인됐던 신청이 반려·취소되면 연결 정산이 pending 이면 자동 on_hold(고정 memo `신청 반려로 자동 보류`, `settlement_events` action='hold', paid 는 미변경). 관리자 정산 화면에 「자동 보류(신청 반려)」 앰버 배지(`memo LIKE '%자동 보류%'`) — 복원은 기존 「보류 해제」(`mark_settlement_revert`). 트리거 `auto_hold_settlement_on_app_reject`(AFTER UPDATE OF status, 246) + `guard_reject_with_paid_settlement`(BEFORE UPDATE, 247 — 송금완료 정산 있으면 반려·되돌리기 `RAISE 22023` 차단). 관리자 UI 가드: `updateAppStatus` 가 반려/되돌리기 전 `guardRejectOrRevert`(A: 송금완료면 경고+차단 `hasPaidSettlementForApplication` / B: 제출된 결과물 있으면 확인 알럿). **인플루언서 미노출**(정산 잠금·알림 없음 정합). 사양서 `docs/specs/2026-07-21-rejected-application-deliverable-and-settlement.md`
- `storage.js`: `fetchSettlements(opts)`(관리자·1000행 페이지네이션)·`fetchMySettlements()`(인플 본인)·`backfillSettlements()`·`hasPaidSettlementForApplication(appId)`(반려 가드용)

### 메일·기준 데이터·알림
- `lookup_values` — 캠페인 기준 데이터. `kind`(channel/category/content_type/ng_item/reject_reason/blacklist_reason/violation_reason/caution/admin_email_kind/cancel_reason/**admin_proxy_reason**), `code`, `name_ko`, `name_ja`, `sort_order`, `active`, `recruit_types[]`. channel 만 recruit_types 사용. `admin_proxy_reason`(마이그레이션 160) 시드 4건: shipping_delay/system_error/inflexible_deadline/other
- `participation_sets` — 참여방법 번들. `name_ko`/`ja`, `recruit_types[]`, `steps jsonb`, `sort_order`, `active`. `campaigns.participation_steps jsonb` + `participation_set_id` FK ON DELETE SET NULL 로 스냅샷·원본 참조
- `caution_sets` — 주의사항 번들. items 구조 `{text_ko, text_ja, link_url?, link_label_ko?, link_label_ja?, text_after_ko?, text_after_ja?}`. `campaigns.caution_items jsonb` + `caution_set_id` FK ON DELETE SET NULL. RLS SELECT 관리자 (인플은 campaigns 스냅샷 경유)
- `ng_sets` — NG 사항 번들. items 구조 `{html_ko, html_ja}` (DOMPurify, inline 서식만). `campaigns.ng_set_id` + `ng_items jsonb` 스냅샷. RLS SELECT `is_admin()`, CUD `is_campaign_admin()` 이상. 신청자 동의 시점 스냅샷 없음 (NG는 표시용 가이드라인)
- `notifications` — 인플루언서 알림. `kind`(deliverable_rejected/deliverable_changed/deliverable_approved/application_cancelled/message_received/application_approved/**deliverable_proxy_submitted**/**settlement_paypal_required**/**settlement_paid**), `ref_table`/`ref_id`, `title`, `body`, `read_at`. deliverables.status 전이 트리거로 자동 생성(deliverable_*), 재제출 시 미읽음 알림 자동 dismiss. `message_received`(마이그레이션 145)는 관리자가 응모건 메시지에 답장 시 send RPC 가 INSERT(ref_table='applications', 미읽음 중복 방지). `application_approved`(마이그레이션 154)는 신청 pending/rejected→approved 전이 시 `record_application_status_event()` 트리거가 INSERT(ref_table='applications', 미읽음 중복 방지 — 되돌리기 후 재승인 대응, title「キャンペーンに当選しました」). `deliverable_proxy_submitted`(마이그레이션 160)는 관리자가 결과물을 대리 등록·자동 승인할 때 `admin_create_deliverable_proxy()` RPC 가 직접 INSERT(ref_table='deliverables', title「結果物が登録されました」). 알림 모달에서 `message_received` 클릭 시 메시지 모달 직접 오픈, `application_approved`/`application_cancelled`는 응모이력으로 이동(kind 로 분기 — ref_table='applications' 공유하므로 kind 한정 필수)
- `admin_daily_digest_runs` — 관리자 통합 다이제스트 발송 로그. `digest_date` UNIQUE(중복 호출 차단 + INSERT 선행 mutex) + `status CHECK(sent|skipped_no_data|failed)` + `sections_summary jsonb`(`{received, cancelled, submitted, reprocessed}`) + `recipients_count` + `error_message` + `run_at`. RLS SELECT `is_admin()`, INSERT 는 service_role 만 우회
- `influencer_daily_digest_runs` / `application_received_admin_digest_runs` — 메일 파이프라인 운영 로그. `digest_date` UNIQUE
- `deadline_reminder_email_sent` — 영수증/결과물 D-5·D-1 임박 메일 재발송 차단. UNIQUE 4-tuple `(influencer_id, campaign_id, kind, d_minus)`
- 캠페인 홍보 메일 4종(주 2회 다이제스트, 모두 RLS SELECT `is_admin()` 한정·INSERT/UPDATE/DELETE 정책 없음 → service_role만 우회):
  - `campaign_promo_digest_runs` — run 로그. `digest_date` UNIQUE mutex + `status`/카운트/`included_campaign_ids`/`started_at`·`finished_at`
  - `campaign_promo_digest_sent` — 인플별 발송 결과. `(influencer_id, digest_date)` UNIQUE 로 1통/cron 보장 + `skip_reason`
  - `campaign_promo_exposure` — 노출 기록. `(campaign_id, influencer_id, kind)` UNIQUE 로 캠페인당 최대 2회 보장(「관심 없는 캠페인 매일 노출」 방지)
  - `campaign_promo_email_clicks` — CTA 클릭 추적. `(campaign_id, influencer_id)` UNIQUE. 클릭된 페어는 다음 다이제스트 자동 제외
  - 관련 RPC: `get_promo_digest_targets(date)`(자격 매칭 인플)·`get_promo_digest_campaign_pool(date)`(관리자용 풀 전체, 개인화 제외)·`mark_promo_digest_sent`·`track_promo_click`(anon)·`unsubscribe_by_token`(anon 1-click 수신거부)·`resubscribe_marketing`(본인 재구독)
- 헬퍼 함수 `_yesterday_kst_window()` STABLE — 어제 KST 윈도우 + 오늘 KST 날짜 반환 (SQL Editor 디버깅용)

### 사용자 앱 에러 수집 (마이그레이션 165, 수집 기반 PR1·2 — 관리자 화면 PR3 예정)
> 인플루언서 앱에서 발생한 에러를 관리자가 모아 보는 기능(실시간 아님). 수집은 백그라운드 무음, 개인정보 마스킹 필수.
- `client_error_logs` — 에러 fingerprint 묶음 테이블. `fingerprint`+`status`(open/resolved/ignored) UNIQUE 로 같은 에러는 1행에 `occurrence_count` 누적. `source`(influencer/admin)·`kind`(unhandled/rejection/handled)·`message`/`stack`/`page_hash`(마스킹됨)·`error_code`·`user_id`(influencers FK, anon 은 NULL)·`first/last_seen_at`·`resolved_by/at/note`. RLS SELECT `is_admin()` 만, INSERT/UPDATE 는 RPC 경유
- `report_client_error(...)` RPC — SECURITY DEFINER, **anon+authenticated** 호출. 빈값·범위 가드 + 길이 제한 + **서버측 2차 마스킹**(이메일·전화·우편번호 `\d{3}-\d{4}`·Bearer 토큰·PostgreSQL `(col)=(val)`) + open fingerprint UPSERT
- `resolve_client_error(id, status, note)` RPC — `is_admin()` 가드, 상태 변경(resolved/ignored/open 되돌리기 시 resolved_* 초기화)
- 클라: `dev/js/error-report.js`(전역 `window.onerror`·`unhandledrejection` 핸들러 + `friendlyErrorJa` 훅 + 1차 마스킹·fingerprint·노이즈필터·60초 디바운스·재진입가드·throw 안 함), `storage.js` `reportClientError()`. 사양서 `docs/specs/2026-06-02-client-error-reporting.md`

### RLS·인증·세션
- 정책 요약: 캠페인 SELECT 공개, 나머지는 본인 데이터 or 관리자만 접근
- `is_admin()` / `is_super_admin()` / `is_campaign_admin()` 함수: admins 테이블에서 auth.uid() 조회 (search_path 고정)
- 트리거: auth.users 생성 시 influencers 레코드 자동 생성
- 세션 만료 대응: `retryWithRefresh()` 로 RLS/JWT 에러 시 세션 갱신 후 1회 재시도

## Test Accounts
- 관리자: admin@kemo.jp / admin1234
- 테스트 인플루언서: sakura.test@reverb.jp, yui.test@reverb.jp, haruka.test@reverb.jp (비밀번호: test1234)

## Dev Workflow
- 개발: dev/ 폴더에서 수정 → 브라우저에서 dev/index.html 열어서 확인
- 배포: `cd dev && bash build.sh` → 루트 index.html 자동 업데이트
- 수정할 파일 찾기: 파일명이 기능과 일치 (캠페인=campaign, 로그인=auth 등)
- DB API: `dev/lib/storage.js` 에 모든 DB 함수 집중 (fetchCampaigns, upsertInfluencer 등)
- 세션 관리: onAuthStateChange 로 SIGNED_IN/TOKEN_REFRESHED/SIGNED_OUT/SESSION_EXPIRED 처리 (인플루언서+관리자 양쪽)
- URL 정제: `cleanUrl()` 로 마크다운 링크 형식 자동 변환 (product_url 등)
- 페이지 전환: 관리자/인플루언서 화면 같은 탭에서 이동 (새 탭 열기 금지). ⚠️ **예외 2종은 새 탭이 맞다** — ①앱 경계를 넘는 전환(인플루언서 앱 → `/admin/`, `dev/js/app.js`) ②**자립형 단독 화면**(`admin-setpw.html`·`event-scan.html`). 특히 현장 확인 화면은 **부스 단말에 계속 띄워 두고 쓰는 도구**라 같은 탭에서 열면 보던 예약 현황이 사라진다. 이 예외를 모르고 「규칙 위반」으로 되돌리는 일이 없도록 여기에 적어 둔다(2026-08-05)
- 깜빡임 방지: visibility:hidden cloak 기법 (인플루언서+관리자 양쪽)
- 마이페이지 서브해시: `#mypage-applications` 등 URL 해시로 서브페이지 복원
- 약관/정책 수정: `docs/{TERMS,PRIVACY}_{kr,ja}.md` 가 source of truth. 인플루언서 앱 푸터 약관은 `dev/lib/legal.js` 가 이 4개 md 를 **런타임 fetch + 마크다운 렌더링** — 별도 복사본 없음, 문서만 고치면 앱 반영(빌드 무관). 변경 시 한·일 동시 수정 + 부칙 갱신 (`.claude/rules/policy.md`)

## Conventions
- 인플루언서 페이지 UI 텍스트: 일본어
- 관리자 페이지 UI 텍스트: 한국어
- 코드 주석: 한국어 (일본어 금지)
- 날짜 포맷: ja-JP
- `lang="ja"`

## Rules
- 관리자 페이지는 반드시 PC 레이아웃 유지 (모바일 쉘 적용 금지)
- 인플루언서 페이지만 모바일 전용 (480px)
- db 참조 시 항상 `db?.from()` 사용 (null-safe)
- `.single()` 대신 `.maybeSingle()` 사용
- localStorage 저장 시 이미지 base64 는 별도 키로 분리 (용량 초과 방지)
- 캠페인 삭제는 **보관 삭제(soft delete)** — 캠페인 행은 `deleted_at` 세팅해 30일 보관(「삭제됨」 탭에서 복구 가능), 관련 applications·deliverables(개인정보)는 삭제 즉시 완전 파기(개인정보 파기원칙 정합). 정산 걸린 캠페인은 삭제 차단(마이그레이션 251 트리거). 복구=campaign_admin·완전삭제=super_admin. 30일 후 pg_cron 자동 완전삭제(`purge_expired_deleted_campaigns`). 사양서 `docs/specs/2026-07-22-campaign-soft-delete-restore.md`
- 이미지 업로드는 Supabase Storage (`campaign-images` 버킷) 사용
- 비밀번호 재설정 시 Supabase Redirect URL 설정 필수: Authentication → URL Configuration → Redirect URLs 에 양 도메인 등록
- 아이콘은 Material Icons 사용 (이모지 사용 금지), `translate="no"` 속성 필수
- 하드코딩 DOM 인덱스 금지 (querySelector 등에서 `:nth-child` 인덱스 직접 사용 금지)
- 이미지 썸네일 표시는 `imgThumb(url, width, quality)` 헬퍼 사용 (Supabase Pro 플랜 transform), `data-orig` + `onerror` 로 원본 URL 폴백 필수
- 채널 비교는 항상 `split(',')` 후 `includes()` 사용 (단일 `===` 비교 금지 — 멀티채널 캠페인 누락 위험)
- **채널 코드 이관은 3곳 동시가 한 세트**(2026-07-30 사고 교훈): 기준 데이터(`lookup_values`) 의 채널 `code` 를 바꾸거나 지울 때는 **①기준 데이터 ②캠페인(`campaigns.channel`) ③이미 제출된 결과물(`deliverables.post_channel`)** 을 **함께** 옮긴다. 셋 중 하나만 남으면 「캠페인 요구 채널 = 결과물 채널」 문자열 비교가 깨지고, 그 비교가 **인플루언서 활동관리 화면 · 관리자 인증 상태(`computeCertStatus`) · 정산 후보(`_settlement_cert_candidates()`)** 3곳에 있어 동시에 누락된다. 실제 사고: @cosme 코드가 `channel-96r9y3`→`cosme` 로 바뀔 때 ③을 안 옮겨 **승인된 리뷰 인증샷 55건이 3곳에서 함께 사라짐**(2026-07-30 교정 완료 — 패치 `supabase/patches/2026-07-30-fix-cosme-review-image-channel-code.sql`, 보고·공지 초안 `docs/specs/2026-07-30-cosme-channel-affected-report.md`). ⚠️ **기존 관리자 도구로 못 고친다** — 마이그레이션 162 의 채널 지정·해제·삭제 3함수는 「채널이 이미 지정됨」·「검수 완료됨」 조건에 걸려 전부 거부하고, 검수 모달의 레거시 구제 장치(`hasLegacyReviewImage`)는 **채널이 빈 값일 때만** 작동해 「값이 있고 그 값이 틀린」 경우는 사각지대다(그래서 두 달간 아무도 발견하지 못했다). ⚠️ 교정 시 `status` 를 건드리면 옛 결과물에 인플루언서 알림이 한꺼번에 나가고, 같은 응모에 옛·새 코드가 **공존하는 행(이번 사고에서 26건)** 은 중복 금지 제약(`deliverables_review_image_app_channel_uniq`)에 걸린다 — 조건으로 미리 배제할 것. **이 26건은 2026-07-31 「그대로 유지」로 결정됐다**(같은 응모의 새 코드 행이 전부 승인이라 화면·인증·정산 모두 정상. 상세는 보고서 §4 「26건 유지 결정 근거」).
  - **재발 방지 — 캠페인 채널 변경 가드**(2026-07-31): ①캠페인 편집에서 **결과물이 이미 제출된 채널을 빼면 확인창**(제거 채널 라벨 + 영향 건수 표시, 막지 않고 묻는다 — 잘못 들어간 채널을 빼야 하는 정당한 경우까지 막으면 편집이 막다른 길이 된다). 건수는 `countDeliverablesByChannels`(반려·취소 신청 제외) ②**모집 형식 라디오를 눌러도 저장된 채널이 증발하지 않는다** — `renderChannelCheckboxes` 의 보존 기준을 「화면 체크값」이 아니라 **`opts.savedCodes`(데이터베이스에 저장된 값)** 로 분리했다. 이 기준이 아니면 둘 중 하나가 깨진다(화면 체크값 기준=신규 캠페인에 형식과 안 맞는 채널이 새로 생김 / 보존 없음=편집에서 라디오 한 번에 저장된 채널이 경고 없이 사라짐). ⚠️ **비활성 처리도 같은 위험** — `fetchLookups` 가 `active=true` 만 반환해, 「삭제 말고 비활성으로」 안내를 따라도 목록에서 빠지는 건 같다. ⚠️ 확인창은 `_editCampOriginal.channel` **스냅샷에 의존**하므로 그 키가 비면 죽은 코드가 된다(브라우저 1회 발동 확인 필수)
  - **감지 장치 — 구현 완료**(2026-07-31, 마이그레이션 277 + **278**(반려 제외) `detect_channel_code_drift()`): 조회 전용·`is_admin()` 가드·인플루언서 개인정보 미반환. 3층 = **A**(결과물 채널이 그 캠페인 요구 채널에 없음 — 실제 피해) · **B**(캠페인 채널 토큰이 기준 데이터에 없음 — A보다 먼저 뜨는 조기 경보) · **C**(결과물 채널이 기준 데이터에 없는 값, `other` 등). ★**제외 규칙 = 「같은 응모·같은 종류에 캠페인 채널과 정확히 일치하는 행이 있으면 뺀다」(상태 불문)** — 이것만으로 옛·새 코드 공존 26건이 **예외 목록·하드코딩·컷오프 없이** 자동 통과하고 55건 같은 진짜 문제는 잡힌다. ⚠️ **`covered` 는 A층·C층 양쪽에 적용**해야 한다(옛 코드는 기준 데이터에서 하드 삭제됐으므로 A에서 빼도 C에 그대로 걸린다 — 검증에서 실제로 잡힌 결함). ⚠️ **승인 상태를 요구하지 않는 이유** = 실제 판정 세 곳(`_finalizeMonitorReprs`·`count_pending_review_applications`·`_settlement_cert_candidates()`)이 전부 「그 채널에 행이 존재하는지」만 보고 상태는 안 본다. 승인을 요구하면 「올바른 채널로 재제출했지만 검수 전」인 정상 건이 오탐된다. ⚠️ **반려된 결과물은 경보하지 않는다**(278) — 이미 무효라 인증·정산 어느 판정에도 안 쓰인다. ⚠️ 그 조건은 **`mismatched`·C층에만** 넣고 `deliv` 에는 넣지 않는다 — `covered` 는 채널 점유를 **상태 불문**으로 봐야 하므로, `deliv` 에서 반려를 빼면 「올바른 채널에 반려 행만 있는」 경우 점유가 사라져 어긋난 행이 엉뚱하게 A층에 뜬다. **화면 노출 완료**(2026-07-31) — **①사이드바 경고 아이콘**(결과물 관리·기준 데이터 두 항목, **부팅 시 1회 조회**해 그 화면에 안 들어가도 보인다 — 「들어가야만 알 수 있으면 늦다」) **②페인 제목 옆 경고 버튼**(0건이면 버튼째 숨김) **③클릭 시 모달**(경고 내용 + **결과물 종류별 조치 방법** + 「캠페인 편집 열기」 버튼). 모달은 `DRAGGABLE_ADMIN_MODALS` 등록으로 **드래그·크기 조정** 가능 — 띄워둔 채 조치를 따라갈 수 있다. 함수 = `refreshChannelDriftIndicators`(조회+갱신) · `applyChannelDriftIndicators`(캐시로 표시만) · `openChannelDriftModal` · `channelDriftModalHtml`/`channelDriftRowHtml` · `channelDriftFixHtml(layer, kind)`(admin-core.js). ⚠️ **조치 안내는 결과물 종류로 분기한다** — 「검수 창의 채널 불일치 표시에서 지운다」 도구는 **게시물 전용**이고 **리뷰 인증샷에는 없다**(채널이 어긋나면 그 채널로 조회되지 않아 검수 창에 아예 안 나타난다 — @cosme 사고가 이 경우). 종류를 안 가리고 안내하면 **없는 도구를 찾게 만든다**. ⚠️ **0건이면 아무것도 안 그린다** — 숫자가 늘 떠 있으면 「원래 빨간 게 있는 화면」으로 학습돼 두 달간 아무도 몰랐던 이번 사고가 반복된다. **운영 실측을 A·B·C 전부 0으로 만든 뒤에** 켰다. 조회 실패 시에도 안 그린다
  - **운영 실측 결과**(2026-07-31): **A층 0 · B층 0 · C층은 옛 @cosme 26건(제외 규칙이 자동 통과) + `other` 2건(둘 다 반려 → 278 로 제외)** → 정리 후 전체 0. 즉 **화면 경보를 0에서 시작할 수 있는 상태**가 됐다
  - **아직 안 한 재발 방지 1종**: 채널이 빈 캠페인에서 게시물 채널로 **「その他」를 고를 수 있는 구멍**(편집 저장이 시딩·방문형의 채널 0개를 허용하는 게 원천. ⚠️순서 = 편집 필수화 → 남은 캠페인 정리 → 「その他」 제거. 먼저 지우면 채널 빈 캠페인의 인플루언서가 고를 값이 0개가 된다)
- 최소 팔로워수 정책: **primary_channel 단일 검증** (캠페인의 `primary_channel` 팔로워수만 `min_followers` 와 비교, 없으면 채널 리스트 첫 번째로 폴백. `recruit_type='monitor'` 는 팔로워 체크 건너뜀) — 상세 `docs/FEATURE_SPEC.md` §10, 구현 `dev/js/application.js`
- **Sales(광고주) 서브도메인 규칙**: `sales.globalreverb.com` / `sales-dev.globalreverb.com` 페이지 UI 는 한국어, `<meta name="robots" content="noindex,nofollow">` 유지(검색 노출 차단). 루트에 choice landing + `/reviewer`·`/seeding` 경로는 Vercel `cleanUrls` 로 HTML 확장자 제거. 파일 업로드 기능 없음 — 텍스트 입력만
- **익명 폼 INSERT 패턴**: anon 이 쓰는 Supabase 테이블은 `.insert().select()` 대신 **SECURITY DEFINER RPC** 로 감쌀 것 (RLS WITH CHECK + RETURNING SELECT 권한 충돌로 42501 발생 사례)
- **관리자 리스트 IntersectionObserver lazy-load**: 8개 목록 페인(campaigns/applications/deliverables/camp-applicants/influencers/lookups/admin-accounts/brand-applications) 모두 sentinel 기반 점진 렌더. 필터·검색·정렬 변경 시 sentinel 리셋 필수. `renderAppCampList` 는 campaigns/applications/influencers 결과 in-memory 캐시 공유
- **PostgREST 1000-row cap 대응**: 대시보드 집계용 fetch(`fetchInfluencers`/`fetchApplications`/`fetchDeliverables` 등)는 반드시 `range(from, from+999)` pagination loop 로 전건 조회. 단일 `.from().select()` 호출은 1000건에서 잘림

## Mobile Layout Rules
- `#appShell` 은 `position:fixed` + `top:0`/`bottom:0` (body 스크롤 차단, 뷰포트 고정)
- html, body 에 `height:100%` + `overflow:hidden` 유지
- 페이지 콘텐츠 스크롤은 `.page.active` 내부에서만 (`flex:1` + `overflow-y:auto`)
- GNB 는 `flex-shrink:0` 으로 고정, 페이지가 나머지 공간 차지 (바텀탭 제거됨 — 햄버거 메뉴로 대체)
- 모바일 키보드 대응: visualViewport API 로 `appShell` 높이 동적 조절
- input/textarea/select 의 `font-size` 는 반드시 16px 이상 (모바일 자동 확대 방지)
- `100vh`/`100dvh` 대신 `position:fixed` + `top:0`/`bottom:0` 사용 (키보드 열림/닫힘 안정성)
- 캠페인 상세 URL 은 `#detail-{id}` 형식 (새로고침 시 복원 가능)

---

## 변경 이력
이력성 메타데이터(마이그레이션 번호·PR 번호·deprecated 메모·과거 변경 사항)는 [`docs/CLAUDE-ARCHIVE.md`](docs/CLAUDE-ARCHIVE.md) 참조.
