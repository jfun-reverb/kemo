---
description: REVERB JP 프로젝트 전체 맥락 로드 (CLAUDE.md + PROJECT_CONTEXT.md + 최근 git log)
---

다음을 읽고 현재 프로젝트 상황을 요약해주세요:

1. `CLAUDE.md` — 프로젝트 전반 규칙 + **현재 구현 상태의 단일 소스**
2. `docs/PROJECT_CONTEXT.md` — 비즈니스·법률·데이터 모델 맥락
3. `.claude/rules/` 의 규칙 파일 — **여기에 목록을 적지 않는다**(계속 늘어난다). `ls .claude/rules/` 로 볼 것
   - ⚠️ 머리말에 `paths:` 가 있는 규칙은 **그 경로의 파일을 열 때만** 읽힌다. 이 요약 시점에는 컨텍스트에 없을 수 있으니, 없다고 「규칙이 없다」로 읽지 말 것
4. 최근 git 커밋 10개 (`git log --oneline -10`)
5. 현재 브랜치 (`git branch --show-current`)

요약 출력 형식:
- **프로젝트 개요** (1~2문장)
- **현재 작업 중 브랜치 + 최근 진행** (커밋 제목 기반)
- **아직 미해결 이슈** — 🔴 **`CLAUDE.md` 를 근거로 삼는다.** 각 절의 「미착수」·「미적용」·「아직 없다」·「후속 과제」 표시가 실제 상태다
  - ⚠️ `PROJECT_CONTEXT` 의 구현 상태 목록은 **낡을 수 있다**(2026-08-31 실측: 넉 달 낡아 「구현됨」·「미구현」 양쪽이 뒤집혀 있었다). 그것만 근거로 「미구현」이라 보고하지 말 것 — **코드로 한 번 확인**하고 적는다
- **최근 수정된 영역** (커밋 로그 기반 추론)

요약 끝에 "무엇을 도와드릴까요?" 질문으로 마무리.
