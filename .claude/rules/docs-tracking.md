# 문서 추적 규칙 — 기획 초안 vs 구현 결과 (영구)

> 기획/설계 세션이 초안을 작성하고, 개발 세션이 실제 구현한 뒤 **무엇이 달라졌는지**를 문서에 남기는 규칙. 다음 기획 세션이 현황을 파악할 수 있도록 하기 위함.

---

## 문서별 역할 분리

| 문서 | 역할 | 작성 주체 |
|---|---|---|
| `docs/specs/YYYY-MM-DD-feature-name.md` | 기획 초안 (무엇을 만들 것인가) + **구현 결과** (어떻게 실제로 만들었나, 무엇이 달라졌나) | 기획 세션이 초안 → **개발 세션이 구현 결과 추가** |
| `docs/FEATURE_SPEC.md` | 현재 구현된 기능의 최신 상태 (레퍼런스) | **개발 세션이 기능 커밋과 동시에 업데이트** |
| `CLAUDE.md` | DB 스키마·아키텍처·URL 등 기술 레퍼런스 | **개발 세션이 변경 시 동시에 업데이트** |
| PR description | 변경 요약 + 요청 외 추가 변경 내역 | 개발 세션 |

---

## 개발 세션 의무 (커밋 전 필수)

### 1. 기능 구현 커밋 = 코드 + 문서 한 세트
- 신규 기능·DB 변경·동작 변경이 있는 커밋에 `FEATURE_SPEC.md` + `CLAUDE.md` 업데이트를 **동일 커밋**에 포함
- 나중에 몰아서 `docs:` 커밋으로 처리하는 것 금지 (마이그레이션 117·118 누락 사례 재발 방지)

### 2. 기획 초안 사양서가 있는 작업 → 구현 결과 섹션 추가 (의무)
`docs/specs/` 안에 해당 일감의 초안 파일이 있으면, **구현 완료 후** 하단에 아래 섹션을 추가:

```markdown
## 구현 결과

**구현일:** YYYY-MM-DD
**관련 커밋:** (커밋 해시 또는 PR 링크)

### 초안 대비 변경 사항
- 추가된 것: (기획 초안에 없었지만 구현 중 추가된 것)
- 빠진 것: (기획 초안에 있었지만 구현에서 제외된 것 + 이유)
- 달라진 것: (방향은 같지만 구현 방식·범위가 바뀐 것)

### 구현 중 기술 결정 사항
- (초안에 없던 마이그레이션·RLS·DB 구조 결정 등)
```

### 3. 기획 초안 없는 작업도 중요 변경은 기록
- 버그 수정이라도 DB 구조·동작 흐름이 달라지면 `FEATURE_SPEC.md` 또는 `CLAUDE.md`에 반영
- 단순 CSS·라벨 변경은 생략 가능

---

## CLAUDE.md 가 부풀지 않게 — 빼는 규칙 (REVERB 세부)

> **원칙 정의처는 글로벌 `~/.claude/rules/common-doc-tracking.md` 「레퍼런스 문서가 부풀지 않게」.** 여기는 이 저장소의 파일 이름과 실측만 적는다.

| | 이 저장소에서 |
|---|---|
| 레퍼런스 문서 | **`CLAUDE.md`** — 자동으로 읽히는 유일한 문서. **한도 150,000자**(넘으면 세션 시작 시 `CLAUDE.md is over the 150.0k-char limit` 경고). ⚠️ **잘림 경고가 아니다** — 공식 문서상 4 MiB 까지는 전부 로드된다. **길수록 준수도가 떨어진다**는 성능 경고이고, 공식 목표치는 **200줄**이다. 🔴 **이 숫자를 맞추려고 경고(⚠️·🔴)를 빼지 말 것** — 뺄 것은 낡은 사실이다 |
| 이력 보관소 | **`docs/CLAUDE-ARCHIVE.md`** — 변경 번호·병합 요청 노트·과거 이력 |
| 설계 근거 | **`docs/specs/*`** — 왜 그렇게 정했는지 |

### 🔴 실측 (2026-08-26)

**150,881자로 한도를 넘었다.** 2026-05-19 슬림화(75KB→), 2026-05-29 재슬림화(65k→53.9k)를 거치고도 **석 달 만에 2.8배**가 됐다.

**재실측 (2026-08-31): 159,446자 → 정리 후 149,631자**(9,815자 감소, 한도 아래). 닷새 만에 8,565자가 더 늘어 있었다.

🔴 **줄어든 것의 대부분은 「이력」이 아니라 「조사 과정」이었다.** 낡은 서술 9건(전부 「운영 미배포」라 적혀 있었으나 운영에 있던 것)을 고쳐도 **1,695자밖에 안 줄었다.** 나머지 8,000자는 **규칙이 어떻게 발견됐는지**를 적은 부분에서 나왔다 — 어느 변경이 어느 CTE 를 고쳤는지, 잘못 센 것을 드러낸 운영 건수, 정책을 바꾸게 한 표본 조사, 변경 번호에 붙은 배포 날짜.

★ **경고는 오히려 늘었다 — ⚠️+🔴 344개 → 352개.** 부피를 줄이면서 경고를 빼지 않았다는 증거다(몇 개는 ⚠️ → 🔴 로 올렸다: 조용히 실패하는 것들). 같은 날 대조에서 **지금도 사실인 「미완」 서술 6건**(정산 잠금·파기 큐 소비 화면 없음·일괄발송 첨부 미구현·정산 도입일 화면 없음·추천 3~5단계 미착수·완전삭제 시 캠페인 이미지 잔존)은 **그대로 두었다** — 「미배포」라고 적혔다고 다 낡은 것이 아니다. **반드시 코드·운영 데이터베이스와 대조하고 고칠 것.**

절별 — `Database Schema` **57%** · `Features — 관리자` 22%. 그 안에서 「정산」 19k자 · 「회원 탈퇴」 17.7k자로 **둘이 24%**.

**부피의 원인은 이력이 아니었다.** 「정산」 절에 이런 것이 **나란히 살아 있었다**:

- 「정산 금액 = **상시가**」(마이그레이션 261·262, 7/23) — ✅ **2026-08-31 정정됨**
- 「위 261·262 가 정한 「상시가」를 **실무에 맞춰 정정** — 영수증 실결제액」(299·300·301, 8/6)

**앞의 것은 이미 뒤집혔는데 본문에 그대로 있었다.** 현재 동작을 알려면 둘 다 읽고 스스로 판정해야 했다. ⚠️ 게다가 그 항목은 **「dev — 운영 미배포」 딱지까지 달고** 1,791자를 차지하고 있었다 — 실제로는 운영에 적용돼 있었다. **뒤집힌 규정과 낡은 배포 딱지는 같이 온다.**

⚠️ 같은 문서가 다른 자리에서는 **몇 달간 없는 기능을 있다고 적고 있었다** — 「현황 대시보드에 Vercel 외부 링크가 있다」(관리자 코드에 그 문자열 0건, 2026-08-18 확인). 낡은 서술을 안 지우면 이렇게 된다.

### 누가 하나

**`CLAUDE.md` 수정은 개발 세션 소관**이다([`session-roles.md`](session-roles.md) §2). 고문·기획은 진단하고 넘긴다.

### 어떻게 줄이나 — 공식 문서 확인 결과 (2026-08-31 조사)

🔴 **`@경로` 로 쪼개는 것은 부피 해결책이 아니다.** 가장 흔히 추천되는 방법인데 공식 문서가 명시적으로 부정한다 — 「imports 로 나누면 **정리에는 도움이 되지만 컨텍스트를 줄이지는 않는다**. 가져온 파일도 시작 시 함께 로드되기 때문」.

**실제로 줄이는 방법은 둘뿐이다** — 둘 다 「**읽을 때만** 로드」가 핵심이다:

| 방법 | 언제 로드되나 |
|---|---|
| **경로 조건부 규칙** — `.claude/rules/*.md` 에 `paths:` 머리말 | 그 경로의 파일을 **읽을 때만** |
| **하위 폴더 CLAUDE.md** — `dev/CLAUDE.md` 같은 | 그 폴더 파일을 **읽을 때만** |

⚠️ **이 저장소에서는 그냥 적용하면 안 된다** — 이 문서의 가장 값어치 있는 부분이 「**같은 판정이 여러 곳에 있다**」는 경고인데(2026-08-31 실측 **약 44건**), 경로로 나누면 **A 파일을 읽을 때 B 규칙이 안 온다.** 실제로 인증 성공 판정이 다섯 곳(서버·검수 화면·엑셀·**메일 2종**)에 있어 메일 두 곳을 놓칠 뻔한 일이 있었다. 나눈다면 **교차 함정은 항상 로드되는 쪽에 지도로 남긴다.**

⚠️ 두 가지 더 — ①**마이그레이션 파일은 경로로 영역이 안 갈린다**(번호 순이라 `supabase/migrations/**` 로 걸면 전부 로드돼 이득이 없다) ②규칙은 **파일을 읽을 때** 로드되므로 **아직 파일을 안 연 기획 단계에서는 안 보인다**.

✅ **`paths:` 는 실제로 작동한다 — 2026-08-31 확인.** 다만 **확인 방법이 두 단계**다:
1. 새 세션(그 커밋 이후 시작)에서 `/context` → 「Memory files」에 **그 파일이 없고**, 동시에 `planning.md` 같이 **항상 읽히는 파일은 보여야** 한다(양성 대조)
2. 🔴 **거기서 멈추면 안 된다** — 1번이 증명하는 건 「목록을 제대로 보고 있다」까지다. 안 보이는 이유가 **①조건부가 잘 도는 것**인지 **②머리말이 또 틀려 파일이 통째로 무시되는 것**인지는 안 갈린다(`globs:` 사고가 정확히 ②였다). **그 경로에 해당하는 파일을 실제로 열어 규칙이 주입되는지**까지 봐야 ①이 확정된다

🔴 **비용은 사라지는 게 아니라 옮겨간다.** `dev/lib/storage.js` 한 줄을 읽자 `supabase.md`·`build.md` 가 통째로 들어왔다(2026-08-31 실측). **`dev/**` 처럼 넓게 건 규칙은 개발 세션이 무슨 파일을 열든 거의 항상 걸린다** — 「안 읽힌다」가 아니라 **「기획·고문 세션에서만 안 읽힌다」**가 정확한 표현이다.

→ **그래서 경로를 좁게 걸수록 효과가 크다.** 한 화면에만 해당하는 내용(예: 정산 → `dev/js/admin-settlements.js`)은 개발 세션에서도 다른 화면을 만질 땐 안 읽힌다. 반대로 `dev/**` 는 사실상 항상 로드에 가깝다(`build.md`·`ui.md` 는 성격상 그게 맞아 그대로 뒀다).

**그 밖에 쓸 것**
- **`/doctor`** — 트림을 제안한다(v2.1.206+). 원칙이 이 규칙과 같다: **코드에서 알아낼 수 있는 것**(폴더 구조·의존성·아키텍처 개요)은 빼고 **함정·근거·기본값과 다른 관행**은 남긴다. ⚠️ 세션이 못 부른다 — **사람이 직접 입력**해야 하는 CLI 명령
- **`<!-- 주석 -->`** 은 컨텍스트에 안 들어간다(주입 전에 제거됨) — 유지보수용 메모를 **부피 없이** 남길 수 있다
- 여러 단계 절차나 한 영역에만 해당하는 것은 **스킬**로 옮긴다

🔴 **증가 속도 실측 (2026-08-31)**: 커밋당 평균 **+975자**, 하루 평균 **+1,924자**(최근 30일, 83커밋). **여유 2,000자 미만이면 하루도 못 간다** — 한도에 딱 맞춰 정리하면 다음 날 다시 넘는다.

### 경고(⚠️·🔴)는 빼지 않는다

2026-08-26 기준 경고 표시가 276개인데, 표본을 보면 대부분 **지금 유효한 함정**이다 — 「`Number(null)` 이 0 이라 ¥0 으로 그려진다」·「화면 셋은 서로 배타여야 한다」. **이 저장소가 실제로 데인 자리**이고 그 문서에서 가장 값어치 있는 부분이다. **뺄 것은 경고가 아니라 낡은 사실이다.**

---

## 작업표는 체크박스로 쓴다 (2026-09-01 사용자 결정)

사양서를 조각으로 나눈 **작업표**(`docs/specs/*-breakdown.md`)는 각 조각을 **체크박스 한 줄**로 적는다.

```markdown
- [ ] 작업 3 — 게시물 제출 버튼을 채널 단위로
- [x] 작업 4 — 미제출을 남긴 채 떠나면 확인
```

- **끝낸 조각은 그 작업을 커밋할 때 함께 체크**한다. 「나중에 몰아서」는 안 온다 — 이 저장소가 이미 「코드와 문서를 같은 커밋에」로 정한 것과 같은 이유다.
- `/현황` 이 이 체크박스를 세어 **`6/10`** 으로 보여준다. 브랜치가 만진 사양서를 자동으로 찾으므로 **따로 연결해 적을 것이 없다.**

⚠️ **진행률(%)로 적지 않는다.** 「대략 70%」는 사람의 판단이라 **틀려도 드러나지 않는다.** 체크박스는 「이 항목이 끝났다」는 **사실**이라, 의심되면 그 항목을 열어 보면 된다.

⚠️ **✅ 이모지를 완료 표시로 쓰지 않는다.** 이 저장소의 기존 작업표 13개를 세어 보니 **✅ 가 작업 항목 수보다 많았다**(항목 5개에 ✅ 107개) — 문장 속에서 「✅ 운영 배포 완료」처럼 쓰이기 때문이다. **세면 엉뚱한 숫자가 나온다.** 완료 표시는 `- [x]` 하나로 통일한다.

⚠️ **기존 작업표에 소급하지 않는다.** 앞으로 쓰는 것부터 적용한다 — 옛 작업표는 진행 표시가 안 나올 뿐 아무것도 깨지지 않는다.

---

## 기획 세션 의무

- 초안 작성 시 `docs/specs/YYYY-MM-DD-feature-name.md` 파일 생성 (없으면 개발 세션이 구현 결과를 어디에 남길지 모름)
- **사양서 본문 작성 전 「현재 상태」 섹션 의무 채움** — 관련 코드·DB·기존 사양서를 직접 읽고 작성. 누락 시 사양서 무효. 상세 규칙 + 섹션 양식: `.claude/rules/planning.md` 「규칙 A」
- 초안에 "구현 결과" 섹션은 비워두거나 `(개발 세션이 채울 것)` 플레이스홀더만 남기기
- 다음 기획 세션 시작 전 `docs/specs/` 내 최근 파일의 "구현 결과" 섹션 먼저 확인 (현황 파악)
- **사용자 제안에 즉답 동조 금지** — 매 제안마다 깨질 가능성·현재 구현 충돌점·의도 모호점 정리 후 권고. 상세: `.claude/rules/planning.md` 「규칙 B」 (반대론자 모드)

## 사양서 기본 구조 (기획 세션 작성 시점)

```markdown
# {제목}
**작성일:** YYYY-MM-DD

## 현재 상태 (planning.md 규칙 A — 의무)
(관련 코드·DB·UI 진입점 / 충돌 가능 동작 / 미해결 백로그)

## 의심·경우의 수 (planning.md 규칙 B — 의무)
(깨질 가능성 3개+ / 현재 구현 충돌점 / 의도 모호점)

## 제안 / 설계
(기능 정의·DB 변경·UI 와이어프레임 등)
(DB 변경 시 마이그레이션 번호는 적지 않음 — 플레이스홀더+개수+상대순서만. 번호는 개발 세션이 「구현 결과」에 사후 기록. planning.md 규칙 A 참조)

## PR 분할 (선택)

## 사용자 확인 필요
(AskUserQuestion 변환 대상 선택지)

## 구현 결과 (개발 세션이 채울 것)
```

---

## PR description 필수 형식

```markdown
## 변경 요약
- (구현한 것 1~3줄)

## 요청 외 추가 변경
- (없으면 "없음"으로 명시)

## 관련 사양서
- docs/specs/YYYY-MM-DD-feature-name.md (있는 경우)
```

---

**Why:** 기획 세션은 큰 줄기 초안만 작성하고 개발 세션에 넘기는 구조라, 개발 세션이 구현 결과를 기록하지 않으면 다음 기획 세션이 현황을 모른 채 설계를 시작하게 됨. 마이그레이션 117·118처럼 신규 DB 구조가 사양서 없이 쌓이면 누락·회귀 위험이 높아짐 (2026-05-13 지적).
