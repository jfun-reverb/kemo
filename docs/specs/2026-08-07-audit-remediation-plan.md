# 전수조사 후속 조치 계획 — 94건을 개발서버에 하나씩 반영하기

**작성일:** 2026-08-07
**조사 결과 문서:** `docs/research/2026-08-07-codebase-audit-findings.md` (근거·`파일:줄`은 전부 그쪽에 있다. 이 문서는 **무엇을 어떤 순서로 고칠지**만 다룬다)
**배포 대상:** 개발서버(dev) 먼저. 운영 반영은 묶음마다 따로 사용자 확인.

---

## 이 문서를 쓰는 법

- **묶음(A~I) 하나 = 개발서버 배포 1회 단위.** 한 묶음을 끝내고 검증한 뒤 다음으로 간다.
- **순서는 두 층으로 나뉘고 정의처가 각각 다르다.**

  | 무엇 | 정의처 |
  |---|---|
  | 그 묶음이 **순서를 지켜야 하는 묶음인지** | 아래 **§묶음 안 순서 표** |
  | 그 묶음 안에서 **어느 조각이 먼저인지** | **각 묶음 본문 표의 「순서」 칸** (칸이 없는 묶음 = 자유) |

  맨 위 「묶음 순서 한눈에」의 화살표는 **읽기 편하라고 둔 요약**이다. 셋이 어긋나면 **본문 표 → §묶음 안 순서 표 → 한눈에** 순으로 믿는다.
- **⚠️ 표시는 「놓치면 실제로 문제가 되는 지점」**이다. 순서·바깥 일정·검증 방법·적용 범위 등 종류가 여럿이니, **무엇이 문제인지는 그 ⚠️ 문장 자체를 읽는다.** 표시가 붙은 줄은 건너뛰지 않는다.
- 각 조각은 착수 전 관련 코드를 직접 읽고 시작한다. 조사 문서의 `파일:줄`은 2026-08-07 기준이라 그 사이 코드가 움직였을 수 있다.

### ⚠️ 데이터베이스와 코드, 어느 쪽을 먼저 올리나 — 변경 성격에 따라 반대다

| 변경 성격 | 순서 | 이유 |
|---|---|---|
| **칸·표를 새로 만드는** 변경 | **데이터베이스 먼저**, 코드 나중 | 칸이 없는데 코드가 먼저 나가면 저장이 실패한다 |
| **접근을 좁히는** 변경 (정책 축소·버킷 비공개) | **코드 먼저**, 데이터베이스 나중 | 지금 그 경로로 읽고 있는 화면이 먼저 다른 길로 옮겨가야 한다. 반대로 하면 화면이 통째로 빈다 |

이번 계획에서 **접근을 좁히는** 쪽은 묶음 **A**(등급 칸 수정 차단)·**B**(버킷 비공개)·**E**(정책 축소) 셋이다. 이 셋은 코드가 앞선다. 다만 A는 조건부다 — 화면이 등급 칸을 안 보내면 고칠 코드가 없다(A-0에서 확인).

- 마이그레이션 번호는 **파일을 만드는 순간 확정**한다. 이 문서에는 개수와 상대 순서만 적었다.

---

## 묶음 순서 한눈에

```
A. 등급 상향 차단          ← 반드시 맨 처음. 다른 권한 작업의 전제
B. 영수증 비공개 전환      ← 🛑 보류(브랜드사가 링크 사용 중) · 목록 조회 차단만 지금 가능
C. 홍보 메일 확인·수신거부  ← C-1(조회) 결과로 나머지 긴급도가 갈림
D. 8월 말 행사 대비        ← ⚠️ D-1을 캠페인 등록 전에. 8/28까지
E. 가림막·주소·자기당선     ← ⚠️ 내부 순서 결정적 (E-1 → E-2)
F. 잘못된 안내 (메일·알림)  ← 조각끼리 의존 없음
G. 화면끼리 어긋남         ← ⚠️ G-1이 G-2보다 먼저
H. 정산                    ← 조각끼리 의존 없음. 가동 스위치 켜기 전까지면 됨
I. 삭제·파기·그 밖         ← 조각끼리 의존 없음
```

**묶음 사이 순서**
- **A가 맨 처음이다.** 등급 상향이 열려 있는 동안은 다른 권한 통제(B·E)가 우회되므로 미룰 수 없다.
- A 다음은 **B·C·D를 아무 순서로나**(서로 독립). 다만 **D-1은 행사 캠페인을 등록하기 전에** 끝나야 하므로, 캠페인 등록 일정이 잡히면 D가 앞으로 온다.
- 그다음 E~I.

**묶음 안 순서**

| 묶음 | 내부 순서 |
|---|---|
| **B · E · G** | **강제** — 순서를 바꾸면 화면이 깨지거나, 고쳐도 안 고쳐진다 |
| **A · C** | **조건부 강제** — 앞 단계(A-0 확인 / C-1 조회)의 결과에 따라 그 앞에 할 일이 하나 더 생긴다 |
| **D** | 조각끼리는 의존 없음. 다만 **D-1만은 바깥 일정(행사 캠페인 등록)보다 먼저**여야 한다 — 묶음 안 순서가 아니라 **바깥 마감**이다 |
| **F · H · I** | 자유 — 조각끼리 의존 없음 |

---

# 묶음 A — 관리자 등급 상향 차단 (맨 처음)

**왜 맨 처음인가**: 이걸 안 막으면 아래 모든 권한 작업이 무의미하다. 가장 낮은 등급이 스스로 최고 관리자가 될 수 있으므로, 어떤 통제를 걸어도 우회된다.

| 순서 | 조각 | 하는 일 |
|---|---|---|
| **0** | A-0 | **착수 전 확인**: 본인 행을 수정하는 화면 코드가 등급 칸을 함께 보내는지 본다(내 계정 화면의 이름 변경 경로). **보낸다면 그 화면에서 등급 칸을 빼는 것이 A-1보다 먼저다** — 안 그러면 이름 변경이 막힌다 |
| 1 | A-1 | `admins` 수정 정책에 **변경 후 행 검사**를 넣어 본인 행에서 등급을 못 바꾸게 한다. 최고 관리자는 종전대로 전부 수정 가능 |

- **데이터베이스 변경**: 마이그레이션 1개(정책 교체)
- **코드 변경**: A-0의 확인 결과에 달렸다. 등급 칸을 안 보내면 코드 변경 없음
- **검증**: 캠페인매니저 계정으로 자기 등급 변경 시도 → 거부. 최고 관리자로 남의 등급 변경 → 성공. 본인 이름 변경 → 성공
- **되돌리기**: 정책을 예전 형태로 복원(1분)

---

# 묶음 B — 영수증·인증샷 비공개 전환 🛑 **보류 — 운영 전제가 다르다**

> ## 🛑 2026-08-07 사용자 확인 — **지금 실행하면 안 된다**
>
> **브랜드사가 그 공개 링크로 영수증을 확인하고 있다.** 비공개로 바꾸면 **브랜드사 업무가 그 자리에서 멈춘다.** 조사·계획 시점에는 이 운영 사실이 반영되지 않았다.
>
> **실행 시점 = 브랜드 페이지(포털)를 만들 때 함께.** 그때 브랜드가 포털을 통해 보게 되므로 공개 링크가 필요 없어진다.
>
> ### 다만 **목록 조회 차단은 지금 해도 된다**(별개 조각)
> | | 브랜드사가 받은 링크 | 파일 목록 조회 |
> |---|---|---|
> | 지금 | 열림 | **열림 — 아무나** |
> | 목록만 차단 | **그대로 열림** | 막힘 |
>
> 브랜드사는 링크를 **받아서** 열므로 파일명을 이미 안다 — 목록 조회를 쓰지 않는다. 반대로 바깥 사람은 목록이 막히면 `1776429329678_etyheg.jpg` 같은 이름을 알아낼 방법이 없다. **브랜드사 영향 0, 바깥 노출만 사라진다.** 마이그레이션 1개, 되돌리기 즉시.
>
> ### ⚠️ 함께 남는 별개 사안 — 브랜드사 공유의 법적 근거
> 브랜드 포털 기획 때 딥리서치 결론이 이미 나와 있다(2026-07-15, [[project_brand_portal]]) — **결과물을 브랜드에게 노출하는 것은 「제3자 제공」 가능성이 높고**(위탁이 아니라 브랜드가 자기 이익으로 이용), 일본 거주자 데이터를 한국 브랜드가 보는 것이라 **국경 간 이전 관문**이 추가되며, **변호사 확인 전제**로 결론지었다. 그 검토 대상은 「게시물 URL·계정명」이었는데 **영수증은 그보다 민감하다**(이름·주문번호·구매처·금액·주소). 이번 조치 범위 밖이지만 **사용자 판단이 필요한 사안으로 남긴다.**

**실측(2026-08-07, 운영):** 로그인 없이 목록 조회 성공(응답 200) · 영수증 **1,000건 이상**(조회 상한) · 리뷰 인증샷 87건 · 실제 사진 열림 확인(`image/jpeg`). 노출은 **가정이 아니라 사실**이다.

---

## (아래는 브랜드 포털 착수 시점에 쓸 원래 계획) ⚠️ 순서 결정적

**왜 순서가 결정적인가**: 지금 화면은 **공개 주소를 그대로 `<img>`에 박아** 영수증을 보여준다. 버킷을 먼저 비공개로 바꾸면 **관리자 결과물 검수 화면의 영수증이 전부 안 보이게 된다.**

| 순서 | 조각 | 하는 일 |
|---|---|---|
| **1** | B-1 | 영수증·인증샷을 **서명 주소**(시간 제한 있는 임시 주소)로 받아오는 함수를 만든다. **이미 같은 패턴이 있다** — 위반 증빙·메시지 첨부가 쓰는 방식을 그대로 따른다 |
| **2** | B-2 | 영수증·인증샷을 그리는 화면 전부를 B-1로 교체한다. 결과물 검수 목록·검수 창·확대 창·엑셀 임베드. **저장된 값이 공개 전체 주소라 경로를 뽑아내는 처리가 필요하다** |
| **3** | B-3 | 그다음에야 **버킷을 비공개로, 조회 정책에서 익명을 뺀다.** 캠페인 이미지는 공개로 남겨야 하므로 같은 버킷 안에서 갈래를 나누거나 버킷을 분리한다 |
| 4 | B-4 | 앞으로 올라가는 파일 이름을 **추측 불가능한 값**으로 바꾼다(기존 파일은 그대로 둔다 — 경로가 바뀌면 저장된 주소가 죽는다) |

- **데이터베이스 변경**: 마이그레이션 1~2개(버킷 설정·저장소 정책). **B-3 단계에서만**
- ⚠️ **B-3을 B-2보다 먼저 하면 안 된다.** 검수 화면이 통째로 빈 이미지가 된다
- ⚠️ **캠페인 이미지도 같은 버킷에 있다.** 인플루언서 앱이 캠페인 사진을 공개로 읽으므로, 그쪽까지 비공개가 되면 **모든 캠페인 사진이 안 보인다.** 갈래를 반드시 나눌 것
- **검증**: ①비공개 전환 전, 로그인 없이 목록 조회가 되는지 확인해 규모를 기록 ②전환 후 같은 조회가 막히는지 ③관리자 화면에서 영수증이 정상으로 보이는지 ④인플루언서 앱에서 캠페인 사진이 정상인지
- **되돌리기**: 버킷을 공개로 되돌리면 즉시 복구(화면 코드는 서명 주소로도, 공개 주소로도 동작하게 만들 것)

---

# 묶음 C — 홍보 메일 (실측 → 수신거부 → 발송 누락)

| 순서 | 조각 | 하는 일 |
|---|---|---|
| **1** | C-1 | **조회 먼저**(= 실측 M1): 예약 실행이 어느 서버를 가리키는지 확인한다. `SELECT jobname, command FROM cron.job;` — 폐기된 시드니 주소면 **홍보 메일이 도쿄 이전 이후 한 통도 안 나간 것**이고, 그러면 C-2·C-3의 긴급도가 달라진다. 잘못 가리키고 있으면 **예약 등록을 현재 주소로 다시 하는 것**이 C-2보다 먼저다 |
| 2 | C-2 | **수신거부 링크 복구** — 주석 처리된 자리를 살린다. 문구·주소를 넣고 텍스트판에도 넣는다. `CLAUDE.md`가 「있다」고 적은 서술도 함께 바로잡는다 |
| 3 | C-3 | **200명 넘을 때 한 무리를 건너뛰는 문제** — 명단을 주는 함수가 이미 「보낸 사람」을 빼주므로, 함수 쪽에서 다시 건너뛰지 않게 한다. 명단에 **정렬 기준**도 넣는다 |
| 4 | C-4 | **초대 전용 캠페인 제외** — 홍보 대상 선정에서 뺀다. 인플루언서 앱이 이미 거르는 방식을 그대로 쓴다 |

- **데이터베이스 변경**: C-3·C-4에 마이그레이션 1개(대상 선정 함수 교체)
- **메일 함수 배포**: 코드 머지와 별개다. 개발서버·운영에 각각 배포해야 반영된다
- ⚠️ **개발서버에서 실제 발송 테스트를 하지 않는다**(프로젝트 규칙). 환경만 맞춰 두고 검증은 운영에서
- **검증**: C-1은 조회 한 줄. C-2는 발송된 메일 본문에 수신거부 줄이 있는지. C-3은 대상 300명 이상인 상황을 만들어 발송 기록 수를 대조
- **되돌리기**: 함수 재배포로 즉시

---

# 묶음 D — 8월 말 행사 대비 (8/28~30, 3주 남음)

**조각끼리 의존 없음 — D-2~D-6은 나눠서 동시에 해도 된다.** 다만 **D-1은 바깥 마감**이 걸려 있다.

| 조각 | 하는 일 |
|---|---|
| **D-1** | **예약 함수에 캠페인 상태·타임 날짜 검사를 넣는다.** ⚠️ **바깥 마감 — 행사 캠페인을 등록하기 전에 끝내야 한다**(등록하는 순간 예약 경로가 열린다). 등록 일정이 잡히면 D 전체를 앞으로 당긴다 |
| D-2 | **정원을 늘리면 대기자를 자동으로 확정**시킨다. 또는 관리자 화면에 「대기자 올리기」 버튼을 만든다. 지금은 올릴 방법이 **아예 없다** |
| D-3 | **정원을 줄일 때 이미 확정된 인원과 비교**해 확인 창을 띄운다. 잔여를 0으로 자르지 말고 **초과 상태를 화면에 보여준다** |
| D-4 | **현장 명단 조회에 정렬 기준**을 넣는다. 관리자 앱의 같은 함수가 쓰는 방식 그대로 |
| D-5 | **묶인 행사를 캠페인 단위로 열었을 때** 범위 밖 손님을 막는 문구에 「화면을 다시 여세요」 안내를 넣거나, 진입 경로가 항상 묶음 단위가 되게 한다 |
| D-6 | 자정을 넘는 타임 일괄 생성이 **미리보기와 저장 결과가 다른** 문제 — 미리보기에서 미리 막거나 실패 사유를 알려준다 |

- **데이터베이스 변경**: D-1·D-2에 마이그레이션 1~2개
- D-2~D-6은 **행사 당일 전까지**면 된다(D-1의 마감은 위 표 참조)
- ⚠️ 행사 예약·취소는 **관리자 도구로 검증할 수 없다**(본인 계정 가드 때문). **테스트 계정 2개로 브라우저에서** 확인해야 한다
- **검증**: 정원 증설 후 대기자가 확정으로 바뀌는지 · 정원 축소 시 경고가 뜨는지 · 명단 1,000건 넘겨 두 번째 페이지에 누락·중복이 없는지 · 공개 전 캠페인에 예약이 거부되는지
- **되돌리기**: 함수·화면 되돌리기로 즉시. 이미 만들어진 예약 데이터는 그대로 유지

---

# 묶음 E — 가림막·주소·자기 당선 ⚠️ 순서 결정적

**왜 순서가 결정적인가**: 원본 표의 관리자 조회 허가를 좁히면, **지금 그 표를 직접 부르는 관리자 화면 3곳이 깨진다.**

| 순서 | 조각 | 하는 일 |
|---|---|---|
| **1** | E-1 | 관리자 화면에서 인플루언서 원본 표를 직접 부르는 곳을 **가림막 통로로 교체**한다. 조사 시점 기준 3곳(운영현황 감사용 계정 조회 2곳, 신청 승인 시 감사용 판정 1곳). **인플루언서 본인 조회 5곳은 손대지 않는다** — 본인 정책으로 통과하므로 영향 없다 |
| **2** | E-2 | 그다음 **원본 표의 관리자 조회 허가를 좁힌다**. 가림막 통로만 남긴다 |
| 3 | E-3 | 같은 표의 **수정 허가**도 좁힌다 — 지금은 하위 등급이 아무 인플루언서 행이나 고칠 수 있다 |
| 4 | E-4 | **응모 신청 표의 주소 사본** — 조회 시 등급에 따라 가리거나, 애초에 신청에 주소를 복사하지 않고 인플루언서 표를 참조하게 바꾼다. 신청자 엑셀의 주소 열도 함께 |
| 5 | E-5 | **응모 삽입 시 상태 값을 서버가 강제**한다. 어떤 값이 들어오든 「심사중」으로 시작하게 한다(리뷰어형은 기존 자동 승인 트리거가 이어서 처리) |
| 6 | E-6 | **숨김·회수한 메시지**를 표 직접 조회로 못 읽게 한다. 조회 정책에 숨김 조건을 넣거나, 본문 열을 조회 함수로만 읽게 한다. **첨부 파일 경로도 함께** |

- **데이터베이스 변경**: E-2·E-3·E-5·E-6에 마이그레이션 각 1개
- ⚠️ **E-2를 E-1보다 먼저 하면 안 된다.** 운영현황·신청 승인 화면이 오류를 낸다
- ⚠️ **E-5는 기존 자동 승인 트리거와 충돌하지 않는지 확인할 것** — 리뷰어형은 삽입 시 자동으로 당선 처리되는 구조다. 강제 순서가 어긋나면 리뷰어형 응모가 전부 심사중에 갇힌다
- **검증**: 캠페인매니저 계정으로 원본 표 직접 조회 → 거부 · 관리자 화면 정상 동작 · 상태를 조작해 응모 삽입 → 심사중으로 저장 · 숨긴 메시지를 표 직접 조회 → 본문 비어 있음
- **되돌리기**: 정책 복원. 단 E-1을 먼저 했으므로 되돌려도 화면은 정상

---

# 묶음 F — 잘못된 안내 (메일·알림)

**조각끼리 의존 없음 — 아무 순서로나.** F-1·F-2·F-4~F-10은 메일 함수라 배포가 코드 머지와 별개다.

⚠️ 착수 전 **실측 M7**(취소 알림이 실제로 생성된 적 있는지)을 먼저 본다 — F-3의 대상 알림 종류가 달라진다.

| 조각 | 하는 일 |
|---|---|
| F-1 | 결과물 승인 메일이 **모집 형식을 구분**하게 한다 — 가구매는 인증샷을 요구하지 않는다. 2026-08-04 사고와 같은 유형 |
| F-2 | 인증샷 승인 메일이 **채널 완성 여부를 확인**하게 한다 — 1채널만 승인됐는데 「전부 완료」라고 하면 나머지를 안 낸다 |
| F-3 | 알림 클릭 시 **같은 종류만** 읽음 처리한다 — 지금은 당첨·기한 변경 알림이 함께 사라진다 |
| F-4 | 다이제스트 4종에 **페이지 나눔**을 넣는다. 1,000건에서 잘리고 참조 조회가 잘리면 이름·캠페인명이 「-」로 나간다 |
| F-5 | 다이제스트 자물쇠 — **실패한 기록이 남으면 그날 재시도가 영원히 막힌다.** 상태를 보고 재시도를 허용하게 한다 |
| F-6 | 인플루언서 다이제스트만 자물쇠가 헐거워 **중복 발송** 가능 — 관리자 다이제스트 방식으로 통일 |
| F-7 | 감사용 계정을 관리자 다이제스트·일괄 발송 대상에서 **제외** |
| ~~F-8~~ | ~~인플루언서 다이제스트의 **날짜가 하루 밀리는** 문제~~ → **해당 없음 (2026-08-10 확인)**. 조사 문서(`2026-08-07-codebase-audit-findings.md`)에 **이 지적 자체가 없다** — 3단계는 3-1~3-10 이고 날짜 밀림 항목이 없다. 코드도 확인했다: `computeWindow()` 는 한국시간 기준 어제 날짜를 만들고 그 날짜의 00:00~24:00(+09:00)을 창으로 쓰며, `dateDiffDays()` 도 양쪽을 `+09:00` 으로 파싱한다 — 밀리는 지점이 없다. **계획서 작성 시 잘못 들어간 항목**으로 판단해 고치지 않는다. ⚠️ 없는 문제를 고치면 멀쩡한 날짜 계산이 실제로 밀어진다 |
| F-9 | 마감을 연장해도 **재안내가 안 나가는** 문제 — 중복 방지 열쇠에 마감일을 넣는다 |
| F-10 | 웹훅 메일 함수 2개에 **호출자 확인**을 넣는다 — 같은 폴더의 다른 함수는 이미 하고 있다 |
| F-11 | 취소된 응모에 메시지를 보내면 생기는 **막다른 골목** — 보내는 쪽에서 막거나, 받는 쪽 진입 차단을 푼다 |
| F-12 | 임시저장을 **인플루언서 쪽도 제외**해 「제출 확인 중」 오표시를 없앤다 |
| F-13 | 알림으로 메시지 화면에 바로 들어갈 때 **반려 사유가 안 보이는** 문제 |
| F-14 | 일괄 발송이 **개별 발송 한도를 잡아먹는** 문제 |

---

# 묶음 G — 화면끼리 어긋남

| 순서 | 조각 | 하는 일 |
|---|---|---|
| **1** | G-1 | 결과물 조회 두 곳에 **「가구매인가」 칸을 추가**한다. 이 한 줄로 결과물 관리·브랜드 운영현황 두 화면이 정산·엑셀과 같은 답을 낸다 |
| 2 | G-2 | 브랜드 운영현황이 캠페인을 **가짜 객체로 흉내 내는 것**을 진짜 캠페인 행으로 바꾼다 |
| 3 | G-3 | 엑셀의 **「가장 최근 영수증」 기준**을 화면·정산과 같게 맞춘다 |
| 4 | G-4 | 정산 판정도 **임시저장을 제외**하게 한다 |
| 5 | G-5 | 어긋남 감지 장치와 실제 판정의 **채널 비교 규칙을 통일**한다. 어느 쪽으로 통일할지는 착수 시 결정 — 판정을 관대하게 바꾸면 기존 데이터가 살아나고, 감지를 엄격하게 바꾸면 경보가 늘어난다 |
| 6 | G-6 | 운영현황 「최근 신청」에서 **취소된 응모에 「되돌리기」 버튼을 숨긴다**(다른 두 화면은 이미 숨김) |
| 7 | G-7 | **「승인 → 되돌리기 → 미승인」에도 정산 자동 보류**가 걸리게 한다 |

- ⚠️ **G-1이 먼저다.** G-2가 같은 판정을 쓰므로 G-1 없이 G-2만 고치면 여전히 틀린다
- **G-5는 사용자 확인 필요** — 아래 §확인 필요 참조

---

# 묶음 H — 정산 (가동 스위치 켜기 전까지)

> 지금 인플루언서에게 잠겨 있고 도입일도 미설정이라 자동 생성 0건. **급하지 않다.** 다만 **가동 스위치를 켜기 전에는 전부 끝나 있어야 한다.**
>
> **조각끼리 의존 없음 — 아무 순서로나.**

| 조각 | 하는 일 |
|---|---|
| H-1 | 영수증 수정 시 재계산이 **모집 형식을 구분**하게 한다 — 지금은 방문형 정산이 엉뚱한 사유로 잠기거나 금액이 교체된다 |
| H-2 | **정산대기 중 새 영수증이 승인되면** 금액을 다시 계산하거나, 최소한 화면에 「영수증이 바뀌었다」를 표시한다 |
| H-3 | **「금액 미확정」 건이 어느 목록에도 안 뜨는** 구멍을 막는다. 화면 코드에 이미 「조용히 누락된다」는 메모가 달려 있다 |
| H-4 | 일괄 「송금완료 기록」에도 **페이팔 확인**을 넣는다 |
| H-5 | **인증 성공 시점**을 정산 행에 저장한다. 지금은 등록일을 인증성공일로 표시하고 진짜 시점은 복구 불가 |
| H-6 | 재계산 이력의 **한국어 라벨** 추가 |
| ~~H-7~~ | ~~정산 잠금 오류 문구를 **일본어로 번역**~~ → **이미 해결됨 (2026-08-10 확인)**. `dev/js/ui.js` 에 `settlement_locked_receipt` 매칭과 일본어·한국어 문구가 모두 등록돼 있다(95·118·140행). 조사 시점(08-07) 이후 오류 기록 작업에서 함께 들어간 것으로 보인다. **할 일 없음** |
| H-8 | 페이팔 등록 안내 알림의 **중복 발송** 차단 |

---

# 묶음 I — 삭제·파기·그 밖

**조각끼리 의존 없음 — 아무 순서로나.** 단 **I-1은 묶음 B 이후**에 하는 편이 낫다(그때는 파일이 공개가 아니라 잔존해도 노출은 아니다).

| 조각 | 하는 일 |
|---|---|
| I-1 | 캠페인 삭제 시 **저장소 파일도 지운다**. 감사용 계정 청소가 쓰는 방식(삭제 전 경로를 모아 반환 → 화면이 파일 정리)을 그대로 이식한다. **묶음 B 이후에 하면 더 안전하다**(그때는 파일이 공개가 아니므로 잔존해도 노출은 아니다) |
| I-2 | 캠페인 삭제 시 **영수증 수정 이력도 파기**한다 — 계정 삭제 경로엔 이미 있다 |
| I-3 | 3년 보존 정기 삭제가 **증빙 파일도 지우게** 한다. 지금 구조로는 그 배치가 저장소를 못 부르므로 방식 변경이 필요하다 |
| I-4 | 오리엔시트 삭제가 캠페인을 **하드 삭제하지 않고 보관 삭제**를 거치게 한다 |
| I-5 | 브랜드 병합이 **오리엔시트도 옮기게** 한다 — 지금은 원본 브랜드가 영구히 못 지워진다 |
| I-6 | 보관 삭제된 캠페인을 **브랜드별 캠페인 수·기준 데이터 사용 중 판정**에서도 제외 |
| I-7 | 오리엔시트: 브랜드가 **발행된 카드를 못 지우게** 하거나, 지울 때 발행 표시를 보존한다 |
| I-8 | 오리엔시트: 작성 폼이 저장 뒤 **카드 번호를 다시 읽게** 해서 메모가 고아가 되는 것을 막는다 |
| I-9 | 마감 후 재제출용 임시저장을 지우면 **반려 이력이 함께 지워지는** 문제 |
| I-10 | 캠페인 자동 상태 전이를 **일본 시각 기준**으로 통일 |
| I-11 | 캠페인 「종료」·노출 끄기가 심사중 응모를 전원 낙첨시키는 것에 **확인 창** 추가 |
| I-12 | 응모 서버 차단이 **캠페인 상태·삭제 여부도** 보게 한다 |
| I-13 | 자주 묻는 질문 **카테고리 비활성이 추천 카드에도 반영**되게 한다 |
| I-14 | 다른 관리자 메시지에 **「회수」 버튼을 숨긴다** |
| I-15 | 관리자 등급 설정 12개가 **실제로 아무것도 제어하지 않는** 문제 — 서버 가드를 권한 판정으로 바꾸거나, 화면에 「이 항목은 등급으로만 제어됩니다」를 표시한다 |
| I-16 | **채널이 빈 시딩·방문형 캠페인**에서 마감 후에도 게시물 폼이 열려 있는 문제 — 화면이 「그 종류의 항목 0건」을 「낼 수 있음」으로 읽는다. 안내문은 「기한이 지났습니다」인데 버튼은 눌리고 서버만 거부한다. **조회 실패(못 물어봄)와 항목 0건을 구분**해 폴백 방향을 맞춘다. 근본은 채널이 빈 캠페인이 남아 있는 것이라, **실측 M8로 그 캠페인을 먼저 세고 정리할지 함께 판단**한다 |

---

# ★ 실측 결과 요약 (2026-08-07 운영 실측 완료)

> **여덟 개를 전부 돌렸다.** 숫자와 근거는 `docs/research/2026-08-07-audit-measurement-queries.md` §실측 결과.

| # | 결과 | 계획 변경 |
|---|---|---|
| M1 홍보 메일 예약 | ✅ **정상** — 운영 주소를 가리키고 발송도 끊김 없음(6/18·7/16·8/6 각 8·9·10통) | **C-1 해소**, C 긴급도 하향 |
| M2 영수증 공개 | ❌ **노출 성립** (1,000건+) | **B는 🛑 보류** — 운영 전제(브랜드사 사용 중) |
| M3 등급 상향 | ❌ **결함 확정** — `admins_update` 의 `with_check` 가 비어 있음 | **A 그대로 최우선** |
| M4 가구매 캠페인 | ✅ **0건** | **G-1·G-2 대상 0** |
| M5 영수증 정렬 | ⚠️ **36건 발현** | **G-3이 G에서 가장 실질적** |
| M6 채널 이상값 | ✅ **0건** | **Q1 불필요** |
| M7 취소 알림 | ❌→✅ 0건이었고 **같은 날 고침**(마이그레이션 309, 운영 첫 알림 생성 확인) | F-3 대상에 포함 |
| M8 채널 빈 캠페인 | ✅ **0건** | **I-16 대상 0, Q5 불필요** |

**정리 — 착수 순서에 미치는 영향**
- **A는 그대로 맨 처음** (실측으로 결함 확정)
- **B는 보류** (기술이 아니라 운영 사정)
- **C는 뒤로** (정상 동작 중이었다)
- **G는 M5(36건) 중심으로 좁힘**
- **사용자 확인 5개 중 2개(Q1·Q5) 자동 해소** — 남은 것은 Q2·Q3·Q4

---

# 실측·검증 조회 (묶음별)

> 실측 번호는 **M1~M8**로 붙였다 — 묶음 이름(A~I)과 글자가 겹치면 「어느 A인지」 헷갈린다.
> **실행 시점이 넷으로 갈린다**
> - **M1·M2** — 각각 묶음 C·B 안의 절차와 **같은 작업**(중복 실행 불필요)
> - **M3** — **두 번 돈다.** 착수 전 1회(운영에도 문서에 없는 다른 장치가 붙어 있는지) + **A-1 적용 후 1회**(실제로 막혔는지). 두 번째는 묶음 A 검증의 첫 항목과 같은 작업이다
> - **M6** — 묶음 G 착수 전이 아니라 **G-5 직전**(Q1을 정할 때)
> - **M4·M5·M7·M8** — 그 묶음 착수 전에 따로 돌린다

| # | 확인 | 언제 | 왜 |
|---|---|---|---|
| M1 | 예약 실행이 가리키는 서버 주소 | **= 조각 C-1** (묶음 C의 첫 단계) | 홍보 메일이 두 달간 안 나갔는지 판명. 한 줄 조회 |
| M2 | 로그인 없이 영수증 목록이 실제로 조회되는지 | **= 묶음 B 검증 ①** (B-3 전환 전) | 실제 노출 규모 기록(전환 후 대조용) |
| M3 | 가장 낮은 등급으로 자기 등급을 올릴 수 있는지 | **묶음 A 착수 전 + A-1 적용 후** | 코드상 성립은 이미 확인됐다(정책에 변경 후 행 검사가 없음). 이 조회는 **A를 미룰지 판단하는 용도가 아니라**, ①운영에도 문서화되지 않은 다른 장치가 붙어 있지 않은지 ②A-1 적용 뒤 실제로 막혔는지를 **같은 방법으로 두 번** 확인하는 용도다 |
| M4 | 가구매 캠페인 수 | 묶음 G 착수 전 | G-1·G-2의 영향 범위 |
| M5 | 영수증이 여러 행 쌓인 응모 + 두 정렬이 갈리는지 | 묶음 G 착수 전 | G-3의 실제 발현 여부 |
| M6 | 게시물 채널에 대소문자·공백이 섞인 값 | **Q1 결정 전** (G-5) | 어느 쪽으로 통일할지의 근거 |
| M7 | 취소 알림이 일반 응모 취소로 생성된 적 있는지 | 묶음 F 착수 전 | 도입 이래 실패했을 가능성 → F-3 대상이 달라짐 |
| M8 | 채널이 빈 시딩·방문형 캠페인 수 | 묶음 I 착수 전 | **I-16**의 영향 범위 |

⚠️ **대상은 운영 데이터베이스.** 개발 데이터베이스에는 운영 복제본이 섞여 있어 근거가 되지 않는다.

---

# 사용자 확인 필요

> **어떤 것을 사용자에게 묻고 어떤 것을 개발이 정하나**: 갈래가 열려 있어도 **구현 방식 선택**(어느 화면에 버튼을 다는가, 어느 쪽에서 막는가)은 개발이 정한다. 사용자에게 묻는 것은 **①이미 저장된 데이터가 바뀌거나 사라지는 것 ②지급·당락처럼 사람에게 결과가 달라지는 것 ③운영 정책 그 자체**다. 아래 다섯이 그에 해당하고, D-2·D-5·F-11·I-15의 갈래는 개발 판단으로 충분하다.

- **Q1 — 채널 비교 규칙(G-5)을 어느 쪽으로 통일할지**: 판정을 관대하게(대소문자 무시) 바꾸면 지금 어긋난 것으로 취급되던 데이터가 살아나 인증 성공·정산이 바뀔 수 있다. 감지를 엄격하게 바꾸면 경보가 늘어난다. **실측 M6의 결과를 보고 정한다**
- **Q2 — 시딩·방문형이 채널 여러 개일 때 한 채널만 승인돼도 인증 성공인 것**(조사 5-6)이 의도인지. 의도가 아니면 전 채널 완성을 요구하도록 바꿔야 하는데, **이미 그렇게 정산된 건이 있으면 소급 문제**가 생긴다
- **Q3 — 행사에서 한 사람이 여러 날 예약**할 수 있는 것이 의도인지. 지금은 캠페인 단위로 막혀 있어 묶인 행사에서는 2건이 가능하다
- **Q4 — 응모 신청의 주소 사본(E-4)**을 없앨지, 가리기만 할지. 없애면 과거 신청의 배송지 이력이 사라진다
- **Q5 — 채널이 빈 시딩·방문형 캠페인(I-16)을 정리할지**. 화면 쪽 수정만으로도 증상은 사라지지만, 근본은 그런 캠페인이 남아 있는 것이다. 정리하면 저장된 캠페인 데이터를 건드리므로 **실측 M8로 건수를 본 뒤 결정**한다

---

# 구현 결과

## 묶음 A — 관리자 등급 상향 차단 ★ **운영 배포 완료** (2026-08-07)

**마이그레이션 310** · 커밋 `647a9fc` · PR #967(개발) · **#969(운영)**.

**운영 적용 확인** — 착수 전 `null` 이던 `admins_update.with_check` 가 채워졌고 `using` 은 그대로다:
```
with_check: (최고 관리자) OR (본인 행 AND role = current_admin_role())
```
⚠️ **운영 관리자 12명 중 10명이 campaign_admin** 이었다. 즉 **10명이 각자 최고 관리자가 될 수 있던 상태**였다.

### 실측으로 확정한 것
운영 `admins_update` 의 `with_check` 가 **비어 있었다**(조사 결과 그대로). `using` 이 「최고 관리자 **또는** 본인 행」을 허용하는데 바뀐 뒤 값을 아무도 안 봐서, 가장 낮은 등급이 자기 행을 고치며 등급을 올릴 수 있었다.

### 🚨 A-0에서 조사보다 더 나쁜 것이 드러났다
계획서는 「본인 행 수정 화면이 등급 칸을 보내는지」를 확인하라고 했는데, 확인해 보니 **관리자 목록의 「수정」 버튼이 조건 없이 모든 행에 렌더**되고 있었다. 즉 **개발자 도구 없이 화면에서 바로** 캠페인매니저가 자기 등급을 올릴 수 있었다. (처음엔 「최고 관리자 전용 화면」으로 잘못 판단했고, supabase-expert 가 코드로 잡아냈다.)

### 무엇을 했나
| 층 | 내용 |
|---|---|
| 데이터베이스 | 정책에 **「바뀐 뒤 값 검사」** 추가. 현재 등급을 돌려주는 정의자 권한 헬퍼를 새로 만들어 정책 재귀를 피함. **`USING` 은 137 그대로 보존**(리뷰에서 문자 단위 대조) |
| 화면 | 「수정」 버튼을 최고 관리자에게만 + 모달 진입 함수에 가드(콘솔 직접 호출 대비) |

⚠️ **배포 순서는 코드 → 데이터베이스**(접근을 좁히는 변경). 그대로 지켰다.

### 검증 (개발서버, 실제 계정으로)
| 확인 | 결과 |
|---|---|
| 캠페인매니저가 자기 등급 상향 | **거부** ✅ |
| 캠페인매니저가 본인 이름 변경 | 성공 ✅ — **A-0이 경고한 사고 없음** |
| 최고 관리자가 남의 등급 변경 | 성공 ✅ |
| 최고 관리자가 본인 이름 변경 | 성공 ✅ |

검증에 쓴 값은 전부 원복했다.

### 범위 밖 (후속으로 남김)
- 목록 **「비밀번호」 버튼도 등급 무관하게 렌더**된다. 다만 서버 함수(`reset_admin_password`)에 최고 관리자 가드가 **이미 있어** 보안 결함은 아니고 화면 혼란만 있다
- **최고 관리자의 자기 강등**은 여전히 가능. 기존 「슈퍼관리자 자기 제한」(268~271)은 기능 접근 수준을 잠그는 별개 축이라 충돌하지 않는다

---

## 묶음 B(일부) — 영수증 **목록 조회 차단** ★ **운영 배포 완료** (2026-08-07)

**마이그레이션 311** · 커밋 `9e79f9f`. **비공개 전환(묶음 B 본체)은 여전히 보류** — 이건 그때까지 노출을 줄이는 **임시 조치**다.

### 무엇을 했나
저장소 조회 정책의 **대상만** 「누구나」 → 「로그인한 사람」으로 좁혔다(`TO authenticated`). **버킷 공개 설정과 나머지 정책 3개는 안 건드림.** `USING` 도 009 그대로.

원리: 버킷이 공개면 `/object/public/...` 경로는 **행 단위 보안 정책을 아예 평가하지 않는다.** 정책이 관여하는 것은 **목록 조회·인증 다운로드·서명 주소 발급**뿐이다. 그래서 링크는 살고 목록만 막힌다.

### 검증 (개발서버 → 운영, 같은 네 가지)
| 확인 | 개발 | 운영 |
|---|---|---|
| **브랜드사 링크(익명 직접 주소)** | 200 ✅ | **200 ✅** |
| 익명 목록 조회 | 0건 ✅ | **0건 ✅** |
| 로그인 목록 조회 | 3건 ✅ | 3건 ✅ |
| 비로그인 캠페인 사진 | 5/5 200 ✅ | **6/6 200 ✅** |

⚠️ **가장 위험했던 지점은 「직접 링크가 정말 정책과 무관한가」**였다. 틀렸다면 브랜드사 업무와 **모든 캠페인 사진**이 동시에 멈춘다. 그래서 개발서버에서 먼저 실측하고 운영에서 다시 확인했다.

### 남는 것
- **비공개 전환(묶음 B 본체)** — 브랜드 포털과 함께. 그때 서명 주소로 갈아탄다
- **브랜드사 공유의 법적 근거** — 묶음 B 머리말의 ⚠️ 참조(제3자 제공·국경 간 이전, 변호사 확인 전제)

---

## 묶음 E-1·E-2·E-3 — 가림막 우회로 차단 ★ **운영 배포 완료** (2026-08-07)

**마이그레이션 312·313** · 커밋 `dd1af71` · PR #973(개발) · **#975(운영)**.

**운영 검증**: 가림막 뷰 **1,487명 정상** · 원본 표 직접 조회 **1건(본인 행만)** · 응모 3,513·메시지 2,929 정상 · 운영현황 브랜드 카드 62개 정상. **1,487명의 민감정보가 뷰를 거치지 않고는 안 나온다.**

⚠️ **운영 적용 중 겪은 것** — 관리자 화면이 로그인 화면으로 튕겨 「312 때문인가」 의심했으나, **검증하던 세션이 사용자 브라우저의 로그인 정보를 지운 것**이 원인이었다(Playwright 가 사용자 크롬에 붙어 있다). 312 와 무관. **앞으로 검증 시 사용자 세션(localStorage)을 건드리지 말 것.**

### 설계 딜레마를 어떻게 풀었나
가림막 뷰는 **원본 표의 정책을 상속**한다(`security_invoker = true`). 원본 조회 정책을 없애면 **뷰도 함께 죽는다** — 계획서의 「가림막 통로만 남긴다」가 그냥은 성립하지 않는다.

**선택**: 뷰를 정의자 권한으로 바꾸고, 원본에서 뺀 판정(`is_admin()`)을 **뷰의 `WHERE` 로 옮겼다.** 판정이 사라지는 게 아니라 **자리를 옮긴다.**

⚠️ 이건 마이그레이션 212가 「하지 말라」고 적어 둔 것이다. 다만 그 경고는 **「판정을 재현하지 않고 뷰만 바꾸는 것」**을 겨냥한 것이고, 여기서는 같은 판정을 1:1로 옮겼다(리뷰에서 212 원문과 대조). **뷰의 `WHERE` 가 유일한 방어선**이므로 그 줄을 지우면 통제가 통째로 사라진다 — 뷰를 고칠 때 반드시 확인할 것.

### 검증 (개발서버, 실제 계정 — SQL 편집기로는 불가)
| 확인 | 결과 |
|---|---|
| 캠페인매니저가 **원본 표 직접 조회** | 전체 13명 중 **본인 행 1건만** ✅ |
| 원본 표 **수정 시도** | 0행 — 막힘 ✅ |
| 가림막 뷰 조회 | 13명 정상 ✅ |
| **뷰의 민감정보 마스킹** | 전화·페이팔 가려짐 + 존재 여부만 ✅ |
| 관리자 화면 3곳(목록·운영현황·신청) | 전부 정상 ✅ |
| **인플루언서 본인 로그인·마이페이지** | 정상 ✅ |

⚠️ **배포 순서 = 코드(E-1) → 312 → 313.** 그대로 지켰다.

## 묶음 E-5·E-6 ★ **운영 배포 완료** (2026-08-07)

**마이그레이션 314·315** · 커밋 `e56b798` · PR #974(개발) · **#975(운영)**. 데이터베이스만 변경(화면 코드 무변경).

### E-5 — 자기 당선 차단
삽입 트리거로 상태를 「심사중」으로 강제. 두 가지가 안 깨지는 것이 관건이었다.

| 확인 | 결과 |
|---|---|
| 상태를 `approved` 로 보내 삽입 | **`pending` 으로 저장됨** ✅ |
| **리뷰어형 자동 승인** | `approved` — **그대로 유지** ✅ |
| 행사 예약 | 마이그레이션 289가 이미 만든 세션 표시를 재사용해 통과(리뷰에서 288·289 원문 대조) |

⚠️ **트리거 이름을 `trg_application_insert_status_guard` 로 지어 알파벳순으로 `trg_monitor_auto_approve` 보다 앞선다.** 먼저 심사중으로 내리고, 곧바로 자동 승인이 덮어쓴다. **이름을 바꾸면 리뷰어형이 심사중에 갇힌다.**

### E-6 — 숨김·회수 메시지 우회 차단

| 확인 | 결과 |
|---|---|
| 회수한 메시지를 **표 직접 조회** | **0건 — 행 자체가 안 보임** ✅ |
| 화면이 쓰는 조회 함수 | 정상 — 본문은 가려지고 **회수 표시만** 나감 ✅ |

⚠️ **삭제 정책은 일부러 안 건드렸다** — 회수 직후 첨부를 지우는 정상 동작이 있어, 막으면 회수 기능 자체가 깨진다.

⚠️ 검증용으로 회수한 메시지 1건이 개발서버에 남는다(메시지 표는 **삭제 정책이 없다** — 감사 목적의 설계). 화면에는 「회수됨」으로만 보여 무해하다.

### 남은 E 조각
- **E-4**(응모 신청의 주소 사본) — **Q4 사용자 확인 필요**(없앨지 가릴지). 없애면 과거 신청의 배송지 이력이 사라진다

---

## 묶음 D — 8월 말 행사 대비 ★ **6조각 전부 완료** (2026-08-07)

| 조각 | 상태 | 마이그레이션 |
|---|---|---|
| **D-1** 공개 전·지난 타임 예약 차단 | ★ **운영 배포** | 316 |
| **D-2** 대기자 승격 | ★ **운영 배포** | 317 |
| **D-3** 정원 초과 경고·표시 | ★ **운영 배포** | — |
| **D-4** 현장 명단 정렬 | ★ **운영 배포** | — |
| **D-5** 묶인 행사 안내 | ✅ 개발서버 | — |
| **D-6** 자정 넘는 타임 | ✅ 개발서버 | — |

### D-1 — 공개 전 캠페인에도 예약이 들어갔다
예약 함수가 **캠페인 상태도 타임 날짜도 안 봤다.** 캠페인 표는 완전 공개, 타임 표는 로그인 전체 조회라 **캠페인을 미리 만드는 순간 열렸다.**

⚠️ **화면과 정확히 같은 기준**으로 맞췄다 — **날짜 단위로만 자르고 오늘 것은 시각이 지나도 통과**(14시 타임을 14시 10분에 현장에서 받아 주는 경우). 서버가 더 엄격하면 정상 예약이 막힌다.

**개발서버 검증**: 지난 날짜 타임 **거부** · 공개 전(`draft`) 캠페인 **거부** · **정상 예약 통과**(티켓 발급).

### D-2 — 대기자를 올릴 방법이 아예 없었다
승격 코드가 **취소 함수 안에만** 있어 확정자가 취소해야만 한 명씩 올라갔다. **현장에서 「이 타임 10명 더 받자」가 성립하지 않았다.**

⚠️ 새 함수는 **기존 승격 도우미를 복사 없이 그대로 호출**한다 — 규칙이 두 벌이 되면 취소 경로와 결과가 갈린다.

**개발서버 검증**(정원 1→3, 확정1·대기1): `promoted:1` · 티켓 대기→확정 · **신청 행도 `approved`**(289 차단 통과) · **승격 알림 발송**.

### D-3 — 정원 초과가 화면에서 안 보였다
잔여가 **음수를 0으로 잘라** 초과 상태가 「마감」으로만 보였다 — **행사 당일에야 좌석이 모자란 걸 안다.** 축소 시 확인 창 + 목록에 「정원 초과 N명」.

### D-5·D-6 — 현장의 막다른 골목 둘
- **D-5**: 묶인 행사를 캠페인 하나로 열면 다른 날 손님이 막히는데 **빠져나갈 길을 안 알려줬다.** 「다시 열라」로 문구를 가름(묶음 없는 단일 행사는 종전 유지)
- **D-6**: 자정 넘는 타임이 미리보기엔 「익일 00:30」으로 나오는데 **저장은 거부된다**(타임에 날짜가 없어 「끝>시작」 제약 위반). 미리보기에서 먼저 알리고, 실패 사유도 건수를 나눠 안내

---

## 묶음 G — 화면끼리 어긋남 (7조각 전부 완료, 2026-08-08)

**구현일:** 2026-08-08 · **관련 PR:** #984(G-3·G-4)·#985(G-5·G-6·G-7)·#986(G-1·G-2) — 전부 개발서버 머지
**마이그레이션:** 318(G-4)·319(G-5)·320(G-7)

### 초안 대비 변경 사항

- **순서가 뒤집혔다.** 계획서는 「G-1이 먼저」였지만 실측(M4)에서 **가구매 캠페인 0개**로 나와 G-1·G-2를 뒤로 미루고 실제 발현이 있는 G-3(36건)·G-4(발견 당시 14건으로 셌으나 실제 1건 — 아래 표 참고)부터 했다. G-1을 안 한 채 G-2만 고치면 틀린다는 의존은 **둘을 같은 커밋에 넣어** 해소
- **G-5의 방향이 실측으로 결정됐다.** 「어느 쪽으로 통일할지 착수 시 결정」이었는데 M6이 0건이라 **감지를 엄격하게**가 명확해졌다(경보가 안 늘어남). 판정을 관대하게 바꾸는 반대 방향은 인증 성공·정산 결과 자체를 바꿔 훨씬 위험
- **계획서에 없던 1건을 함께 고쳤다** — `fetchDeliverablesByCampaign` 만 임시저장을 안 걸러, G-4로 정산을 맞춰 놓자 **이 조회만 세 번째 기준**이 됐다. 같은 커밋에서 정리

### 조각별 실제 발현 건수 (운영 실측)

| 조각 | 발현 | 내용 |
|---|---|---|
| G-3 | **36건** | 영수증 여러 행 쌓인 응모 65건 중 엑셀과 화면·정산이 다른 행을 고름. **구매 금액이 곧 정산 금액**이라 대조 자체가 틀렸다 |
| G-4 | **1건** (처음 14건으로 보고 → 정정) | 임시저장이 최신이면 화면엔 승인인데 정산 후보에서 조용히 빠짐. ⚠️ **14는 「임시저장이 최신인 응모」 수**였고 「승인이 가려진 응모」 수가 아니었다 — 재실측 결과 뒤에 승인 행이 있던 것은 2건(게시물 1·영수증 1), 후보로 실제 살아난 것은 **영수증 1건**. 나머지 13건은 임시저장만 있어 원래도 후보가 아니다 |
| G-5 | 0건 | 대소문자만 다른 값 없음 — 사각지대만 닫음 |
| G-1·G-2 | 0건 | 가구매 캠페인 0개 — **하나 생기면 그날 갈림** |
| G-6·G-7 | — | G-7 과거분은 320 파일 하단 조회로 사람이 처리 |

### 구현 중 기술 결정 사항

- **319의 범위를 A층으로 한정했다.** 기준 데이터 대조(B·C층)는 실제 판정이 하지 않는 비교라 통일 대상이 아니고, 캠페인 토큰 쪽 대소문자는 실측하지 않아 엄격하게 하면 경보만 는다. `covered` 가 `(응모, 결과물 종류)` 로만 결합해 C층 제외는 안 깨짐을 확인
- **320은 `WHEN` 만 넓히고 함수 본문은 그대로 뒀다.** 「pending 만 보류·paid 는 무변경」 성질이 유지된다. 승인된 적 없는 응모는 정산 행이 없어 조기 반환하므로 캠페인 종료 자동 낙첨·행사 대기 취소에서 오작동하지 않는다(경로별 확인)
- **318·319·320 모두 「현재 유효한 원본이 몇 번인지」를 파일 상단과 `CLAUDE.md` 양쪽에 적었다.** 과거에 옛 번호를 베이스로 재작성해 기능이 소실된 전례가 있다

### 남은 일 (사람이 해야 함)

1. **318 적용 뒤** — 정산 「과거 미등록」 화면에 1건이 더해진다(위 정정 참고). ⚠️ 그 화면에는 **정산 공개 전부터 쌓인 과거분 수백 건**이 원래 있다 — 되돌릴 수 없는 「송금완료 기록」 버튼이 있는 자리라, 공개 전환 전에 그 목록 전체를 어떻게 처리할지가 별도 판단거리다(묶음 H)
2. **319 적용 전** — 파일 하단 「사전 확인용 1」 조회로 0건인지 재확인
3. **320 적용 뒤** — 이미 어긋난 과거분은 자동으로 안 고쳐진다. 파일 하단 조회로 찾아 정산 화면에서 직접 보류 처리

---

## 묶음 C — 홍보 메일 (3조각 완료, 2026-08-09~10)

**관련 PR:** #987(개발)·#990(운영) · **마이그레이션:** 321

| 조각 | 실제 발현 |
|---|---|
| C-2 | **수신거부 링크가 약 3개월간 메일에 없었다**(일본 특정전자메일법). 코드는 주소를 만들어 템플릿에 넘기고 있었는데 **본문에서만 빠져** 있었고, 「PR 4 머지 시 복구」 주석이 달린 그 PR 4 는 **하루 뒤 머지**됐다. `CLAUDE.md` 가 「있다」고 적고 있어 아무도 몰랐다 |
| C-3 | 명단이 스스로 줄어드는데 **또 건너뛰어** 1,000명이면 200~399번 자리가 영영 못 받았다 |
| C-4 | 초대 전용 캠페인이 홍보 메일 두 함수 **어디에도 없었다**. 8/28 팝업이 전체 발송에 실릴 뻔 |

⚠️ **C-1은 실측(M1)으로 해소** — 예약이 정상이었다.
⚠️ **앞에서부터 자르기로 바꾸면 「번호가 커지며 저절로 끝나던」 안전장치가 사라진다** → 정체 감지 + 이어달리기 상한 2겹. 그래도 **개인별 기록 실패로 인한 중복**은 못 잡아, 기록 실패 시 한 번 재시도하고 두 번 다 실패하면 눈에 띄게 남긴다

---

## 묶음 F — 잘못된 안내 (13조각 완료 + 1조각 해당 없음, 2026-08-10)

**관련 PR:** #988·#989·#991·#993·#995(개발) · #990·#992·#994(운영) · **마이그레이션:** 322·323

| 조각 | 무엇이었나 |
|---|---|
| F-1 | 영수증 승인이면 **무조건** 「인증샷을 내라」 — 가구매는 요구하지 않고, 방문형은 다음 단계가 게시물 URL. **2026-08-04 사고와 같은 뿌리** |
| F-2 | 1채널만 승인돼도 「전부 완료, 보수 지급 준비」 — **받은 사람은 나머지를 안 낸다** |
| F-3 | 알림 하나를 누르면 **종류 불문 전부** 읽음 처리 → 「메시지 도착」을 누르면 **당첨 알림이 사라졌다** |
| F-4 | 다이제스트 4종에 페이지 나눔 **0회** — 잘리면 이름·캠페인명이 「-」로 나가고 **기록되는 건수도 잘린 값** |
| F-5 | 한 번 실패하면 **그날은 영원히 못 보낸다** — 재호출이 정상 응답을 줘서 운영자가 오인 |
| F-6 | 인플루언서 다이제스트만 자물쇠가 헐거워 **전원이 두 번 받을 수** 있었다 |
| F-7 | 감사용 계정 활동이 관리자 일일 보고에 **실제처럼** 섞였다 |
| ~~F-8~~ | **해당 없음** — 조사 문서에 지적 자체가 없고 코드도 정상(위 표 참고) |
| F-9 | 마감을 연장해도 **재안내가 안 나갔다** — 마감일 칸은 있는데 열쇠에만 없었다 |
| F-10 | **공개 키로 메일 함수를 부를 수 있었다**(운영에서 200 직접 확인) |
| F-11 | 취소된 응모는 **눌러도 못 들어가는 막다른 길** — 배지가 영영 안 지워졌다 |
| F-12 | 임시저장을 「낸 것」으로 세어 **안 냈는데 「확인 중」** |
| F-13 | 알림으로 들어가면 **반려 사유가 안 보였다**(응모이력 경유는 정상 — 경로에 따라 달랐다) |
| F-14 | 일괄 발송이 **개별 답장 한도를 먹었다** — 주석은 「별도 카운터」라 적혀 있었다 |

### 판단이 갈린 지점

- **F-10 설계를 뒤집었다.** 처음엔 「특정 키여야 통과」였는데, **정상 웹훅이 무엇을 보내는지 확인할 방법이 없다**(설정이 대시보드에만 있음). 그 값이 아니면 메일이 통째로 죽는다 — **메일이 죽는 쪽이 임의 촉발보다 나쁘다.** → **공개 키만 거부**. 규칙으로 `.claude/rules/supabase.md` 에 남김
- **F-11 방향은 사용자 결정**(읽기만 허용). 관리자 발송을 막는 쪽은 취소 사유를 묻는 경로가 사라지고 **이미 고정된 배지도 안 풀린다**
- **재시도가 안전해진 범위를 정확히 해 뒀다** — 동시 재시도끼리의 충돌은 막혔지만 **「절반 보내다 죽은 뒤 재시도」의 중복은 남는다**(사람 단위 기록이 없는 곳 네 곳). 각 자리에 주석

### 리뷰가 새 코드에서 잡은 것 (같은 유형의 반복)

- **F-5**: 자물쇠를 **잡을 때는** 확인하는데 **끝낼 때는** 확인하지 않았다 → 정상 실행이 쿨다운보다 길면 넘겨져 **둘 다 돈다**
- **F-1/F-2**: 채널이 기록 안 된 옛 캠페인에 「전부 완료」를 약속 → 그 캠페인은 **어느 판정으로도 인증 성공이 안 된다**
- **F-11**: 자주 묻는 질문 「직접 문의」 버튼이 읽기 전용에서 **무반응 + 전환수만 기록**

---

# 구현 결과 (나머지 묶음 — 개발 세션이 채울 것)
