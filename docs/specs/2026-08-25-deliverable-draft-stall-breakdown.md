# 📋 작업 분해표 — 결과물이 「임시저장」으로 멈춰 관리자에게 안 보이는 결함

**사양서:** 없음 — 이 작업표가 사양서를 겸한다(선행 사양서 미작성. 아래 「현재 상태」·「의심·경우의 수」가 규칙 A·B 몫)
**분해일:** 2026-08-25
**총 작업 조각:** 12개 · **병렬 가능:** 3개 · **순차 필수:** 9개

> **이미 한 일** — 운영에서 임시저장으로 멈춰 있던 **26건을 2026-08-25 에 `pending` 으로 올려 구제 완료**했다(사용자 승인 「26건 전부」). 되돌리기 SQL 은 그 세션의 작업 폴더에 보관. 이 작업표는 **재발 방지**만 다룬다.

---

## 현재 상태 (규칙 A — 2026-08-25 직접 확인)

### 넘겨받은 사실의 재확인 결과 — 2건 정정

| 넘겨받은 내용 | 판정 | 확인한 것 |
|---|---|---|
| 관리자 목록이 임시저장을 뺀다 — `storage.js` 3곳(1028·1201·1823) | **정정 — 5곳이다** | 위 3곳 + `dev/lib/storage.js:4251` + `dev/js/admin-deliverables.js:1160`(**검수 모달 자체 조회** — 공용 함수를 안 거친다) |
| 게시물 제출 버튼이 마감 게이트를 안 본다 | **정정 — 절반만 맞다** | `applyFormGating`(1708행)이 `submitPostsBtn` 을 `buttonIds` 에 넣어 **비활성은 건다**. 실제 비대칭은 **종류 단위 vs 채널 단위** |
| 두 단계 구조 | 맞음 | 제출 실체는 `submitDrafts`(storage.js:1506) — **행별로 나눠** UPDATE |
| 추가 안내가 「リストに追加しました」로 끝남 | 맞음 | **열쇠말 `activity.draftAdded` 하나를 세 화면이 공용**(application.js 2124·2190·2225) |
| `application.js:1794` 에 `hasDraft` 기준 하나 더 | 맞음 | `renderActivityReceiptList` — **영수증 + 방문형 현장 사진**이 같은 함수 |

### 제출 버튼 비대칭의 실제 모습

| 화면 | 버튼 노출 | 마감 게이트 | 판정 단위 |
|---|---|---|---|
| 영수증·현장 사진 | 임시저장 있으면 표시(1794) | 비활성 | **종류 단위** |
| 리뷰 인증샷 | **낼 수 있는** 임시저장 있을 때만(1884) | 숨김 + 비활성 | **채널 단위** |
| 게시물 | 임시저장 있으면 표시(1930) | 비활성 | **종류 단위** |

→ 게시물의 진짜 구멍은 **채널 단위가 아닌 것**이다. A채널은 낼 수 있고 B채널 임시저장만 남은 상황에서 버튼이 활성이라, 누르면 부분 실패하고 **무엇이 못 나갔는지 안 알려준다**.

### 인플루언서가 「아직 안 냈다」를 알 수 있는 자리 — 사실상 한 곳

- 활동관리 목록 행의 **회색** 배지 `未提出`(ja.js:709). **그 화면에 다시 들어가야만 보인다.**
- 🔴 **응모이력은 임시저장을 일부러 감춘다** — `dev/js/mypage.js:265`. 재료(`_myDelivsByApp`)에는 **이미 들어 있다** — 조회 추가 없이 표시만 안 하는 상태다.
- 알림·메일 없음.

### 같은 유형을 이미 한 번 고쳤다 — 그런데 한 곳만

`dev/lib/shared.js:1085` `faqComputeStatus` 주석에 그대로 적혀 있다 — 「사진만 올려 두고 제출을 안 한 사람에게 『제출하신 결과물을 확인 중입니다』가 떴다. **그 사람은 끝난 줄 알고 마감을 놓친다.**」

→ 문의 게이트의 상태 한 줄만 고쳤고, **「그럼 아직 안 냈다고 알려 주자」는 데까지 안 갔다.**

### 🔴 재발 이력

`supabase/migrations/073_fix_deliverable_event_trigger_for_draft.sql` 머리말 — **2026-04-27 운영 사고, 결과물 5건이 임시저장으로 멈춤.** 당시 **트리거만 고치고 원인인 두 단계 화면은 안 고쳤다.** 그때 「임시저장이 낀 전이는 이력을 건너뛴다」로 정한 것이 **오늘 「누가 언제 멈췄나」를 볼 수 없는 이유**다.

### 관리자 구제 경로

- `submit_deliverable`(035) — `user_id = auth.uid()` 검사, 관리자는 거부
- 트리거 `record_deliverable_status_event`(073) — 임시저장이 끼면 **조기 반환**
- `deliverable_events.from_status` CHECK(035)가 **`draft` 를 안 받는다**
- ✅ 관리자 UPDATE 정책(035 `deliverables_update_admin`)에 **상태 제한이 없다** → 관리자가 직접 올리는 것은 원래 가능. 다만 **아무 기록도 안 남는다**

### 관리자 화면에서 그 응모는 어떻게 보이나

목록에서 **사라지는 게 아니라 「미제출」로 뜬다.** → **아예 안 낸 사람과 화면상 구분이 안 된다.** 이게 「안 보인다」의 정확한 모습이다.

---

## 의심·경우의 수 (규칙 B)

1. **UX — 경고를 늘리면 「원래 빨간 화면」이 된다.** 정상 흐름에서도 「추가」와 「제출」 사이는 반드시 미제출이다. **경고(빨강)가 아니라 다음 동작 안내(주황)** 여야 한다.
2. **UX — 자동 스크롤이 모바일 키보드와 충돌한다.** 스크롤은 키보드가 닫힌 뒤 1회만, 또는 스크롤 대신 **화면 아래 고정 줄**.
3. **기술 — `activity.draftAdded` 는 세 화면 공용이다.** 「투고 URL」 기준으로 쓰면 영수증 화면에서 틀린 말이 된다.
4. **기술 — 게이트 폴백 방향.** `gateAllows` 는 그 종류 행이 0건이면 `true`(안 막음)다. 채널이 빈 캠페인에서 **버튼이 사라지지 않는지** 브라우저로 확인.
5. **데이터 — 감지 장치는 「지금 멈춰 있는 것」만 본다.** 「도입했더니 0건이니 문제가 없었구나」로 읽히면 안 된다.
6. **권한 — 감지 결과를 누가 보나.** 결과물 관리에는 캠페인 매니저도 들어온다.
7. **충돌 — 사이드바 「결과물 관리」에는 이미 장치가 둘이다.** 배지 함수가 항목 내용을 **통째로 다시 쓰고** 직후에 어긋남 표시를 다시 입힌다.
8. **약관·법률 — 영향 없음.** 수집 항목·목적·보관 기간 무변경.

### 현재 구현과 어긋나는 지점

- **두 단계 유지** 제약과 「추가하면 자동 제출」은 양립하지 않는다 → **자동 제출은 후보에서 뺀다.**
- `mypage.js:265` 는 **일부러 넣은 줄**이다. 뒤집으면 **왜 뒤집는지 주석**을 남겨야 다음 사람이 또 되돌린다.
- 073 의 결정을 관리자 구제 경로가 뒤집는다. `deliverable_events` 의 **CHECK 두 개**(action 원본 162 / from_status 원본 035)를 함께 봐야 한다.

### 의도가 다르게 읽힐 수 있는 곳

- **「관리자에게 안 보인다」** — ①목록에 나오게인지 ②「냈다가 멈춤」과 「아예 안 냄」이 **구분되게**인지. 이 작업표는 **②로 읽고 설계**했다.
- **「재발 방지」의 범위** — ①앞으로 안 생기게 ②생겨도 빨리 알게 ③생긴 걸 고칠 수 있게. 셋은 별개다.

---

## 🚦 착수 전 선결 조건

| # | 선결 조건 | 막는 조각 |
|---|---|---|
| **S1** | 게시물 제출 버튼을 **채널 단위**로 맞출 것인가 | 작업 3 |
| **S2** | 주소 형태 검사를 추가할 것인가 (임시저장 멈춤과 **별개 문제**) | 작업 10 |
| **S3** | 관리자 쪽 범위 — ①표시만 ②감지까지 ③구제 함수까지(**③만 데이터베이스 변경**) | 작업 7·8·9 |
| **S4** | (②이상 선택 시) 감지 표시를 사이드바 어느 항목에 붙일 것인가 | 작업 8 |
| **S5** | 인플루언서 알림을 **화면 밖**(일일 메일)까지 넓힐 것인가 | 작업 11 |

> **어느 것도 안 정해져도 작업 1·2·4·5·6 은 착수 가능하다.** 그쪽이 재발 방지의 본체다.

---

## 한눈에 보는 의존 순서

```
작업 1(문구 열쇠말 한꺼번에)
   ├─→ 작업 2(활동관리 미제출 안내 — 세 화면) ─→ 작업 3(게이트 · S1) ─→ 작업 4(이탈 경고)
   ├─→ 작업 5(응모이력 미제출 배지)            ∥
   └─→ 작업 6(문의 게이트 상태 한 줄)          ∥

작업 7(관리자 구분 표시 · S3) ─→ 작업 8(감지 · S3·S4) ─→ 작업 9(구제 함수 · S3, DB)

작업 10(주소 검사 · S2)   ← 독립
작업 11(일일 메일 · S5)   ← 작업 1·2 운영 배포 뒤
작업 12(자주 묻는 질문, DB) ← 작업 2·5 운영 배포 뒤에만
```

---

## 작업 조각 표

| 번호 | 제목 | 담당 파일 | 산출 계약 | 선행 | 병렬? |
|---|---|---|---|---|---|
| 1 | 문구 열쇠말 **한꺼번에** 정의 | `dev/lib/i18n/ja.js`·`ko.js` | 열쇠말 8종 | 없음 | ✗(가장 먼저) |
| 2 | 활동관리 미제출 안내 + 제출 버튼 (세 화면) | `application.js`·`dev/index.html`·`campaign.css` | id `activityDraftPendingBar` | 1 | ✗ |
| 3 | 게시물 제출 버튼 채널 단위로 | `application.js` | — | 2, **S1** | ✗ |
| 4 | 미제출 남긴 채 이탈 시 확인 | `application.js`·`app.js` | — | 3 | ✗ |
| 5 | 응모이력 「미제출」 표시 | `mypage.js` | — | 1 | ✓ |
| 6 | 문의 게이트 상태 한 줄 | `shared.js`·`messaging.js` | 반환 key `draft_pending` | 1 | ✓ |
| 7 | 관리자 — 「냈다가 멈춤」 구분 | `admin-deliverables.js`·`storage.js` | `fetchStalledDraftApplications()` | **S3** | ✗ |
| 8 | 관리자 — 감지 표시(0건이면 안 그림) | `admin-core.js`·`admin-deliverables.js`·`dev/admin/index.html` | — | 7, **S3·S4** | ✗ |
| 9 | 관리자 대신 제출 기록(감사) | 마이그레이션 **1~2개** + `storage.js`·`admin-deliverables.js` | 서버 함수 1개 | 8, **S3** | ✗ |
| 10 | 게시물 주소 형태 검사 | `shared.js` 또는 호출부 | — | **S2** | ✓ |
| 11 | 일일 메일에 「미제출로 남음」 | `supabase/functions/notify-influencer-daily-digest/*` | — | 1·2 **운영 배포 뒤**, **S5** | ✗ |
| 12 | 자주 묻는 질문 노드 | 마이그레이션 1개 | — | 2·5 **운영 배포 뒤** | ✗ |

---

## 조각별 상세 — 핵심만

### 작업 1 — 문구 열쇠말 한꺼번에

뒤 조각들이 쓸 문구를 **이 조각에서 전부** 넣는다. 뒤 조각은 번역 파일을 안 건드린다(충돌 제거).

| 열쇠말 | 일본어 | 한국어 뜻 |
|---|---|---|
| `activity.draftAddedNeedSubmit` | `リストに追加しました。まだ提出されていません` | "리스트에 추가했습니다. 아직 제출되지 않았습니다" |
| `activity.draftPendingTitle` | `まだ提出していないものが {n} 件あります` | "아직 제출하지 않은 것이 {n}건 있습니다" |
| `activity.draftPendingStep` | `1. 下の「管理者へ提出」ボタンを押してください　2. 押すまで運営には届きません` | "1. 아래 「관리자에게 제출」 버튼을 눌러 주세요 2. 누르기 전까지 운영팀에 전달되지 않습니다" |
| `activity.leaveWithDraft` | `まだ提出していないものがあります。このまま画面を離れますか？` | "아직 제출하지 않은 것이 있습니다. 이대로 화면을 나가시겠습니까?" |
| `appHistory.deliv.draftPending` | `未提出（提出ボタンを押してください）` | "미제출(제출 버튼을 눌러 주세요)" |
| `messaging.faqStatus.draftPending` | `アップロードしたものがまだ提出されていません` | "올리신 것이 아직 제출되지 않았습니다" |
| `activity.submitPartialFailed` | `「{channels}」は提出できませんでした` | "「{channels}」은(는) 제출하지 못했습니다" |
| `activity.badUrlShape` | `リンク（URL）が正しくないようです。もう一度貼り直してください` | "링크(URL)가 올바르지 않은 것 같습니다. 다시 붙여넣어 주세요" |

> ⚠️ **문구는 확정이 아니다** — 뜻과 눈높이만 맞춘 초안. 착수 시 `reverb-ui-copy` 스킬로 다듬는다. **열쇠말 이름은 확정.**
> ⚠️ 기존 `activity.draftAdded` 는 **지우지 않는다** — 세 호출부를 작업 2 에서 옮긴 뒤 정리.

**완료 정의:** 양쪽 파일에 8개가 있고 개수가 같다 / 빌드 통과 / 화면 동작은 아직 안 바뀐다

### 작업 2 — 활동관리 미제출 안내 (세 화면)

①추가 성공 알림 교체(**세 호출부 전부**) ②제출 버튼 **바로 위**에 안내 줄 ③제출 버튼 눈에 띄게.

**완료 정의:** 세 화면 모두 — 임시저장 추가 → 안내 줄 즉시 표시·건수 정확 / 제출 → 사라짐 / **0건이면 아무것도 안 그림** / 480픽셀 폭에서 제출 버튼을 안 가림

⚠️ **세 화면을 다 봐야 한다** — 운영 26건이 게시물 23·인증샷 2·영수증 1로 **세 종류 전부**였다.
⚠️ 안내 줄은 **주황(행동 유도)**. 빨강이면 정상 흐름에서도 매번 떠 「원래 빨간 화면」이 된다.

### 작업 3 — 게시물 버튼 채널 단위로 (S1)

리뷰 인증샷과 같은 모양으로. 겸해서 부분 실패 시 못 나간 채널 이름을 알린다.

⚠️ `gateAllows` 는 **그 종류 행이 0건이면 `true`**. 채널이 빈 시딩 캠페인에서 버튼이 사라지지 않는지 확인.

### 작업 4 — 이탈 시 확인

⚠️ **뒤로가기에서 주소를 손대기 전에 화면이 실제로 그 화면인지 확인** — 세션 만료로 안에서 로그인으로 튕겨도 나머지 줄이 이어져 「화면은 로그인, 주소는 활동관리」가 된 전례.
⚠️ 캠페인 폼 이탈 경고에서 **확인 창이 죽은 코드로 배포**된 전례. **브라우저에서 실제로 뜨는 것**을 보기 전에는 완료가 아니다.

### 작업 5 — 응모이력 미제출 표시

`mypage.js:265` 의 제외 줄을 걷어내고 **눈에 띄는 색**으로.

⚠️ 그 줄은 **일부러 넣은 줄**이다. 왜 뒤집는지 주석을 남기지 않으면 다음 사람이 또 되돌린다.
**완료 정의에 포함:** **새 조회를 추가하지 않았다**(재료가 이미 있다)

### 작업 6 — 문의 게이트 상태 한 줄

`faqComputeStatus` 가 전용 key 를 돌려주게. **`stage` 는 기존 값 유지**(트리 노드 매칭이 stage 로 걸린다).

🔴 `shared.js` 핫스팟 — 작업 10 과 **동시 진행 금지**.

### 작업 7 — 관리자 구분 표시 (S3)

🔴 **임시저장을 빼는 자리가 5곳이다.** **이 조각은 그 필터를 풀지 않는다** — 풀면 임시저장이 검수 목록·인증 판정·정산 후보에 섞여 훨씬 큰 사고가 된다(마이그레이션 318 과 정반대 방향). **별도 조회로 「있다/없다」만 얹는다.**

### 작업 8 — 감지 표시 (S3·S4)

🔴 사이드바 「결과물 관리」에는 **이미 장치가 둘**이고 배지 함수가 항목 내용을 **통째로 다시 쓴다**. 세 번째를 얹으려면 **그 재입힘 목록에 함께** 넣어야 한다 — 안 넣으면 배지 갱신 때마다 조용히 지워진다.
⚠️ **조회 실패 시에도 안 그린다**(0건인 척하지 않는다).

### 작업 9 — 관리자 대신 제출 기록 (S3, 데이터베이스)

마이그레이션 순서 — ①`deliverable_events` 제약 완화 → ②관리자 전용 서버 함수. **①이 먼저.**

🔴 **CHECK 두 개가 서로 다른 파일에 있다** — `action` 원본 **162**, `from_status` 원본 **035**. **번호가 가장 큰 정의를 베이스로.**
🔴 **적용 성공 = 동작 확인이 아니다.** 신규 함수는 개발서버에서 **최소 1회 실제 호출**. 관리자 가드가 걸려 **SQL 편집기로는 재현 불가** — 로그인한 브라우저에서.
⚠️ 트리거는 **그대로 두고** 이 함수만 이력을 넣는다(트리거를 건드리면 인플루언서 제출 경로 전체가 위험).

### 작업 10 — 주소 형태 검사 (S2)

⚠️ `normalizeUrlInput` 은 **인플루언서와 관리자 대리 등록이 함께 쓴다.**
⚠️ 정상 주소를 막는 쪽이 뭉개진 주소를 통과시키는 쪽보다 나쁘다 — **막지 말고 경고만** 하는 선택지.

### 작업 11 — 일일 메일 (S5)

⚠️ **개발서버 발송 시험 금지.** 검증은 **운영 데이터로 대상자 목록을 재현**하는 방식으로.
⚠️ 메일 양식은 `docs/email-templates/` 가 원본이고 미러를 **배포 전 동기화**.
⚠️ 재발송 억제가 없으면 **매일 같은 메일**이 간다.
⚠️ **코드 머지 ≠ 배포.** 함수는 사람이 따로 배포한다.

### 작업 12 — 자주 묻는 질문 노드

🔴 **화면보다 먼저 넣으면 안 된다** — 화면에 안내가 없는데 「안내 줄을 확인하세요」라고 답하면 **없는 것을 찾게 만든다**(회원 탈퇴 작업에서 같은 이유로 순서를 지켰다).

---

## ⚠️ 공유 지점 경고

| 파일 | 만지는 조각 | 판정 |
|---|---|---|
| `dev/js/application.js` | 2·3·4 | **순차 필수** — 같은 함수를 셋이 건드린다 |
| `dev/lib/shared.js` | 6·10 | 🔴 **핫스팟 — 병렬 금지** |
| `dev/lib/storage.js` | 7·(9) | 🔴 **핫스팟 — 병렬 금지** |
| `dev/lib/i18n/{ja,ko}.js` | **작업 1 뿐** | 작업 1이 한꺼번에 넣어 충돌 제거 |
| `dev/js/admin-core.js`·`dev/admin/index.html` | 8 | 다른 기능이 같은 자리를 쓴다 |
| `dev/js/admin-deliverables.js` | 7·8·(9) | 순차 |
| `dev/build.sh` | **없음** | 새 파일을 안 만드는 설계. 만들면 **파일 목록과 태그 제거 정규식 두 곳** |
| 마이그레이션 번호 | 9·12 | **한 세션에서만** |

---

## 🧭 배분 제안

**개발 1명 순차를 권한다.** 핵심 3개 조각(2·3·4)이 같은 파일이고 나머지도 핫스팟에 걸린다.

- **묶음 A(재발 방지 본체 · 선결 조건 없이 착수 가능):** 1 → 2 → 5 → 6
  - 여기까지만 나가도 인플루언서가 **세 자리**(추가 직후 알림 · 활동관리 안내 줄 · 응모이력 배지)에서 보게 된다
- **묶음 B(비대칭·이탈 · S1 이후):** 3 → 4
- **묶음 C(관리자 쪽 · S3 이후):** 7 → 8 → 9

10·11·12 는 각 선결 조건이 정해진 뒤 별도로.

---

## 검증 계획 (묶음 A 배포 전 최소선)

1. **브라우저 눈 확인 — 세 화면 각각**(리뷰어형 영수증 · 리뷰어형 인증샷 · 시딩형 게시물). 화면 하나만 보고 넘어가면 이번 26건이 세 종류에 걸쳐 있던 이유를 그대로 놓친다
2. 임시저장 **0건**일 때 새 표시가 **하나도 안 뜨는지**
3. 추가 → 제출 → 안내가 사라지는 왕복 1회
4. 모바일 폭에서 안내 줄이 제출 버튼·키보드와 겹치지 않는지
5. 한국어 전환 시 새 문구 8종이 전부 한국어로 나오는지(누락은 일본어가 그대로 뜬다)
6. 회귀 — 반려 재제출 / 마감 지난 캠페인 / 방문형(같은 영수증 폼) 세 경로가 종전과 같은지

---

## 구현 결과 (개발 세션이 채울 것)
