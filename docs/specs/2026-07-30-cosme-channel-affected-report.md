# @cosme 리뷰 인증샷 누락 — 영향 범위 정리 및 공지 초안

**작성일:** 2026-07-30
**작성 세션:** 개발
**관련 사양서:** `docs/specs/2026-07-30-review-image-channel-code-drift.md`
**관련 인수인계:** `docs/specs/2026-07-30-HANDOFF-review-image-channel-drift.md`
**교정 패치:** `supabase/patches/2026-07-30-fix-cosme-review-image-channel-code.sql`

> 이 문서는 **담당자 보고와 관리자 공지사항 작성용 정리**다.
> 인플루언서 이메일·이름이 든 개별 명단은 개인정보라 이 문서에 넣지 않는다.
> 개별 명단은 운영 데이터베이스 조회 결과를 표 파일(CSV)로 받아 사내에서 보관한다
> (조회문은 이 문서 맨 아래 「개별 명단 조회문」).

---

## 1. 상태 (2026-07-30 기준)

| 항목 | 상태 |
|---|---|
| 원인 규명 | 완료 |
| 개발 데이터베이스 리허설 | 완료 — 문법 정상, 인플루언서 알림 0건 실측, 흔적 되돌림 완료 |
| 운영 시험 교정 1건 | 실행했다가 **되돌림**(사용자 지시로 교정 일시 중단) |
| **운영 55건 교정** | **미실행** — 인플루언서 55건이 지금도 가려진 상태 |
| 결과물 제출 마감 차단(마이그레이션 274) 운영 적용 | **금지 상태 유지** — 이 교정보다 먼저 적용하면 대상 55건이 재제출조차 막힌다 |

---

## 2. 무슨 일이 있었나 (사실관계)

@cosme 채널을 시스템 안에서 가리키는 **내부 코드가 바뀌었다**(옛 코드 → 새 코드).
바꿀 때 세 곳 중 두 곳만 새 코드로 정리되고, **이미 제출된 인증샷의 채널 값은 옛 코드로 남았다.**

| 대상 | 코드 상태 |
|---|---|
| 기준 데이터(채널 목록) | 새 코드만 남고 옛 코드 행은 삭제됨 |
| 캠페인의 요구 채널 | 새 코드로 정리 완료 (2026-07-30 재확인 — 옛 코드 잔존 0건) |
| **이미 제출된 리뷰 인증샷** | **옛 코드로 방치** ← 이번 문제 |

시스템은 「캠페인이 요구한 채널」과 「제출된 인증샷의 채널」이 **같은 값인지 글자 그대로 비교**한다.
그 비교가 **세 곳에서 동시에 쓰이기 때문에**, 채널 값 하나가 어긋나자 세 곳이 함께 깨졌다.

1. 인플루언서 활동관리 화면 — 제출한 인증샷이 화면에 안 보인다
2. 관리자 인증 상태 — 「인증성공」으로 올라가지 못한다
3. 정산 후보 — 정산 대상에서 빠진다

**데이터는 사라지지 않았다.** 인증샷 파일과 승인 기록은 그대로 있고, 채널 값만 어긋나 있다.
그래서 **채널 값을 맞추면 위 세 곳이 함께 회복된다**(화면·판정 로직 수정 불필요).

### 왜 아무도 먼저 발견하지 못했나
관리자 검수 화면에는 「채널이 지정되지 않은 옛 인증샷」을 찾아 주는 구제 장치가 있는데,
그 장치는 **채널 값이 비어 있을 때만** 작동한다. 이번 건은 **값이 있고 그 값이 틀린** 경우라
구제 장치의 사각지대에 들어가, 정리 대상으로도 표시되지 않았다.

---

## 3. 영향 범위

- **결과물 55건** (모두 이미 관리자가 **승인**한 인증샷)
- **캠페인 8개**
- **인플루언서 34명**
- 제출 시기: 2026-05-12 ~ 2026-06-16

### 캠페인별 건수

| 캠페인번호 | 캠페인명 | 건수 |
|---|---|---|
| B0032-C003 | アットコスメ@cosme ASCEplus カーミング アンプルプロ | 17 |
| B0003-C002 | スピキュールレチノールPDRNクリーム | 8 |
| B0035-C004 | アットコスメ@cosme レビュアー・グアバ70スキントナーパッド, 70枚 | 7 |
| B0018-A002-C001 | レチノール10倍ペプチド毛穴引き締めケアアンプル 30ml | 6 |
| B0035-C005 | アットコスメ@cosme レビュアー・ビタミンCセラム 30ml | 6 |
| B0003-C001 | スピキュールコラーゲンPDRNクリーム | 5 |
| B0018-A002-C002 | バイオーム8X 毛穴うるおい美容液＆クリームセット | 3 |
| B0047-C001 | @Cosme レビュアー アップルサイダービネガー14包 | 3 |
| **합계** | | **55** |

### 이번 대상이 아닌 것
- **같은 응모에 옛 코드와 새 코드가 둘 다 있는 26건** — 채널 값을 바꾸면 「한 응모에 같은 채널 인증샷이 두 개」가 되어 저장이 거부된다. 처리 방침(그대로 둘지·정리할지)은 **미결**이라 이번 교정에서 제외한다.
- **게시물(리뷰 인증샷이 아닌 SNS 게시물) 2건** — 채널이 「기타」로 저장돼 있으나 **둘 다 이미 반려 처리**돼 인증 성공률·정산에 영향이 없다. 다만 채널이 지정되지 않은 캠페인에서는 아직 「기타」를 고를 수 있는 구멍이 남아 있어, 재발 방지 항목으로 별도 관리한다.

---

## 4. 조치 계획

| 순서 | 내용 | 상태 |
|---|---|---|
| 1 | 옛 코드로 남은 인증샷 55건의 채널 값을 새 코드로 교정 | **대기**(사용자 지시로 중단) |
| 2 | 교정 이력을 결과물 이력에 기록 (누가·언제·무엇을 바꿨는지 남김) | 1과 동시 |
| 3 | 관리자 화면에서 인증 성공 회복 확인 | 1 이후 |
| 4 | 정산 후보 생성 건수·금액 확인 (정산 도입일 설정과 함께 점검) | 1 이후 |
| 5 | 광고주 보고서 정정 필요 여부 판단 | 운영·영업 판단 |
| 6 | 인플루언서 안내 | 운영팀 판단 |
| 7 | 재발 방지 — 채널 코드를 지울·바꿀 때 이미 제출된 결과물에 그 코드가 남아 있으면 막기 | 미착수 |

### 교정 작업의 안전성 (확인된 사항)
- 결과물의 **상태 값은 건드리지 않는다.** 그래서 5~6월 건에 대해 인플루언서에게 **지금 알림이 나가지 않는다** — 개발·운영 양쪽에서 실측 확인 완료(알림 0건).
- 처리 대상을 조회 조건으로 미리 좁혀, 위 「대상이 아닌 26건」은 손대지 않는다.
- 시험 1건 → 확인 → 나머지 일괄 순서로 진행하고, 되돌리는 조회문도 함께 준비돼 있다.

---

## 5. 관리자 공지사항 초안

> 교정을 **실행한 뒤** 아래 초안의 날짜·건수를 확정해 관리자 공지사항으로 등록한다.
> 등록 시에는 서식 있는 본문(HTML)으로 변환이 필요하다 — `/공지초안-관리자` 참고.
> 분류는 「경고」 또는 「시스템 변경」이 적절하다.

**제목:** @cosme 리뷰 인증샷 55건이 검수 화면에 안 보이던 문제를 고쳤습니다

**본문:**

@cosme 캠페인의 리뷰 인증샷 일부가 관리자 검수 화면과 인플루언서 화면에서 보이지 않던 문제가 있었습니다. 원인을 찾아 데이터를 바로잡았습니다.

**어떤 문제였나**

@cosme 채널을 시스템 안에서 가리키는 이름이 바뀌었는데, 그 전에 이미 제출된 인증샷은 옛 이름을 그대로 갖고 있었습니다. 시스템은 캠페인이 요구한 채널과 제출된 인증샷의 채널이 같은지 비교하기 때문에, 이름이 어긋난 인증샷은 「제출되지 않은 것」처럼 취급됐습니다.

그 결과 세 가지가 함께 어긋났습니다.

1. 인플루언서 활동관리 화면에 본인이 올린 인증샷이 보이지 않음
2. 관리자 결과물 화면에서 「인증성공」으로 올라가지 않음
3. 정산 대상 후보에 잡히지 않음

**영향 범위**

- 인증샷 55건 (모두 이미 승인 처리된 것)
- 캠페인 8개, 인플루언서 34명
- 제출 시기 2026년 5월 12일 ~ 6월 16일

인증샷 파일과 승인 기록은 지워진 적이 없습니다. 채널 이름만 어긋나 있었습니다.

**어떻게 고쳤나**

옛 이름으로 남아 있던 55건의 채널 이름을 현재 이름으로 맞췄습니다. 승인·반려 상태는 전혀 바꾸지 않았으므로, 이 작업 때문에 인플루언서에게 알림이 가지는 않습니다. 누가 언제 무엇을 바꿨는지는 각 결과물의 이력에 남겨 두었습니다.

**담당자가 확인해 주실 것**

1. 해당 캠페인 8개의 **인증 성공률이 올라갔는지** 결과물 화면에서 확인해 주세요.
2. 이 캠페인들의 **광고주 보고서를 이미 보냈다면**, 인증 성공 수치가 달라졌으니 정정이 필요한지 판단해 주세요.
3. 인플루언서가 「인증샷이 안 보인다」고 문의하면, **다시 올리지 않아도 된다**고 안내해 주세요. 이미 승인된 것이 화면에 다시 나타납니다. 같은 인증샷을 또 올리면 중복이라 오류 화면이 나옵니다.

**앞으로**

채널 이름을 지우거나 바꿀 때, 이미 제출된 결과물에 그 이름이 남아 있으면 변경을 막는 장치를 추가할 예정입니다.

---

## 6. 개별 명단 조회문 (담당자 전달용 — 표 파일로 저장해 사내 보관)

> ⚠️ **교정을 실행하면 이 조회는 0행이 된다.** 반드시 교정 전에 뽑아 둘 것.
> 교정 후에 명단이 필요하면 결과물 식별자 55개로 조회해야 한다(그 목록은 이 문서에 넣지 않는다 —
> 교정 패치 파일의 검증 조회 3번 항목에 그대로 들어 있다).

```sql
SELECT c.campaign_no       AS 캠페인번호,
       c.title             AS 캠페인명,
       i.name              AS 인플루언서_한자,
       i.name_kana         AS 인플루언서_가나,
       i.email             AS 이메일,
       d.id                AS 결과물id,
       d.status            AS 결과물상태,
       d.created_at::date  AS 제출일,
       d.receipt_url       AS 인증샷주소,
       a.status            AS 신청상태
  FROM deliverables d
  JOIN campaigns c         ON c.id = d.campaign_id
  LEFT JOIN applications a ON a.id = d.application_id
  LEFT JOIN influencers i  ON i.id = d.user_id
 WHERE d.kind = 'review_image'
   AND d.post_channel = 'channel-96r9y3'
   AND NOT EXISTS (
     SELECT 1 FROM deliverables x
      WHERE x.application_id = d.application_id
        AND x.kind = 'review_image'
        AND x.post_channel = 'cosme'
   )
 ORDER BY c.campaign_no, i.email;
```
