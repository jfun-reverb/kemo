# 넓은 화면(아이폰 폴드 「iPhone Duo」·아이패드) 대응 — 사양서

**작성일:** 2026-09-11 · **작성:** 기획 세션 · **받는 곳:** 개발 세션(웹) + iOS 개발 세션
**근거 조사:** `docs/specs/2026-09-11-wide-screen-current-state-survey.md`(iOS 개발 세션, 2026-09-11) — 이 사양서는 그 조사의 주장을 **코드로 다시 대조**한 뒤 쓴다(§1-1).
**사용자 결정(2026-09-11, iOS 세션 경유):** ①대상 기기 = 아이폰 폴드 + 아이패드 전 기종 ②범위(폭만 채우기 / 열 수를 늘리는 새 배치)는 기획이 정한다.

---

## 0. 한 줄 결론 — 범위는 「폭 채우기 + 카드 3열」까지, 「새 배치」는 뒤로

**기둥(`#appShell`)을 없애지 않고, 화면 폭 600 이상에서만 상한을 480 → 720 으로 올린다**(600 미만은 지금 그대로). 같은 조건(600 이상)에서 글과 입력칸은 **읽기 폭 560** 으로 묶고, 캠페인 카드만 **3열**로 늘린다. 상세 2단·마이페이지 사이드바 같은 **「새 배치」는 이 사양의 범위 밖**이다 — 그것은 Apple 지침 기반 화면 재설계(`.claude/rules/planning.md`)에서 화면 통째로 다룬다.

왜 이렇게 자르나 —
- 기둥이 **떠 있는 요소 전부의 기준 상자**다(원리는 조사 §2-1, 목록은 조사 §2-3 — 이 사양은 개수를 따로 세지 않고 그 목록을 그대로 가리킨다). 기둥을 버리면 그 전부를 다시 앉혀야 하고, 그 작업은 화면 재설계와 겹친다.
- iOS 앱은 **iOS 27 SDK 이전 빌드라 Duo 속 화면에서 아직 폭을 받지 못한다**(§1-2). 지금 새 배치를 만들어도 앱에서 볼 수 없다.
- 새 배치는 화면마다 설계 결정이 따로 든다. 얇은 「폭 대응 층」을 먼저 깔고, 재설계가 그 위에서 열 수를 정하는 편이 두 번 일하지 않는다.

---

## 1. 현재 상태 (규칙 A — 2026-09-11 기준, 기획 세션이 직접 확인)

### 1-1. 조사 문서 대조 — 주장과 코드가 일치하는가

| 조사 문서의 주장 | 코드에서 확인(`dev`, 커밋 `6d672b59`) | 비고 |
|---|---|---|
| `#appShell` 이 `position:fixed`+`transform`+`max-width:480px` 기둥 | `dev/css/base.css` `#appShell` 규칙 그대로 | 일치 |
| 480 이 박힌 자리 8곳(기둥 별도) | `.container`·`.container-sm`·`.container-md`(base.css) · `.modal`·`#detailFloatBar`·`#legalModal .legal-sheet`·`#policyNoticeModal .legal-sheet`(components.css) · `#loginPromptOverlay` 안쪽 인라인(index.html) = **8곳** | 일치 |
| 폭 미디어 쿼리는 `@media(min-width:481px){body{background}}` 하나 | 인플루언서 CSS·index.html 에서 그 한 줄뿐. 나머지 `@media` 는 전부 `admin.css` | 일치 |
| `clamp()`·컨테이너 쿼리·방향 쿼리 0건 / `safe-area-inset-left/right` 0건 | 0건 / 0건 | 일치 |
| 폭을 읽는 자바스크립트 0건 | `innerWidth`·`matchMedia`·`ResizeObserver`·`orientationchange`·`resize` 리스너 — 인플루언서 쪽 0건. `visualViewport` `resize` 하나(app.js)뿐 | 일치 |
| 썸네일 저장 폭 `campaigns:720 · content:720 · receipts:480 · review-images:480`, `RICH_DISPLAY_WIDTH=960` | `storage.js` `THUMB_WIDTH_BY_PREFIX`·`shared.js` `RICH_DISPLAY_WIDTH` | 일치. ⚠️ 코드의 `thumb:720`(상세 첫 장)·`thumb:480`(둘째 장부터·카드) 숫자는 **죽은 값**이다 — `renderCroppedImg`(ui.js)는 그 숫자를 안 보고 「사본을 쓸지」만 보며, 사본은 `storageThumbUrl` 이 폴더 규칙으로 찾는다. 캠페인 사진은 전부 **저장된 720 사본**을 쓴다(첫 장·둘째 장·카드 모두) |
| `.camp-grid` 2열 고정 | `grid-template-columns:1fr 1fr` | 일치 |
| iOS 테마: `@media` 0건, 고정 요소 3개 | `@media` 0건. `position:fixed` 를 **직접 쓴 규칙은 2개**(`.ios-tabbar`·`.ios-tab-back`) — `#detailFloatBar` 는 웹 규칙의 fixed 를 물려받고 위치만 바꾼다 | 「셋 다 기둥 기준」이라는 결론은 같다 |
| 아이폰 세로만 / 아이패드 네 방향 / `UIRequiresFullScreen` 없음 | `Info.plist` 그대로 | 일치 |
| iOS 브랜치는 dev 보다 712커밋 뒤 | 오늘 기준 **718커밋** | 계속 벌어진다 |
| 관리자 캠페인 미리보기가 인플루언서 앱을 띄운다 | 미리보기 모달 상자 **400px** 고정, 옆 미리보기 패널 420px → 기둥 상한을 올려도 **영향 없음** | 조사에 없던 확인 |

→ **조사 문서의 사실 관계는 그대로 쓴다.** 어긋난 것은 숫자 둘(고정 요소 2/3, 커밋 712/718)뿐이고 결론은 안 바뀐다.

### 1-2. 기기 사실 (외부 — 2026-09-11 웹 확인)

아이폰 폴드의 공식 이름은 **iPhone Duo**(2026-09-09 발표, 10월 출시). 개발자에게 중요한 것만:

| 항목 | 값 | 우리에게 뜻하는 것 |
|---|---|---|
| 밖 화면 | 5.4형, **466 × 678 pt** | 세로(466)는 480 보다 좁다 → **지금 폰과 같은 취급**. 가로(678)는 폰 가로와 같이 「넓음」(아래 폭 구간) |
| 속 화면 | 7.6형, **669 × 951 pt** (세로 기준. Apple 이 pt 를 공표하지 않아 개발자 커뮤니티 계산값 — 626×890 이라는 계산도 있다. **어느 쪽이든 600 보다 넓어 「넓음」이고 720 상한 안에 든다**) | 기둥 720 이면 **속 화면을 꽉 채운다** |
| 기기 분류 | **아이폰**(idiom). 속 화면은 가로·세로 모두 「regular」 크기 클래스 | 아이패드 전용 설정(`~ipad`)은 안 탄다 |
| 방향 | 🔴 **속 화면에서는 앱의 방향 잠금(`UISupportedInterfaceOrientations`)을 시스템이 무시한다** | 세로 잠금이 있어도 속 화면은 **가로(951×669)로도 뜬다** |
| 안전영역 | 다이내믹 아일랜드가 세로로 서 있어 **좌우 인셋이 비대칭** | `env(safe-area-inset-left/right)` 가 0건인 지금은 그 자리에 내용이 깔린다 |
| 접힘선 | 시트·알림·메뉴는 시스템이 접힘선을 피해 옮긴다 | 웹뷰 안 요소는 우리 몫 |
| 🔴 옛 SDK 빌드 | **iOS 27 SDK 이전으로 빌드한 앱은 속 화면에서 「익숙한 크기·비율」로만 뜬다**(폭을 다 못 받는다). iOS 27 SDK 는 상태바 왼쪽까지, iOS 27.1 SDK 는 화면 끝까지 | 우리 앱은 **Xcode 26.6** 빌드 → Duo 속 화면 대응은 **Xcode 27.1(9월 말 베타) 재빌드 뒤에야** 효과가 난다. 그 전에는 **Safari 웹에서만** 넓은 화면을 받는다 |

아이패드 CSS 폭(세로 기준, 근사): 미니 **744** · 11형 **820~834** · 13형 **1032**. 가로 1133~1376. 분할 화면은 **320** 부터.

→ **폭 구간**: 320(분할·슬라이드 오버) · 375~440(폰 세로) · 466(Duo 밖 세로) · **667~932(폰 가로·Duo 밖 가로 678 — Safari 웹에서만, 앱은 세로 잠금)** · **669(Duo 속)** · 744~1032(아이패드 세로) · 951(Duo 속 가로) · 1133~1376(아이패드 가로). ⚠️ **폰 가로는 600 을 넘어 「넓음」이다** — 지금도 폰 가로는 480 기둥에 양옆 회색이라 폰 관습이 아니었고, 이 사양 뒤에는 720 기둥이 된다(§3-4). 이 사양은 **600 을 경계**로 「좁음(지금 그대로)」과 「넓음(이 사양)」을 가른다(§3-0).

### 1-3. 관련 결정·문서

- **앱 화면 로딩 방식(A 운영 주소 직접 / B 번들+무선 업데이트 / C 번들+재심사)은 아직 미결**(`docs/specs/2026-07-14-influencer-app-transition-handoff.md` §3, 2026-07-14 고문 → 기획 인계, 기획 미착수). 지금 앱은 **번들 스냅샷**이라 웹을 고쳐도 앱은 재빌드 전엔 안 바뀐다. **이 사양은 그 결정과 독립**이다 — 어느 쪽이든 CSS 는 같다. 다만 「언제 앱에 닿나」가 그 결정에 달렸다.
- **iOS 브랜치의 웹 공용 수정 2건 미이식**(`docs/specs/2026-08-26-ios-shared-fixes-handoff.md`). 이 사양과 파일이 겹칠 수 있다(`campaign.css`·`components.css` — 자바스크립트는 이 사양이 안 고치므로 안 겹친다) — 병합 순서는 §3-6.
- **Apple 지침 기반 화면 재설계**(`.claude/rules/planning.md` 「인플루언서 화면 설계 기준」, 2026-09-02 사용자 결정 — 기존 화면도 순차 재작업, 단위는 화면 통째). 이 사양은 **재설계가 아니다**(색·글자·구성 요소를 안 바꾼다). 재설계는 이 사양이 만드는 변수(§3-0) 위에서 열 수를 정한다.
- 썸네일 규칙: `CLAUDE.md` 「이미지 썸네일 — 유료 변환을 쓰지 않는다」 — 저장 폭 목록 세 곳 동시 유지, 원본 압축 금지.

### 1-4. 이 제안과 충돌 가능성 있는 기존 동작 (6개 영역 확인 — 표 8줄, 실제로 바뀌는 것은 2줄 §2-2)

| 자리 | 지금 | 이 사양과의 관계 |
|---|---|---|
| `@media(min-width:481px){body{background:#E5E5E5}}` | 481 부터 회색 | **그대로 둔다** — 기둥 상한이 600 미만에서 480 그대로라 481~599 는 지금과 같고, 600~720 은 기둥이 화면을 다 채워 회색이 안 보이며, 721 부터 다시 보인다(§3-2 `base.css`) |
| `#page-signup` 의 `auth-card` 인라인 `max-width:560px` | 회원가입만 560 | 읽기 폭 560 의 **선례**. **그대로 둔다** — `≥600` 공용 규칙과 같은 값이라 겹쳐도 무해(§3-2 `index.html`) |
| `#cropModal` — 실행 중 `document.body` 로 옮겨져 뷰포트 기준·`max-width:500px` | 기둥 규칙의 예외 | **그대로 둔다**(이미 480 을 넘고 뷰포트 기준이 맞다) |
| `#page-admin .form-row` 2열 — 공용 `components.css` 안의 관리자 예외 | 관리자만 2열 | **안 건드린다**(관리자는 대상 아님) |
| 관리자 캠페인 미리보기(모달 400px·패널 420px) | 인플루언서 앱을 iframe 으로 | 상한을 올려도 400/420 < 480 이라 **화면 변화 없음**(확인 완료) |
| `.camp-grid{grid-template-columns:1fr 1fr}` | 2열 고정 | 넓은 폭에서 3열(§3-2). 좁은 폭은 그대로 |
| `campaign.js` 필터 영역 `maxHeight:'80px'` | 스크롤 시 접었다 펴는 상한 | 폭이 넓어지면 칩 줄 수가 **줄지 늘지 않는다** → 잘릴 위험 없음. 변경 없음 |
| `application.js` 상세 슬라이더 `aspect-ratio:1/1`(인라인) | 기둥 폭 정사각 | 기둥 720 이면 안쪽 688px 정사각 → 가로 화면에서 화면을 통째로 차지(§2-1 ①). 상한을 둔다(§3-2 「상세 히어로」) |

### 1-5. 미해결 백로그·관련 작업
- 화면 오른쪽 아래 회색 호(⌒) 원인 미확정(조사 §1) — 넓은 화면에서만 보인다. **이 사양의 검증 단계에서 웹 검사기로 함께 본다**(§3-5).
- iOS 브랜치 718커밋 병합 · 웹 공용 수정 2건 이식 — 별도 작업(§3-6 순서만 정한다).
- 로딩 방식 A/B/C 결정 — 별도 기획.

---

## 2. 의심·경우의 수 (규칙 B)

### 2-1. 깨질 수 있는 경우

1. **(기술) 상세 정사각 히어로가 화면을 삼킨다.** 기둥 720 에서 `#campSlider` 는 안쪽 폭 688(=720−32, §3-4 계산식)의 정사각이라 688px 높이. 아이패드 가로(높이 820)·Duo 속 가로(높이 669)에서 **첫 화면이 사진 하나**가 된다. → 히어로에 상한을 둔다(§3-2 「상세 히어로」).
2. **(기술) 접었다 펴는 순간.** 폭을 읽는 자바스크립트가 0건이라 CSS 가 다시 계산하면 끝난다. `adjustHeight` 는 높이만 다시 쓰고 폭은 `width:100%` 라 저절로 맞는다. 사진 넘김은 퍼센트 이동이라 안 어긋난다. **다만 실기기·시뮬레이터가 없어 확인 못 한다** — Duo 는 Xcode 27.1 뒤, 그 전엔 크롬 반응형 모드에서 폭을 바꿔 보는 것으로 대신한다(§3-5).
3. **(데이터) 썸네일이 흐려진다.** 카드 한 장 폭은 **§3-4 의 계산식 한 벌**로 센다 — 넓은 폭 3열 최대 약 221px(기둥 720), 지금 2열 약 218px(기둥 480). 3px 차이이고 221 × 2배 화면 = 442 기기 픽셀 < 720 파일이라 **카드는 그대로 충분**. 상세 사진(첫 장·둘째 장 모두 720 파일, §1-1)은 560 상한 × 2배 화면 = 1120 기기 픽셀을 720 으로 그린다(지금은 웹 기둥 안쪽 448 × 2배 = 896 → 이미 1.24배 확대, 이 사양 뒤 1.56배). **눈에 띄는지는 실기기로 봐야 안다.** 띄면 저장 폭을 올리고 소급 도구(`scripts/backfill-storage-thumbs.js`)를 다시 돌린다 — 이 사양은 **저장 폭을 건드리지 않는다**(§3-3).
4. **(환경) iOS 앱은 두 겹으로 늦다.** ①dev 보다 718커밋 뒤라 병합 전엔 앱에 안 실린다 ②Xcode 26.6 빌드라 Duo 속 화면에서 폭을 못 받는다. **Duo 대응의 첫 효과는 Safari 웹**이고, 앱은 Xcode 27.1 재빌드 뒤다. 사용자에게 이 순서를 미리 말한다.
5. **(UX — 필수) 아이패드 가로에서 「폰 앱을 가운데 놓은 모양」이 남는다.** 1180 폭에 720 기둥이면 양옆 230px 씩 회색. 이건 **범위를 「새 배치 없이」로 자른 결과**이고 이 사양의 가장 큰 의도적 한계다. 사용자가 이 모습을 받아들이기로 **확정**(§4 Q1).
6. **(UX) 읽기 폭을 빠뜨린 화면만 줄이 길어진다.** 규칙을 「목록에 있는 선택자」에만 걸면, 목록에서 빠진 화면 하나가 688px 한 줄 일본어를 보여 준다. → 적용 목록을 사양서에 박고(§3-2 「읽기 폭」), 검증 때 **화면별로 한 번씩** 본다.
7. **(UX) 아래 붙는 시트가 720 폭으로 늘어난다.** 시트 폭이 기둥을 따라가므로(§3-2) 넓은 화면에서 응모 모달·약관 창이 720px 짜리 아래 붙는 시트가 된다 — 폰 관습을 태블릿 폭으로 늘린 모양. Apple 지침은 넓은 폭(regular)에서 시트를 **가운데 카드**로 바꾼다. → 가운데 카드형으로 확정(§3-2 「시트」·§4 Q2).
8. **(UX) iOS 알약 탭바가 688px 로 늘어난다.** `calc(100% - 32px)`(과 뒤로 단추 켜짐 변형)이 기둥을 따라간다. 탭 4개에 688px 은 손이 닿는 거리를 넘는다. → 탭바 폭 규칙 2벌에 상한 480(§3-2 「iOS 테마」).
9. **(UX) 좁은 폰이 같이 바뀌면 안 된다.** 카드 열을 `auto-fill` 로 하면 375 폭 폰(SE·mini)이 **1열로 줄어든다**(343px 안에 200px 두 장이 안 들어간다). → 자동 채움이 아니라 **폭 경계 600 에서 3열로 바꾸는 규칙**으로 하고, 검증 때 390·375 폭 스크린샷이 **변경 전과 픽셀 단위로 같은지** 본다(§3-5 ① 양성 대조).
10. **(환경) PC 웹도 함께 바뀐다.** `dev/css` 를 고치면 PC 크롬에서 보는 인플루언서 웹이(창 폭 600 이상이면) 480 → 720 기둥이 된다. 관리자 미리보기(400px)는 안 바뀐다. **PC 웹의 모습이 바뀌는 것**을 사용자가 알고 수용하기로 **확정**(§4 Q3).
11. **(환경) 분할 화면·슬라이드 오버(320~).** 기둥이 `width:100%` 라 320 에서도 지금 폰과 같이 뜬다. 이미 열려 있고 이 사양은 막지 않는다(§3-1 #6). 다만 **320 은 어떤 폰보다 좁아**(가장 좁은 폰 375) 지금도 본 적 없는 폭이다 — 검증 목록에 넣는다.
12. **(법률·약관)** 없음 — 수집 항목·외부 서비스·개인정보 흐름 변화 0.
13. **(권한)** 없음 — 인플루언서 화면만, 로그인 여부와 무관.

### 2-2. 현재 구현과 어긋나는 지점
§1-4 표 8줄 중 **실제로 이 사양이 바꾸는 2줄** — `.camp-grid` 2열 · 상세 슬라이더 정사각(둘 다 `≥600` 에서만). 나머지 6줄(회색 배경 미디어 쿼리·회원가입 카드 인라인 560·`#cropModal`·관리자 `.form-row`·관리자 미리보기·필터 80px)은 **그대로 두거나 영향 없음**. 그 밖에 **충돌 없음 — 6개 영역(CSS 5파일·index.html·인플루언서 JS·iOS 테마·Info.plist·관리자 미리보기) 확인 완료.**

### 2-3. 사용자 의도와 다르게 읽힐 수 있는 부분
- 「아이패드 전 기종」에 **가로 화면과 분할 화면**이 포함되는가 — 이 사양은 **포함**으로 읽고 막지 않는다(§4 Q4 확정).
- 「폭만 채우기」에 **카드 열 수 조정**이 포함되는가 — 카드 열은 「새 배치」가 아니라 폭 채우기의 일부로 본다(열을 안 늘리면 카드가 약 338px 정사각으로 부풀어 — §3-4 계산식 — 폭을 채운 것이 아니라 확대한 것이 된다). §4 Q1 확정.
- 「대응」이 **앱만**인가 **Safari 웹도**인가 — 코드가 공용이라 둘 다이고, 시간상 웹이 먼저다(§2-1 ④).
- 「폴드」= Duo **속 화면**(669) 이야기다. 밖 화면 **세로**(466)는 지금 폰과 같아 할 일이 없고, 밖 화면 **가로**(678)는 폰 가로와 같이 「넓음」으로 처리된다(§1-2 폭 구간).

---

## 3. 설계

### 3-0. 원칙
- **화면 재설계가 아니다.** 색·글자 크기·구성 요소·기능 동작은 그대로. 바뀌는 것은 **넓은 폭(≥600)에서의 상자 크기와 자리**뿐 — 시트가 아래 붙음에서 가운데 카드(페이드 등장)로 옮겨 앉는 것(§3-1 #5)까지가 그 범위다.
- **스타일만 고친다** — 고치는 파일: 웹 CSS **5파일**(`base`·`components`·`auth`·`campaign` + `mypage` — 마지막은 읽기 폭 선택자 몫만, §3-2 전체 목록 1항) + `index.html` 인라인 스타일 1곳(마크업 구조는 그대로, 클래스 하나 붙이는 것까지) + iOS 테마 1파일. **자바스크립트 0줄, `Info.plist` 0줄, 데이터베이스 0.**
- **값은 한 곳, 그리고 기본값은 지금 값.** `base.css` `:root` 에 변수 둘 — `--shell-max:480px`(기둥 상한, **기본은 지금과 같은 480**) · `--reading-max:560px`(읽기 폭). 그리고 `@media(min-width:600px){:root{--shell-max:720px}}` — **넓은 폭에서만 720 으로 올린다.** 변수를 쓰는 9자리(아래)는 이 한 줄로 함께 넓어진다 — 그중 시트 4자리는 같은 조건에서 560 으로 다시 좁힌다(§3-2 「시트」).
- 🔴 **600 미만은 한 픽셀도 안 바뀐다 — 결과 기준, 예외 없음.** 보장하는 방법은 둘 중 하나 — ①새 규칙을 `min-width:600px` 블록 안에 둔다(읽기 폭·시트·카드 열·안전영역·상한 720) ②블록 밖에 두는 규칙은 **600 미만에서 지금과 같은 값을 낸다**(`:root` 기본값 480 = 지금 값 / `.login-prompt-sheet{max-width:var(--shell-max)}` = 옮겨 온 인라인 480 / iOS 탭바 폭 규칙 2벌(§3-2 「iOS 테마」)을 `min(…, 480px)` 으로 감싼 것 = 좁은 폭에선 앞 항이 이겨 지금과 같다). 그래서 481~599(폰 가로 아님·창을 줄인 PC 등)도 지금처럼 「480 기둥 + 옆 회색」이고, 회색 배경 미디어 쿼리(`min-width:481px`)도 **손대지 않는다**. 폰 **세로**(375~440)가 양성 대조(§3-5 ①), 481~599 는 §3-5 ②의 540 이 본다. 폰 **가로**(667~932)는 「넓음」이라 바뀐다(§1-2).
- **480 이 박힌 8곳 + 기둥(`#appShell`) = 9자리 전부 `var(--shell-max)` 로 바꾼다**(그중 `#loginPromptOverlay` 인라인 1곳은 지우고 `components.css` 의 `.login-prompt-sheet` 로 옮긴다 — 자리 수는 그대로 9). 숫자를 남기면 다음에 상한을 옮길 때 또 센다.
- **미디어 조건은 셋뿐** — `min-width:600px` · `min-width:600px and (orientation:landscape)`(상세 히어로 한 곳) · iOS 테마의 `max-width:599px`(같은 경계의 반대쪽). **이 사양이 새로 넣는 미디어 조건은 이 셋뿐**(기존 `min-width:481px` 는 그대로 남는다 — 「변경분」 기준). 블록 수는 세지 않는다(같은 조건은 파일마다 한 블록에 모은다 — 조건이 다르면 블록도 다르다. 개수는 「구현 결과」에 사실만 적는다).

### 3-1. 조사 §5 의 결정 지점 9개에 대한 답

| # | 결정 지점 | 결정 | 근거 |
|---|---|---|---|
| 1 | 기둥을 넓힐 것인가 버릴 것인가 | **넓힌다(720)** | 떠 있는 요소 전부(조사 §2-3 목록)의 기준 상자. 버리면 전부 재배치 = 재설계 범위 |
| 2 | 상한 | 기둥 **720** · 읽기 폭 **560** (둘 다 ≥600 에서만 — 미만은 지금 그대로) | 720 = Duo 속 화면(669)을 다 채우고 썸네일 천장과 같다. 560 = 회원가입 카드가 이미 쓰는 값. 일본어 14px 로 한 줄 약 38자 — 읽기 한도 안 |
| 3 | 카드 열 수·홈 3칸·상세 1열 | 카드 **≥600 에서 3열**, 홈 3칸·상세 1열 **그대로** | 카드 한 장 크기를 폰과 같게 유지하려는 것. 상세 2단은 재설계 몫 |
| 4 | 본문 줄 길이 상한 | **560(≥600 에서만), §3-2 전체 목록의 자리에** | 없으면 688px 한 줄 |
| 5 | 아래 붙는 시트 | **≥600 에서 가운데 카드형(사방 둥근 모서리, 상한 560, 등장은 페이드)** | Apple 지침(regular 폭에서 시트는 가운데). §4 Q2 확정 |
| 6 | 아이패드 가로·분할 허용 | **지금처럼 연 채로 둔다** + `#appShell` 에 `env(safe-area-inset-left/right)` 좌우 여백(**≥600 블록 안에만**, §3-2 `base.css`) | 기둥이 320~720 어느 폭이든 처리한다. 막으면 Duo 속 화면(방향 잠금 무시)과도 어긋난다 |
| 7 | 폴드 방향 잠금 | **손대지 않는다** | Duo 는 아이폰 분류이고 속 화면은 잠금을 무시한다 — 어느 값을 넣어도 속 화면 가로는 온다. CSS 가 가로를 견디면 된다 |
| 8 | PC 웹도 바뀜 | **수용** (두 벌 안 만든다) | iOS 테마에만 넣으면 폭 규칙이 두 벌. §4 Q3 |
| 9 | 재설계·앱 전환과의 선후 | **이 사양 먼저**, 재설계는 이 변수 위에서, A/B/C 는 독립 | §0 |

### 3-2. 변경 목록

**`dev/css/base.css`**
- `:root{--shell-max:480px;--reading-max:560px}` 추가.
- `@media(min-width:600px){:root{--shell-max:720px} #appShell{padding-left:env(safe-area-inset-left);padding-right:env(safe-area-inset-right)}}` — 상한 올리기와 좌우 안전영역을 **한 블록**에. 기둥이 `overflow:hidden` 이라 안쪽 요소는 자동으로 그 안에 앉는다.
- `#appShell{max-width:var(--shell-max)}` · `.container`·`.container-sm`·`.container-md` → `max-width:var(--shell-max)`. (셋이 같은 값이라는 사실은 그대로 — 이름 정리는 범위 밖.)
- `@media(min-width:481px){body{background}}` — **그대로**(§3-0). 481~599 는 기둥 480 + 회색으로 지금과 같고, 600~720 은 기둥이 화면을 다 채워 회색이 안 보이며, 721 부터 다시 보인다.

**`dev/css/components.css`**
- `.modal`·`#detailFloatBar`·`#legalModal .legal-sheet`·`#policyNoticeModal .legal-sheet` → `max-width:var(--shell-max)`. 그리고 새 규칙 `.login-prompt-sheet{max-width:var(--shell-max)}`(index.html 인라인을 옮겨 온 것 — 9자리 중 하나).
- **시트(결정 5, 확정 — §4 Q2)**: `@media(min-width:600px)` 안에서 `.modal-overlay{align-items:center}` 와, 아래 붙는 시트 **4자리** `.modal`·`#legalModal .legal-sheet`·`#policyNoticeModal .legal-sheet`·`.login-prompt-sheet`(`#loginPromptOverlay` 안쪽에 새로 붙이는 클래스 — 아래 `index.html`) 에 `{max-width:var(--reading-max);border-radius:20px;animation:none}` + 등장은 `opacity` 페이드. ⚠️ **`#cautionCompareModal`·`#applyActionModal`·`#cancelDetailModal`·`#alertModal`·`#profileAlertOverlay`·`#ageGateOverlay`·`#cropModal` 은 이미 가운데형**(상한 360~500) — 손대지 않는다. `.notif-modal-body`(`top:15%`)는 가운데형에 준해 그대로.
- `@media(min-width:600px){#appShell .form-row{max-width:var(--reading-max);margin-left:auto;margin-right:auto}}` — ⚠️ **`#appShell` 로 좁혀서** 인플루언서 앱에만 걸리게 한다. `.form-row` 는 관리자 문서도 쓰는 공용 클래스(`#page-admin .form-row` 2열 예외가 그 증거)라 맨 `.form-row` 에 걸면 관리자까지 바뀐다. 개발이 확인할 것 둘 — ①관리자 문서(`dev/admin/index.html`)에 `#appShell` 이 없는지 ②공용 파일이 이름을 댄 `#page-admin` 이 `#appShell` **안쪽에 있는지**(안쪽이면 `#appShell .form-row` 로도 걸리므로 `#page-admin` 을 제외하는 선택자를 더한다).

**`dev/index.html`(인라인 1곳)**
- `#loginPromptOverlay` 안쪽의 인라인 `max-width:480px` 를 **지우고** 그 요소에 클래스 `login-prompt-sheet` 를 붙인다. 폭은 `components.css` 가 정한다 — 기본 `.login-prompt-sheet{max-width:var(--shell-max)}`, `≥600` 에서 가운데형 560(위 「시트」). 인라인을 남기면 **인라인 480 이 클래스의 720/560 을 이겨 넓은 폭에서 안 넓어진다**(인라인이 우선) — 그래서 지운다. 마크업 구조는 안 바뀐다.
- `#page-signup` `auth-card` 인라인 `max-width:560px` — **손대지 않는다.** 아래 `auth.css` 의 `≥600` 규칙과 같은 값이라 겹쳐도 무해하고, 지우면 600 미만에서 달라진다(§3-0 「예외 없음」).

**`dev/css/auth.css`**
- `@media(min-width:600px){.auth-card{max-width:var(--reading-max);margin-left:auto;margin-right:auto}}` — 로그인·가입·비밀번호 찾기·재설정 카드가 넓은 폭에서 같은 폭.

**`dev/css/campaign.css`**
- `.camp-grid` — 기본 2열 유지, `@media(min-width:600px){.camp-grid{grid-template-columns:repeat(3,1fr)}}`. 🔴 **`auto-fill` 금지**(§2-1 ⑨ — 375 폰이 1열이 된다).
- 상세 히어로: `#campSlider` 는 `application.js` 가 인라인으로 만든다 — 인라인 `aspect-ratio:1/1` 은 두고 **CSS 로 `@media(min-width:600px){#campSlider{max-width:var(--reading-max);margin-left:auto;margin-right:auto}}`** 를 준다(자바스크립트 0줄 원칙). 넓은 폭에서 560 정사각. ⚠️ Duo 속 가로(높이 669)에서는 560 이 여전히 크다 — `@media(min-width:600px) and (orientation:landscape){#campSlider{max-width:min(var(--reading-max),70vh)}}`. 정사각이라 `max-width` 로 높이도 잡힌다. ⚠️ iOS 테마의 `#campSlider{width:calc(100% + 32px);margin:… -16px}`(여백을 뚫는 전면 사진)과 **충돌한다** — iOS 테마 쪽은 `@media(max-width:599px)` 로 좁은 폭에 한정한다(아래 「iOS 테마」).
- `.detail-img{height:200px}` — 그대로(상세 안 작은 상품 이미지, 폭과 무관).

**읽기 폭 560(`var(--reading-max)`)을 받는 자리 — 전체 목록(파일 불문, 17자리, 전부 `min-width:600px` 블록 안. 결정 4·5)**
이 목록이 **유일한 정본**이고, 위 파일별 항목은 전부 여기에 다시 나온다:
1. 글 블록 10 — `.legal-body`·`.legal-page-body`·`.rich-content`·`.guide-val`·`.caution-box`·`.msg-faq-answer-body`·`.ticket-msg`·`.dpb-step`·`.invite-gate-hint`·`.hero-sub`. **왼쪽 정렬 그대로**(가운데 두지 않는다). 이 열 개는 **campaign.css·components.css·mypage.css 세 파일에 흩어져 있다**(조사 §2-5) — 각 선택자가 셋 중 어느 파일인지는 개발이 찾아 **그 파일의 `≥600` 블록**에 넣는다. `.hero-sub{max-width:100%}` 는 「일부러 상한 없음」 주석이 있으니 **주석도 함께 고친다**. ⚠️ `.dpb-step`·`.invite-gate-hint` 는 `white-space:pre-line` 이라 넓히면 줄이 안 접혀 「단계처럼 보이던」 모양이 사라진다 — 560 안에서 지금 480 기둥 안쪽(448)과 비슷하게 접히는지 **눈으로** 본다(§3-5 ④).
2. 폼·카드 2 — `#appShell .form-row`·`.auth-card`. **가운데**(`margin:auto`).
3. 이미지 1 — `#campSlider`(가운데, 가로일 때 `70vh` 상한 — 위 「상세 히어로」).
4. 시트 4 — `.modal`·`#legalModal .legal-sheet`·`#policyNoticeModal .legal-sheet`·`.login-prompt-sheet`(위 「시트」).

**`ios-app/www/ios-theme.css`(iOS 브랜치, iOS 세션)**
- 탭바 **폭 규칙 2벌**을 `min(…, 480px)` 으로 감싼다 — `.ios-tabbar{max-width:calc(100% - 32px)}` · `.ios-tab-back.on ~ .ios-tabbar{max-width:calc(100% - 32px - var(--ios-tab-h) - 12px)}`(뒤로 단추가 켜졌을 때). `.kb-hidden` 변형은 위치만 바꾸고 폭 규칙이 없어 손대지 않는다. **자리**는 지금 규칙 그대로 — 기본은 기둥 가운데, 뒤로 단추가 켜지면 `translateX` 가 34px(=(56+12)÷2) 오른쪽으로 밀어 **뒤로 단추 오른쪽 빈 자리의 가운데**에 앉는다. 그래서 겹치지 않는다: 기둥 600 에서 뒤로 단추 16~72, 탭바 94~574 / 기둥 720 에서 16~72, 154~634. **뒤로 단추 자체(`.ios-tab-back`, 기둥 왼쪽 16px 에 `--ios-tab-h` 56px 정사각 원)는 안 건드린다** — 크기가 px 고정이라 기둥이 넓어져도 지금 값 그대로다.
- `#campSlider` 여백 뚫기 → `@media(max-width:599px)` 안으로(599 = 600 경계의 반대쪽, §3-0. 이 파일에서 유일한 미디어 블록).
- → 바뀌는 항목은 위 **2개**(탭바 폭 규칙 2벌 = 1항목, `#campSlider` = 1항목)뿐. `.ios-tab-back` 은 px 고정이라 그대로, `.gnb-apply-filter{max-width:46%}` 같은 백분율 규칙은 손대지 않아도 기둥을 따라 넓어진다(의도).
- ⚠️ **iOS 테마는 dev 를 병합한 뒤에 고친다**(718커밋). 병합 전 고치면 병합 때 다시 맞춘다.

**바꾸지 않는 것(확인 완료)** — `storage.js` 썸네일 폭 · `shared.js` `RICH_DISPLAY_WIDTH` · `app.js` `adjustHeight` · `campaign.js` 필터 80px · `#cropModal` · `#toast`(`min(90vw,440px)` — 기둥 720 안에서 440 은 여전히 맞다) · `.nav-panel-body`(`min(320px,82vw)`) · 홈 특징 3칸 · `.detail-layout` 1열 · `Info.plist` · `capacitor.config.json`.

### 3-3. 하지 않는 것 (범위 밖 — 이유와 함께)
- **글자 크기 `clamp()`·`rem` 전환** — 재설계(iOS 타입 스케일)와 한 몸. 지금 넣으면 두 번 바꾼다.
- **상세 2단·마이페이지 사이드바·응모이력 표 형태** — 새 배치. 재설계에서 화면 단위로.
- **썸네일 저장 폭 상향** — §2-1 ③ 실기기 확인 뒤 결정. 올리면 소급 도구 재실행이 딸려 온다. (코드에 남은 `thumb:480/720` 죽은 값 정리도 자바스크립트라 이 사양 밖.)
- **접힘선(크리스) 대응** — 웹뷰 안에서 접힘선 위치를 알 방법이 없다(시스템이 알려 주는 API 를 웹이 못 받는다). 시트를 가운데 카드로 두면 접힘선 위에 걸칠 수 있으나 Duo 실기기 없이 판단 불가 — 실기기 뒤 후속.
- **회색 호(⌒) 원인** — 검증 때 함께 보되, 이 사양의 완료 조건은 아니다.
- **iOS 두 커밋 이식·718커밋 병합** — 별도 작업. 순서만 §3-6.
- **관리자 화면** — 대상 아님(`planning.md`).

### 3-4. 폭 구간별로 무엇이 보이나 (완료 기준)

카드 한 장 폭 계산식(문서 전체가 이 한 벌을 쓴다): **내용 폭 = 기둥 폭 − 32(좌우 여백 16×2)**, 칸 사이 12 → 2열 = (내용 폭 − 12) ÷ 2, 3열 = (내용 폭 − 24) ÷ 3. 예: 480 → 218 · 320 → 138 · 466 → 211 · 600 → 181 · 720 → 221. ⚠️ ≥600 에서 좌우 안전영역 인셋이 0 이 아닌 기기(폰 가로·Duo 속 화면)는 기둥 안쪽이 그만큼 더 좁아 아래 표의 값보다 몇 픽셀 작다. 표의 값은 소수점을 반올림한 것이라(166·181·221) 600 미만 행까지 포함해 **전부 「약」**으로 적는다.

| 폭 | 기둥 | 카드 | 글·폼 | 시트 |
|---|---|---|---|---|
| 320(분할) | 320 (100%) | 2열 · 약 138px | 그대로 | 아래 붙음 |
| 375~440(폰 세로)·466(Duo 밖 세로) | 100% | 2열 · 약 166~211px | 그대로 | 아래 붙음 |
| 481~599(창을 줄인 PC·아이패드 분할 화면 일부 — 폰 세로·Duo 밖 세로는 해당 없음) | **480 + 옆 회색(지금과 같음)** | 2열 · 약 218px | 그대로 | 아래 붙음 |
| 600~720(Duo 속 669·폰 가로 667~720) | 100% | **3열** · 약 181~221px | **560** | **가운데 카드 560** |
| 721~932(폰 가로 큰 기종) | **720 + 옆 회색** | 3열 · 약 221px | 560 | 가운데 카드 560 |
| 744~1032(아이패드 세로)·951(Duo 속 가로) | **720 + 옆 회색** | 3열 · 약 221px | 560 | 가운데 카드 560 |
| 1133~1376(아이패드 가로) | 720 + 옆 회색 넓게 | 3열 | 560 | 가운데 카드 560 |

⚠️ 「600~720」 줄에서 기둥이 100% 인데 body 회색 규칙(481 부터)이 그대로인 것이 맞다 — 기둥이 화면을 다 채우므로 회색이 보일 자리가 없다. ⚠️ 표는 대표 구간만 적는다 — 적히지 않은 폭(321~374·441~465·467~480 등)은 기둥 상태가 같은 인접 행(100% / 480+회색 / 720+회색)을 따른다.

### 3-5. 검증 절차

1. **양성 대조(먼저)** — 크롬 반응형 모드 **390 폭과 375 폭**(§2-1 ⑨가 지목한 SE·mini 폭) 두 가지에서 홈·캠페인 목록·상세·로그인·응모 모달·약관 시트 6장씩을 **변경 전**에 찍어 둔다. 변경 후 같은 12장을 찍어 **픽셀 단위 diff 0**. 이게 0이 아니면 「좁은 폭은 안 바뀐다」 원칙이 깨진 것.
2. 크롬 반응형 모드 폭 **320 · 466 · 540 · 669 · 744 · 951(가로) · 1180(가로)** × 같은 6화면. 540 은 481~599 구간의 대표 — **480 기둥 + 옆 회색이 지금과 같은지**(§3-0). 🔴 폭을 **한 창에서 연속으로 바꾸며**(접었다 펴기 대신) 화면이 스스로 다시 앉는지 본다 — 새로고침 없이.
3. 떠 있는 요소 **전부**(조사 §2-3 의 `position:fixed` 목록을 그대로 체크리스트로 쓴다. `#cropModal` 은 뷰포트 기준 예외라 제외)를 669·1180 에서 각각 한 번씩 띄워, **①기둥 밖으로 나가지 않는지 ②각자 정해진 폭대로인지** 본다. 정해진 폭은 셋으로 갈리고, 어느 부류인지는 §3-2 가 정한다(여기서 이름을 다시 세지 않는다 — §0) — **560 가운데 카드**: §3-2 「시트」의 4자리 / **원래 상한 그대로**: §3-2 「바꾸지 않는 것」에 적힌 것과 이미 가운데형인 대화창들 / **기둥 폭 그대로**(669 에서는 669, 1180 에서는 720): 그 밖의 전부. iOS 전용 2개(`.ios-tabbar`·`.ios-tab-back`)는 ⑤-b 에서.
4. 읽기 폭 — 두 겹으로 본다. **④-a 자리별**: §3-2 「읽기 폭 560 을 받는 자리」 17자리를 720 에서 한 번씩(`.dpb-step`·`.invite-gate-hint` 는 모양·줄 접힘까지). **④-b 화면별**(§2-1 ⑥ 의 「목록에서 빠진 화면」을 잡는 쪽): 인플루언서 앱의 **모든 화면**을 720 에서 한 번씩 훑는다 — 홈·캠페인 목록·상세·로그인·회원가입·비밀번호 찾기·마이페이지 폼 7종·활동관리·응모건 메시지(자주 묻는 질문 포함)·알림·약관 2종·행사 티켓·초대 안내·탈퇴. 688px 로 길어진 일본어 줄이 보이면 **그 선택자를 §3-2 목록에 더하고**(「구현 결과」에 기록) ④-a 를 다시 돈다.
5. 아이패드 미니 시뮬레이터(iOS 세션) — 두 번으로 나뉜다. **⑤-a(웹, 병합 전 가능)**: Safari 로 dev 서버를 열어 세로·가로·**분할 1/3** 세 상태. **⑤-b(앱, §3-6 ③ 병합 + iOS 테마 반영 뒤)**: 앱을 빌드해 같은 세 상태에서 iOS 테마 2항목(탭바 480 상한·`#campSlider` 599 한정)과 iOS 전용 고정 요소 2개(`.ios-tabbar`·`.ios-tab-back`)를 ③과 같은 두 기준으로 — ①기둥 밖으로 나가지 않는지 ②정해진 폭대로인지(탭바는 기본·뒤로 단추 켜짐 두 상태 모두 **480 을 넘지 않는지** — §3-2 가 정한 값 / 뒤로 단추는 **56px 그대로**인지 — §3-2 가 안 바꾸는 지금 값). 여기에 iOS 만의 셋째 확인 — ③뒤로 단추가 켜진 상태에서 **탭바와 뒤로 단추가 안 겹치는지**(§3-2 「iOS 테마」의 자리 계산 — 아이패드 미니 세로·가로는 기둥 720 이라 뒤로 단추 16~72 / 탭바 154~634).
6. Duo 실기기(사용자 손에 들어온 뒤 — **Safari 로는 §3-6 ① 배포 뒤 바로**, 앱으로는 §3-6 ④ 뒤) — 크롬 폭으로는 못 보는 것만: **접힘선 위에 시트가 걸치는지**, 좌우 안전영역 인셋이 실제로 비대칭으로 들어오는지. (폭 자체는 ②의 669·951 로 이미 본다. 시뮬레이터는 Xcode 27.1 전까지 없다.)
7. 회색 호(⌒) — 744 에서 웹 검사기로 요소를 짚어 원인만 적는다(고치는 건 별도).
8. 관리자 캠페인 미리보기 모달 — 변화 없음을 한 번 본다(400 < 480).
9. 수 대조(웹 병합 요청 리뷰 때) — ①`var(--shell-max)` 자리 **9**(§3-0) ②`var(--reading-max)` **선택자 자리 17**(§3-2 전체 목록 기준 — 변수 **출현 횟수**는 `#campSlider` 가 가로 조건에서 한 번 더 써 18) ③**변경분(diff)에 새로 들어간** 미디어 조건이 §3-0 의 셋 외에 **없음**(이 웹 PR 에는 앞의 둘만 나온다 — `599` 는 iOS PR. 기존 `481` 은 변경분이 아니다). 다르면 「구현 결과」에 이유.

### 3-6. 순서·배포

1. **웹(dev)** — **착수 전에 §3-5 ①의 「변경 전」 스크린샷 12장을 개발서버(배포 전 현재 판)에서 먼저 찍는다** → 이 사양 §3-2 의 웹 파일 구현, PR 1개 → 병합 요청 리뷰에서 §3-5 ⑨(수 대조) → 개발서버 배포 → §3-5 ①(변경 후 12장을 **같은 개발서버**에서 찍어 diff — 환경이 다르면 diff 가 이 사양과 무관한 이유로 0 이 아니게 된다)·②~④·⑤-a·⑦·⑧.
2. **운영 배포 여부는 사용자 확인**(PC 웹·Safari 폴드·아이패드 Safari 가 이때 바뀐다).
3. **iOS 브랜치** — dev 병합(718+) → iOS 테마 2항목(§3-2) → 앱 빌드 → 아이패드 미니 시뮬레이터 §3-5 ⑤-b(⑤-a 는 ①단계에서 Safari 로 먼저 해도 된다). ⚠️ 웹 공용 수정 2건 이식(08-26 인수인계)과 순서가 겹치면 **병합이 먼저**.
4. **Xcode 27.1** 로 재빌드 → Duo 속 화면에서 폭을 받는다. 그 전 빌드는 속 화면에서 「익숙한 크기」로 뜬다(§1-2). Duo 실기기가 생기면 §3-5 ⑥(Safari 몫은 ① 뒤에 이미 가능, 앱 몫은 여기서).

---

## 4. 사용자 확인 — 확정 (2026-09-11, 기획 세션에서 확인)

| # | 물은 것 | 결정 |
|---|---|---|
| Q1 범위 | 「≥600 에서 기둥 720 + 읽기 560 + 카드 3열, 새 배치 없음」 — 아이패드 가로에서 양옆 회색이 넓게 남는 모습(§2-1 ⑤) 포함 | **권고안대로** |
| Q2 시트 | 넓은 폭에서 가운데 카드형 / 아래 붙는 시트 그대로(폭만 기둥을 따라 720) | **가운데 카드형** |
| Q3 PC 웹 | PC 브라우저의 인플루언서 웹도 720 기둥이 됨 | **수용** |
| Q4 아이패드 가로·분할 | 지금처럼 열어 둔다 / 세로 잠금·전체 화면 강제 | **열어 둔다** |

→ 이 표가 §3-1 결정표의 근거이고, 둘은 같은 말을 해야 한다. 바뀌면 둘 다 고친다.

---

## 5. 구현 결과 (개발 세션이 채울 것)

- 구현일 / 관련 커밋·병합 요청
- `var(--shell-max)` 자리 수(기대 **9**) / `var(--reading-max)` 자리 수(기대 **17**) / 변경분에 새로 들어간 미디어 조건 종류(기대: §3-0 의 셋 외 없음) / 미디어 블록 수(사실만 — 기대값 없음) — 다르면 이유
- 읽기 폭을 준 선택자 목록 실제(§3-2 와 대조)
- 양성 대조 결과(390·375 각 6장 diff)
- 회색 호(⌒) 원인 — 못 찾았으면 「미확인」이라고 적는다(완료 조건 아님, §3-3)
- 초안 대비 변경(추가/빠짐/달라짐)
