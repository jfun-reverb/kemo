# 회원 탈퇴 — 기능·문구·약관 정합

**작성일:** 2026-08-18
**작성:** 기획 세션
**요청:** 사용자 — 「회원탈퇴가 정상 작동하는지, 사유·안내를 하는지, 관리자가 어디서 보는지」 확인 → **「모두 처리」**
**관련:** `docs/TERMS_kr.md` 제6조 · `docs/PRIVACY_kr.md` · 메모리 `project_line_minimization` · `project_copy_vs_behavior_audit`

---

## 배경 한 줄

**탈퇴 버튼은 있으나 아무 일도 일어나지 않는다.** 확인창과 안내 문구만 뜨고 **서버 호출이 0줄**이다. 그런데 회원에게는 「접수했습니다」라고 말하고, 약관·개인정보처리방침은 그 접수를 전제로 한 규정을 여럿 두고 있다.

---

## 1. 현재 상태 (규칙 A — 2026-08-18 코드 실측)

### 1-1. 탈퇴 기능의 전부

`dev/js/mypage.js:602` — **이것이 코드 전부다.**

```javascript
function handleWithdraw() {
  const confirmMsg = t('mypage.withdrawConfirm');   // 「정말 탈퇴하시겠습니까?」
  const toastMsg   = t('mypage.withdrawToast');     // 「탈퇴 신청을 접수했습니다. 운영팀에 LINE으로 연락해 주세요.」
  if (confirm(confirmMsg)) toast(toastMsg);
}
```

진입점은 **햄버거 메뉴 하단 「退会する」** 한 곳(`dev/js/notifications.js:97`).

| 확인 | 결과 |
|---|---|
| 서버 호출 | **0줄** |
| 탈퇴 사유 수집 | **없음** (확인창 하나뿐) |
| 안내 내용 | 토스트 **한 줄**. 약관·방침이 정한 안내는 **전무** |
| 기록 | **어디에도 안 남음** |
| 관리자 확인 화면 | **없음** — 관리자 코드 전체에서 탈퇴 관련 항목 **0곳** |
| 데이터베이스 | 인플루언서 탈퇴를 담는 **표·칸 없음** |
| 자주 묻는 질문 | 탈퇴 항목 **없음** |

### 1-2. 「LINE으로 연락」의 정체 — 미완성 흔적이다

**2026-04-06 최초 커밋(`793c0c62`)에 이미 있었다.** 화면 뼈대를 만들 때 서버 기능이 없으니 「일단 사람이 연락하게」 둔 자리다. **설계된 절차가 아니다.**

⚠️ **그리고 방침에 어긋난다.** 2026-05-21 사용자 명시(메모리 `project_line_minimization`):
> LINE 의존을 최대한 줄이고 관리자 응대 창구를 앱 하나로 통합한다. **LINE에 남기는 건 앱에 못 들어오는 경우만**(가입 전·로그인 불가·장애). **앱 안에서 LINE 유도 문구는 제거.**

탈퇴 화면은 **앱 안에서 LINE으로 내보내는** 자리라 정확히 제거 대상이다.

⚠️ **그렇다고 응모건 메시지로 보낼 수도 없다.** `application_messages.application_id` 가 **NOT NULL** 이라 **응모 1건에 붙어 있고**, 탈퇴는 응모와 무관하다. **응모를 한 번도 안 한 회원**은 창구가 아예 없다. 같은 메모리가 「**응모건 밖 일반 문의 창구 신설**이 LINE 축소의 핵심」이라 적어 뒀으나 **미착수**다.

### 1-3. 약관·방침이 지킬 수 없는 약속을 하고 있다

| 문서 | 약속 | 실제 |
|---|---|---|
| 약관 제6조 1항 | 「마이페이지에서 **언제든지 탈퇴 신청**을 할 수 있습니다」 | 버튼은 있으나 **접수되지 않음** |
| 약관 제6조 2항 | 「진행 중인 캠페인 신청(심사중/승인)은 **자동으로 철회 처리**됩니다」 | 철회하는 코드 **없음** |
| 방침 §보유기간 | 「원칙적으로 **탈퇴일로부터 5일** 경과 후 지체 없이 파기합니다(**변심에 의한 탈퇴 취소 대비**)」 | **탈퇴일이 기록되지 않아 셀 수 없음** |
| 방침 수집 표 | 회원가입 정보 「**탈퇴 후 5일** 경과 시 파기」 / PayPal 이메일 「**탈퇴 후 5년**(세법)」 / 프로필·주소·신청 기록 「**탈퇴 시 파기**」 | 〃 |

⚠️ **탈퇴일이 어디에도 없으므로 방침의 보관 기간 규정을 집행할 수단 자체가 없다.** 한국 개인정보 보호법·일본 개인정보보호법 양쪽 준수 대상이다.

### 1-4. 재사용할 수 있는 것들 (설계가 단순해지는 근거)

| 무엇 | 어디 | 이번에 어떻게 쓰나 |
|---|---|---|
| **응모 취소 함수** `cancel_application` | 마이그레이션 113 → **306**(현재 원본) | 약관 제6조 2항의 「자동 철회」에 재사용 |
| **인플루언서 상태 이력** `influencer_flags` | `action`(verify/violation/blacklist/clear) + 사유·메모·시각 | 탈퇴 이력을 같은 표에 담을 수 있다 |
| **관리자 인플루언서 상세 모달** 「상태 관리」 패널 | `dev/js/admin-influencers.js:362·449` | 탈퇴 상태·사유를 여기에 붙인다 |
| **정산 조회** | `fetchMySettlements` · `settlements.status` | 미지급 판정에 쓴다 |
| **취소 사유 기준 데이터** | `lookup_values(kind='cancel_reason')` | 탈퇴 사유도 같은 방식으로 |

### 1-5. 이 제안과 충돌 가능성이 있는 기존 동작

**⚠️ ㄱ. 「자동 철회」가 서버에서 막힌다.** 마이그레이션 **247** 이 「**송금완료된 정산이 있는 응모는 반려·되돌리기를 차단**」한다. 약관 제6조 2항을 글자 그대로 실행하면 **그 응모에서 반드시 실패**한다.

**⚠️ ㄴ. 응모 취소에도 조건이 있다.** `cancel_application`(306)은 **결과물이 승인된 건을 차단**하고, 구매 기간 이후에는 **사유·동의를 강제**한다. 탈퇴가 이 함수를 재사용하면 **같은 조건에 걸린다.**

**⚠️ ㄷ. 알림·메일이 계속 나간다.** 일일 다이제스트·홍보 메일·마감 안내가 **인플루언서 표를 기준으로** 대상을 고른다. 탈퇴 상태를 안 보면 **탈퇴한 사람에게 계속 메일이 간다.**

**⚠️ ㄹ. 로그인이 계속 된다.** 탈퇴는 인증 계정(`auth.users`)과 별개다. **표시만 바꾸면 그 사람은 여전히 로그인해 응모할 수 있다.**

**⚠️ ㅁ. 감사용 계정**(`is_audit=true`)은 운영팀이 쓰는 공용 계정이라 **탈퇴 대상이 아니다.**

**⚠️ ㅂ. 정산 행은 삭제할 수 없다.** `settlement_events` 가 `ON DELETE RESTRICT` 다. 「탈퇴 시 파기」를 글자 그대로 하면 **정산이 걸린 회원은 파기 자체가 불가능**하다. 방침도 PayPal 이메일은 **5년 보관**이라 해 서로 맞물린다.

### 1-6. 미해결 백로그

- **응모건 밖 일반 문의 창구** — 미착수. 이번 범위 밖이나 **탈퇴 문의가 갈 곳이 없다는 문제와 같은 뿌리**다
- 2026-08-12 문구 대조 조사에서 이 건이 「**미정 — 가장 무거움**」으로 기록됨

---

## 2. 의심·경우의 수 (규칙 B)

**① 진행 중 응모가 있는데 철회가 막히면 탈퇴가 통째로 실패한다.** §1-5 ㄱ·ㄴ. 「전부 철회하고 탈퇴」로 짜면 **한 건만 막혀도 전체가 멈춘다.**

**② 5일 유예를 어떻게 구현하나.** 방침이 「변심 대비 5일」이라 했으니 **그 사이 취소할 수 있어야 한다.** 그런데 취소하려면 **로그인이 돼야** 하고, 로그인이 되면 「탈퇴했는데 왜 들어가지나」가 된다.

**③ 데이터: 파기 범위가 항목마다 다르다.** 방침은 회원가입 정보 5일 · 프로필·주소·신청 기록 즉시 · **PayPal 이메일 5년**이다. **한 번에 지우는 것이 아니라 항목별로 다른 시점**이다.

**④ 데이터: 재가입.** 같은 이메일로 다시 가입하면? 인증 계정을 지웠으면 새 사람이 되고, 안 지웠으면 **가입이 막힌다.**

**⑤ 권한: 관리자가 대신 탈퇴시키는 경로**(약관 제6조 3항 「자격 상실」)와 본인 탈퇴는 **다른 사건**이다. 같은 표에 담되 **구분**해야 감사에서 갈린다.

**⑥ 사용자 경험 — 사유를 몇 개나 묻나.** 떠나는 사람에게 긴 설문을 내밀면 **끝까지 안 한다.** 그렇다고 안 물으면 **왜 떠나는지 영영 모른다.**

**⑦ 사용자 경험 — 「대기」 상태의 회원이 로그인하면 무엇이 보이나.** 미지급 때문에 대기 중인 사람이 들어왔을 때, **평소 화면 그대로면 탈퇴가 안 된 줄 안다.**

**⑧ 사용자 경험 — 되돌릴 수 없는 동작이다.** 확인창 하나로 끝내면 **잘못 누른 사람이 계정을 잃는다.** 지금은 아무 일도 안 일어나서 문제가 없었지만, **진짜로 동작하기 시작하면 그때부터 위험하다.**

**⑨ 법률: 탈퇴일 기록은 선택이 아니다.** 방침이 그 날짜를 기준으로 파기 기한을 정하고 있다. **기록이 없으면 방침 자체를 못 지킨다.**

**⑩ 시간대.** 「탈퇴일로부터 5일」의 하루 경계는 **일본 시각** 기준이어야 한다.

---

## 3. 확정된 결정

| # | 결정 | 근거 |
|---|---|---|
| **1** | **받을 돈이 남으면 지급이 끝난 뒤 탈퇴된다** — 신청은 받되 「대기」 상태 | 2026-08-18 사용자 결정. 돈 문제가 안 생기고, 회원도 때가 되면 자동으로 빠진다 |
| **2** | **앱 안에서 완결한다. LINE으로 내보내지 않는다** | 2026-05-21 사용자 명시 방침(§1-2) |
| **3** | **탈퇴 사유를 받는다** | 사용자 요청. 안 받으면 왜 떠나는지 영영 모른다 |
| **4** | **관리자가 볼 화면을 만든다** | 사용자 요청. 지금은 기록이 없어 볼 수도 없다 |
| **5** | **약관·방침을 실제 동작에 맞춘다** | 지금은 지킬 수 없는 약속을 하고 있다(§1-3) |
| **6** | **재가입은 6개월 막는다** — 이메일은 **해시로만** 보관 | 위반 이력을 지우려는 우회 차단. 원문을 안 남겨 유출 위험도 없다(§5 ④) |
| **7** | **홍보만 끊고 정산·진행 안내는 유지** | 「대기」는 돈을 받아야 끝나는 구조라 지급 안내가 가야 한다(§5 ①) |
| **8** | **파기까지 이번에 만든다** — 단 **행 삭제가 아니라 칸 비우기** | 사용자 결정(§5 ③). ⚠️ 정산 감사 이력 때문에 **행은 지울 수 없다**(§4-7) |
| **9** | **응모건 밖 일반 문의 창구를 함께 만든다** — **사양서는 별도** | 사용자 결정(§5 ⑥). 탈퇴보다 큰 주제라 문서를 나눈다 |

---

## 4. 설계

### 4-1. 상태 — 탈퇴는 「즉시」가 아니라 「단계」다

```
신청  →  [대기]  →  [예정]  →  [완료]
              ↑         ↑
        미지급 있음   5일 유예
```

| 상태 | 뜻 | 회원 화면 | 로그인 |
|---|---|---|---|
| **대기**(`pending_payout`) | 받을 돈이 남았다 | 「지급이 끝나면 안내드립니다」 ⚠️ **예정일을 띄우지 않는다 — 아직 모른다** | ✅ 된다 |
| **예정**(`scheduled`) | 5일 유예 중 | 「◯월 ◯일에 탈퇴 처리됩니다 · **탈퇴 취소**」 | ✅ 된다 |
| **완료**(`done`) | **로그인 차단 · 파기 대상이 됨**(⚠️ **파기 실행은 다음 단계** — §4-6·§5 ③) | 로그인 불가 | ❌ |

⚠️ **「대기」와 「예정」에서 로그인이 되는 것은 의도다** — 방침이 「변심에 의한 탈퇴 취소」를 전제하므로 **취소할 수단이 있어야** 한다(경우의 수 ②). 대신 **화면 맨 위에 상태를 크게 띄워** 「탈퇴가 안 된 줄」 알게 되는 것을 막는다(경우의 수 ⑦).

⚠️ **「대기」에는 유예를 걸지 않는다** — 지급이 끝나는 시점이 언제일지 모르므로, 지급 완료 시점에 **「예정」으로 옮기고 거기서 5일을 센다.**

**★ 상태를 옮기는 주체 — 매일 도는 예약 실행 하나가 둘 다 한다**(§4-6 ④)

| 언제 | 무엇을 보고 | 어디로 |
|---|---|---|
| 매일 1회 | **대기** 중인데 **미지급 0건**이 됐나 | → **예정**(그때 예정일 = **오늘 + 5일**을 채운다) |
| 매일 1회 | **예정**인데 **예정일이 지났나** | → **완료** |

⚠️ **이 전이 장치가 없으면 「대기」·「예정」에서 영영 안 나간다.** 신청 함수는 신청할 때 한 번만 돌므로, **나중에 일어나는 일**을 할 자리가 따로 있어야 한다.
⚠️ **로그인 판정도 함께 건다**(§4-6 ⑤) — 예약 실행이 안 돌아도 **「예정일이 지났으면 로그인을 막는다」**가 별도로 판정한다. 이 저장소가 마감 강제에서 쓴 것과 같은 이중 방어다.

**★ 「탈퇴일」의 정의 — 방침이 말하는 그 날짜**

| 날짜 | 뜻 |
|---|---|
| **신청일** | 회원이 버튼을 누른 날 |
| **탈퇴 확정일**(= 「완료」가 된 날) | **로그인이 막히고 파기 대상이 되는 날** |

⚠️ **방침의 「탈퇴일로부터 5일」은 그대로 쓸 수 없다.** 대기가 길어지면 신청일로부터 5일이 아니라 **몇 달 뒤**가 되기 때문이다. **5일은 「예정 → 완료」 사이의 취소 유예**이고, **파기 기준일은 확정일**이다. **방침 문구를 이 구조에 맞춰 고친다**(§4-5).
⚠️ **모든 날짜 계산은 일본 시각 기준**이다(경우의 수 ⑩). 하루 경계가 시간대에 따라 흔들리면 유예가 4일이 되거나 6일이 된다.

### 4-2. 진행 중 응모 — 「전부 아니면 전무」로 짜지 않는다

약관 제6조 2항의 「자동 철회」를 **할 수 있는 것만 하고, 못 하는 것은 남긴다.**

| 응모 상태 | 처리 |
|---|---|
| 심사중 · 승인(결과물 없음) | **자동 철회**(`cancel_application` 재사용) |
| **결과물이 승인됨** | ⚠️ **철회 안 함** — 취소 함수가 차단한다(§1-5 ㄴ). 이미 브랜드에 전달된 성과물이다 |
| **송금완료 정산이 있음** | ⚠️ **철회 안 함** — 서버가 차단한다(§1-5 ㄱ) |
| **구매·방문 기간이 지남** | ✅ **철회하되 사유·동의를 함께 넘긴다** — `cancel_application` 이 그때 사유·동의를 강제하므로(§1-5 ㄴ), **탈퇴 사유를 그대로 넘기고 동의는 「탈퇴 신청으로 갈음」**한다. ⚠️ **이걸 안 정하면 세 번째 경로로 「한 건이 막혀 전체가 멈추는」 상황이 되살아난다** |

⚠️ **철회 못 한 건이 있어도 탈퇴는 진행한다**(경우의 수 ①). 한 건이 막혔다고 전체가 멈추면 **회원이 영영 못 나간다.** 대신 **관리자 화면에 「철회 못 한 건 N개」를 표시**하고, **약관을 이 동작에 맞춰 고친다**(§4-5).

### 4-3. 사유 — 한 번만 묻고, 고르게 한다

- **기준 데이터로 관리**한다(`lookup_values` 새 종류 하나 — 취소 사유와 같은 방식). 항목은 운영팀이 화면에서 늘린다
- **선택 1개 + 자유 입력(선택)**. ⚠️ **여러 개 고르게 하거나 긴 설문으로 만들지 않는다** — 떠나는 사람은 끝까지 안 한다(경우의 수 ⑥)
- **자유 입력은 필수가 아니다.** 「기타」를 고른 경우에만 권한다

### 4-4. 화면

**회원 쪽 — 확인창 하나를 화면 하나로 바꾼다**

```
탈퇴 안내

 · 취소할 수 있는 응모 2건이 취소됩니다
   (이미 결과물이 승인됐거나 보수를 받은 응모는 그대로 남습니다)
 · 받지 못한 보수 2건(¥6,500)이 있어, 지급이 끝난 뒤 처리됩니다
 · 지급이 끝나면 탈퇴 예정일을 알려드리고,
   그날까지 5일간 취소할 수 있습니다
 · 세금 신고 때문에 지급 기록은 5년간 보관됩니다

 떠나시는 이유를 알려 주세요
  ( ) 캠페인이 잘 안 맞아서    ( ) 응모해도 잘 안 돼서
  ( ) 앱이 불편해서            ( ) 기타 [        ]

              [ 취소 ]  [ 탈퇴하기 ]
```

⚠️ **되돌릴 수 없는 동작이므로 확인창 하나로 끝내지 않는다**(경우의 수 ⑧). **무엇이 사라지는지 먼저 보여준 뒤** 누르게 한다.
⚠️ **LINE 안내 문구를 지운다**(결정 2).
⚠️ **★ 그런데 「문의는 어디로」가 아직 안 풀린다.** §1-2 가 적었듯 **응모를 한 번도 안 한 회원은 창구가 없고**, 「응모건 밖 일반 문의 창구」는 **미착수**다(§1-6). 이번 범위에서 할 수 있는 것은 **자주 묻는 질문에 탈퇴 항목을 넣어 「무엇이 어떻게 되는지」를 스스로 확인하게** 하는 것까지다(§4-6 ⑥) — **사람에게 물을 길을 만드는 것은 아니다.**
→ **응모가 있는 회원**은 기존 응모건 메시지로 물을 수 있다. **응모가 없는 회원**은 여전히 길이 없다 → **§5 ⑥ 으로 확인받는다.**
⚠️ 인플루언서 화면이므로 **일본어**이고 **초등학생 눈높이**로 쓴다.
⚠️ **★ 화면 문구가 실제로 일어나는 일만 말해야 한다.** 이 사양서가 고치려는 문제가 바로 「접수했습니다」라고 말하고 아무 일도 안 하는 것이었다 — **같은 실수를 여기서 되풀이하면 안 된다.**
- 「**자동으로 취소됩니다**」 ❌ → 「**취소할 수 있는 응모가** 취소됩니다」 (§4-2 의 예외를 화면도 담는다 — 약관에만 넣으면 회원은 못 본다)
- 「**정보가 지워집니다**」 ❌ → **이번 배포에서는 안 지워진다**(파기 실행이 범위 밖 — §4-6). **취소할 수 있다는 것까지만** 말한다
- ⚠️ **★ 「5일」 안내는 미지급 유무로 갈린다.** 위 예시는 **미지급이 있는 회원**이라 확정까지가 5일이 아니라 **「지급 완료 + 5일」**이다. **미지급이 없는 회원**에게만 「5일간 취소할 수 있습니다」를 그대로 띄우고, **있는 회원**에게는 「**지급이 끝나면 예정일을 알려드리고 그날까지 5일**」로 쓴다. **한 문구로 둘 다 덮으려 하면 한쪽이 거짓말이 된다**
⚠️ **약관·방침·화면 문구 세 곳이 같은 예외를 함께 담아야 짝이 맞는다.**

**관리자 쪽 — 두 자리에 붙인다**

| 자리 | 무엇 |
|---|---|
| **인플루언서 상세 모달 「상태 관리」** | 탈퇴 상태·신청일·사유·예정일. 기존 패널에 한 줄 추가 |
| **인플루언서 목록 상태 필터** | 「탈퇴 신청」으로 걸러 보기 + 목록에 배지 |

⚠️ **전용 페인을 새로 만들지 않는다** — 인플루언서 관리에 이미 상태 관리·필터·이력이 다 있다. 거기 얹는 편이 운영자가 찾기 쉽다.

### 4-5. 약관·방침 정합 (C)

| 문서 | 지금 | 고칠 방향 |
|---|---|---|
| **약관 제6조 2항** | 「진행 중 캠페인 신청은 **자동으로 철회**」 | 「**철회 가능한 신청은** 자동 철회됩니다. **결과물이 승인되었거나 보수 지급이 완료된 신청은 철회되지 않습니다**」 |
| **약관 제6조 1항** | 「언제든지 탈퇴 신청」 | 유지 + 「**미지급 보수가 있으면 지급 완료 후 처리**됩니다」 |
| **방침 보유기간** | 「**탈퇴일로부터 5일** 경과 후 지체 없이 파기(변심에 의한 탈퇴 취소 대비)」 | **고친다** → 「**탈퇴 신청 후, 미지급 보수가 있으면 지급이 끝난 뒤에 탈퇴 예정일을 안내합니다. 예정일까지 5일간 취소할 수 있으며, 탈퇴가 확정되면 지체 없이 파기합니다**」
⚠️ **★ 「신청 후 5일」로 쓰면 안 된다** — 그러면 바로 옆 경고가 지적한 결함(대기 때문에 몇 달 뒤가 됨)을 **똑같이 갖는다.** 5일은 **「예정 → 완료」 구간**이고, **미지급 대기는 그 앞에 붙는다**(§4-1). **방침이 그 앞 구간도 말해야** 회원이 오해하지 않는다 ⚠️ **「탈퇴일로부터 5일」을 그대로 두면 안 된다** — 미지급 대기가 길어지면 신청일로부터 몇 달 뒤가 되어 **방침과 실제가 또 어긋난다**(§4-1 「탈퇴일의 정의」) |

| **방침 수집 표** | (없음) | **추가한다** → 「**부정 재가입 방지**: 탈퇴한 이메일의 **해시값**을 **6개월** 보관 후 파기. 원문은 보관하지 않음」 ⚠️ **「탈퇴 시 파기」의 예외가 하나 생기는 것이므로 반드시 명시**한다(§5 ④) |

⚠️ **★ 「지킬 수 있다」와 「지키게 된다」는 다르다.** 날짜가 기록돼도 **지우는 장치가 없으면** 파기 기한은 여전히 안 지켜진다. **파기 실행은 이번 범위 밖**(§4-6·§5 ③)이므로, 이 사양서로 할 수 있는 말은 **「다음 단계에서 집행할 근거가 마련된다」**까지다. **「방침 준수 완료」로 보고하면 안 된다.**

| **방침 파기 범위** | 프로필·주소·신청 기록 「**탈퇴 시 파기**」 | **고친다** → 「**개인을 알아볼 수 있는 정보는 파기하고, 거래 기록(응모·정산)은 개인을 알아볼 수 없는 상태로 보관합니다**」 ⚠️ **응모 기록은 구조상 파기할 수 없다**(§4-7) — 그대로 두면 또 지킬 수 없는 약속이 된다 |

⚠️ **회원에게 불리한 변경인가** — 제6조 2항은 「자동 철회」 범위가 **좁아지고**, 파기 범위도 「파기」에서 「익명 보관」으로 **좁아진다.** 둘 다 **불리한 변경으로 본다**(보수적 판정). **사전 통지 30일** 대상이다(`.claude/rules/policy.md`).
⚠️ **시행일 규칙**(`.claude/rules/release-timing.md`) — **문서는 미리 고치지 않는다. 기능 배포에 맞춘다.** 코드는 미리 배포해도 되고, **집행은 시행일부터**.

### 4-7. 파기 — ⚠️ 「지우기」가 아니라 「비우기」다

> §5 ③ 이 「함께 만든다」로 확정되면서 이 절이 생겼다. **조사 결과 방침을 한 번 더 고쳐야 한다.**

#### ★ 회원 행을 통째로 지울 수 없다 (2026-08-18 구조 실측)

```
influencers 행 삭제
   → settlements 가 CASCADE 로 함께 삭제 시도
      → settlement_events 가 ON DELETE RESTRICT 로 막음  (금전 감사 보호)
         → 삭제 자체가 실패
```

**정산 기록이 하나라도 있는 회원은 행을 지울 수 없다.** 마이그레이션 217 이 「`settlements` 를 하드 삭제하는 경로를 두지 않는다」고 못 박았고, 그 위에 감사 이력이 얹혀 있다.

⚠️ **응모도 마찬가지다.** `applications` 를 지우면 `settlements.application_id` 가 CASCADE 로 따라가고 같은 벽에 부딪힌다.

**→ 파기는 「행 삭제」가 아니라 「개인정보 칸 비우기(익명화)」다.**

| 무엇 | 확정 시점 | 방식 |
|---|---|---|
| 이름(한자·가나) · 이메일 · 전화 · 주소 · 우편번호 · SNS 계정 · LINE | **확정 즉시** | **칸을 비운다** |
| **PayPal 이메일** | **5년 뒤**(세법) | 그때 비운다 — `influencers` 와 **`settlements.paypal_email` 두 곳** ⚠️ 한쪽만 지우면 남는다 |
| **이메일 해시**(재가입 차단용) | **6개월 뒤** | §5 ④ |
| 응모 · 결과물 · 정산 · 메시지 | **안 지운다** | 위 제약 때문. **사람을 특정할 수 없게 된 상태로 남는다** |

⚠️ **결과물 이미지(영수증·인증샷)는 별도 판단이 필요하다** — 개인정보가 찍혀 있을 수 있는데 저장소 파일이라 칸 비우기로 안 지워진다 → **§5 ⑦**.

#### ⚠️ 방침을 한 번 더 고쳐야 한다

현재 방침은 프로필·주소·신청 기록을 **「탈퇴 시 파기」**라고 한다. **응모 기록은 위 제약 때문에 파기할 수 없다.** 그대로 두면 **또 지킬 수 없는 약속**이 된다 — 이 사양서가 고치려는 바로 그 문제다.

**고칠 방향**: 「**개인을 알아볼 수 있는 정보는 파기하고, 거래 기록(응모·정산)은 개인을 알아볼 수 없는 상태로 보관합니다**」

⚠️ **이건 「파기한다」에서 「보관한다」로 바뀌는 것이라 회원에게 불리한 변경**이다 — §4-5 의 통지 대상에 함께 넣는다.

### 4-6. 데이터베이스

> **마이그레이션 5개 + 화면 작업**(번호는 만드는 시점에 확정 — **미리 박지 않는다**)
> **상대 순서**: **① 먼저**(나머지가 그 표를 쓴다) → **③(사유 시드)은 회원 화면보다 먼저** → ②·④·⑤·⑦ 은 서로 제약 없음
> ⚠️ **③ 을 뒤로 미루면 안 된다** — 사유가 기준 데이터인데(§4-3) 시드가 없으면 **고를 항목이 0개**여서, **사유 필수인 신청이 아무도 성공하지 못한다.**

| # | 무엇 | 왜 |
|---|---|---|
| **①** | **탈퇴 신청 표** 신설 — 회원 · 상태 · 사유 · 자유입력 · **신청 시각** · **예정일** · **확정 시각** · **누가 신청했나(본인/관리자)** · 처리자 | 별도 표로 두는 이유: 한 사람이 **탈퇴했다 재가입 후 또 탈퇴**할 수 있어 이력이 여러 줄이 된다. 인플루언서 표에 칸으로 두면 마지막 것만 남는다.<br>⚠️ **「누가 신청했나」 칸이 필요하다** — 본인 탈퇴와 **관리자에 의한 자격 상실**(약관 제6조 3항)은 **다른 사건**이라, 같은 표에 담되 **구분돼야 감사에서 갈린다**(경우의 수 ⑤).<br>⚠️ **예정일은 「예정」이 되는 그 자리에서 채운다** — **미지급이 없으면 신청 함수가**(신청일 + 5일), **있으면 예약 실행이**(지급 완료일 + 5일) 채운다. ⚠️ **「신청 시점에는 모른다」는 미지급 대기를 거치는 경우에만 맞다.** 이걸 헷갈리면 **예정일이 빈 채로 「예정」이 되어 예약 실행이 영영 완료로 못 옮긴다** |
| **②** | **신청 함수** — 본인만 · 사유 필수 · **감사용 계정 제외** · 미지급 있으면 「대기」·**없으면 「예정」(그 자리에서 예정일 = 오늘 + 5일을 채운다)** · **철회 가능한 응모만 취소** | 부분 실패를 허용해야 하므로(§4-2) 함수 안에서 건별로 처리하고 **결과를 돌려준다**.<br>⚠️ **감사용 계정(`is_audit=true`)은 거부한다** — 운영팀 공용 계정이라 탈퇴 대상이 아니다(§1-5 ㅁ). **이 조건이 없으면 운영팀이 실수로 공용 계정을 없앨 수 있다** |
| **③** | **사유 기준 데이터** 시드 | 운영팀이 화면에서 늘릴 수 있게 |
| **④** | **★ 상태 전이 예약 실행** — 매일 1회 (대기→예정 · 예정→완료) | **이게 없으면 「대기」·「예정」에서 영영 안 나간다**(§4-1). 이 저장소에 이미 같은 방식의 예약 실행이 여럿 있다 |
| **⑤** | **로그인 차단** — 「완료」이거나 「예정일이 지났으면」 거부 | ⚠️ **표시만 바꾸면 그 사람은 여전히 로그인해 응모할 수 있다**(§1-5 ㄹ). ⚠️ **예정일 판정을 함께 거는 것은 이중 방어** — 예약 실행이 안 돌아도 막힌다 |
| **⑥** | **자주 묻는 질문에 탈퇴 항목** 추가 | 지금 **탈퇴 항목이 0개**다(§1-1). 무엇이 어떻게 되는지 스스로 확인할 자리 |
| **⑦** | **★ 탈퇴 취소** — 함수 + 회원 화면 버튼 | **5일 유예의 존재 이유가 취소다.** 화면(§4-4)·방침(§4-5)·상태표(§4-1) **세 곳이 「취소할 수 있다」고 약속**하는데 실행하는 자리가 없으면 **지키지 못할 약속이 하나 더 느는 것**이다 — 이 사양서가 고치려는 바로 그 유형.<br>⚠️ **「대기」·「예정」에서만 취소된다.** 「완료」는 되돌리지 않는다(그때부터 파기 대상이다).<br>⚠️ **취소하면 자동 철회된 응모는 되살아나지 않는다** — 응모 취소는 별개 사건이고 캠페인이 이미 마감됐을 수 있다. **회원에게 이 점을 미리 알린다**(§4-4 안내에 한 줄) |
| **⑨** | **파기 실행** — 확정 시 개인정보 칸 비우기 · 5년 뒤 PayPal 비우기(**두 곳**) · 6개월 뒤 해시 삭제 | §5 ③ 확정. ⚠️ **행을 지우지 않는다**(§4-7 — 지우면 감사 이력이 막아 실패한다). ⚠️ **되돌릴 수 없다** — 개발서버에서 먼저 돌리고, **몇 건이 대상인지 먼저 세어 본 뒤** 실행한다. ⚠️ **시점이 셋(즉시·5년·6개월)이라 예약 실행에서 각각 판정**한다 |
| **⑧** | **재가입 차단** — 탈퇴 확정 시 **이메일 해시 보관**(6개월) · 가입 시 대조 · 6개월 경과분 해시 자동 삭제 | §5 ④ 확정. ⚠️ **원문이 아니라 해시**(`admin_password_reset_requests` 선례). ⚠️ **6개월 경과분을 지우는 예약 실행이 함께 필요하다** — 안 지우면 「6개월 뒤 허용」이 영영 안 온다. ⚠️ 이 저장소에 **보관 기간 자동 삭제 예약 실행 선례**가 여럿 있다(위반 기록 3년·비밀번호 찾기 30일) |

⚠️ **모든 날짜 판정은 일본 시각 기준**이다(경우의 수 ⑩) — 유예 5일의 하루 경계가 시간대에 따라 흔들리면 안 된다.
⚠️ **알림·메일 차단**(§5 ①)은 어느 쪽으로 정하든 **대상을 고르는 조회에 탈퇴 상태 조건을 더하는** 일이다 — 정해지면 그 자리를 함께 고친다(§1-5 ㄷ).

⚠️ **인증 계정을 지우는 것은 이 범위에 넣지 않는다**(§5 ②). **「완료」로 옮기고 로그인을 막는 것**까지가 이번 범위이고, **그 전이 장치(④)와 차단(⑤)이 작업 목록에 들어 있다** — 안 넣으면 「완료」에 도달할 방법이 없어 **로그인 차단이 통째로 빠진 채 배포된다.**
⚠️ **파기 실행(5일 뒤 자동 삭제)도 이번 범위 밖**(§5 ③) — 무엇을 언제 지울지가 항목마다 달라(경우의 수 ③) 별도 설계가 필요하다.

---

## 5. 사용자 확인 필요

**① 알림·메일을 언제부터 끊는가?** → ✅ **확정(2026-08-18): 신청 즉시 홍보성만 끊고, 정산·진행 안내는 유지.**

| 신청 즉시 **끊는 것** | 계속 **보내는 것** |
|---|---|
| 캠페인 홍보 메일(주 2회) · 새 모집 알림 | 보수 지급 안내 · **탈퇴 진행 안내**(예정일 확정 등) · 남은 응모의 결과물 검수 결과 |

⚠️ **정산 안내를 끊으면 안 되는 이유** — 「대기」 상태는 **돈을 받아야 끝나는** 구조라, 지급 안내를 못 받으면 **몇 달을 기다리는 사람이 연락을 못 받는다.**
⚠️ **구현은 「대상을 고르는 조회에 탈퇴 상태 조건을 더하는」 일**이다(§1-5 ㄷ). 홍보 메일·모집 알림 쪽만 조건을 걸고, 정산·검수 쪽은 **건드리지 않는다.**
⚠️ **탈퇴를 취소하면 홍보도 되살아나야 한다**(§4-6 ⑦) — 조건이 「탈퇴 상태」를 보므로, 취소로 상태가 풀리면 자동으로 복귀한다. **별도 처리를 만들지 않는다.**

**② 「완료」 상태에서 인증 계정을 지우는가?**
- (가) **안 지우고 로그인만 막는다** ← 기획 권고. **되돌릴 수 있고**, 재가입 시 같은 이메일 충돌이 없다
- (나) 지운다 — 되돌릴 수 없고, 정산 기록이 남아야 하는 것과 부딪힌다(§1-5 ㅂ)
⚠️ **(가)를 전제로 §4-6 ⑤(로그인 차단)를 이미 작업으로 넣었다.** **(나)를 고르면 ⑤ 를 다시 짜야 한다** — 계정을 지우면 차단할 대상 자체가 없어지기 때문이다. 그래서 이 항목은 **사실상 (가)로 기울어 있다.**

**③ 파기 실행을 이번에 함께 만드는가?** → ✅ **확정(2026-08-18 사용자 결정): 함께 만든다.**
⚠️ 기획 권고는 「분리」였으나 사용자가 함께 진행을 택했다. **우려는 기록해 둔다** — 파기는 **되돌릴 수 없고**, 항목마다 시점이 다르며(즉시/5년/6개월), 아래 §4-7 이 드러낸 구조 제약 때문에 **방침을 한 번 더 고쳐야 한다.**

**④ 재가입을 허용하는가?** → ✅ **확정(2026-08-18 사용자 결정): 탈퇴 후 6개월간 막고, 그 뒤 허용.**

| | |
|---|---|
| **막는 기간** | **6개월** — 위반 이력을 지우려는 우회는 막고, 진짜 돌아오려는 사람은 너무 오래 안 막는다 |
| **어떻게 아나** | 탈퇴 시 **이메일을 해시로만** 보관하고, 가입 때 그 해시와 대조 |
| **6개월 뒤** | **해시도 함께 지운다** → 새 사람으로 가입 |

⚠️ **★ 이메일 원문을 보관하지 않는다.** 이 저장소에 선례가 있다 — 관리자 비밀번호 찾기 기록(`admin_password_reset_requests`)이 **`email_hash`(sha256)만** 저장한다. 이유도 같다: 「요청만 하면 행이 쌓이는 경로라 유출 시 이메일 명단이 되면 안 된다」.
✅ **해시면 「이 이메일이 탈퇴한 것인지」만 알 수 있고 「누구였는지」는 알 수 없다.** 재가입 차단에는 그것으로 충분하다.
⚠️ **그래서 위반 이력을 이어붙이는 것은 안 된다** — 6개월 뒤 재가입하면 **새 사람**이다. 이번 결정은 **우회를 늦추는 것**이지 영구 차단이 아니다.
⚠️ **방침에 이 보관을 명시해야 한다** — 「탈퇴 시 파기」의 예외가 하나 생기는 것이다(§4-5).
⚠️ **가입 화면 문구는 언제부터 되는지 알려준다** — 「이 이메일로는 아직 가입할 수 없습니다. **2027년 2월 18일부터** 가능합니다」. 이유를 안 밝히면 회원은 고장으로 안다.

**⑦ 결과물 이미지(영수증·인증샷)를 파기하는가?** — §4-7 에서 새로 드러났다.
개인정보가 찍혀 있을 수 있는데(영수증에 이름·주소, 인증샷에 얼굴) **저장소 파일이라 칸 비우기로는 안 지워진다.**
- (가) **함께 지운다** — 방침에 맞다. ⚠️ **브랜드가 이미 받아 간 것은 못 지운다.** 그리고 **결과물 승인 이력이 가리키는 파일이 사라져** 나중에 분쟁 시 근거가 없어진다
- (나) **안 지운다** — ⚠️ 개인정보가 저장소에 남는다. **방침에 그 사실을 적어야** 한다
- (다) **얼굴·이름이 찍힌 것만 골라 지운다** — ⚠️ 자동 판정이 불가능하다. 사람이 하나씩 봐야 한다

⚠️ **이 저장소에 비슷한 미결이 이미 있다** — 위반 증빙 파일이 「지울 경로를 쌓아 두기만 하고 실제로 지우는 화면이 없는」 상태다(마이그레이션 325). **같은 형태로 미루면 또 쌓이기만 한다.**

**⑤ 약관 개정 시행일** — 사전 통지 30일이 필요하다(§4-5). 기능 배포와 시행일을 언제로 맞출지.


**⑥ 응모가 한 번도 없는 회원의 문의 경로를 이번에 만들 것인가?** → ✅ **확정(2026-08-18 사용자 결정): 함께 만든다.**
⚠️ **다만 사양서는 나눈다.** 「응모건 밖 일반 문의 창구」는 **탈퇴보다 큰 주제**이고 데이터 구조를 새로 정해야 한다(메모리에 「미확정」으로 남아 있다). **별도 사양서로 쓰되 같은 시점에 배포**한다 — 한 문서에 두 주제를 담으면 나중에 어느 쪽을 고쳐야 할지 못 찾는다.
⚠️ **탈퇴 사양서는 그 창구가 「있다」를 전제로 화면 문구만 연결**한다. 창구 자체의 설계는 그 사양서가 단일 소스다.

*(참고 — 원래 선택지)* 응모가 있는 회원은 기존 응모건 메시지로 물을 수 있지만, **없는 회원은 길이 없다**(§1-2). 「응모건 밖 일반 문의 창구」는 미착수다.
- (가) **이번엔 안 만든다** ← 기획 권고. 자주 묻는 질문으로 스스로 확인하게 하고, 창구는 별도 사양으로. ⚠️ **그 회원들은 물을 데가 없는 상태가 유지된다**
- (나) 이번에 함께 만든다 — 범위가 크게 늘고, 데이터 모델을 새로 정해야 한다(메모리에 「미확정」으로 남아 있다)

⚠️ **LINE 을 남기는 안은 선택지에 두지 않는다** — §3 확정 결정 2(앱 안에서 LINE 유도 제거)를 뒤집는 것이라, 정말 다시 열 문제라면 **결정 2 자체를 재확인 대상으로 되돌려야** 한다. 확정표와 확인 항목이 서로 다른 말을 하면 안 된다.
---

## 6. 구현 결과

(개발 세션이 채울 것)
