# 최소 팔로워수 채널 묶음 판정 — 1단계 작업표

**작성일:** 2026-08-27 · **작성:** 개발 세션
**사양서:** [`2026-08-27-min-followers-channel-match.md`](2026-08-27-min-followers-channel-match.md)
**범위:** **1단계만** — 「채널 1개」·「또는」 판정 + 그 두 갈래 관리자 화면
**브랜치:** `feature/팔로워기준-1단계`

> 사양서 「확정된 결정」 1번 = **단계를 나눠 내고 1단계를 먼저 낸다**(2026-08-27 사용자).
> 2단계(「그리고」 채널별 입력)·3단계(인플루언서 표시·차단 문구)는 **이 작업표 범위 밖**이다.

---

## 착수 조건 — 통과 (2026-08-27 09:44 한국·일본 시각, 운영 실측)

사양서 「각 단계 착수 조건」을 첫 커밋 전에 돌렸다. 운영 캠페인 **195건**(보관 삭제 제외) 전수.

| 조건 | 결과 | 판정 |
|---|---|---|
| ① **채널 둘 이상 + `min_followers > 0` + 모집 중** (갈래 안 물음) | **0건** | ✅ 진행 |
| ② 「그리고」 갈래인데 **네 채널을 하나도 안 담음** (팔로워 조건 안 물음) | **5건** | ⚠️ 아래 |

**②의 5건** — `B0018-A002-C001`·`B0018-A002-C002`·`CAMP-2026-0111`·`B0003-C001`·`B0003-C002`.
전부 `qoo10,cosme` · **리뷰어형** · **전부 종료(`ended`)**.

⚠️ **②는 1단계를 막지 않는다** — 「그리고」 갈래는 2단계에서 다룬다. **2단계 착수 시 담당자에게 알릴 것**(사양서 설계 4-1: 조건을 걸 수 없는 캠페인).
⚠️ **①이 0건이라고 기존 48건까지 확인한 것이 아니다** — 그 48건이 안전한 근거는 「전부 단일 채널이라 「채널 1개」 갈래로 그대로 동작한다」이고, 새 캠페인이 생겨도 안 바뀐다(사양서가 명시).

---

## 🔴 이 작업의 가장 큰 함정 — 같은 판정이 **두 곳에 따로** 산다

판정하는 자리가 **화면(자바스크립트)** 과 **데이터베이스 함수(SQL)** 로 나뉘어 있어 **코드를 공유할 수 없다.**

| 자리 | 무엇 |
|---|---|
| `dev/js/application.js` | 응모 버튼을 눌렀을 때 |
| 마이그레이션 (베이스 **141** `_meets_min_followers`) | 홍보 메일 대상 고를 때 |

**둘이 어긋나면** — 홍보 메일은 오는데 응모는 막히거나(반대도) 한다. **어느 쪽도 오류를 내지 않는다.**

→ **작업 1에서 갈래 판정을 한 함수로 뽑고, 작업 3의 SQL 은 그 함수를 줄 단위로 옮겨 적는다.** 옮겨 적은 자리에 「이 판정은 `shared.js` 의 ○○와 같아야 한다」를 양쪽에 적는다.

---

## 작업 목록

### 작업 1 — 갈래 판정 공용 함수 (`dev/lib/shared.js`)

사양서 설계 1의 세 갈래를 **한 함수**로. 이름은 갈래를 돌려주는 것이라 판정과 분리한다.

- `campaignFollowerKind(camp)` → `'single'` | `'or'` | `'and'`
  - 채널 토큰 1개 → `'single'` (🔴 **`channel_match` 를 보지 않는다** — 사양서 설계 1)
  - 채널 2개+ 이고 `channel_match === 'and'` → `'and'`
  - 그 외 → `'or'`
- `meetsMinFollowers(camp, profile)` → 통과 여부
  - 리뷰어형이면 **항상 통과**(검사 안 함 — 종전 그대로)
  - `min_followers <= 0` 이면 통과
  - `'single'` → 그 채널 팔로워 ≥ `min_followers`
  - `'or'` → **모집 채널 중 하나라도** ≥ `min_followers`
  - `'and'` → **1단계에서는 종전 동작 유지**(기준 채널 하나) — 2단계에서 채널별로 바꾼다

⚠️ **`'and'` 를 1단계에서 안 바꾸는 이유** — 사양서 「단계 사이 어긋남」 표가 「1단계까지 「그리고」 캠페인만 아직 기준 채널 하나를 본다(종전 그대로)」로 정해 뒀다. **화면도 같은 갈래로 자른다**(작업 4).

⚠️ 채널→팔로워 칸 매핑은 **응모 판정의 기존 것을 그대로 옮긴다**: `instagram`·`x`·`tiktok`·`youtube` + **`qoo10` 은 Instagram 값**(사양서 설계 4-1). 목록에 없는 채널(LIPS·@cosme)은 **0**.

- 파일: `dev/lib/shared.js`
- 빌드: 인플루언서·관리자 **양쪽**에 들어감(공용)

---

### 작업 2 — 응모 화면 판정 교체 (`dev/js/application.js`)

현재 1158~1173줄의 「기준 채널 단일 검증」을 작업 1의 함수로 교체.

- 통과/차단 **판정만** 바꾼다
- ⚠️ **차단 문구는 이번에 안 고친다**(3단계) — 사양서가 그 어긋남을 알고 미룬 것이라 **결함으로 보고하지 말 것**
- ⚠️ 리뷰어형 건너뛰기(`recruit_type !== 'monitor'`)를 **함수 안으로 옮기되 동작은 같아야** 한다

---

### 작업 3 — 홍보 메일 판정 함수 재정의 (마이그레이션 1개)

- **베이스는 마이그레이션 141** 의 `_meets_min_followers` — 🔴 **141 이 이 함수의 유일한 정의다**(143·259·321·360 은 **호출만** 한다)
- `CREATE OR REPLACE` 로 재정의. 인자 8개 시그니처는 **그대로 둔다**(호출부 네 곳을 안 건드리기 위해)
- 리뷰어형 조기 통과·`min_followers <= 0` 통과는 **그대로**
- 갈래 판정을 작업 1과 **줄 단위로 같게** 옮긴다

🔴 **`get_promo_digest_targets`(문지기가 든 함수)는 이번에 안 건드린다** — 사양서 「확정된 결정」 3번. 그 함수의 **현재 원본은 360**이고, 141 을 베이스로 재작성하면 321(비공개 캠페인 제외)·360(탈퇴 신청자 제외)이 통째로 사라진다.

🔴 **그 함수를 나중에 고치는 날 볼 것** — 마이그레이션 **375** 가 그 함수의 실행 권한을 회수했다(회수 전에는 로그인한 회원 누구나 **다른 회원 이메일·수신거부 토큰**을 받을 수 있었다). 375 의 세 줄은 **부여 1 + 회수 2**이고 **부여가 먼저**다.
- `CREATE OR REPLACE` 는 권한을 보존한다 → 안전
- 🔴 `DROP` 후 `CREATE` 하면 **회수가 통째로 풀린다.** 오류도 표시도 없다
- 부여를 빼고 회수만 넣으면 **홍보 메일이 통째로 죽는다**(서비스 계정 권한이 공개 권한을 통해서만 왔다면 그 경로까지 끊긴다)

---

### 작업 4 — 관리자 등록·편집 화면 (`dev/js/admin.js` · `dev/admin/index.html`)

| 갈래 | 기준 채널 칸 | 최소 팔로워수 칸 안내 |
|---|---|---|
| 채널 1개 | **숨김** | 그 채널 이름을 그대로 표시 |
| **또는** | **숨김** | **「모집 채널 중 하나 이상」** |
| **그리고** | **그대로 보임**(1단계에서는 아직 쓰임 — 2단계에서 숨김) | 종전 |

🔴 **판정(작업 1·2·3)과 화면을 같은 갈래로 잘라야** 「쓰이는데 안 보이는 칸」이 안 생긴다. 사양서가 「경우의 수 2가 걱정한 것보다 이쪽이 더 나쁘다」로 못 박은 자리다.

🔴 **왜 사라지는지 한 줄 안내** — 담당자가 그 칸을 최소 팔로워수와 한 세트로 써 왔다(운영 48건 전부 채워져 있음). 안 적으면 **빠뜨린 줄 안다.**

⚠️ **칸은 지우지 않고 숨기기만** — 기존 57건의 저장값을 버리지 않는다.
⚠️ **저장 로직(`admin.js` 신규·편집)은 안 건드린다** — 리뷰어형이면 `null` 로 비우는 현재 동작 유지.
⚠️ **미리보기 객체 만들기(`admin.js` 3455줄)도 안 건드린다** — 폼을 숨기면 저절로 빈다.
⚠️ **`dev/lib/shared.js` 의 변경 이력 「대표 채널」 이름표 2곳은 지우지 말 것** — 과거 기록을 그릴 때 필요하다.

**착수할 때 판단할 것**(사양서가 열어 둠) — 리뷰어형 폼에 「이 형식은 팔로워를 보지 않습니다」 한 줄을 넣을지. 넣는다면 1단계.

---

### 작업 5 — `CLAUDE.md` 갱신 (작업 1~4와 **같은 커밋**)

- 최소 팔로워수 정책 서술을 「기준 채널 단일」 → **갈래 셋**으로 고쳐 쓴다 (🔴 **덧붙이지 말고 옛 서술을 고친다** — [`docs-tracking.md`](../../.claude/rules/docs-tracking.md) 「빼는 규칙」)
- 홍보 메일 절에 **채널 문지기** 한 줄 — 네 채널만 알아서 **Qoo10·LIPS·@cosme 만 쓰는 캠페인은 홍보 메일에서 빠진다**(결함이 아니라 결정)
- 같은 자리에 **375 함정** 한 줄 (작업 3 참조)

⚠️ **사양서가 아니라 `CLAUDE.md` 에 적는 이유** — 사양서는 이 작업이 끝나면 잘 안 열리는데 `CLAUDE.md` 는 매번 읽힌다. **문지기를 고치는 날은 이 사양서와 무관한 다른 작업에서 온다.**

---

## 검증

🔴 **판정이 두 곳이라 검증도 두 곳이다.** 한쪽만 보면 어긋남을 못 잡는다.

| # | 무엇 | 어떻게 |
|---|---|---|
| 1 | **「또는」 다채널에서 한 채널만 넘어도 통과** | 개발서버에 `instagram,x,tiktok` · `or` · 최소 1,000 캠페인을 만들고, 인스타 100·틱톡 1만인 계정으로 **실제로 응모** |
| 2 | **단일 채널은 종전 그대로** | 인스타 단일 · 최소 1,000 에서 999명 차단 / 1,000명 통과 |
| 3 | 🔴 **채널 1개인데 `channel_match='and'`** | 갈래가 `'single'` 로 가는지 — 「그리고」로 빨려 들어가면 **검사가 통째로 사라진다**(사양서 설계 1의 경고) |
| 4 | **리뷰어형은 여전히 검사 안 함** | 최소 팔로워수를 넣어도 통과 |
| 5 | **홍보 메일 판정이 응모 판정과 같은 답** | 같은 캠페인·같은 회원으로 **양쪽을 각각** 확인 — 데이터베이스 함수는 직접 호출 |
| 6 | **관리자 폼** | 채널 1개·「또는」에서 기준 채널 칸이 사라지고 **안내 한 줄이 뜨는지**. 「그리고」에서는 **그대로 보이는지** |
| 7 | **저장값 보존** | 기준 채널 값이 있는 캠페인을 편집·저장해도 **그 값이 안 지워지는지** |
| 8 | **회귀 — 기존 48건** | 단일 채널 캠페인의 응모 판정이 안 바뀌었는지 |

⚠️ **데이터베이스 함수는 적용 성공이 동작 확인이 아니다** — 신규·재정의 함수는 **한 번은 실제로 불러 본다**([`supabase.md`](../../.claude/rules/supabase.md)).
⚠️ **개발서버에서 메일을 실제로 보내지 않는다** — 판정 함수만 직접 호출해 확인한다.

---

## 순서

**작업 1 → 2 → 4 → (여기서 화면 검증) → 3 → (판정 두 곳 대조) → 5 → 커밋**

🔴 **작업 3(데이터베이스)을 화면보다 뒤에 두는 이유** — 화면 쪽에서 갈래 판정이 확정된 뒤에 SQL 로 옮겨 적어야 **옮겨 적을 원본이 흔들리지 않는다.**
🔴 **배포는 데이터베이스 먼저** — 마이그레이션을 개발서버에 적용한 뒤 코드를 머지한다.

---

## 구현 결과

**구현일:** 2026-08-27 · **개발 브랜치 반영 완료** (운영 배포 전)
**관련 병합 요청:** #1240(화면·판정) · #1241(마이그레이션·문서)
**마이그레이션 번호:** **384** — 개발 데이터베이스 적용 완료(2026-08-27, SQL 편집기)

### 작업표 대비

| 작업 | 결과 |
|---|---|
| 1 갈래 판정 공용 함수 | 완료 — `campaignFollowerKind`·`campaignChannelTokens`·`followerCountForChannel`·`meetsMinFollowers` (`dev/lib/shared.js`) |
| 2 응모 화면 판정 교체 | 완료 |
| 3 홍보 메일 판정 재정의 | 완료 — 마이그레이션 **384**(베이스 141) |
| 4 관리자 폼 | 완료 — 기준 채널 칸 숨김 + 안내(`applyFollowerKindUI`) |
| 5 `CLAUDE.md` | 완료 — 정책 갈아 끼움 + 홍보 메일 문지기 + 375 함정 |

### 🔴 초안에 없던 제약 — 2단계에서 반드시 닫을 것

**홍보 메일 판정 함수는 `channel_match` 를 인자로 받지 않는다.** 그래서 **「그리고」를 가릴 수 없고 다채널을 전부 「또는」으로 본다.**

- 작업표·사양서는 「1단계까지 「그리고」는 기준 채널 하나(종전 그대로)」로 적었는데, **홍보 메일 쪽에서는 참이 아니다.** 응모 화면만 그렇다
- 지금은 도달하지 않는다 — 운영의 「그리고」 5건이 **전부 리뷰어형**이고 리뷰어형은 함수 첫머리에서 조기 반환된다
- **2단계에서 인자를 더하거나 호출부에서 갈라야 한다.** 인자를 더하면 시그니처가 바뀌어 호출부(`get_promo_digest_targets`, **현재 원본 360**)를 함께 고쳐야 한다

### 화면에서만 드러난 결함 하나

채널을 **하나도 안 고른 상태**에서 「모집 채널이 하나(**—**)」라고 말했다. 갈래 판정은 채널 0개를 `'single'` 로 돌려주는데 그 문구는 사실이 아니다. **채널이 정해지기 전에는 아무 말도 안 하도록** 고쳤다. 코드로는 안 보이고 브라우저에서만 보였다.

### 밟은 함정

채널 체크박스 이름을 `newCampChannel` 로 짐작했으나 실제는 **`newChannel`**(`CAMP_FORM_CFG` 의 `chName`). 그대로 갔으면 **한 건도 안 걸려 늘 「채널 1개」로 보이면서 오류는 안 났을** 것이다.

### 검증 결과

**착수 조건**(2026-08-27 09:44 운영 실측) — 채널 둘 이상 + `min_followers > 0` + 모집 중 = **0건**. 진행.
⚠️ 「그리고」 5건이 **네 채널을 하나도 안 담음**(`qoo10,cosme`, 전부 종료·리뷰어형) → **2단계 착수 때 담당자에게 알릴 것.**

**화면**(개발서버 브라우저) — 채널 1개·「또는」에서 기준 채널 칸이 사라지고 안내가 뜬다. 「그리고」에서는 그대로 보인다.

**판정 — 화면과 데이터베이스 양쪽에서 같은 사례로 확인**(이번 작업의 핵심. 두 곳이 코드를 공유할 수 없다):

| 사례 | 화면 | 데이터베이스 |
|---|---|---|
| 또는 다채널 — 틱톡만 넘음(인스타 100·틱톡 1만) | 통과 | `true` |
| 또는 다채널 — 아무것도 못 넘음 | 차단 | `false` |
| 단일 999명 / 1000명 | 차단 / 통과 | `false` / `true` |
| 🔴 채널 1개인데 `and` | 「그리고」로 안 빨려 들어감 | (해당 없음 — 인자 없음) |
| 리뷰어형 | 검사 안 함 | `true` |
| Qoo10 | 인스타 값을 빌림 | `true` |
| 팔로워 없는 채널만 | — | `false` |

### 아직 안 한 것

- **운영 배포** — 개발 브랜치까지만. 운영은 마이그레이션 384 를 **운영 데이터베이스에 먼저** 적용한 뒤 머지
- **2단계·3단계** — 범위 밖
- 🔴 **3단계를 오래 미루지 말 것** — 1단계 직후부터 인플루언서 화면이 어긋난다. 판정은 「하나라도」인데 **차단 문구는 「Instagram 1,000명 필요」라 막힌 이유를 틀리게 말한다**

### 리뷰 — 예외 기록

`reverb-reviewer`·`reverb-planner`·`reverb-supabase-expert` **모두 호출하지 않았다.** 이 세션은 사용자 지시로 에이전트를 부르지 않는다. 커밋 가드가 마이그레이션 때문에 막아 **`[guard-skip: …]` 로 사유를 커밋 기록에 남겼다.** 대신 브라우저 확인과 데이터베이스 확인 조회 8줄로 대신했다.
