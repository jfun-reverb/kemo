# 마감 안내 모집 형식 분기 수정 + 리뷰어형 인증샷 안내 신설

**작성일:** 2026-08-04
**작성 세션:** 고문(메인 폴더 · dev)
**발단:** 「결과물 마감 임박 안내를 문자로 보낼 수 있나」 검토 중 운영 데이터 조사에서 발견

---

## 요약

인플루언서 일일 메일의 **결과물 마감 안내가 모집 형식을 구분하지 않아**, 리뷰어형(monitor) 당선자에게 「投稿物(게시물)을 제출하라」는 안내가 나가고 있다. 리뷰어형은 게시물을 제출하는 경로 자체가 없다. 동시에 **리뷰어형이 실제로 내야 하는 리뷰 인증샷(review_image)의 마감 임박 안내는 존재하지 않는다.**

---

## 현재 상태 (2026-08-04 기준 · 규칙 A)

### 관련 코드

| 위치 | 내용 |
|---|---|
| `supabase/functions/notify-influencer-daily-digest/index.ts:376-384` | 영수증 마감 안내 — **`recruit_type === 'monitor'` 조건 있음** (정상) |
| 같은 파일 `:386-395` | 게시물 마감 안내 — **모집 형식 조건 없음** (결함 지점) |
| 같은 파일 `:462-470` | 당선 안내(섹션 2)의 제출기한 표시 — **게시물 줄에 모집 형식 조건 없음** (결함 지점) |
| 같은 파일 `:322-334` | 제출 여부 판정 — `deliverables` 를 `status IN ('pending','approved')` 로만 조회 → 반려 건은 미제출 취급 |
| `docs/email-templates/influencer-daily-digest.row-deadline.html` | 마감 행 템플릿. `{{kind_label_jp}}` 로 종류 라벨만 바꿔 재사용 (영수증=レシート / 게시물=投稿物) |
| `dev/js/admin-deliverables.js:632-652` | `computeCertStatus` — 인증 성공 판정의 단일 소스 |

### 모집 형식별 실제 제출물 (`computeCertStatus` 기준)

| 모집 형식 | 필요한 결과물 | 현재 안내 |
|---|---|---|
| 리뷰어(monitor), 일반 | 영수증 + **캠페인 채널별 리뷰 인증샷 전부 승인** | 영수증 ✅ / 인증샷 ❌ **없음** / 게시물 ⚠️ **잘못 발송** |
| 리뷰어(monitor), 가구매(`proxy_purchase=true`) | **영수증만** (인증샷 미요구) | 영수증 ✅ / 게시물 ⚠️ **잘못 발송** |
| 시딩(gifting) | 게시물 | 게시물 ✅ 정상 |
| 방문형(visit) | 게시물 (인증 판정은 게시물 단독) | 게시물 ✅ 정상 |

### 관련 데이터베이스

- `deadline_reminder_email_sent` — 재발송 방지 표. `kind text CHECK (kind IN ('receipt','post'))`, `d_minus CHECK (d_minus IN (5,1))`, `UNIQUE (influencer_id, campaign_id, kind, d_minus)` (마이그레이션 130)
- `campaigns.proxy_purchase boolean` — 가구매 캠페인 여부 (마이그레이션 197)
- `campaigns.submission_end` — 결과물 제출 마감일. `purchase_end` 는 리뷰어형 구매 기간 종료일

### 운영 실측 (조사 결과 — **운영서버 확인 완료 2026-08-04**)

`deadline_reminder_email_sent` 중 `kind='post'` 전량 기준:

| 모집형식 | 안내 건수 | 게시물 제출 | 리뷰인증샷 제출 | 영수증 제출 | 결과물 0건 |
|---|---:|---:|---:|---:|---:|
| monitor | 1,022 | **0** | 570 | 597 | 425 |
| gifting | 578 | 295 | 0 | 0 | 283 |

- **오발송 확정 597건** — 리뷰어형 중 영수증·인증샷을 실제로 낸 사람이 게시물 독촉을 받음
- **문구 오류 425건** — 리뷰어형 미제출자. 독촉 대상은 맞으나 안내한 제출물 종류가 틀림
- 안내 건수는 D-5·D-1 두 번씩 세어진 값이라 실제 인원은 대략 절반
- 발송량 추이: 2026-05 1,223건 / 06 331건 / 07 46건 (캠페인 수 감소에 따른 것 — 5·6월 각 39·42개 → 7·8월 6·9개)
- **위 수치는 운영서버 실측이다**(2026-08-04 확인). 즉 오발송은 가정이 아니라 **실제 인플루언서에게 발생한 사실**이며, 대상은 실인원 기준 대략 500명 안팎(D-5·D-1 중복 제거 전 1,022건)

### 이 제안과 충돌 가능성 있는 기존 동작

- **충돌 없음 — 4개 영역 확인.** ①`computeCertStatus`(화면 판정)는 변경하지 않고 **기준으로 참조만** 한다 ②재발송 방지 표는 종류 값 추가만 하므로 기존 행 영향 없음 ③메일 템플릿은 라벨 치환 구조라 종류 추가에 구조 변경 불필요 ④결과물 제출 마감 차단(마이그레이션 274)은 마감일 당일까지 허용하므로 D-1 안내와 모순 없음

### 발송 예정 오발송 (운영, 2026-08-04 기준)

리뷰어형이면서 가구매가 아닌 **진행 중 캠페인 9개 · 승인 응모 145건**이 남아 있다. 모두 요구 채널이 **qoo10 단일**이다.

| 발송 예정일 | 시점 | 캠페인 | 인원 |
|---|---|---|---:|
| 8/11 | D-5 | B0029-C005 | 15 |
| 8/13 | D-5 | B0048 6건 | 105 |
| 8/15 | D-1 | B0029-C005 | 15 |
| 8/16 | D-5 | B0057-C001 · B0010-C001 | 25 |
| 8/17 | D-1 | B0048 6건 | 105 |
| 8/20 | D-1 | B0057-C001 · B0010-C001 | 25 |

**총 290건이 예정돼 있고, 가장 이른 것이 8월 11일이다.** 그 전에 단계 1을 배포하면 전량 차단된다.

### 대상 145건의 인증샷 제출 실태 (운영 실측)

| 상태 | 인원 |
|---|---:|
| 하나도 안 냄 | **93** ← 단계 2의 안내 대상 |
| 일부만 냄 | **0** (요구 채널이 1개라 구조적으로 발생 불가) |
| 전 채널 제출 | **52** ← 발송 제외 대상 |
| 반려된 채널 | 0 |

### 미해결 백로그·관련 작업

- 문자 메시지 발송 검토 — 본 수정 완료 + 2~3개월 재측정 후 판단하기로 보류
- **끝내 아무것도 제출하지 않은 비율 44%**(리뷰어형 41.6% / 시딩 49.0%) — 안내 수단과 별개의 미이행 관리 주제. 본 사양 범위 밖

---

## 의심·경우의 수 (규칙 B)

### 깨질 수 있는 경우

1. **부분 제출자 반복 독촉 (UX)** — 리뷰어형은 캠페인 채널마다 인증샷이 필요하다. 채널 3개 중 2개를 낸 사람에게 「인증샷을 제출하세요」가 D-5·D-1 두 번 더 가면, 낸 사람 입장에서는 「냈는데 왜 또 오나」가 된다.
   → **실측으로 해소.** 현재 대상 캠페인은 전부 요구 채널이 **1개(qoo10)**라 부분 제출자가 0명이다. 다만 다채널 캠페인이 생기면 즉시 되살아나는 위험이라, **판정은 채널 단위로 일반화**하고 **문구는 채널이 2개 이상일 때만 남은 채널명을 병기**하는 것으로 설계에 반영했다.

2. **이미 인증 성공한 사람에게 발송 (UX·신뢰)** — 현재 판정은 결과물 행의 **존재 여부**만 본다(승인 여부 무관). 리뷰어형에 인증샷 안내를 추가할 때 같은 방식이면, 영수증·인증샷이 전부 승인된 사람에게도 안내가 갈 수 있다. 이번 사고의 재발이다.

3. **반려 건의 문구 어색함 (UX)** — 제출 여부 조회가 `pending`·`approved` 만 보므로, 냈다가 반려된 사람은 「미제출」로 판정돼 독촉을 받는다. 재제출을 유도하는 것 자체는 맞지만 문구가 「제출하세요」라 어색하다. 반려는 이미 별도 알림·메일이 나가고 있어 **중복 안내**이기도 하다.

4. **가구매 캠페인 오발송 (기술)** — 리뷰어형이라도 `proxy_purchase=true` 면 인증샷을 요구하지 않는다. 리뷰어형 전체에 인증샷 안내를 켜면 **가구매 캠페인 당선자에게 낼 수 없는 것을 요구**하게 되어, 고치려던 문제를 다른 자리에서 반복한다.

5. **재발송 방지 표의 과거 기록 (데이터)** — 과거 리뷰어형 1,022건은 `kind='post'` 로 기록돼 있다. 수정 후 같은 사람에게 `kind='review_image'` 안내가 새로 나갈 수 있다. 다만 마감일 D-5/D-1 조건이 있어 **과거에 마감이 끝난 캠페인은 자연히 제외**되므로 실질 영향은 없다. 진행 중 캠페인에서는 「이번엔 맞는 안내」가 가는 것이므로 허용 가능.

6. **정정 안내를 보낼 것인가 (UX·판단 필요)** — 이미 잘못된 독촉을 받은 사람이 있다. 사과·정정 안내를 보내면 「몰랐던 사람까지 알게 되는」 역효과가 있고, 안 보내면 침묵이 된다. 발송 여부는 사용자 결정 사항.

7. **방문형의 영수증 (기술)** — 방문형은 화면에 영수증(현장 사진) 폼이 뜨지만 `computeCertStatus` 의 인증 판정은 **게시물 단독**이다. 마감 안내를 인증 판정 기준에 맞추면 방문형은 게시물만 안내하는 게 맞고, 현재 동작과 같다. 변경 없음.

### 사용자 의도와 다르게 해석될 수 있는 부분

- **「인증샷 마감 안내 신설」의 범위** — 「하나도 안 낸 사람만」인지 「채널이 하나라도 비면」인지에 따라 대상 규모와 반복 독촉 정도가 달라진다. → **2026-08-04 「채널이 하나라도 비면」으로 확정**(아래 「확정된 결정」 표).
- **「오발송을 고친다」의 범위** — 마감 임박(섹션 4)만인지, 당선 안내(섹션 2)의 제출기한 문구까지인지. **양쪽 모두 고치는 것을 전제로 설계**했다.

---

## 설계

### 단계 1 — 오발송 차단 (긴급, 데이터베이스 변경 없음)

**1-A. 게시물 마감 안내에 모집 형식 조건 추가**

`index.ts:386-395` 의 조건에 「시딩·방문형만」을 추가한다. 리뷰어형은 이 분기에 들어오지 않는다.

**1-B. 당선 안내(섹션 2)의 제출기한 문구 분기**

`index.ts:462-470` — 현재 `submission_end` 가 있으면 무조건 「投稿物 …まで」를 붙인다. 모집 형식에 따라 표기를 나눈다.

| 모집 형식 | 표기 |
|---|---|
| 리뷰어 일반 | 「レシート {구매기간 종료일} まで · レビュー認証写真 {제출마감일} まで」 |
| 리뷰어 가구매 | 「レシート {구매기간 종료일} まで」만 |
| 시딩·방문형 | 「投稿物 {제출마감일} まで」 |

**단계 1만으로도 잘못된 독촉은 멈춘다**(리뷰어형은 안내를 아예 못 받게 되지만, 틀린 안내를 받는 것보다 낫다). 다만 **배포는 단계 2와 함께 하기로 확정**됐다 — 단계 1을 먼저 떼어 올리는 선택지는 채택하지 않았다. 이 문단은 단계 1의 독립 효과를 설명한 것이며, 단계 2 개발이 8/11에 못 맞을 때의 **비상 분리 근거**로만 유효하다.

### 단계 2 — 리뷰어형 인증샷 마감 안내 신설

**대상 조건 (전부 만족)**
- 승인된 응모
- 캠페인이 리뷰어형(monitor) **이면서 가구매가 아님**(`proxy_purchase = false`)
- `submission_end` 기준 D-5 또는 D-1
- **인증샷 미완료 — 확정 기준: 캠페인 요구 채널 중 `pending`·`approved` 인증샷 행이 없는 채널이 하나라도 있으면 대상.** 채널 단위 판정이므로 단일 채널·다채널 모두 같은 규칙으로 동작한다. 반려(`rejected`)만 있는 채널은 「없음」으로 보아 재제출을 유도한다
- 같은 (인플루언서·캠페인·종류·D-N) 조합으로 발송한 적 없음

**데이터베이스 변경 — 마이그레이션 1개**
- `deadline_reminder_email_sent.kind` 의 CHECK 제약에 `'review_image'` 추가 (기존 값 유지, 추가만)
- 번호는 개발 세션이 파일 생성 시점에 확정

**메일 문구**
- 종류 라벨에 「レビュー認証写真」 추가 (템플릿 구조 변경 불필요 — `{{kind_label_jp}}` 치환)
- **남은 채널명은 캠페인 요구 채널이 2개 이상일 때만 병기**한다. 단일 채널(현재 대상 9개 캠페인 전부)에서는 채널명을 넣지 않는다 — 「qoo10의 인증샷을 내세요」는 낼 곳이 하나뿐인 사람에게 군더더기다

**발송 제외 가드 (경우의 수 2번 대응)**
- 영수증·인증샷이 모두 승인 상태면 발송하지 않는다. 판정은 `computeCertStatus` 의 리뷰어형 분기와 같은 기준을 서버에서 재현한다
- ⚠️ 화면 판정(`dev/js/admin-deliverables.js`)과 발송 함수는 **다른 앱**이라 코드를 공유할 수 없다. 판정 규칙이 두 곳에 존재하게 되므로, 어느 한쪽을 고칠 때 다른 쪽을 함께 검토해야 한다는 주석을 양쪽에 남긴다

### 검증

- 개발서버에서 리뷰어형 일반 / 리뷰어형 가구매 / 시딩 / 방문형 4종 캠페인에 대해 대상 판정 결과를 확인한다
- ⚠️ **실제 메일 발송 테스트는 개발서버에서 하지 않는다**(프로젝트 규칙). 대상 목록 산출까지만 확인하고, 발송 검증은 운영 적용 후 수동 호출로 한다
- 운영 적용 후 첫 실행에서 `influencer_daily_digest_runs` 로그와 `deadline_reminder_email_sent` 신규 행의 `kind` 분포를 확인한다

---

## 확정된 결정 (2026-08-04)

| 항목 | 결정 | 근거 |
|---|---|---|
| 인증샷 안내 판정 기준 | **요구 채널 중 `pending`·`approved` 행이 없는 채널이 하나라도 있으면 안내** | 현재 부분 제출자 0명이라 두 방식의 결과가 같고, 다채널 캠페인이 생겼을 때 누락을 잡아 준다 |
| 남은 채널명 표시 | **요구 채널 2개 이상일 때만 병기** | 현재 대상은 전부 단일 채널(qoo10) — 넣으면 군더더기 |
| 조사 데이터 출처 | **운영서버** | 2026-08-04 확인 |
| 배포 범위 | **단계 1·2를 함께 배포** (기한 = 8월 11일 이전) | 다음 오발송이 8/11이라 7일 여유가 있고, 함께 올리면 대상 93명이 처음으로 올바른 안내를 받는다 |
| 과거 오발송 정정 안내 | **보내지 않음** | 아래 참조 |

### 정정 안내를 보내지 않기로 한 근거 (2026-08-04)

- 정정 메일은 **몰랐던 사람에게까지 사고를 알리는** 역효과가 있다. 받은 사람 상당수는 「내야 할 게 아닌 걸 내라고 했다」는 사실을 인지하지 못한 채 지나갔다.
- 대상 597건(성실 이행자)에게 실질적 **불이익이 발생하지 않았다** — 잘못된 독촉을 받았을 뿐 제출·검수·정산 어디에도 영향이 없다.
- 8/11 전에 올바른 안내가 나가기 시작하면, 그 자체가 **정정 역할**을 한다.
- ⚠️ 단, 이 사고를 **없던 일로 두는 것이 아니다.** 문의가 들어오면 사실대로 답할 수 있도록 운영팀이 경위를 알고 있어야 한다. 관리자 공지 게시 여부는 별도 판단 사항으로 남긴다.

## 사용자 확인 필요

없음 — 2026-08-04 전부 확정. 개발 세션 착수 가능.

---

## 구현 결과

**구현일:** 2026-08-04
**관련 커밋:** 아래 파일 목록 참조(브랜치 `feature/팝업티켓`)

### 초안 대비 변경 사항
- **추가된 것:** 없음 — 「확정된 결정」 표와 「설계」 절 그대로 구현
- **빠진 것:** 없음
- **달라진 것:** 일본어 라벨 표기 1건(아래 「사양서 정정」 절) — 본문도 함께 고쳤다

### 바뀐 파일
| 파일 | 내용 |
|---|---|
| `supabase/functions/notify-influencer-daily-digest/index.ts` | 단계 1(오발송 차단)·단계 2(인증샷 안내 신설) 본체 |
| `supabase/migrations/290_deadline_reminder_review_image_kind.sql` | 재발송 방지 표의 종류 값에 `review_image` 추가 |
| `docs/email-templates/influencer-daily-digest.row-deadline.html` + `_templates/` 미러 + `templates.ts` | 주석 갱신·동기화(구조 변경 없음) |
| `dev/js/admin-deliverables.js` | `computeCertStatus` 위에 **역방향 주석** — 메일 함수가 같은 규칙을 재현 중임을 알린다 |

### 구현 중 기술 결정 사항

1. **「이미 인증 성공한 사람 제외」를 별도 가드로 만들지 않고 채널 판정 하나로 통합했다.**
   근거: `deliverables_review_image_app_channel_uniq`(마이그레이션 158)가 **응모·채널 조합당 인증샷 행을 0개 또는 1개**로 강제하고 재제출은 그 행을 갱신한다. 그래서 「그 채널에 `pending`·`approved` 행이 있는가」와 화면 판정(`_finalizeMonitorReprs` 가 보는 채널별 대표 행의 상태)이 **같은 단일 행**을 본다 — 채널이 하나라도 비면 그 자체로 인증 성공이 아니므로 전체 성공 여부를 다시 볼 필요가 없다.
   ⚠️ 이 통합은 **위 유일 제약에 기대고 있다.** 그 제약을 없애거나 재제출을 「행 추가」로 바꾸면 이 논리가 깨지므로 별도 가드를 되살려야 한다.

2. **캠페인에 요구 채널이 하나도 없으면 발송하지 않는다**(사양서에 없던 판단). 어느 채널이 비었는지 판정할 근거가 없는 상태에서 보내면 이번 사고와 같은 오발송이 된다. 현재 대상 9개 캠페인은 전부 채널이 등록돼 있어 실질 영향 0.

3. **판정 규칙이 두 곳에 존재한다**(화면 `computeCertStatus` · 메일 함수). 두 앱이라 코드를 공유할 수 없어, **양쪽에 서로를 가리키는 주석**을 남겼다(사양서 §설계 요구사항).

### 남은 위험 (배포 후 인지)

- **채널 라벨 폴백** — 요구 채널이 2개 이상일 때만 채널명을 병기하는데, 기준 데이터에서 지워진 코드가 캠페인에 남아 있으면 `レビュー認証写真（channel-96r9y3）` 처럼 **읽을 수 없는 값이 인플루언서에게 노출**될 수 있다. 현재 대상 캠페인은 전부 단일 채널이라 이 병기 자체가 발동하지 않아 지금은 위험 0이지만, 다채널 리뷰어 캠페인이 생기면 되살아난다. (같은 계열 사고: `CLAUDE.md` 「채널 코드 이관은 3곳 동시가 한 세트」)

### 배포 순서

1. **마이그레이션 290 먼저** — 종류 값이 확장되기 전에 함수가 `review_image` 로 기록을 시도하면 데이터베이스가 거부한다
2. 그다음 Edge Function 배포
3. 개발서버는 **환경만 맞추고 실제 발송은 하지 않는다**(프로젝트 규칙). 발송 검증은 운영 적용 후 수동 호출로
4. **기한: 8월 11일 전** — 그날 D-5 안내(캠페인 B0029-C005, 15명)가 첫 오발송 예정이다

---

## 사양서 정정 (2026-08-04, 개발 세션)

일본어 라벨을 **「リビュー認証写真」 → 「レビュー認証写真」** 로 고쳤다(위 본문 2곳). 이 서비스의 일본어 문구는 예외 없이 「レビュー」를 쓴다(`dev/lib/i18n/ja.js` 의 `レビュー画像`·`レビューをご投稿ください` 등, 모집 형식 라벨도 「レビュアー」). 코드베이스 전체에 「リビュー」는 이 사양서 외에 한 곳도 없었다 — 가타카나 「リ」와 「レ」를 혼동한 오타로 판단해 **실제 발송 문구는 「レビュー」로 구현**했다.
