# 「또는」 캠페인인데 채널을 전부 요구하고 있다 — 마감 메일 오발송 · 인증 성공 차단

**작성일:** 2026-09-21 · **작성:** 기획 세션 · **긴급도:** 🔴 높음(회원 CS 발생 중 · 정산 80건 누락)
**계기:** 회원 문의 — 「投稿も報告も完了していると思うのですが…提出するようにとメールが来るのはなぜでしょうか？」(게시물도 보고도 다 마쳤다고 생각하는데 제출하라는 메일이 오는 건 왜인가요?)

---

## 0. 한 줄 요약

채널을 여럿 모집하면서 **「또는」**(`channel_match='or'`)으로 낸 캠페인인데, **회원이 실제로 겪는 두 곳**(마감 안내 메일 · 인증 성공 판정)**이 그 조건을 안 보고 채널을 전부 요구한다.** ⚠️ 같은 판정 사본이 **여섯 곳**이라 나머지 넷도 지금 같은 말(미완료)을 한다(§2-②). 회원은 조건대로 하나만 올렸는데 **「미완료」로 남아** 마감 메일이 계속 가고 **인증 성공·정산이 막혀 있다.**

🔴 **회원 잘못이 아니다. 우리 결함이다.**

---

## 1. 현재 상태 (2026-09-21 운영 실측 — 규칙 A)

### 1-1. 운영 피해 — 80명

`channel_match='or'` + 채널 2개 이상 캠페인 **4건** · 승인 응모 **110건**:

🔴 **「4건」은 「승인 응모가 있는 캠페인」만 센 수다 — 조건에 맞는 캠페인 자체는 6건이다**(개발 세션 실측, 2026-09-21). 내 조회가 승인 응모를 결합해 **승인이 0인 캠페인 2건이 빠졌다.**
⚠️ **피해 수 80 은 안 바뀐다** — 피해자는 승인 응모에서만 나오므로 그 2건에는 애초에 대상이 없다. **세는 대상이 다른 것이지 수가 틀린 것이 아니다.**

| 캠페인 | 형식 | 리워드 | 채널 | 제출 마감 | 승인 | 🔴 **막힌 건** | 정산 행 |
|---|---|---|---|---|---|---|---|
| CAMP-2026-01… | 방문형 | ¥1,000 | 3 | **2026-09-18** | 39 | **30** | 0 |
| B0037-C003 | 방문형 | ¥1,000 | 3 | **2026-09-18** | 33 | **28** | 0 |
| CAMP-2026-01… | 방문형 | ¥1,000 | 3 | **2026-09-18** | 24 | **22** | 0 |
| CAMP-2026-01… | 리뷰어형 | 0 | 2 | 2026-09-18 | 14 | 0 | 0 |

🔴 **「막힌 건」의 기준은 「승인된 게시물의 채널이 1개 이상이지만 요구 채널 수보다 적다」**(제출이 아니라 **승인**). 그 수가 **80**이고, 리워드 합계 **¥80,000** 이 정산에 안 잡혀 있다.

🔴 **승인이 0인 분이 따로 있다 — 방문형 3건 96명 중 약 15명**(미제출이거나 전부 반려). **이번 수정으로 구제되지 않는다** — 판정을 고쳐도 승인된 채널이 없기 때문이다. 게다가 **제출 마감이 지나 재제출도 못 한다.** §5-3 으로 뺀다.
⚠️ 세 캠페인의 제출 마감은 **2026-09-18 로 이미 지났다** — 그대로 두면 80명이 「마감을 놓친 사람」으로 굳는다.

### 1-2. 원인 — 두 곳이 `channel_match` 를 안 본다

| 어디 | 지금 하는 일 |
|---|---|
| **일일 마감 안내 메일** `notify-influencer-daily-digest/index.ts` **944–946** (게시물) · **965** (리뷰 인증샷) | `camp.channel` 을 쉼표로 잘라 **전부** 요구. 안 낸 채널마다 D-5·D-1 안내를 보낸다 |
| **인증 성공 판정** `_settlement_cert_candidates()`(현재 원본 **331**) | 「요구한 채널 **전부** 승인」. `channel_match` 문자열이 그 파일에 **0건** |

🔴 **일일 메일은 조회에 `channel_match` 칸 자체가 없다**(759행 `select` 목록). 값을 알 수조차 없다.

### 1-3. 이미 있는 갈래 판정 — 여기에 맞춘다

최소 팔로워 쪽은 이미 같은 축을 다룬다(`campaignFollowerKind`, `dev/lib/shared.js:1029`):

```
채널 1개 이하                          → single (그 채널만. 🔴 0개면 요구할 채널이 없어 이 판정은 안 돈다 — 지금과 같다)
channel_match 가 'and'(소문자·공백제거) → and    (채널마다 전부)
그 밖 (NULL 포함)                      → or     (하나라도)
```

🔴 **결과물 판정도 글자 그대로 이 기준을 쓴다.** 새 기준을 만들지 않는다 — 두 축이 갈리면 「응모는 되는데 결과물은 안 되는」 어긋남이 생긴다.
⚠️ **기본값은 `or` 다**(「`and` 가 아니면 `or`」). 반대로 적으면 지금 정상인 캠페인이 깨진다.
⚠️ **운영에 `channel_match` 가 NULL·빈값인 캠페인은 0건이다**(2026-09-21 실측) — 그 갈래는 지금 아무 캠페인에도 안 쓰인다. 🔴 **그래도 방향을 반대로 적으면 `or` 6건이 그대로 깨진다** — NULL 갈래가 비어 있다는 것이 「틀려도 된다」는 뜻이 아니다.

### 1-4. 이 제안과 충돌 가능성 있는 기존 동작

- ⚠️ **마이그레이션 331 이 막으려던 것과 방향이 반대로 보이지만 아니다.** 331 은 「시딩·방문형이 **게시물 1건**만 보고 통과시켜, 여러 채널을 요구해 놓고 하나만 내도 전액 지급되던」 것을 막았다. 🔴 **331 은 `and`·`or` 를 구분하지 않고 전부 요구하게 만들었고, 이번 수정은 그중 `or` 만 요구에서 빼는 것**이다. `and` 캠페인에 대한 331 의 효과는 그대로 남는다
- ⚠️ 331 의 운영 실측은 「채널 2개 이상 요구한 시딩 캠페인 0건」이었다 — **그때는 `channel_match` 를 안 세었고 방문형도 안 셌다.** 지금 보니 방문형에 `or` 로 3건이 있다
- **충돌 없음 — 3개 영역 확인.** ①제출된 견적·정산 스냅샷과 무관 ②화면의 응모 자격 판정(`campaignFollowerKind`)은 이미 같은 기준이라 그쪽이 바뀌지 않는다 ③`and` 캠페인은 동작이 그대로다

---

## 2. 의심·경우의 수 (규칙 B)

### ① 🔴 조회 칸을 안 더하면 **조용히 맞는 것처럼 보이고 `and` 캠페인이 깨진다**

일일 메일 조회(759행)에 `channel_match` 가 없어 `camp.channel_match` 는 `undefined` 다. 판정만 고치면 `'and'` 가 아니므로 **전부 `or` 로 잡혀** 이번 증상은 사라진다 — **고쳐진 것처럼 보인다.**
그런데 진짜 `and` 캠페인의 마감 안내까지 「하나만 내면 끝」으로 바뀌어 **남은 채널 안내가 한 통도 안 나간다.**
→ **결정: 조회 칸(`channel_match`)을 반드시 함께 더한다.** 🔴 검증에 `and` 캠페인을 넣어 **안내가 여전히 가는지** 본다.

### ② 🔴 인증 판정을 고치면 **같은 판정이 여섯 곳**이라 한쪽만 고치면 화면과 메일이 다른 말을 한다

`CLAUDE.md` 가 이미 경고하고 있다 — 서버(331) · 검수 화면(`computeCertStatus`·`_finalizePostReprs`) · 엑셀(`_excelCertStatusKo`) · 결과물 검수 결과 메일 · 인플루언서 일일 메일 · `_campaign_cert_success_counts`(433).
→ **결정: 1단계(메일)와 2단계(판정)를 나눈다.** 1단계는 한 파일이라 오늘 나갈 수 있고, 2단계는 여섯 곳을 함께 본다.
⚠️ **1단계만 나가면 어긋남은 생긴다 — 다만 해가 없는 방향이다.** 일일 메일은 그 여섯 곳 중 하나라 1단계는 정확히 「한쪽만 고치는」 일이다. 그래도 괜찮은 이유는 **바뀌는 방향이 「메일이 덜 가는 쪽」**이고 인증·정산은 **지금도 이미 막혀 있어** 더 나빠지지 않기 때문이다.
🔴 **그 논리가 성립하려면 게시물만 고쳐야 한다** — 리뷰 인증샷까지 고치면 방향이 뒤집힌다(§3-1).

### ③ 리뷰 인증샷(`review_image`)도 같은 축인가

일일 메일 **965행**이 같은 방식으로 `camp.channel` 을 전부 요구한다. 리뷰어형 전용이고 운영 실측에서 막힌 건은 0이었지만, **그것은 게시물 기준으로 센 수**다.
→ **결정: 고치되 2단계에서 판정과 함께 고친다**(§3-1·§3-2). 🔴 **1단계에 넣지 않는다** — 안내만 멈추면 「안내는 안 오는데 인증은 막힌」 상태가 되어 게시물 쪽과 방향이 뒤집힌다.
⚠️ 「0건」은 **아직 안 센 것**이다 — 착수 시점에 **리뷰 인증샷 기준으로 다시 센다**(§4 검증 0).

### ④ 소급 — 80명을 어떻게 되살리나

2단계가 나가면 인증 성공 판정이 바뀌어 **그 80건이 저절로 「인증 성공」이 된다.** 정산은 **「과거 미등록」 화면에 나타나** 관리자가 처리할 수 있다(도입일 미설정이라 자동 등록은 여전히 0건).
⚠️ **되살아나는 순간 결과물 검수 결과 알림이 한꺼번에 나가지 않는지** 확인한다 — 331 계열 판정은 알림을 만들지 않지만, 검수 화면의 상태가 바뀌므로 관리자 일일 메일의 「조치가 필요한 캠페인」에서 빠진다(그건 정상).

### ⑤ UX — 마감이 이미 지났다

세 캠페인의 제출 마감은 **9월 18일**이다. 2단계가 늦으면 80명이 「마감을 놓친 사람」으로 남는다.
⚠️ **제출 마감 차단(274)에는 손대지 않는다** — 그 80명은 **승인된 채널이 이미 있으므로** 추가 제출이 필요 없다. 판정만 바뀌면 된다.
🔴 **승인이 0인 약 15명은 다르다** — 판정을 고쳐도 안 살아나고 마감이 지나 재제출도 못 한다(§1-1·§5-3).

### ⑥ 의도 모호점

- **회원에게 따로 알릴 것인가** — 이번 범위에서는 **안 보낸다**(사과 메일이 또 하나의 메일이 된다). CS 로 들어온 분께만 답한다. 필요하면 별도 판단
- **`and` 인데 채널이 1개**인 캠페인 — `single` 로 잡혀 그 하나만 요구한다(기존 판정과 같다)

---

## 3. 설계

### 3-1. 1단계 — 마감 안내 메일 (오늘 나갈 수 있는 것)

`supabase/functions/notify-influencer-daily-digest/index.ts`

| 자리 | 무엇 |
|---|---|
| **759행 조회** | `select` 목록에 **`channel_match` 추가**(🔴 §2-① — 이걸 빠뜨리면 `and` 가 깨진다) |
| **944–946행** 게시물 | 갈래가 `or` 면 **하나라도 냈으면 안내를 안 보낸다.** `and`·`single` 이면 지금 그대로 |
| ~~**965행** 리뷰 인증샷~~ | 🔴 **1단계에서 뺀다** — 아래 참조 |

🔴 **1단계는 게시물(944–946)만 고친다.** 리뷰 인증샷(965)을 여기서 함께 고치면 **「안내는 안 오는데 완료 판정은 여전히 막힌」** 상태가 된다 — 회원은 안내를 못 받아 아무 문제 없다고 믿는데 인증은 안 되는, **지금보다 나쁜** 모양이다. 게시물은 2단계가 판정을 함께 고치므로 그 구멍이 없다.
⚠️ **리뷰 인증샷은 2단계에서 안내와 판정을 같이** 고친다(§3-2).

**갈래 판정은 §1-3 의 세 줄을 그대로 옮긴다.** ⚠️ Edge Function 은 공유 모듈이 없어 `campaignFollowerKind` 를 부를 수 없다 — **같은 기준을 글자 그대로** 다시 쓰고, 그 자리에 **「`dev/lib/shared.js` 의 `campaignFollowerKind` 와 같은 기준이다」**를 적어 둔다(사본이라는 것을 드러낸다).

⚠️ **「하나라도 냈으면」의 「냈다」 기준은 지금 것을 그대로 쓴다** — 이 함수가 이미 `postChannels`/`reviewChannels` 집합을 만드는 방식(행의 존재). 여기서 상태 기준을 바꾸지 않는다(범위 밖이고, 바꾸면 이번 수정의 효과를 가릴 수 있다).

### 3-2. 2단계 — 인증 성공 판정

`_settlement_cert_candidates()`(베이스 **331**)의 `post_channel_cert` 를 갈래별로:

```
single·and → 지금 그대로 (요구 채널 전부 승인)
or         → 요구 채널 중 하나라도 승인이면 충족
```

🔴 **2단계에서 함께 볼 자리는 다섯이다** — 검수 화면 · 엑셀 · 결과물 검수 결과 메일 · 운영현황 집계(`_campaign_cert_success_counts`) · **일일 메일의 리뷰 인증샷(965행)**. ⚠️ 여섯 곳 중 서버(331)는 이 절이 직접 고치고 **일일 메일의 게시물 쪽은 1단계가 이미 고쳤다**. 어느 것을 고치고 어느 것을 안 고치는지 **한 자리에 목록으로 남긴다** — 안 그러면 다음 사람이 「빠진 것」과 「일부러 둔 것」을 구분 못 한다.

🔴 **리뷰어형의 리뷰 인증샷(`channel_cert`)도 같은 축으로 고친다**(1단계에서 965행을 뺀 것이 여기로 온다). ⚠️ **운영 피해가 0이라는 뜻이 아니다 — 아직 안 셌다**(§4 검증 0). 0이더라도 기준이 갈린 채로 두지 않는다.

### 3-3. 3단계 — 소급

2단계 적용 뒤 **「과거 미등록」 화면**에서 80건을 확인하고 관리자가 처리한다.
⚠️ **자동 등록은 여전히 0건**이다(정산 도입일 미설정) — 목록에 뜨는 것까지가 이 작업의 끝이다.

---

## 4. 검증

| # | 무엇 | 어떻게 |
|---|---|---|
| 0 | 🔴 **2단계 착수 전 다시 센다 — 무엇을 세는지 먼저 적는다** | **세는 단위는 「승인된 응모」**이지 캠페인이 아니다(§1-1 의 「4건 ↔ 6건」이 그 차이에서 나왔다).<br>① **막힌 건** = `channel_match='or'` + 채널 2개 이상 캠페인의 승인 응모 중 **승인된 채널이 1개 이상이고 요구 수보다 적은** 것 — 오늘 게시물 **80** · 리뷰 인증샷 **아직 안 셈**<br>② **구제 안 되는 건** = 같은 모집단에서 **승인된 채널이 0** — 오늘 약 15<br>③ **`and` + 2채널** 캠페인의 같은 수 — 2단계는 이쪽도 건드린다(운영 5건) |
| 1 | ✅ **`and` 캠페인이 안 깨졌나** | **1단계에서 확인 완료**(§6). 운영에 `and` + 2채널이 **5건 실재**했다(전부 리뷰어형 · `qoo10,cosme` · 마감 경과라 이번 수정 자리에 **안 닿는다**). 판정은 실제 파일에서 읽어 10가지 경우로 돌려 통과. ⚠️ **2단계에서 다시 본다** — 그 5건은 리뷰어형이라 **2단계가 건드리는 자리에는 닿는다** |
| 2 | `or` 캠페인 | 한 채널만 낸 승인 응모에 **마감 안내가 안 간다** |
| 3 | 채널 1개 | 동작이 **그대로**다 |
| 4 | 2단계 뒤 인증 성공 | 그 80건이 **「인증성공」**으로 바뀐다(결과물 관리 화면 인증 상태 열) |
| 5 | 2단계 뒤 | **건수를 보여주는 네 곳**(검수 화면 인증 상태 · 엑셀 · 운영현황 집계 · 관리자 일일 메일)이 **같은 수**를 말한다. ⚠️ 인플루언서 일일 메일과 검수 결과 메일은 **건수를 안 보여줘** 이 대조 대상이 아니다 — 그 둘은 검증 2·7 이 본다 |
| 6 | 정산 | **「과거 미등록」 화면에 80건이 뜬다** |
| 7 | 🔴 되살아날 때 | **회원에게 알림·메일이 한꺼번에 나가지 않는다** |

---

## 5. 확인·결정이 필요한 것

1. ✅ **1단계(메일 멈추기)부터 즉시** — 사용자 지시(2026-09-21)
2. ✅ **1단계는 게시물만** — 리뷰 인증샷은 2단계에서 안내·판정을 함께(§3-1)
3. 🔴 **승인이 0인 약 15명을 어떻게 할 것인가** — 판정을 고쳐도 안 살아나고 **마감(9/18)이 지나 재제출도 못 한다.** 개별 확인이 필요하고 **이 사양서 범위 밖**이다
4. **회원에게 일괄 안내를 보낼 것인가** — 이번 범위에서는 안 보내기로 했다(§2-⑥). CS 로 들어온 분께만 답한다
5. **소급 80건의 정산을 누가 언제 처리하나** — §3-3 은 「목록에 뜨는 것까지」가 끝이다. 그 뒤는 관리자 몫

## 6. 구현 결과

### 1단계 — 마감 안내 메일 (2026-09-21)

**고친 것:** `supabase/functions/notify-influencer-daily-digest/index.ts` **한 파일, 네 곳**

| 자리 | 무엇 |
|---|---|
| `CampRow` 타입 | `channel_match: string \| null` 추가 |
| 조회 `select`(현행 **772행**) | `channel_match` 추가 — §2-① 의 함정을 막는 자리 |
| **신설** `campaignChannelKind(camp)`(현행 **195행**) | §1-3 의 세 줄을 그대로 옮긴 사본. 그 자리에 「`dev/lib/shared.js` 의 `campaignFollowerKind` 와 같은 기준」이라고 적어 뒀다 |
| 게시물 판정(현행 **966행**) | `or` 면 하나라도 냈을 때 안내하지 않는다. `and`·`single` 은 종전 그대로 |

🔴 **리뷰 인증샷(현행 985~1010행)은 한 글자도 안 건드렸다** — §3-1 대로. 검토자가 `git diff` 에 그 블록 hunk 가 없음과 원래 로직이 그대로임을 따로 확인했다.

### 검증한 것

**판정 시험 10/10 통과** — ⚠️ **실제 파일에서 판정 두 조각(헬퍼·`missingPost` 식)을 읽어** 돌렸다(사본을 새로 쓰지 않았다). 경우: 또는 3채널 1개 냄 / 또는 0개 / **그리고 3채널 1개 냄** / 그리고 3개 / 값 없음 / **대문자 `" AND "`** / 채널 1개 안 냄·냄 / 채널 없음 게시물 있음·없음.
🔴 **셋째·여섯째가 §2-① 의 함정을 직접 본다** — 「그리고」가 여전히 안내를 보내는지.

**운영 실측(2026-09-21)** — 검증 1 의 ①

| 갈래 | 캠페인 | 그중 채널 2개 이상 |
|---|---|---|
| `or` | 247 | **6** |
| `and` | 5 | **5** |
| NULL·빈값 | **0** | — |

🔴 **`and` + 채널 2개 이상이 운영에 5건 있다** — 사양서는 「없으면 개발서버에 시험 캠페인을 만들라」고 했는데 **실제로 있었다.** 다만 **다섯 건 모두 리뷰어형(`monitor`)이고 채널이 `qoo10,cosme`, 제출 마감이 2026-06-01~07-01 로 이미 지났다** → 이번에 고친 자리(기프팅·방문형 게시물)에 **애초에 안 걸리고** D-5·D-1 대상도 아니다. **운영 영향 0.** 미래의 `and` 캠페인에 대한 보장은 위 판정 시험이 한다.

⚠️ **`channel_match` 가 NULL·빈값인 캠페인은 운영에 0건**이다 — 기본값 `or` 갈래는 지금 아무 캠페인에도 안 쓰이지만, 그 기본값을 `and` 로 적었다면 이번 6건이 그대로 깨졌다.

### 🔴 사양서 수치와 다른 것 — 다음 단계에서 다시 셀 것

§1-1 은 「`or` + 채널 2개 이상 = **4건**」인데 실측은 **6건**이다. §1-1 은 **승인 응모가 있는 캠페인만** 센 것으로 보인다(그 표에 승인 수가 붙어 있다). **2단계의 피해 규모(80명)도 같은 기준 차이를 가질 수 있어** §4 검증 0 을 다시 셀 때 기준을 명시할 것.

### ⚠️ 코드 병합만으로는 메일이 안 고쳐진다

Edge Function 은 `git` 병합으로 반영되지 않는다 — **`supabase functions deploy notify-influencer-daily-digest` 를 개발·운영 각각** 해야 한다(`.claude/rules/supabase.md`). **배포까지 확인해야 이 작업이 끝난다.**

### 아직 안 한 것

- **2단계**(인증 성공 판정 + 리뷰 인증샷 안내) — 같은 판정이 여섯 곳이라 함께 본다(§3-2)
- **3단계**(소급 80건) — 2단계 뒤
- §5-3 의 **승인 0인 약 15명** — 범위 밖

---

### 2단계 — 인증 성공 판정 (2026-09-21)

**고친 곳 일곱** — 사양서 §2-② 의 「여섯 곳」에 **공유용 서버 함수 하나가 더** 있었다.

| 조각 | 무엇 | 파일 |
|---|---|---|
| A | 서버 인증 성공 판정 | 마이그레이션 **455**(베이스 331) — `channel_cert`·`post_channel_cert` 두 CTE |
| F | 운영현황·관리자 메일 집계 | 마이그레이션 **456**(베이스 433) |
| **C-서버** | 🔴 **브랜드 공유 데이터** | 마이그레이션 **457**(베이스 449) — **작업표에 없던 것** |
| B | 검수 화면 대표 상태 | `dev/js/admin-deliverables.js` — `_finalizeMonitorReprs`·`_finalizePostReprs` |
| C | 엑셀 + **브랜드 공유 화면** | `dev/js/report-rows.js` + 호출부 `dev/js/admin-excel.js` 4곳 |
| D | 결과물 검수 결과 메일 | `notify-deliverable-decision` — 조회 칸 + 갈래 보정 |
| E | 일일 메일 리뷰 인증샷 | `notify-influencer-daily-digest` |

### 🔴 착수 전 코드 대조에서 사양서와 달랐던 것 셋

1. **엑셀 판정은 `admin-excel.js` 가 아니라 `report-rows.js` 에 있고 브랜드 공유 화면과 공용**이다(2026-09-04 에 옮겨졌다). 고치면 **브랜드가 보는 표가 함께 바뀐다** — 이 작업이 바꾸는 **유일한 외부 노출**이다.
2. **그 공유 화면에는 `shared.js` 가 없다**(빌드가 `report-rows.js` 만 인라인한다) → 갈래를 스스로 계산할 수 없어 **인자로 받아야** 했고, 그 값을 주려면 **공유용 서버 함수까지 고쳐야** 했다(457). 🔴 **그 함수는 `DROP` 하면 비로그인 실행 권한이 풀려 공유 링크가 전부 죽는다** — `CREATE OR REPLACE` 로만 했고, 449 본문과 대조해 **정확히 한 줄만 다름**을 확인했다.
3. **`computeCertStatus` 는 안 고쳐도 된다** — 채널을 세는 것은 대표 상태를 만드는 두 함수이고 그 함수는 결과만 읽는다.

### ✅ 덤으로 함께 맞아 들어간 곳

**운영현황 간트뷰의 인증성공 진행바**(`countCertSuccess`)도 같은 두 함수를 타므로 자동으로 새 규칙을 쓴다(검토에서 확인). 사양서 목록에 없던 여덟 번째 자리다.

### 🔴 운영 사전 확인 — 적용 **전** 실측 (2026-09-21, 455 의 `[V0]`)

| 형식 | 갈래 | 응모수 | 새로 성공 | **성공 상실** | 그대로 성공 | 그대로 실패 |
|---|---|---|---|---|---|---|
| 방문형 | **or** | 96 | **80** | **0** | 1 | 15 |
| 리뷰어형 | or | 14 | 0 | **0** | 7 | 7 |
| 리뷰어형 | **and** | 63 | 0 | **0** | 30 | 33 |
| 리뷰어형 | single | 1,924 | 0 | **0** | 981 | 943 |
| 시딩 | single | 499 | 0 | **0** | 207 | 292 |

- ✅ **성공을 잃는 응모 0건** — 적용 조건 충족(455 가 「1건이라도 나오면 즉시 멈추라」고 정한 칸)
- ✅ **새로 성공 = 80** — §1-1 의 수치와 **정확히 일치**
- ✅ **`and` 63건 변화 0** — 그 5건이 안 깨지는 것이 실제 데이터로 확인
- 🔴 **§2-③ 이 「아직 안 셌다」고 남긴 리뷰 인증샷 피해 = 0건**(「또는」 리뷰어형 14건 모두 변화 없음). **기준이 갈린 채로 두지 않으려고 고쳤을 뿐 소급 피해는 없었다.**

### 구현 중 결정 둘

1. **`or` 일 때 일일 메일에 채널 이름을 안 붙인다.** 지금 문구가 「レビュー認証写真（인스타그램・X）」처럼 남은 채널을 나열하는데, 하나만 내면 되는 캠페인에서 그러면 **셋 다 내라는 말**로 읽힌다. `missingChannels`·`requiredChannelCount` 를 둘 다 안 넘겨 기본 라벨로 떨어뜨렸다(채널 이름표 조회도 함께 안 돈다).
   ⚠️ **「어느 하나만 내면 된다」는 안내를 새로 넣는 것은 일본어 문구 신설이라 별도 판단으로 남긴다.**
2. **`or` 의 `cert_at`(인증성공일) = 최초로 승인된 채널의 승인 시각**(영수증이 있으면 그것과의 늦은 쪽). 「성공 조건이 **처음 참이 된 시점**」이라는 기존 정의를 그대로 연장한 것이다(`and` 는 마지막 채널 승인 시각 — 모두 모여야 참이므로).
3. **456 의 후보 조건은 433 그대로 뒀다** — 「A 와 같은 규칙」은 **갈래 판정만** 같다는 뜻이고, 그 함수는 정산이 아니라 운영현황 집계라 대상 집합이 원래 다르다(433 이 스스로 그렇게 선언하고 있다).

### 검증

**판정 시험 11/11 통과** — 실제 파일에서 판정 함수를 읽어 돌렸고(사본을 새로 쓰지 않았다), **검수 화면과 엑셀·공유 화면이 같은 답을 내는지**까지 나란히 봤다. 핵심 경우 「**또는 · 하나 승인 + 하나 반려 → 완료**」 포함(반려된 채널은 낼 의무가 없던 채널이다).
검토 **GO**(지적 0건) — 갈래 판정 사본 넷이 같은 기준인지, `and` 분기가 331·433 과 **공백까지 같은지**, 호출부 누락이 없는지 대조 완료.

### 운영 반영 (2026-09-21)

순서는 **데이터베이스 → Edge Function → 화면 코드**로 했다. 사용자가 개발서버 브랜드 공유 화면을 먼저 확인했다.

| 단계 | 결과 |
|---|---|
| 455 적용 전 `[V0]` | 방문형 「또는」에서 **새로 성공 80**, 성공을 잃는 건 **모든 행에서 0**, `and` 63건은 변화 0 — 개발 단계 실측과 같다 |
| 455 적용 → 검증 | 성공 수: 방문형 81(기존 1 + 80) · 기프팅 207 · 리뷰어형 1,018 / 공개 실행 권한 0 / 정산 도입일 `NULL` 그대로 / 무보수 시딩·방문형 섞임 0 |
| 456 적용 전 `[V0]` | 캠페인 3건(8/28 팝업 1·2·3부) 차이 28·22·30 = **80**, 음수 0 |
| 456 적용 → 검증 | 합계 81 · 실행 권한은 `postgres`·`service_role` 만(로그인·비로그인 false) |
| 457 적용 → 검증 | 비로그인 실행 **true**(공유 링크 살아 있음) · PUBLIC 없음 · 본문에 `channel_match` 포함 |
| Edge Function 둘 | `notify-deliverable-decision`·`notify-influencer-daily-digest` 운영 배포 |

⚠️ **456 의 `[V0]` 조회가 운영에서 한 번 실패했다** — `target_campaigns` 가 `proxy_purchase` 칸을 안 가져와 `column c.proxy_purchase does not exist`. 검증용 주석 안의 오타라 마이그레이션 본문과는 무관하다. 칸을 더해 고쳤다(개발에서는 이 조회를 돌리지 않아 드러나지 않았다).

### 남은 것

- **3단계(소급)** — 2단계 적용 뒤 「과거 미등록」 화면에 80건이 뜨면 관리자가 처리
- **승인 0인 약 15명** — 범위 밖(§5-3)
