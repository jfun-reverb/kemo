# 운영 오류 로그 18건 — 원인 조사와 처리 방침

**작성일:** 2026-08-31
**작성:** 기획 세션
**대상:** 운영서버(globalreverb.com) 관리자 화면 「오류 로그」 미해결 18건
**조사 시점 화면 상태:** 상태=미해결 · 종류=예상 못 한 오류만 · 앱=전체 · 기간=전체

---

## 현재 상태 (규칙 A — 직접 확인함)

### 확인 방법
- 운영 관리자 화면 `/admin#errors` 를 크롬으로 직접 열어 18건 전수 열람 (상세 모달 2건은 스택까지)
- 운영 데이터베이스(`reverb-jp-production-tokyo`) SQL 편집기에서 함수 실행 권한 직접 조회
- 저장소 코드(`dev/lib/storage.js`·`dev/js/app.js`·`dev/js/auth.js`·`dev/js/error-report.js`·`dev/lib/shared.js`)
- 운영 빌드 산출물(`origin/main:index.html`) 두 시점(현재·8/20 판)

### 관련 코드·데이터베이스 진입점
| 대상 | 위치 |
|---|---|
| 오류 수집 | `dev/js/error-report.js` — `_toMessage`(33) · `NOISE`(15~30) · `_isNoise`(84) · `collectClientError`(109) |
| 오류 기록 호출 | `dev/lib/shared.js` `logAppError`(2228) |
| 메시지 조회 | `dev/lib/storage.js` `fetchApplicationMessages`(3808) |
| 응모 수 조회 | `dev/lib/storage.js` `countActiveApplications`(912) |
| 재설정 링크 착지 | `dev/js/app.js` `handleRecoveryTokenLink`(204) |
| 재설정 재요청 | `dev/js/auth.js` `handleForgotPassword`(250) |
| 만료 안내 화면 | `dev/index.html` `#resetPwExpired`(841~851) |
| 안내 문구 | `dev/lib/i18n/ja.js` `genericError`(881) |

### 이 제안과 충돌 가능성 있는 기존 동작
- **충돌 없음 — 4개 영역 확인.** ①함수 실행 권한(369·370·375)은 이 함수들과 무관함을 파일에서 확인 ②`326`은 권한을 정상적으로 다시 부여(449~450) ③운영 데이터베이스 현재 권한 정상 ④노이즈 필터의 「2층 일반 규칙」은 설계대로 동작 중(스택 없는 건 안 버림)

---

## 조사 결과 — 18건 분류 (5 + 6 + 6 + 1 = 18)

### A. 진짜 결함 — 코드를 고쳐야 함 (5건 / 10회)

| # | 오류 | 횟수 | 최근 | 원인 |
|---|---|---|---|---|
| A1 | `permission denied for function get_application_messages` | 5 | 8/28 | **원인 확정** |
| A2 | `[object Object]` (`countActiveApplications`) | 2 | 8/21 | **원인 확정** |
| A3 | `decode_failed` (`uploadMessageAttachment`) | 1 | 8/19 | 미확인 |
| A4 | `invalid input syntax for type uuid: "demo-4"` | 1 | 8/24 | 미확인 |
| A5 | 오류 자리에 캠페인 데이터가 통째로 (`fetchCampaigns`) | 1 | 8/29 | 미확인 |

#### A1 — 메시지 화면이 세션 끊김에서 안 살아남는다 ★가장 급함
- **증상:** 아이폰 사파리 회원이 응모건 메시지 화면을 열면 거부. 8/25~8/28에 5회.
- **원인:** `storage.js:3808` 의 조회가 `retryWithRefresh` 없이 호출된다. 바로 아래 발송 함수(3819)는 감싸져 있다.
- **왜 그렇게 되나:** 운영 데이터베이스 실측상 이 함수는 로그인 회원(`authenticated`)만 실행 가능하고 비로그인(`anon`)은 거부다. 세션이 끊긴 채 호출하면 비로그인 권한으로 가서 정확히 이 메시지가 나온다.
- **같은 유형 전수 조사:** `storage.js` 안에서 원격 호출 함수를 부르는 함수 95개 중 **보호 없음 31개**. 그중 **비로그인 호출이 설계상 정상인 9개**(수신거부·오리엔시트 3종·재가입 차단 확인·행사 초대 확인·방문 기록·오류 기록·조회수)를 빼면 **판단 대상 22개**. **개발 세션이 화면별로 판정해야 한다.**
  - ⚠️ **초판은 27개라 적었다** — `async function` 선언만 세고 일반 `function` 을 놓친 계산이었다. 개발 세션 대조로 31 로 정정(2026-08-31). 「정상인 9개」 목록은 양쪽이 정확히 일치했다.
  - ⚠️ 22개 중 `markBrandAppMemosRead`·`markOrientMemosRead` 는 조회가 아니라 **쓰기**다 — 실패 방향이 다르므로 따로 판정할 것.
- ⚠️ 특히 `fetchDeliverableGate`(결과물 제출 가부 판정)는 조회 실패를 「막지 않음」으로 처리하도록 설계돼 있어 **실패해도 조용하다** — 우선 검토 대상.

#### A2 — 오류를 기록했는데 무슨 오류인지 모른다
- **원인:** `error-report.js:33` `_toMessage` 가 `message` 없는 객체를 만나면 `String(v)` → `[object Object]`.
- **왜 단순 표시 문제가 아닌가:** 무슨 오류였는지 알 수 없어 **추적이 불가능**하고, 앞으로 날 다른 오류도 같은 방식으로 사라진다.

##### ⚠️ 이 항목의 초판 결론은 틀렸다 — 개발 세션 대조로 정정 (2026-08-31)
초판은 `countActiveApplications`(storage.js:912) 주석을 근거로 「**정원 판정이 2회 틀렸을 수 있다**」고 썼다. 호출부를 **전수로 세니 4곳이고 각각 다르다**:

| 호출부 | 조회 실패(0)일 때 |
|---|---|
| `application.js:63` (상세 화면) | `if (cnt > 0) actualApplied = cnt;` — **캐시값을 그대로 유지**한다. 안 뚫린다 |
| `application.js:975` (제출 직전) | 통과시킨다. 다만 실제 삽입은 **데이터베이스 트리거가 막는다**(048, 현재 원본 179) |
| `admin.js:2776` (마감일 연장 확인창) | 「N명이 응모함」 숫자가 0으로 표시돼 **경고가 약해진다** |
| 🔴 `admin.js:2938` (민감 항목 변경 확인창) | `if (_historyAppCount >= 1)` 이라 **확인창이 아예 안 뜬다** |

🔴 **진짜 위험은 정원이 아니라 마지막 줄이다** — 신청자가 있는 캠페인의 주의사항·참여방법·NG 를 **경고 없이** 바꾸게 된다.

⚠️ **정원 쪽 반증은 이 문서 안에 이미 있었다** — 항목 D 의 「모집 정원이 마감되었습니다 (slots: 5, current: 5)」 2회가 **그 트리거가 운영에서 실제로 막고 있다는 증거**다. 문구 원본은 `048_monitor_slots_guard.sql:46`(현재 원본은 179).

### B. 안내가 막다른 길 (6건 / 36회) ★횟수가 가장 많음

| # | 오류 | 횟수 |
|---|---|---|
| B1~B3 | `Email link is invalid or has expired` | 14 + 14 + 3 = **31** |
| B4~B6 | `For security purposes, you can only request this after N seconds.` | 3 + 1 + 1 = **5** |

- **B1~B3 은 그 자체로는 정상 거부다.** 재설정 링크는 시간이 지나면 못 쓴다. 그리고 링크를 주소의 `#` 뒤에 담아 **메일 클릭 추적이 미리 태우지 못하게** 이미 조치돼 있다(`app.js:199~203` 주석 + 실제 구현 확인).
- **만료 화면도 잘 만들어져 있다.** `#resetPwExpired` 는 이유 3줄과 「もう一度メールを送る」(다시 메일 보내기) 버튼을 준다.
- 🔴 **문제는 그 버튼을 누른 다음이다.** 연타 방지에 걸리면 회원이 보는 것은 `エラーが発生しました`(오류가 발생했습니다) 하나뿐이다(`auth.js:280~282` → `ja.js:881`). **몇 초 기다리면 되는지 안 알려준다.**
- 즉 **「다시 보내세요」라고 안내해 놓고 그대로 했더니 오류만 나오는 막다른 길**이다. B4~B6 5회가 실제로 그 벽에 부딪힌 흔적이다.
- ⚠️ 문구를 일반화한 것은 **계정 존재 여부를 숨기기 위함**이고 그 취지는 옳다(`security.md` 「계정 열거 방지」). 다만 **연타 방지는 계정 존재와 무관하게 걸리므로**, 「잠시 후 다시 시도해 주세요」로 바꿔도 계정 정보가 새지 않는다.

### C. 우리 코드가 아님 — 노이즈 (6건 / 13회)

| # | 오류 | 판정 근거 |
|---|---|---|
| C1 | `Can't find variable: DarkReader` | 다크 모드 확장 프로그램 주입 |
| C2 | `Can't find variable: __gCrWeb` | 아이폰 크롬 내부 객체. **필터에 `window.__gCrWeb` 로 적혀 있어 접두어 없는 메시지를 못 잡음** |
| C3 | `Can't find variable: _AutofillCallbackHandler` | 아이폰 자동완성 |
| C4 | `undefined is not an object ('window.webkit.messageHandlers')` | 아이폰 웹뷰. **필터 목록에 없음**(`webkit-masked-url` 만 있음) |
| C5 | `Object Not Found Matching Id:1, MethodName:update, ParamCount:4` | 알려진 확장 프로그램 오류 |
| C6 | `Maximum call stack size exceeded.` (7회) | **아래 근거 3종** |

#### C6 판정 근거 — 7회나 나서 따로 검증했다
1. 스택이 `Hk` ↔ `Fk` 두 함수의 무한 상호 호출인데, **이름이 축약형**이다. 이 저장소 빌드는 단순 이어붙이기라 **축약을 하지 않는다**(`build.md`).
2. 스택이 가리킨 줄은 `index.html` 의 **187·194·195·224**. 발생 시점(8/20) 운영 빌드에서 **우리 자바스크립트는 2111번째 줄부터** 시작하고, 그 앞은 CSS다. 즉 **우리 코드일 수 없다.**
3. 브라우저가 `GSA/434.2` — **구글 앱 안의 웹뷰**다.

→ 구글 앱이 주입한 스크립트의 무한 재귀가 우리 페이지 주소로 보고된 것.
⚠️ **노이즈 필터의 「2층 일반 규칙」(스택에 우리 파일이 없으면 노이즈)이 이걸 못 잡은 이유**는, 스택 문자열에 `globalreverb.com` 이 들어 있기 때문이다. 규칙은 「우리 파일 이름이 있나」를 볼 뿐 **줄 번호가 우리 코드 영역인지는 안 본다.**

### D. 정상 거부 (1건 / 2회)
- `모집 정원이 마감되었습니다 (slots: 5, current: 5)` — 막은 것이 맞는 동작. **다만 「예상 못 한 오류」로 분류돼 목록에 남는다.**

---

## 의심·경우의 수 (규칙 B)

### 이 방향에서 깨질 수 있는 경우 5가지

1. **[기술] A1 을 고쳐도 세션 끊김 자체는 안 없어진다.** `retryWithRefresh` 는 갱신 토큰이 살아 있을 때만 산다. 갱신 토큰까지 만료됐으면 재시도도 실패하고, 그때 회원은 **여전히 오류를 본다**. → 재시도 실패 시 **로그인 화면으로 유도**하는 처리가 함께 없으면 절반만 고치는 것.
2. **[기술] 노이즈 필터에 이름을 추가하는 것은 두더지 잡기다.** 코드 주석이 이미 그렇게 경고한다. 다음 달에 다른 확장이 다른 이름으로 또 들어온다. → **줄 번호 기반 판정**(우리 자바스크립트 시작 줄보다 앞이면 우리 코드 아님)이 근본적이지만, **빌드마다 그 줄이 바뀌므로** 빌드 시점에 그 값을 심어야 한다. 난이도가 있다.
3. **[UX] B 를 고칠 때 남은 시간을 정확히 보여주면 오히려 나쁠 수 있다.** 「43초 후 다시」라고 적으면 회원이 43초를 세고 있다가 눌러야 한다. → **「1분쯤 뒤에 다시 눌러 주세요」** 정도로 뭉뚱그리고, 가능하면 **버튼을 그 시간 동안 비활성**으로 두는 편이 낫다. 문구만 고치면 회원은 여전히 연타한다.
4. ~~**[UX] 정상 거부를 목록에서 치우면 「진짜 조용해진 것」과 구분이 안 된다.**~~ — 🔴 **이 우려는 틀렸다(2026-08-31 정정).** `report_client_error`(현재 원본 **308**)는 `status='open'` 인 행만 찾아 횟수를 올리고, **없으면 새로 넣는다**(`status` 미지정 → `DEFAULT 'open'`). 유일 제약이 `(fingerprint, status)` 라 **무시 처리한 행과 충돌하지 않는다.** 즉 **무시해도 재발하면 새 행으로 목록에 다시 뜬다** — 「하루 500회 나면 안 보인다」는 일이 일어나지 않는다.
   → 그래도 **「예상된 거부」로 분류하는 편(`APP_ERROR_EXPECTED_PATTERNS`)이 여전히 낫다.** 무시는 사람이 매번 손으로 눌러야 하지만 분류는 자동이기 때문이다. 다만 그건 **편의 문제이지 안전 문제가 아니다.**
5. **[데이터] A2 를 고쳐도 이미 난 2회의 원인은 영영 모른다.** 기록에 남은 것이 `[object Object]` 뿐이라 복구 불가. 고치는 것은 **앞으로만** 유효하다.
   - ⚠️ 초판은 「해당 시각 캠페인 응모 수를 대조하라」고 했으나, 위 A2 정정으로 **정원 쪽은 대조할 이유가 없어졌다**(트리거가 막고 화면은 캐시를 유지한다). 대신 확인할 것이 있다면 **그 시각 민감 항목 변경 이력**(`campaign_change_history`)이다 — 확인창이 안 뜬 채 저장된 건이 있는지. 다만 그 2회는 **관리자 화면이 아니라 인플루언서 앱에서 난 오류**라(앱 구분 = 인플루언서) `admin.js` 경로일 수 없다. **대조 불필요.**

### 사용자 의도와 다르게 읽힐 수 있는 부분
- **「처리」가 두 가지다.** ①원인을 고치는 것 ②관리자 화면에서 「해결됨/무시」를 눌러 목록을 비우는 것. 이 사양서는 ①을 다루고, ②는 아래 「사용자 확인 필요」에 따로 뒀다.
- **「18건」은 오류 종류 수이지 발생 횟수가 아니다.** 실제 발생은 **61회**이고, 그중 **36회(59%)가 비밀번호 재설정 한 갈래**다.

---

## 제안 — 고치는 순서

| 순서 | 무엇 | 왜 이 순서 | 담당 |
|---|---|---|---|
| 1 | **A1** 메시지 조회에 세션 갱신 재시도 추가 + 재시도도 실패하면 로그인 유도 | 지금도 회원이 못 여는 유일한 건. 원인 확정됨 | 개발 |
| 2 | **B** 연타 방지에 걸렸을 때 안내 문구 분리 + 버튼 일시 비활성 | 발생 횟수의 59%. 문구 변경이라 위험 낮음 | 개발 |
| 3 | **A2** `_toMessage` 가 `message` 없는 객체도 풀어내게 | 이걸 고쳐야 나머지 원인 미확인 건들이 다음에 보인다 | 개발 |
| 3-B | `admin.js:2938` 민감 항목 확인창이 조회 실패(0)에 조용히 안 뜨는 것 | A2 대조에서 새로 드러남. 조회 실패와 「0명」을 구분해야 한다 | 개발 |
| 4 | **C** 노이즈 필터 보강 | 목록이 조용해야 진짜 결함이 눈에 띈다 | 개발 |
| 5 | **A3·A4·A5** 원인 미확인 3건 | 재현이 필요해 시간이 든다 | 개발 |
| 6 | **D** 정상 거부를 「예상된 거부」로 분류 | 급하지 않음 | 개발 |

### 데이터베이스 변경
**없음.** 여섯 항목 모두 화면 코드(`dev/`)만 건드린다.

---

## 사용자 확인 필요

1. ✅ **목록 「무시」 처리 — 2026-08-31 완료.** 사용자 승인 후 **C 6건 + D 1건 = 7건**을 무시로 바꿨다(각 건에 근거 메모 + 이 사양서 항목 번호 기재). 운영 목록 **18건 → 11건**.
   - ⚠️ 초판의 「경우의 수」 4번이 D 를 두고 「지우지 말라」고 경고했는데 D 를 지웠다 — **문서가 자기 자신과 모순**이었다(개발 세션 규칙 C 점검에서 발견). 위 4번을 정정했다: **그 경고의 전제가 틀렸으므로 되돌리지 않는다.**
2. **「못 읽는 표」 경고 해제** — 별개 항목. `_cert_at_backup_20260819`(마이그레이션 342 백업 표)를 코드가 읽는 자리가 **0곳**임을 점검 스크립트로 확인했다. `--record` 로 그 결과를 남기면 경고가 사라지는데, **운영 데이터베이스에 기록**하는 동작이라 승인이 필요하다.
3. **Supabase 이미지 변환 한도 초과** — 아래 「별건」 참조. 조사까지 마쳤고 **결정만 남았다.**

---

## 별건 — Supabase 이미지 변환 한도 초과 (조사 완료, 결정 대기)

조사 중 운영 프로젝트에 `EXCEEDING USAGE LIMITS` 딱지가 떠 있어 함께 봤다. **오류 로그와 무관한 별개 건**이다.

### 실측
| 항목 | 사용량 | 비율 |
|---|---|---|
| **Storage Image Transformations** | **1,683 / 100** | **1,683%** |
| Egress | 24.73 / 250 GB | 10% |
| Storage Size | 1.556 / 100 GB | 2% |
| 월간 활성 사용자 | 282 / 100,000 | <1% |
| Edge Function 호출 | 1,695 / 2,000,000 | <1% |

**초과한 것은 이미지 변환 하나뿐**이고 나머지는 전부 여유다.

### 지금 막혀 있나 — **아니다** (운영 화면에서 직접 확인)
캠페인 목록의 Supabase 이미지 **168개가 전부 변환 주소로 정상 표시**됐다. 원본 폴백 0개·깨진 이미지 0개.
⚠️ 다만 화면 안내가 「지출 한도를 끄면 제한 없이 계속 쓸 수 있다」이고 초과분이 과금되지 않는 상태라, **언제 차단돼도 이상하지 않다.** 차단되면 원본이 그대로 나가 **모바일 로딩이 느려지고 전송량이 는다**(회원 대부분이 휴대폰).

### 왜 1,683개인가 — 낭비가 아니다
캠페인 목록 **한 화면**의 실측:
- 변환 요청 168건 → **고유 원본 이미지 103개**
- 요청한 가로 크기 **480 한 종류뿐**
- 저장통 `campaign-images` 하나

🔴 **목록을 한 번 여는 것만으로 고유 이미지 103개**를 쓴다. 월 한도가 100개다. **한 사람이 한 번만 열어도 그달 한도를 넘는다.**

⚠️ **줄일 여지가 거의 없다.** 크기가 이미 한 종류라 종류를 줄여 아낄 게 없고, 중복 요청 65건은 같은 원본이라 **과금에 영향이 없다**(Supabase 는 변환 횟수가 아니라 **원본 이미지 수**로 센다).

### 선택지 (사용자 결정)
| | 내용 | 비용·영향 |
|---|---|---|
| ① 지출 한도 해제 | 초과분을 과금받고 계속 사용 | 초과 1,583개 ≈ **월 $8 안팎**(1,000개당 $5 기준). 코드 변경 없음 |
| ② 업로드 시점에 썸네일을 미리 만들어 저장 | 변환 기능을 안 쓰고 작은 사본을 함께 저장 | 변환 과금 **0**. 저장 용량은 여유(2%). **개발 작업 필요** — 업로드 경로 변경 + 기존 이미지 일괄 처리 |
| ③ 그대로 둠 | 차단되면 원본 폴백으로 동작 | 화면은 나오지만 **느려지고 전송량 증가** |

⚠️ ②가 근본 해법이지만 **이 사양서 범위 밖**이다. 하기로 하면 별도 사양서가 필요하다.

---

## 이 사양서의 한계 (밝혀 둔다)

- ✅ **규칙 C 1회차 완료 (2026-08-31, 개발 세션 `reverb-jp-e6`).** 이 세션은 요청 없이 서브 에이전트를 부르지 않는다는 지시를 받아 자가 재독만 했고, **다른 눈은 개발 세션이 맡았다.** 설계 축 지적 **3건 + 숫자 1건**이 나왔고 **네 건 모두 지적이 옳아** 위와 같이 고쳤다(A2 결론 교체 · 27→31 · 경우의 수 4·5 정정 · 확인 필요 1번 갱신 · 제안표 3-B 신설).
  - 🔴 **가장 무거웠던 것은 A2** — 코드에서 성립하지 않는 결론이었고, **반증이 같은 문서 안(항목 D)에 있었다.**
  - ⚠️ **규칙 C 는 지적 0건이 나올 때까지 돌린다.** 이번 회차에서 바꾼 곳은 위 여섯 군데다 — **2회차는 그 여섯 군데를 지목해** 봐 주길 요청한다.
- **A3·A4·A5 는 원인 미확인이다.** 각 1회씩이라 재현 정보가 부족하다. 추측으로 채우지 않았다.

---

## 구현 결과

(개발 세션이 채울 것)
