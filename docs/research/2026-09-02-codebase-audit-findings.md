# 전수 조사 결과 (2차) — 구현된 기능의 논리 결함 + 문서·규칙 층

**조사일:** 2026-09-02
**요청:** 「우리 사이트 전수조사 한번 더 하자」
**직전 조사:** `docs/research/2026-08-07-codebase-audit-findings.md`(94건). 그 94건은 묶음 A·C·D·E·F·G·I 가 조치 완료됐고, 남은 것은 **의도적 보류 2건**(영수증 비공개 = 브랜드 포털 만들 때 · 정산 H-2)뿐이다. 그래서 이번은 **새 조사**다.
**대상 구간:** 2026-08-07 이후 — **752커밋 · 마이그레이션 85개**(310~395)
**조사 기준:** 사양서가 아니라 **실제 코드·데이터베이스가 무엇을 하는지**. 문서가 모순이어도 구현이 맞으면 문제가 아니고, 문서가 깨끗해도 구현이 틀렸으면 문제다.
**방법:** 8개 영역을 나눠 병렬 조사. 각 조사에 「추측 금지 · `파일:줄` 근거 필수 · 이미 `CLAUDE.md` 에 경고로 적힌 것 제외 · 의도된 동작 제외 · **「없다」를 쓸 때 찾은 범위를 함께 적을 것** · 애매하면 「의심 — 실측 필요」로 분류」를 걸었다.

---

## 총계

| 영역 | 높음 | 중간 | 낮음 | 계 |
|---|---|---|---|---|
| 메일·일괄발송·팔로워 기준 | 7 | 6 | 9 | 22 |
| 관리자 화면(8/07 이후) | 0 | 12 | 13 | 25 |
| **문서·규칙 층** | **6** | 2 | 3 | 11 |
| 행사·응모 상태 전이 | 1 | 6 | 4 | 11 |
| 이미지·썸네일·파기 | 1 | 5 | 4 | 10 |
| 정산·금액 | 1 | 4 | 5 | 10 |
| 회원 탈퇴 | 1 | 3 | 5 | 9 |
| 가입·인증·권한 | 1 | 3 | 2 | 6 |
| **합계** | **18** | **41** | **45** | **104** |

⚠️ **겹침 — `get_event_roster` 한 결함이 세 영역에 각각 세어져 있다.** 두 조사가 **독립적으로** 찾았고(행사 쪽이 「캠페인 번호가 규칙적이라 순회 가능」을 추가로 짚었다), 그 뒤 문서 층에서도 「목록에 없다」로 다시 걸렸다. 지금 표에 남은 자리는 셋 — **행사 높음 1 · 가입·권한 9-1(중간) · 문서 6-6(높음)**. 🔴 **「실질 고유 N」이라는 숫자는 두지 않는다.** 겹침을 하나 찾을 때마다 그 수가 바뀌고, 그 수가 다른 자리에 적혀 있으면 **바뀔 때마다 틀린다**(정합성 점검 3회차에서 실제로 그렇게 됐다). **겹침은 아래 목록으로만 관리하고, 조치 단위는 계획 문서의 조각 표가 정의처다.**

### 알려진 겹침 (조각으로는 하나인 것)

| 겹치는 자리 | 조각 |
|---|---|
| §1-2 = 행사 높음 1 · 9-1(중간) · 6-6(높음) | **A-2** (+ 문서 쪽 H-6) |
| §1-1 = 4-4(방침 통지 이스케이프) | **A-1** — 계획이 「호출자 검증 + 이스케이프」를 한 조각으로 묶는다 |
| 2-4(중간) = 5-16(낮음) — 「정산대기 추가」 순서 | **B-4** |
| 5-4(중간) = 7-7~7-10 중 「원본 폴백 재귀 방지」(낮음) — **근거 `파일:줄` 이 글자 그대로 같다** | **E-7~E-10 쪽 하나** (F 에서는 세지 않는다) |
| 6-9(낮음) = 7-6(중간) — **같은 사양서의 같은 줄**(`…image-thumbnail-two-copies.md:397`) | **E-6** (H-9 는 그것을 가리키기만 한다) |

⚠️ **같은 결함인데 자리마다 심각도가 다른 것이 다섯 중 넷이다.** 우선순위를 매길 때는 **높은 쪽**을 따른다.

⚠️ **위 표는 「영역별로 센 것」이라 재게가 포함돼 있다.** 조치 계획은 **조각 기준**(재게를 한 번만 센다)이므로 두 문서의 숫자가 다른 것이 정상이다.

### 이번 조사에서 확인된 공통 성질

1. **직전 조사의 결론이 그대로 반복된다** — 「잘못된 것들이 오류를 내지 않고 **조용히** 일어난다.」 실패한 파일 삭제도, 잘린 1,000행도, 반쪽만 회수된 메시지도, 열린 안전 게이트도 전부 정상 응답을 돌려준다.
2. 🔴 **문서·규칙 층이 높음 비율 1위**(11건 중 6건). 직전 조사는 코드만 봤다. **이번에 새로 넣은 축이고, 가장 무거운 것이 여기서 나왔다.**
3. **같은 판정이 여러 곳에 사는 문제가 여전히 최다** — 화면↔서버, 건수 집계용↔표시용, 화면↔엑셀↔메일.
4. **「나중에 생긴 칸이 옛 결정을 그대로 통과」하는 유형이 새로 보인다** — 정산 보류 해제(224)가 「금전 칸 보존」을 정할 때 실제 송금액 칸은 존재하지도 않았고(338 신설), 338이 그 칸을 만들 때 224를 다시 안 봤다.

---

# 1. 보안 (즉시 조치 — 사용자 승인 후 개발 세션 착수)

## 1-1. 방침 개정 통지 메일 함수를 공개 키로 아무나 부를 수 있다

- **근거**: `supabase/functions/notify-policy-change/index.ts` — 호출자 검증 0건 · `escapeHtml` 0건 · `admins` 조회 0건
- **양성 대조**(같은 방법으로 다른 함수는 걸린다):

| 함수 | 공개키 거부 | admins 조회 | 이스케이프 |
|---|---|---|---|
| **notify-policy-change** | **0** | **0** | **0** |
| notify-brand-application | 4 | 2 | 13 |
| notify-withdrawal-scheduled | 4 | 0 | 3 |
| notify-admin-invite | 0 | 5 | 8 |
| notify-orient-sheet | 0 | 3 | 4 |

- 🔴 **운영에 ACTIVE 로 배포돼 있다** — `supabase functions list --project-ref nrwtujmlbktxjgdwlpjj` 확인(2026-06-22 · 버전 4)
- **발현**: 사이트에 박힌 공개 키로 호출 → **전체 회원에게 실제 메일 발송**. 발송 자물쇠(`policy_notice_runs.notice_key`)가 **요청 본문 값**으로 열려 값을 바꿔 가며 반복 가능. 이스케이프가 없어 시행일 값에 **임의 HTML 삽입**
- **화면 진입점 0건**(`dev/` 전수, 양성 대조로 `notify-admin-invite` 는 3건) — 손으로 부르는 용도라 아무도 안 보고 있었다
- ⚠️ **이 함수를 호출하지 말 것** — 확인하는 행위가 곧 전 회원 발송이다. **읽기 전용 조회는 허용**(그게 없으면 「선점 먼저, 발송 그다음」의 조치 범위를 못 정한다)
- **고치는 법이 이미 저장소 안에 있다** — `notify-brand-application` 의 `rejectPublicKeyCaller` 를 옮기고 발송 값마다 `escapeHtml`

## 1-2. 열쇠말 하나로 모든 캠페인의 승인자 명단이 비로그인에 열린다

- **근거**: `supabase/migrations/392_event_roster.sql:78`(`grant execute … to anon, authenticated`), `:66-75`(조회 조건에 **행사 여부 없음** — `campaign_no` 만 맞으면 통과), `:23-27`(방어 = 함수에 박힌 sha256 해시 하나)
- **발현**: 캠페인 번호가 규칙적(`B{4자리}-A{3자리}-C{3자리}`)이라 **순회 가능**. 승인자 이름(한자·가나)·대표 채널·4채널 계정·팔로워가 나온다. **요청 제한·시도 기록·만료 전부 없다**
- **대조**: 같은 저장소의 다른 비로그인 창구(관리자 비밀번호 찾기, 마이그레이션 243)는 정확히 이 이유로 **3층 요청 제한**을 건다. 다른 익명 함수는 전부 **행마다 다른 토큰**을 데이터베이스에서 대조한다 — 이 함수만 **전역 고정 비밀**
- 🔴 **`CLAUDE.md` 「함수 실행 권한」 절에 이 함수가 없다.** 그 절은 비로그인 노출 함수를 세는 **유일한 목록**이고 「아직 안 닫은 것 하나」라 적혀 있어, 다음 점검이 하나만 보고 끝낸다
- **두 조사가 독립 발견**(가입·권한 / 행사)

## 1-3. 회원이 자기 계정의 「감사용」 표시를 켤 수 있다

- **근거**: `supabase/migrations/313_influencers_update_allow_narrow.sql:66-71` — `USING (auth.uid()=id) WITH CHECK (auth.uid()=id)`, **칸 제한 없음** / `059_influencer_verify_blacklist.sql` 의 보호 칸 목록에 `is_audit` **0건** / `179_audit_influencer_account.sql:118-126` — 그 칸이 켜져 있으면 정원 검사를 **맨 앞에서 통과** / 쓰기 경로가 표를 직접 고침 `dev/lib/storage.js:894`
- **발현**: 개발자 도구로 자기 행의 그 칸을 켜면 ①정원이 찬 리뷰어 캠페인에 응모 통과 ②`applied_count`·대시보드·운영현황·엑셀·정산 자동 등록에서 **통째로 빠짐** ③🔴 관리자가 「감사용 흔적 청소」(`purge_audit_data_all`)를 누르면 **그 회원의 진짜 응모·결과물·메시지·알림이 지워진다**
- **전례**: 059가 인증·블랙리스트 8칸을 같은 방식으로 지킨다. 한 칸 더하는 형태

---

# 2. 돈 (정산)

## 2-1. [높음] 보류 해제하면 옛 송금액이 남아 그 금액대로 다시 보낸다

- **근거**: `223_settlement_hold_cancel_fns.sql:90-94`·`:152-156`, `224_settlement_revert_fn.sql:86-90` — 셋 다 `status`·`memo`·`version` 만 바꾸고 송금 기록 3칸을 그대로 둔다 / `dev/lib/shared.js:2337-2341` — 합계 함수가 `paid_amount_jpy` 를 **우선** / `dev/js/admin-settlements.js:1757`·`1775` — 지급 준비 화면의 금액이 그 값
- **원인**: 🔴 224가 「금전 칸 보존」을 정할 때 **실제 송금액 칸은 존재하지도 않았다**(338에서 신설). 338이 그 칸을 만들 때 224를 다시 안 봤다
- **발현**: 계산액 ¥3,200 을 실수로 ¥1,000 만 보내 기록 → 보류(환수) → 사유 해소 후 보류 해제. 이제 `status='pending'` · `amount_jpy=3200` · `paid_amount_jpy=1000`. 지급 준비 화면 소계가 **¥1,000** 으로 잡히고 **그게 곧 페이팔에 치는 숫자다** → **¥2,200 과소 송금**. 화면상 「정산대기」인데 「송금완료일」에 날짜가 찍혀 이미 보낸 것으로 읽힌다
- ⚠️ **단건과 일괄이 다르게 동작한다** — 단건(`343:181`)은 무조건 덮어쓰고, 일괄(`343:297-310`)은 안 건드린다. **같은 행이 어느 버튼을 누르느냐에 따라 다른 금액으로 기록된다**

## 2-2. [중간] 검색으로 화면에서 사라진 선택이 그대로 송금 확정된다

- **근거**: `dev/js/admin-settlements.js:2325-2329`(검색이 선택을 안 비움) / `:2516-2521`(요약은 **걸러진** 목록에서만 셈) / `:2245-2251`(실제 처리는 **전체**에서 고름)
- **대조**: 같은 파일 `:529-530` 은 정산 목록에서 「화면에서 사라진 선택은 버린다」를 명시적으로 하고, 미등록도 `:1755-1758` 에서 비운다. **지급 준비 화면만 이 방어가 없다**
- **발현**: A·B·C 선택 후 검색 → 상단은 「2건 ¥5,000」인데 확인 창은 「3명 7건 ¥21,000」 → B·C까지 **되돌릴 수 없는** 송금완료로 확정

## 2-3. [중간] 「보수 지급」 메일이 지급이 영원히 없는 캠페인에도 나간다

- **근거**: `supabase/functions/notify-deliverable-decision/index.ts:322-326`·`:381-384`·`:427-430` — 세 곳 모두 「担当者の最終確認のうえ、報酬のお支払いを進めます」(담당자 최종 확인 후 보수를 지급하겠습니다) / 후보 제외 `331:164-170`(시딩·방문형인데 `reward`≤0 이면 **통째로 제외**), `:264-274`(리뷰어형인데 `product_price`≤0 이면 `amount_issue`)
- **발현**: 무보수 시딩(제품 제공만) 캠페인의 게시물이 승인될 때마다 「보수를 지급하겠다」는 메일이 나간다. **지급은 없다.** 문의가 오면 운영팀은 정산 화면에서 그 사람을 **찾을 수조차 없다**(미등록 목록에도 안 뜬다)
- **대조**: 같은 파일 `:361-366` 은 「채널이 없는 옛 캠페인」에는 **일부러 완료를 단정하지 않는다.** 같은 원칙이 「금액이 없는 캠페인」에는 적용되지 않았다

## 2-4~2-10 (중간 2 · 낮음 5)

| # | 내용 | 근거 |
|---|---|---|
| 2-4 | 「정산대기 추가」가 정산 행을 **먼저 송금완료로** 처리한다(현재 진입점이 하나라 잠복) | `admin-settlements.js:1046-1056`(무조건) → `:1057-1077`(모드 검사가 뒤) |
| 2-5 | 일괄 등록에서 후보에 없는 응모가 **아무 사유 없이 사라진다** — 고른 건수와 대조하지 않음 | `339:229-246`(targets 밖은 어느 카운트에도 없음) / `admin-settlements.js:1090-1093` |
| 2-6 | `recruit_type` 이 세 값 밖이면 서버는 인증 성공, 화면은 절대 성공으로 안 봄. **CHECK 제약이 없다** | `002_sync_schema.sql:58` / `331:305-330` / `admin-deliverables.js:710-713` |
| 2-7 | `fetchDeliverablesByCampaign` 에 1,000행 대응 없음(같은 파일 다른 함수는 있음) | `dev/lib/storage.js:1230-1245` ↔ `:1052-1055` |
| 2-8 | 「금액 확인 필요」 배너가 **일어날 수 없는 경우**를 안내(기프팅·방문형은 후보에서 빠져 이 목록에 안 뜸) | `admin-settlements.js:308` ↔ `331:264-274` |
| 2-9 | `fetchPastUnregisteredSettlements` 가 `!db` 일 때 **「실패=null, 0건=[]」 계약을 어긴다**(주석이 「[] 로 바꾸지 말 것」이라 적음) | `dev/lib/storage.js:5216` ↔ `:5221-5226` |
| 2-10 | `backToPayoutSummary` 가 검색 토큰을 안 비워 「대상 없음」 대신 「찾는 중…」이 남음 | `admin-settlements.js:2304-2309` |

---

# 3. 개인정보 (회원 탈퇴)

## 3-1. [높음] 탈퇴해도 응모 표의 실명·이메일·주소가 남는다

- **근거**: `supabase/migrations/352_purge_withdrawn_personal_data.sql` — 비우는 표가 `influencers`·`withdrawal_requests` **둘뿐**이고 `applications` 참조 **0건**(전수) / 값을 넣는 곳 `dev/js/application.js:1033-1038`(`user_email`·`user_name`·`user_ig`·`address`) / 사양서 약속 `docs/specs/2026-08-18-member-withdrawal.md:502`(「사람을 특정할 수 없게 된 상태로 남는다」)
- 🔴 **352 파일은 「일부러 안 비우는 것」 목록을 따로 적어 뒀는데 응모 표는 그 목록에도 없다** — 결정이 아니라 **누락**이다
- **발현**: 확정 후에도 관리자 신청 관리·신청자 엑셀·진행현황이 **탈퇴자의 실명과 배송지를 계속 보여준다.** 방침 문구와 실제 동작이 어긋난다
- ⚠️ **지금 피해 0건**(확정 탈퇴가 아직 없음). **시행하는 순간 발생한다**

## 3-2~3-9 (중간 3 · 낮음 5)

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 3-2 | 중간 | 행사 예약 자동 취소가 `cancel_application` 보호를 우회 → **정산대기가 보류로 바뀌어 회원이 「지급 대기」에 영구히 갇힌다** | `350:509-518`(paid 만 검사), `:541-552`(직접 UPDATE) / `320:155-158` / `350:308-320` |
| 3-3 | 중간 | 예정일 안내 메일이 **영구 유실 가능**하고 그 실패가 어느 화면에도 안 뜬다(정산 알림을 없앤 뒤 **유일한 통지**) | `notify-withdrawal-scheduled/index.ts:179-181` / `350:285-288`(칸 주석이 「작업 14가 보여줄 수 있다」인데 안 만들어짐) / `372:131-141` |
| 3-4 | 중간 | 관리자를 겸한 회원의 탈퇴 카드가 **진행 중 신청·되돌리기 버튼을 통째로 감춘다** — 「탈퇴 처리 점검」 경고가 그 회원을 지목하는데 열면 아무것도 안 보임 | `dev/js/admin-influencers.js:803-809`(조기 반환) ↔ `:812-846` |
| 3-5 | 낮음 | 행사 예약 취소가 세운 통과 표시를 **끄지 않는다**(짝이 되는 함수는 끈다) | `350:526` ↔ `356:223`·`:249` |
| 3-6 | 낮음 | 확정된 회원이 새 탈퇴 신청을 또 만들 수 있다(활성 행만 검사) | `357:139-155` |
| 3-7 | 낮음 | 확정 회원의 **결과물 제출을 막는 장치가 없다** — 359의 「안 막는 것」 목록에도 언급 없음(검토 흔적 없음) | `359:127-129,171-173,257-259`, `:29-34` |
| 3-8 | 낮음 | 밀린 파기 건수 조회 함수가 **호출부 0곳**이고 메시지 첨부 몫도 안 셈 | `dev/lib/storage.js:3755-3781` |
| 3-9 | 낮음 | 359 헤더·작업표·`CLAUDE.md` 가 「352가 생년월일을 비운다」고 적었으나 **352는 일부러 안 비운다**(근거가 사실과 반대) | `359:67-68` ↔ `352:166-171` |

---

# 4. 메일·일괄발송·팔로워 기준

## 4-1. [높음] 「또는」 채널 조건이 절반만 구현됐다 — 팔로워는 하나만, 계정은 전부

- **근거**: 계정 검사가 먼저 끊는다 `dev/js/application.js:1160-1178` ↔ 팔로워 판정 `:1187` / 화면 문구는 「모집 채널 중 하나가 N명 이상」 `:1541` / 홍보 메일 문지기는 **또는** `387:371-374`·`:416-419`
- **발현**: 채널 2개 이상 캠페인에서 인스타만 가진 회원이 문지기·팔로워 판정을 **통과해 메일 수신** → 눌러 들어가면 「TikTok ID 미등록」으로 차단. 384가 고치려던 「자격 있는데 막힌다」가 **계정 관문에서 그대로 재현**
- ⚠️ **기획 결정 필요** — 계정도 「하나면 된다」로 풀 것인가, 화면 문구를 「모든 채널 계정 필요」로 바로잡을 것인가

## 4-2. [높음] 대상 1,200명 넘는 날 1,000명 이상이 메일을 못 받는다

- **근거**: 응답이 1,000행에 잘린다는 전제 `notify-campaign-promo-digest/index.ts:1093-1099` / 정체 감지가 **잘린 값**을 그대로 비교 `:1134`·`:1406`
- **대조**: 같은 파일이 **누적 집계는** 페이지 나눔으로 감쌌다(`:1144`·`:1423`)
- **발현**: 1라운드 200명 발송 → `1000 >= 1000` 성립 → 2라운드 즉시 중단. run 이 「정체 감지로 중단」으로 기록되는데 **원인이 진짜와 달라 운영자가 엉뚱한 곳을 본다**

## 4-3. [높음] 「전체 회수」가 사슬을 안 따라간다 — 안내문이 거짓

- **근거**: 함수가 단건 `167_bulk_message.sql:294-296`(**이후 재정의 없음**) / 화면도 단건 `dev/js/admin-messaging.js:1972` / 버튼 이름 `dev/admin/index.html:4602` 「전체 회수」 / 🔴 **거짓 안내문** `:4642` 「받은 사람 **모두**의 화면에서 가려집니다」
- **발현**: 1차 200건 + 추가 150건으로 나간 메시지를 회수 → **보고 있는 발송의 수신자만** 가려지고 150명은 계속 본다. **되돌릴 수 없는 조치**라 오인 대가가 크다
- **원인**: 후속 발송(388~391)이 한 메시지를 여러 발송으로 나눴는데 회수 함수는 그대로 남았다

## 4-4~4-21 (높음 3 · 중간 6 · 낮음 9)

⚠️ **메일 영역 높음 7건 중 하나(방침 통지 함수의 호출자 검증 없음)는 §1-1 에 있다.** 여기 표에는 나머지 여섯만 있다(4-1~4-3 · 4-4~4-6).
⚠️ **중간이 6인 이유** — 원 조사는 1,000행 미대응을 발생 위치별로 셋(인플루언서·관리자·홍보)으로 세었으나, 고치는 자리가 같은 성질이라 **4-11 한 줄로 합쳤다**(그 안에 8곳을 적었다). 그래서 원 조사 8 → 이 표 6이다.

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 4-4 | 높음 | 방침 통지 메일 — **이스케이프 0건**(§1-1과 한 묶음) | `notify-policy-change/index.ts:81` |
| 4-5 | 높음 | 광고주 신청 알림 — 제품 **금액·수량**이 이스케이프 없이 메일 표에 들어감. 익명 함수가 원소 스키마를 **전혀 안 봄** | `notify-brand-application/index.ts:249-251` / `206:175-178` |
| 4-6 | 높음 | 검수 결과 메일 — 「확인 후 발송, 기록은 그 뒤」라 **중복 발송**이 열려 있음. 다이제스트 3종은 이미 「작업 전 소유권 확보」로 고쳐졌다 | `notify-deliverable-decision/index.ts:526-545`·`:681`·`:695-700` ↔ `notify-influencer-daily-digest/index.ts:369-450` |
| 4-7 | 중간 | 홍보 메일 채널 문지기의 `LIKE '%x%'` — 자동 생성 코드가 **15.3% 확률**로 글자 x 를 포함(20만 회 시행 실측). 영문 이름이면 확정적 | `387:373`·`:418` ↔ **일괄 발송은 정확 일치** `389:263-270` |
| 4-8 | 중간 | 추가 발송 사슬 가드에 **행 잠금이 없다** — 200건 반복문이 도는 수 초 동안 두 번째 호출이 형제 없음으로 봄 → **최대 200명이 두 번 받음** | `390:109-127`·`:169-192`·`:196-282` / 유일 제약 없음 `388:27-40` |
| 4-9 | 중간 | 「리뷰어형 + 제품가 0」 리워드 문구가 **세 곳에서 다르고**, 관리자 미리보기는 지급 안 되는 현금액을 덧붙임(바로 위 주석이 금지한 동작) | `dev/js/campaign.js:309-313` / `notify-campaign-promo-digest/index.ts:229-231` / `dev/js/admin.js:3722`·`:3731-3735` |
| 4-10 | 중간 | 방침 통지 — 「선점 먼저, 발송 그다음」이라 중단 시 **영구 미발송이 「발송됨」으로** 남음(유일 제약이 재시도 차단) | `notify-policy-change/index.ts:264-270`·`:274`·`:280-287` |
| 4-11 | 중간 | 일별 집계 1,000행 미대응 **8곳** — 인플루언서 2 · 관리자 5 · 홍보 1. **같은 파일 안에 제대로 한 자리가 있다** | `notify-influencer-daily-digest/index.ts:502-506`·`:515-520` ↔ `:538-543` / `notify-admin-daily-digest/index.ts:956-990` / `notify-campaign-promo-digest/index.ts:1224-1228` |
| 4-12 | 중간 | 다이제스트 3종 — 개별 수신자 발송 기록이 없어 **손으로 재시도하면 전원 재발송** | `notify-influencer-daily-digest/index.ts:879-1053` 외 2종 |
| 4-13~4-21 | 낮음 | 검수 메일 날짜가 시간대 보정 없이 **하루 이르게**(자매 3종은 보정함) · 채널별 기준값 검사가 서버는 정수만·화면은 `Number()` · 채널 빈 캠페인 폴백 불일치 · 협정세계시와 한국시간 혼용 · 재계산 중 「다음」 버튼 눌림 · 브랜드 보고 1,000행 · 인플루언서 다이제스트에 **감사용 제외가 구조적으로 불가능** · 검수 완료가 리뷰어형에 「報酬」 사용 · 오리엔 초대 수신자를 **화면이 지정 가능**(`CLAUDE.md` 서술과 다름) | 각 `notify-*/index.ts` |

---

# 5. 관리자 화면 (8/07 이후 128커밋)

**높음 0 · 중간 12 · 낮음 13.** 먼저 볼 셋:

| # | 내용 | 근거 |
|---|---|---|
| 5-1 | 🔴 **예약 건수 조회 실패가 0으로 돌아와 안전 게이트 넷이 동시에 열린다.** 호출부 `try/catch` 는 함수가 이미 오류를 삼켜 **도달하지 않는다** → 선정형 예약이 있는 캠페인이 **경고 없이 선착순형으로 저장**되고 되돌릴 화면 경로가 없다 | `dev/lib/storage.js:5553-5562` / 사용처 `admin-event.js:254-255`·`:325-328`, `admin.js:2688-2690`·`:2720-2722` |
| 5-2 | 지급 준비에서 처리해도 **「미등록」 건수·사이드바 배지가 안 줄어든다**(주 동선이라 매번 발현) | `admin-settlements.js:1793-1804`·`:1791-1826` ↔ 소비처 `:339` |
| 5-3 | 금액 미확정 건이 지급 준비에서 **「¥0」으로 그려지고 합계에서 빠진다** → 지급대장 대조 금액이 낮게 나옴. 같은 행이 다른 탭에서는 빨간 배지로 정상 표시 | `admin-settlements.js:1758` / `shared.js:2337-2341` / 렌더 `:2093`·`:2104`·`:2399` |

### 나머지 22건 (중간 9 · 낮음 13)

⚠️ **이 목록은 여기에만 있다.** 조치 계획 F 묶음은 이 절을 가리킨다.

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 5-4 | 중간 | 썸네일 폴백 **두 곳만 무한 루프에 열림**(나머지는 전부 가드 있음) | `admin.js:1667` · `admin-deliverables.js:1500` ↔ `admin-brand-ops.js:377` 외 5곳 |
| 5-5 | 중간 | 캠페인 목록의 「선정기간」 열만 **다섯째 자리**로 조건 없이 그림 — 나머지 넷은 글자 그대로 같다 | `admin.js:555` ↔ `application.js:276-277`·`admin.js:3815-3816`·`admin-applications.js:646-648`·`admin-excel.js:358-362` |
| 5-6 | 중간 | 정산 상태 탭 건수가 **캠페인 필터·검색을 무시**(같은 파일 안에 두 규약이 섞임) | `admin-settlements.js:325-332` ↔ `:474-485`·`:487-491` |
| 5-7 | 중간 | 정산 목록 **선택 상태만** 페인 진입 시 안 비워짐(다른 둘은 비운다) | `admin-settlements.js:231-233`·`:1439` ↔ 진입 경로에 clear 없음 |
| 5-8 | 중간 | 정산 엑셀 「송금완료일」에 **실제 송금일과 기록일이 표시 없이 섞임**(화면은 회색 표로 구분) | `admin-settlements.js:1349` ↔ `:637-641`·`:69-79` |
| 5-9 | 중간 | 정산 **조회 실패가 「0건」과 구분 안 됨** → 통신 오류 시 전 화면이 「보낼 것 없음」, 사이드바 배지도 사라짐 | `dev/lib/storage.js:4934` / `admin-settlements.js:277-278`·`:1812-1816` |
| 5-10 | 중간 | 「입장 처리」만 진행현황 **요약 카드를 다시 안 그림**(뽑기·탈락·취소는 그린다) | `admin-event.js:1858` ↔ `:1737-1738`·`:1774-1775`·`:1819-1820` |
| 5-11 | 중간 | 발송 상세가 수신자를 「명」이라 부르는데 **실제 단위는 「건」** — 같은 창 안 추가 발송 줄은 「건」 | `admin-messaging.js:1766-1767`·`:1786-1787`·`:1832` ↔ `:1897-1899` |
| 5-12 | 중간 | 발송 이력 **조회 실패가 「이력이 없습니다」로** 보이고, 0건 분기가 캐시 갱신 **전에** 되돌아감 | `dev/lib/storage.js:4249` / `admin-messaging.js:1513-1517` |
| 5-13 | 낮음 | 다른 캠페인을 열 때 탭·필터·검색·달력은 초기화되는데 **열 제목 정렬만** 안 비워짐 | `admin-applications.js:23-52` ↔ `:449-450` |
| 5-14 | 낮음 | 취소 사유 조회 실패가 **빈 배열을 세션 내내 캐시** — 화면은 안전하게 감추는데 **엑셀은 코드값 노출** | `dev/lib/storage.js:3363` / `admin-influencers.js:540-548` ↔ `admin-excel.js:603`·`:1067` |
| 5-15 | 낮음 | 「올려두고 미제출」 집합을 진행현황 조회가 **무조건 덮어씀** — 그쪽이 실패하면 잘 받아둔 값이 사라짐 | `admin-applications.js:246-248` ↔ `admin-deliverables.js:313` |
| 5-16 | 낮음 | 「정산대기 추가」가 정산 행을 **먼저 송금완료로** 처리(§2-4와 같은 건 — 화면 쪽 기술) | `admin-settlements.js:1047` → `:1060` |
| 5-17 | 낮음 | 지급 준비 사람 검색이 입력값을 **이스케이프 없이** 조회 필터에 이어 붙임(`escapeLikePattern` 미적용) | `admin-settlements.js:2576-2579` |
| 5-18 | 낮음 | 미등록 건수 갱신이 목록을 다시 안 그려 **탭 숫자와 「총 N건」이 갈릴 수 있음** | `admin-settlements.js:303-306` ↔ `:1435-1444` |
| 5-19 | 낮음 | 「캠페인별」 보기로 바꾸면 선택 요약·「선택한 건 보냄」이 사라짐 — **주석은 반대로** 적혀 있음 | `admin-settlements.js:2274-2275` ↔ `:2483-2504` |
| 5-20 | 낮음 | 조회는 성공했는데 그 사람만 없으면 「확인 실패」가 아니라 **「페이팔 미등록」으로 단정** | `admin-settlements.js:2040`·`:2049-2053`·`:2249-2251` |
| 5-21 | 낮음 | 예약 요약 문구가 공용 셈 함수를 안 쓰고 다시 계산하며 「심사중」을 **이 줄만 「대기」**라 부름(현재 죽은 코드) | `admin-event.js:1403-1416` ↔ `:1418-1430` |
| 5-22 | 낮음 | 전체 정원 합계는 「사용 안 함」 타임을 빼는데 **인원은 안 뺌** → 남은 자리가 실제보다 적게 나옴 | `admin-event.js:1560-1566` ↔ `:1542-1548` |
| 5-23 | 낮음 | 추가 발송이 부모의 관리자 전용 제목을 **안 물려받고** 창에 입력칸도 없음 | `admin-messaging.js:1937-1940` ↔ `:1487` |
| 5-24 | 낮음 | 상세 제목을 **최근 100건 캐시에서만** 읽음 → 캐시 밖 옛 발송은 제목이 있어도 「없음」, 그 상태에서 이름을 붙이면 **기존 제목이 조용히 덮임** | `admin-messaging.js:1817-1819`·`:1698` / `storage.js:4246` |
| 5-25 | 낮음 | 사슬 정보가 없으면 「마지막 발송」으로 간주해 **버튼이 켜짐** — 다른 네 조건은 값이 없으면 막는 방향 | `admin-messaging.js:1647-1648` |

**⚠️ 이 조사는 「확인했으나 문제 없었던 것」을 상세히 남겼다** — 표 머리글 ↔ 행 칸 수 ↔ `colspan` 이 **아홉 표 전부 일치**, `refreshPane` 호출 12개가 **전부 등록됨**, 인증 성공일 열은 두 곳 판정이 같음, 방문자수 시각 계산 검산 통과. **이 저장소가 반복해서 데인 유형이 이번엔 대부분 지켜졌다.**

---

# 6. 문서·규칙 층 (이번에 새로 넣은 축 — 높음 비율 1위)

## 6-1. [높음] 후크가 아직도 실재하지 않는 스킬 이름을 출력한다 ✅ **2026-09-02 조치 완료**

- **근거**: `.claude/hooks/frontend-skill-reminder.js:100` — `Skill("document-skills:frontend-design")`. 그 플러그인 아래는 xlsx·docx·pptx·pdf 넷뿐이라 **부르면 실패한다**
- 🔴 **같은 날 `ui.md` 만 고쳐졌다** — 그런데 `ui.md` 는 `paths:` 조건부라 **화면 파일을 열어야** 읽히고, 후크는 **만지는 순간** 뜬다. **세션이 실제로 읽는 쪽은 여전히 틀린 이름이었다 — 정정이 사실상 무효**
- **조치**: 커밋 `c7b0e65d` — 이름 정정 + Apple 지침 우선순위를 후크에도 실었다(같은 이유로 그 규칙도 안 읽히던 자리라)

## 6-2. [높음] 「운영에 머지하지 않음」이라는 내부 문서 2개가 운영에 공개돼 있다

- **근거**: `CLAUDE.md:69-73` 「내부 직원용 참고 자료 · **운영서버(main)에 머지하지 않음**」 ↔ 실측 `curl -sL https://globalreverb.com/docs/service-flow.html` → **HTTP 200**, `flowchart-i18n.html` → **HTTP 200**. **양성 대조**: 없는 파일 → HTTP 404
- main 진입 커밋 `d0fe5717`·`f33f3284`. `vercel.json` 이 **없어** `docs/` 를 막는 설정도 없다
- **발현**: 「내부용이니 운영에 없다」를 전제로 내부 프로세스·미출시 기능·관리자 동선을 계속 그린다. 실제로는 **주소만 알면 열린다.** 다음 보안 점검이 `CLAUDE.md` 를 근거로 「운영 공개 문서 없음」이라 판정한다

## 6-3. [높음] 「현재 원본은 이것」이라는 경고 두 개가 낡았다

| 함수 | `CLAUDE.md` 서술 | 실제 정의 이력 | 현재 원본 |
|---|---|---|---|
| `_settlement_cert_candidates` | 「318이다」(`CLAUDE.md:351`) | 232→262→264→300→318→**331** | **331** |
| `cancel_application` | 「356이다」(`CLAUDE.md:397`) | 104→113→306→309→356→**394** | **394** |

- 🔴 **하필 그 경고가 「파일 번호가 가장 큰 정의를 베이스로 삼을 것」이다 — 그 경고 자신이 낡았다**
- **발현**: 318을 베이스로 재작성하면 331의 「시딩·방문형도 요구 채널 전부 승인」이 사라져 **한 채널만 승인돼도 리워드 전액**이 나가던 결함이 되살아난다. 356을 베이스로 하면 394가 걷어낸 **「취소 시 관리자 공지 자동 등록」이 되살아난다**(직전 실측 공지 75건 중 64건이 그것이었고, 본문에 회원 이름·이메일 사본이 쌓이는데 파기 함수는 `admin_notices` 를 안 본다)
- ⚠️ **394 파일은 자기 베이스를 정확히 적었는데 `CLAUDE.md` 가 안 따라왔다**

## 6-4. [높음] 세션 조율 규칙이 근거로 삼는 브라우저 서버가 꺼져 있다

- **근거**: `.claude/rules/multi-session.md:118`·`.claude/agents/reverb-qa-tester.md:14` 가 `.mcp.json` 의 `--extension` 기동 인자를 근거로 「단일 크롬 1개 · 동시 실행 금지」를 정함 ↔ `.claude/settings.local.json` 의 `disabledMcpjsonServers` 에 **그 네 서버 전부 비활성**
- `reverb-qa-tester.md:4` 의 `tools:` 는 전부 **다른 플러그인 서버**(`mcp__plugin_ecc_playwright__*`). `.claude/` 전체에 `mcp__playwright__` 는 **0건**(양성 대조: `plugin_ecc_playwright` 는 2개 파일에서 걸림)
- **발현**: 「단일 자원이니 동시 실행 금지」가 네 규칙 파일에 퍼져 세션 간 대기를 만드는데, 그 근거가 **꺼진 서버의 기동 인자**다
- ⚠️ **확인 범위**: 설정 파일 내용까지. `plugin_ecc_playwright` 의 런타임 브라우저 기동 방식은 확인하지 않았다

## 6-5. [높음] 「최신 상태 레퍼런스」가 185커밋 동안 0번 갱신됐다

- **근거**: `docs/FEATURE_SPEC.md` 머리말 — 「최종 업데이트: **2026-05-07**」, 「**~migration 076**」(저장소 최신은 395). 마지막 실제 변경 `172989ab`(2026-08-12)
- 2026-08-13 이후 `dev/` 또는 `supabase/migrations/` 를 건드린 커밋 **185건**, 그중 이 문서를 함께 고친 커밋 **0건**
- 그런데 네 규칙이 이 문서를 의무로 지목한다 — `docs-tracking.md:12`·`:21`, `git.md:108`(배포 전 체크리스트), `interaction.md:213`
- **발현**: 회원 탈퇴 운영 배포·일괄발송 후속·채널별 최소 팔로워·썸네일 두 사본·취소 공지 중단·행사 선정형이 **전부 이 문서에 없다.** 「최신 상태」라는 이름을 믿고 여는 사람은 **5월 스냅샷**을 현재 동작으로 읽는다. 동시에 배포 전 체크리스트의 그 칸이 **185번 연속 사실과 다르게** 체크된 셈이라 체크리스트 전체의 신뢰가 깎인다

## 6-6. [높음] 비로그인 노출 함수 목록에 `get_event_roster` 가 없다

§1-2 참조. `CLAUDE.md`·`event-ticketing.md`·`security.md`·`CLAUDE-ARCHIVE.md` **전부 0건**.

## 6-7~6-11 (중간 2 · 낮음 3)

⚠️ 옛 6-9(「§1-2와 동일」)는 **6-6 과 같은 말이라 지웠다** — 같은 절 안에서 한 사실을 두 번 세고 있었고 심각도까지 달랐다.

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 6-7 | 중간 | 🔴 **`globs:` 사고의 실패 모드를 정반대로 적었다** — 문서는 「머리말이 틀려 파일이 통째로 무시된다」인데, 고친 커밋 본문은 「그 키는 Cursor 것이라 **모든 세션에 로드됐다**」이고 **일부러 그대로 뒀다**고 밝힌다. 직접 관측으로도 `globs:` 셋은 로드되고 `paths:` 넷은 안 된다 | `docs-tracking.md:100` ↔ 커밋 `91aed173` |
| 6-8 | 중간 | 되돌려진 도구를 사양서·인계가 「만든 것」으로 계속 안내(철회 표시 없음) | `2026-08-20-session-status-visibility-handoff.md:92` ↔ 커밋 `3616217f` |
| 6-9 | 낮음 | 썸네일 소급 도구 이름이 사양서와 실제가 다름(`backfill-campaign-thumbs.js` 는 없다, 실제는 `backfill-storage-thumbs.js`) | `2026-08-31-image-thumbnail-two-copies.md:397` |
| 6-10 | 낮음 | dev 머지 위임 예외가 `git.md` 에만 있고 `multi-session.md` 는 정반대를 말함 | `multi-session.md:37-38` ↔ `git.md` |
| 6-11 | 낮음 | `.claude/skills/frontend-design-toolkit` — `.gitmodules` 없는 중첩 저장소가 커밋돼 있고 `SKILL.md` 가 없어 **로드되지 않는다**. 형제 8개는 심볼릭 링크 | `git ls-files -s` |

---

# 7. 이미지·썸네일·파기

## 7-1. [높음] 파기 경로가 경로를 나눠 보내지 않는데 경로 수가 2배가 됐다

- **근거**: `dev/lib/storage.js:4530-4538` — `remove(paths)` **단일 호출, 청킹 없음** / `:4509-4524` — `_withThumbPaths` 가 경로를 **정확히 2배**로 / 대조: 같은 일을 하는 `supabase/functions/purge-withdrawal-media/index.ts:83-85`·`:253` 은 **100개씩 나누며 그 이유를 명시**
- **발현**: 응모가 많은 캠페인을 보관 삭제 → `soft_delete_campaign` 이 **데이터베이스 행을 먼저 지우고** 경로만 돌려줌(`325:186-189`) → 배열이 커져 `remove()` 거부/시간 초과 → `{ok:false}` 를 **조용히 반환**(예외 안 던짐) → **영수증·인증샷 원본과 썸네일이 공개 통에 남고, 경로는 이미 사라져 다시 찾을 방법이 없다.** 마이그레이션 325가 막으려던 바로 그 상태
- `purgeAuditDataAll()` 은 전 캠페인 결과물을 한꺼번에 모아 배열이 더 크다

## 7-2~7-10 (중간 5 · 낮음 4)

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 7-2 | 중간 | `content/` 썸네일을 만들어 놓고 **렌더러 4개 중 3개가 안 부른다** — 인플루언서 상세·신청 모달이 원본(최대 5MB)을 받음 | `shared.js:267-273`·`:242`·`:225` / `application.js:345`·`:888-905`·`:912-923` |
| 7-3 | 중간 | 고아 이미지 정리(`deleteCampImages`)만 썸네일을 안 지움 — **다섯 번째 삭제 경로**. 참조가 없어 나중에 찾을 수도 없음 | `dev/lib/storage.js:1953-1958` / 호출부 `admin.js:2985`·`3032`·`3090`·`4607` |
| 7-4 | 중간 | 썸네일 규칙이 **6곳**에 흩어짐(문서·주석은 3곳만 경고). 아웃바운드 폭 480 하드코딩 · 삭제 인라인 조립 · Edge Function 은 폴더 필터 없음 | `storage.js:5182`·`:5194-5202` / `purge-withdrawal-media/index.ts:243-250` |
| 7-5 | 중간 | 썸네일 표시가 **죽은 함수 `imgThumb` 의 존재 여부**에 걸려 있음 — 그 함수를 지우면 조용히 원본으로 되돌아감 | `shared.js:267` |
| 7-6 | 중간 | 사양서 「구현 결과」가 1단계만 적고 **이미 구현된 것을 「미결」로** 둠. 이름을 대는 파일은 **삭제됐다** | `2026-08-31-image-thumbnail-two-copies.md:387-397`·`:335-356`·`:453` ↔ 커밋 `6a0282a3` |
| 7-7~7-10 | 낮음 | 목표 폭보다 좁고 2MB 넘는 이미지는 썸네일이 **영구히** 안 만들어지고 소급 실패 수가 0이 될 수 없음 · `RICH_DISPLAY_WIDTH=960` 이 이제 안 쓰임 · 원본 폴백 두 곳에 재귀 방지 없음 · 브라우저 파기에 참조 세기 없음(Edge Function 에는 있음) | `image-compress.js:141-145` / `shared.js:505-506` / `admin.js:1667`·`admin-deliverables.js:1500` / `purge-withdrawal-media/index.ts:225-227` |

**세 목록 대조 결과**: 화면 `THUMB_FOLDERS`(`ui.js:319`) · 저장 `THUMB_WIDTH_BY_PREFIX`(`storage.js:1726-1731`) · 소급 `FOLDERS`(`scripts/backfill-storage-thumbs.js:32`) — **차집합 없음, 폭 값도 일치.** 이 축은 어긋나지 않았다.

---

# 8. 행사·응모 상태 전이

**높음 1**(§1-2와 동일) **· 중간 6 · 낮음 4**

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 8-1 | 중간 | 행사 캠페인에서 **「승인」 버튼만** 차단이 빠짐(세 화면 전부). 미승인·되돌리기는 안내 창으로 막는데 승인만 그대로 나가 서버가 거부 | `admin-applications.js:207`·`:1042`, `admin-dashboard.js:123` ↔ 차단 `:275`·`:302` / `289:20-23` |
| 8-2 | 중간 | 선정형에서 **뽑히지 않은 응모를 닫는 자동 경로가 없다** — 행사가 끝나도 영원히 「심사중」, 낙선 통지 0통, 같은 묶음 다른 날 예약 불가 | `289:170`(행사 제외) / `admin-event.js:1748-1780` / `378:255-258` |
| 8-3 | 중간 | 6개월 파기가 **예약 행만** 지우고 짝이 되는 신청 행(이름·이메일·계정)은 남김. **명단 조회 함수는 그 남은 행을 읽어 파기 후에도 명단이 나온다** | `286:11-20` / `378:291-309` / `392:38-42` |
| 8-4 | 중간 | 「예약이 있으면 접수 방식을 못 바꾼다」가 **브라우저에만** 있음(서버는 범위만 강제) | `376:11-13` / `admin.js:2717-2735` / `admin-event.js:245-268` |
| 8-5 | 중간 | 관리자 화면이 **「탈락」과 「운영진 취소」를 구별하지 못함**(인플루언서 화면은 구별). 구별에 쓸 메모도 화면이 항상 비워 보냄 | `fetchEventTicketsByCampaign()`(신청 미조인) / `admin-event.js:1336-1345`·`:1763` ↔ `event-ticket.js:44-54` |
| 8-6 | 중간 | 탈락시킨 사람이 마감 전이면 **그대로 다시 접수** 가능하고, 되살아난 신청에 **탈락 당시 심사자·시각이 남음** | `378:242-248`·`:314-323` / `379:211-216` |
| 8-7~8-10 | 낮음 | 잠금 순서가 함수마다 반대라 **교착 가능** · 순번 필수 제약이 선착순형까지 함께 풀림 · 탈퇴 철회 반복문이 행사에서 **항상 실패**해 「철회 못 한 수」가 상시 오탐 · 탈락 사유 메모 칸을 화면이 항상 비워 보냄 | `379:34`·`:67` ↔ `377:103-106`·`378:36-39` / `282:75-78` ↔ 378 / `356` ↔ `289:14-23` / `admin-event.js:1763` |

**⚠️ 「없다」로 확인한 것**: `UPDATE public.applications` **26곳 전수** — 행사 신청을 차단 표시 없이 건드리는 경로는 **탈퇴 철회 반복문 하나뿐**. 브라우저에서 응모 상태를 바꾸는 경로는 `storage.js:951` **한 곳**. 선정 기간 표시 조건 네 곳은 **글자 그대로 동일**. 394가 없앤 공지의 대체 표시는 **빠진 자리 없음**.

---

# 9. 가입·인증·권한

**높음 1**(§1-3) **· 중간 3 · 낮음 2**

**함수 전수 훑기 방법과 숫자** (이번 조사의 기준점):

| 단계 | 수 |
|---|---|
| 마이그레이션에서 정의된 함수 이름(중복 제거) | **237** |
| 그중 최신 정의가 `SECURITY DEFINER` 이고 트리거 함수가 아닌 것 | **153** |
| 그중 본문에 호출자 검사가 **하나도 없는** 것 | **46** |
| 그 46개 중 `anon`·`authenticated` 회수가 **어느 마이그레이션에도 없는** 것 | **15** |
| 그 15개 중 **완전히 열린 것**(두 방향 다) | **1** (`get_event_roster`) |
| 그 15개 중 **회수 방향이 한쪽만인 것** | **3** (9-4 — `PUBLIC` 만 회수) |
| 나머지 | **11** — **비로그인에 일부러 연 것**(토큰으로 신원을 확인하는 익명 함수 8종 · 조사 지시에서 제외한 2종 · 항상 예외만 던지는 폐기 함수 1종) |

- **`DROP` 후 재생성으로 회수가 풀린 곳: 0건.** 최신 `CREATE` 가 최신 `REVOKE` 보다 뒤인 함수 21개를 전수 확인 → 21개 모두 시그니처 불변 `CREATE OR REPLACE`. 실제 `DROP` 후 재생성 3건(387·389·390)은 **모두 같은 파일에서 회수를 다시 걸었다**
- **8/07 이후 신규 표 7개, 전부 행 단위 보안 정책이 켜져 있다**

| # | 심각도 | 내용 | 근거 |
|---|---|---|---|
| 9-1 | 중간 | §1-2와 동일 | — |
| 9-2 | 중간 | 가입 **동의 시각·생년월일을 브라우저 값 그대로** 저장(범위 검사 없음). **같은 함수가 가입 시각은 일부러 서버 값을 쓰며 「브라우저 시계를 믿지 않는다」고 적었다** | `382:67-79`·`:143-152` ↔ `:132` / `dev/js/auth.js:129-140` |
| 9-3 | 중간 | `invite_admin` 이 이메일을 **대소문자 구분해** 찾아, 대문자가 섞이면 **로그인 불가능한 관리자**가 만들어지고 설정 링크가 **그 사람의 인플루언서 비밀번호를 바꾼다** | `245:112`·`:116`·`:139-158` / 화면 `admin-accounts.js:338` / ⚠️ `CLAUDE.md` 는 **화면 쪽에만** 이 규칙을 세웠다 |
| 9-4 | 낮음 | 「로그인한 사람만」 함수 3종이 `PUBLIC` 만 회수 — 두 방향 중 한쪽만(369·370·375가 세운 기준 미적용) | `240`·`353`·`180:147-152` |
| 9-5 | 낮음 | `register_push_token` 이 토큰 주인을 부른 사람으로 바꿈 — **이미 백로그에 기록됨**(발송 기능이 없어 현재 피해 0) | `374:73-79`·`:154` |

---

## 조사 범위의 한계 (「없다」의 경계)

- **운영·개발 데이터베이스의 실제 상태는 조회하지 않았다** — 전부 저장소 파일(마이그레이션 SQL·화면 코드·Edge Function) 기준이다. 특히 **함수 실행 권한은 개발과 운영이 달랐던 전례가 있어** 파일만으로는 확정할 수 없다
- 예외 둘: ①`supabase functions list` 로 운영 배포 목록 확인(§1-1) ②`curl -sL` 로 운영 공개 문서 확인(§6-2)
- `docs/specs/` 는 2026-08 이후 57개만 대상
- `plugin_ecc_playwright` 의 런타임 브라우저 기동 방식은 확인하지 않았다(§6-4)

## 다음 문서

- **실측 조회** — `docs/research/2026-09-02-audit-measurement-queries.md`
- **조치 계획** — `docs/specs/2026-09-02-audit-remediation-plan.md`

🔴 **직전 조사에서 실측 8건 중 절반이 「해당 없음」이었다.** 「조사 문서를 그대로 믿고 착수했으면 없는 문제를 고칠 뻔했다」가 그 메모리의 첫 줄이다. **이번에도 실측을 먼저 돌린다.**
